{
  "schema": 1,
  "waivers": [
    {
      "check": "AH001",
      "path": "harness/tests/**",
      "reason": "Check fixtures carry deliberately provider-shaped strings (AWS's documented example access key id, a synthetic JWT) so that AH001 can be shown to fire on them. They authenticate against nothing. No expiry is set because this is a permanent property of the fixture files, not deferred work."
    }
  ]
}
