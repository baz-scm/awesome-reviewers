[
  {
    "discussion_id": "3498064754",
    "pr_number": 2244,
    "pr_file": "open-sse/handlers/chatCore/streamingHandler.js",
    "created_at": "2026-06-30T11:00:34+00:00",
    "commented_code": "});\n   }\n \n-  // Warn when upstream returns unexpected Content-Type for a streaming response.\n-  // This often means the provider returned an HTML error page or plain-text error\n-  // that the SSE transform stream would forward as garbage to the client.\n+  // When upstream returns HTML/text instead of SSE (e.g. Cloudflare 5xx error\n+  // page), piping it through the SSE transform stream causes Next.js\n+  // \"failed to pipe response\" and crashes the chat router. Read the body,\n+  // pull a short human-readable message from the <title>, and return a\n+  // clean JSON error instead.\n   const upstreamContentType = (providerResponse.headers.get('content-type') || '').toLowerCase();\n   if (upstreamContentType && !upstreamContentType.includes('text/event-stream') && !upstreamContentType.includes('application/json')) {\n-    console.warn('[STREAM] ' + provider + ' | ' + model + ' | unexpected Content-Type: ' + upstreamContentType);\n+    const bodyText = await providerResponse.text().catch(() => '');\n+    const titleMatch = bodyText.match(/<title>([^<]+)<\\/title>/i);\n+    const shortMsg = titleMatch?.[1]?.trim()\n+      || (bodyText.length < 200 ? bodyText.trim() : `Upstream returned non-SSE response (${upstreamContentType})`);\n+    const status = providerResponse.status || 502;\n+    console.warn(`[STREAM] ${provider} | ${model} | blocked pipe: ${shortMsg} [${status}]`);",
    "repo_full_name": "decolua/9router",
    "discussion_comments": [
      {
        "comment_id": "3498193910",
        "repo_full_name": "decolua/9router",
        "pr_number": 2244,
        "pr_file": "open-sse/handlers/chatCore/streamingHandler.js",
        "discussion_id": "3498064754",
        "commented_code": "@@ -52,12 +52,26 @@ export function handleStreamingResponse({ providerResponse, provider, model, sou\n       });\n   }\n \n-  // Warn when upstream returns unexpected Content-Type for a streaming response.\n-  // This often means the provider returned an HTML error page or plain-text error\n-  // that the SSE transform stream would forward as garbage to the client.\n+  // When upstream returns HTML/text instead of SSE (e.g. Cloudflare 5xx error\n+  // page), piping it through the SSE transform stream causes Next.js\n+  // \"failed to pipe response\" and crashes the chat router. Read the body,\n+  // pull a short human-readable message from the <title>, and return a\n+  // clean JSON error instead.\n   const upstreamContentType = (providerResponse.headers.get('content-type') || '').toLowerCase();\n   if (upstreamContentType && !upstreamContentType.includes('text/event-stream') && !upstreamContentType.includes('application/json')) {\n-    console.warn('[STREAM] ' + provider + ' | ' + model + ' | unexpected Content-Type: ' + upstreamContentType);\n+    const bodyText = await providerResponse.text().catch(() => '');\n+    const titleMatch = bodyText.match(/<title>([^<]+)<\\/title>/i);\n+    const shortMsg = titleMatch?.[1]?.trim()\n+      || (bodyText.length < 200 ? bodyText.trim() : `Upstream returned non-SSE response (${upstreamContentType})`);\n+    const status = providerResponse.status || 502;\n+    console.warn(`[STREAM] ${provider} | ${model} | blocked pipe: ${shortMsg} [${status}]`);",
        "comment_created_at": "2026-06-30T11:00:34+00:00",
        "comment_author": "KunN-21",
        "comment_body": "Fixed in commit 01ef243: added streamController.handleError(...) on the early-return path, and the message now strips HTML tags and clamps length before reflecting into the client error message to avoid XSS via untrusted upstream text.",
        "pr_file_module": null
      }
    ]
  }
]