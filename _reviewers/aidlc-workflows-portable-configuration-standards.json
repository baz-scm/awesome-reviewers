[
  {
    "discussion_id": "2623666161",
    "pr_number": 25,
    "pr_file": "README.md",
    "created_at": "2025-12-16T15:06:35+00:00",
    "commented_code": "Set up the AI-DLC rules/steering files as part of your [supported platform](#prerequisites).\n \n-Clone this repo:\n+Clone this repo and set the environment variable:\n ```bash\n git clone <this-repo>\n+export AIDLC_WORKFLOWS=~/dev/aidlc-workflows",
    "repo_full_name": "awslabs/aidlc-workflows",
    "discussion_comments": [
      {
        "comment_id": "2623666161",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 25,
        "pr_file": "README.md",
        "discussion_id": "2623666161",
        "commented_code": "@@ -6,19 +6,15 @@ AI-DLC is an intelligent software development workflow that adapts to your needs\n \n Set up the AI-DLC rules/steering files as part of your [supported platform](#prerequisites).\n \n-Clone this repo:\n+Clone this repo and set the environment variable:\n ```bash\n git clone <this-repo>\n+export AIDLC_WORKFLOWS=~/dev/aidlc-workflows",
        "comment_created_at": "2025-12-16T15:06:35+00:00",
        "comment_author": "raj-jain-aws",
        "comment_body": "Should we add an instruction here that this env var should be set as per your dir structure? Also, this needs to work on Windows OS also where the env var command will be different.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2908153782",
    "pr_number": 111,
    "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
    "created_at": "2026-03-09T21:58:41+00:00",
    "commented_code": "- Load `common/question-format-guide.md` for question formatting rules\n - Reference these throughout the workflow execution\n \n-## MANDATORY: Extensions Loading\n-**CRITICAL**: At workflow start, scan the `extensions/` directory recursively for all `.md` files. These are extension rule files that apply as cross-cutting constraints across the entire workflow.\n+## MANDATORY: Extensions Loading (Context-Optimized)\n+**CRITICAL**: At workflow start, scan the `extensions/` directory recursively but load ONLY lightweight opt-in files \u2014 NOT full rule files. Full rule files are loaded on-demand after the user opts in.\n \n **Loading process**:\n 1. List all subdirectories under `extensions/` (e.g., `extensions/security/`, `extensions/compliance/`)\n-2. Load every `.md` file found within those subdirectories\n-3. Each extension file defines its own verification criteria and enforcement rules as cross-cutting constraints\n+2. In each subdirectory, load ONLY `*.opt-in.md` files \u2014 these contain the extension's opt-in prompt. The corresponding rules file is derived by convention: strip the `.opt-in.md` suffix and append `.md` (e.g., `security-baseline.opt-in.md` \u2192 `security-baseline.md`)\n+3. Do NOT load full rule files (e.g., `security-baseline.md`) at this stage\n \n-**Enforcement**:\n+**Deferred Rule Loading**:\n+- During Requirements Analysis, opt-in prompts from the loaded `*.opt-in.md` files are presented to the user\n+- When the user opts IN for an extension, load the corresponding rules file (derived by naming convention) at that point\n+- When the user opts OUT, the full rules file is never loaded \u2014 saving context\n+- Extensions without a matching `*.opt-in.md` file are always enforced \u2014 load their rule files immediately at workflow start\n+\n+**Enforcement** (applies only to loaded/enabled extensions):",
    "repo_full_name": "awslabs/aidlc-workflows",
    "discussion_comments": [
      {
        "comment_id": "2908153782",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 111,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2908153782",
        "commented_code": "@@ -25,22 +25,28 @@ All subsequent rule detail file references (e.g., `common/process-overview.md`,\n - Load `common/question-format-guide.md` for question formatting rules\n - Reference these throughout the workflow execution\n \n-## MANDATORY: Extensions Loading\n-**CRITICAL**: At workflow start, scan the `extensions/` directory recursively for all `.md` files. These are extension rule files that apply as cross-cutting constraints across the entire workflow.\n+## MANDATORY: Extensions Loading (Context-Optimized)\n+**CRITICAL**: At workflow start, scan the `extensions/` directory recursively but load ONLY lightweight opt-in files \u2014 NOT full rule files. Full rule files are loaded on-demand after the user opts in.\n \n **Loading process**:\n 1. List all subdirectories under `extensions/` (e.g., `extensions/security/`, `extensions/compliance/`)\n-2. Load every `.md` file found within those subdirectories\n-3. Each extension file defines its own verification criteria and enforcement rules as cross-cutting constraints\n+2. In each subdirectory, load ONLY `*.opt-in.md` files \u2014 these contain the extension's opt-in prompt. The corresponding rules file is derived by convention: strip the `.opt-in.md` suffix and append `.md` (e.g., `security-baseline.opt-in.md` \u2192 `security-baseline.md`)\n+3. Do NOT load full rule files (e.g., `security-baseline.md`) at this stage\n \n-**Enforcement**:\n+**Deferred Rule Loading**:\n+- During Requirements Analysis, opt-in prompts from the loaded `*.opt-in.md` files are presented to the user\n+- When the user opts IN for an extension, load the corresponding rules file (derived by naming convention) at that point\n+- When the user opts OUT, the full rules file is never loaded \u2014 saving context\n+- Extensions without a matching `*.opt-in.md` file are always enforced \u2014 load their rule files immediately at workflow start\n+\n+**Enforcement** (applies only to loaded/enabled extensions):",
        "comment_created_at": "2026-03-09T21:58:41+00:00",
        "comment_author": "scottschreckengaust",
        "comment_body": "1. Is there only one `*.opt-in.md` per directory and only one associated rules file?\n2. Are conditional enforcement AND opt-in compatible within a directory?\n\n```tree\n.\n\u2514\u2500\u2500 extensions\n \u00a0\u00a0 \u251c\u2500\u2500 new\n      \u00a0 \u251c\u2500\u2500 example-001.md\n      \u00a0 \u251c\u2500\u2500 example-001.opt-in.md\n      \u00a0 \u251c\u2500\u2500 example-002.md\n      \u00a0 \u251c\u2500\u2500 example-002.opt-in.md\n      \u00a0 \u2514\u2500\u2500 example-003.md\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "2908419090",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 111,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2908153782",
        "commented_code": "@@ -25,22 +25,28 @@ All subsequent rule detail file references (e.g., `common/process-overview.md`,\n - Load `common/question-format-guide.md` for question formatting rules\n - Reference these throughout the workflow execution\n \n-## MANDATORY: Extensions Loading\n-**CRITICAL**: At workflow start, scan the `extensions/` directory recursively for all `.md` files. These are extension rule files that apply as cross-cutting constraints across the entire workflow.\n+## MANDATORY: Extensions Loading (Context-Optimized)\n+**CRITICAL**: At workflow start, scan the `extensions/` directory recursively but load ONLY lightweight opt-in files \u2014 NOT full rule files. Full rule files are loaded on-demand after the user opts in.\n \n **Loading process**:\n 1. List all subdirectories under `extensions/` (e.g., `extensions/security/`, `extensions/compliance/`)\n-2. Load every `.md` file found within those subdirectories\n-3. Each extension file defines its own verification criteria and enforcement rules as cross-cutting constraints\n+2. In each subdirectory, load ONLY `*.opt-in.md` files \u2014 these contain the extension's opt-in prompt. The corresponding rules file is derived by convention: strip the `.opt-in.md` suffix and append `.md` (e.g., `security-baseline.opt-in.md` \u2192 `security-baseline.md`)\n+3. Do NOT load full rule files (e.g., `security-baseline.md`) at this stage\n \n-**Enforcement**:\n+**Deferred Rule Loading**:\n+- During Requirements Analysis, opt-in prompts from the loaded `*.opt-in.md` files are presented to the user\n+- When the user opts IN for an extension, load the corresponding rules file (derived by naming convention) at that point\n+- When the user opts OUT, the full rules file is never loaded \u2014 saving context\n+- Extensions without a matching `*.opt-in.md` file are always enforced \u2014 load their rule files immediately at workflow start\n+\n+**Enforcement** (applies only to loaded/enabled extensions):",
        "comment_created_at": "2026-03-09T23:13:33+00:00",
        "comment_author": "raj-jain-aws",
        "comment_body": "1. No. The pairing is through name match. That's how you can have multiple extensions in a single directory.\r\n\r\n2. Not following this question. Pls. clarify",
        "pr_file_module": null
      },
      {
        "comment_id": "2913304599",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 111,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2908153782",
        "commented_code": "@@ -25,22 +25,28 @@ All subsequent rule detail file references (e.g., `common/process-overview.md`,\n - Load `common/question-format-guide.md` for question formatting rules\n - Reference these throughout the workflow execution\n \n-## MANDATORY: Extensions Loading\n-**CRITICAL**: At workflow start, scan the `extensions/` directory recursively for all `.md` files. These are extension rule files that apply as cross-cutting constraints across the entire workflow.\n+## MANDATORY: Extensions Loading (Context-Optimized)\n+**CRITICAL**: At workflow start, scan the `extensions/` directory recursively but load ONLY lightweight opt-in files \u2014 NOT full rule files. Full rule files are loaded on-demand after the user opts in.\n \n **Loading process**:\n 1. List all subdirectories under `extensions/` (e.g., `extensions/security/`, `extensions/compliance/`)\n-2. Load every `.md` file found within those subdirectories\n-3. Each extension file defines its own verification criteria and enforcement rules as cross-cutting constraints\n+2. In each subdirectory, load ONLY `*.opt-in.md` files \u2014 these contain the extension's opt-in prompt. The corresponding rules file is derived by convention: strip the `.opt-in.md` suffix and append `.md` (e.g., `security-baseline.opt-in.md` \u2192 `security-baseline.md`)\n+3. Do NOT load full rule files (e.g., `security-baseline.md`) at this stage\n \n-**Enforcement**:\n+**Deferred Rule Loading**:\n+- During Requirements Analysis, opt-in prompts from the loaded `*.opt-in.md` files are presented to the user\n+- When the user opts IN for an extension, load the corresponding rules file (derived by naming convention) at that point\n+- When the user opts OUT, the full rules file is never loaded \u2014 saving context\n+- Extensions without a matching `*.opt-in.md` file are always enforced \u2014 load their rule files immediately at workflow start\n+\n+**Enforcement** (applies only to loaded/enabled extensions):",
        "comment_created_at": "2026-03-10T17:23:33+00:00",
        "comment_author": "scottschreckengaust",
        "comment_body": "The second question:\r\n\r\n> 2. Are conditional enforcement AND opt-in compatible within a directory?\r\n\r\nis to clarify my assumption that an extension can have both opt-in rules as well as those that are always enforced within the same directory. The `new` example directory above has three example extensions: two with a `*.opt-in.md` pattern (that only load their pair when opted in) and one without (that is always loaded).",
        "pr_file_module": null
      },
      {
        "comment_id": "2915913962",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 111,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2908153782",
        "commented_code": "@@ -25,22 +25,28 @@ All subsequent rule detail file references (e.g., `common/process-overview.md`,\n - Load `common/question-format-guide.md` for question formatting rules\n - Reference these throughout the workflow execution\n \n-## MANDATORY: Extensions Loading\n-**CRITICAL**: At workflow start, scan the `extensions/` directory recursively for all `.md` files. These are extension rule files that apply as cross-cutting constraints across the entire workflow.\n+## MANDATORY: Extensions Loading (Context-Optimized)\n+**CRITICAL**: At workflow start, scan the `extensions/` directory recursively but load ONLY lightweight opt-in files \u2014 NOT full rule files. Full rule files are loaded on-demand after the user opts in.\n \n **Loading process**:\n 1. List all subdirectories under `extensions/` (e.g., `extensions/security/`, `extensions/compliance/`)\n-2. Load every `.md` file found within those subdirectories\n-3. Each extension file defines its own verification criteria and enforcement rules as cross-cutting constraints\n+2. In each subdirectory, load ONLY `*.opt-in.md` files \u2014 these contain the extension's opt-in prompt. The corresponding rules file is derived by convention: strip the `.opt-in.md` suffix and append `.md` (e.g., `security-baseline.opt-in.md` \u2192 `security-baseline.md`)\n+3. Do NOT load full rule files (e.g., `security-baseline.md`) at this stage\n \n-**Enforcement**:\n+**Deferred Rule Loading**:\n+- During Requirements Analysis, opt-in prompts from the loaded `*.opt-in.md` files are presented to the user\n+- When the user opts IN for an extension, load the corresponding rules file (derived by naming convention) at that point\n+- When the user opts OUT, the full rules file is never loaded \u2014 saving context\n+- Extensions without a matching `*.opt-in.md` file are always enforced \u2014 load their rule files immediately at workflow start\n+\n+**Enforcement** (applies only to loaded/enabled extensions):",
        "comment_created_at": "2026-03-11T05:03:14+00:00",
        "comment_author": "raj-jain-aws",
        "comment_body": "Good question. Tested the scenario you mentioned @scottschreckengaust and found that it is working fine. See the test setup and results in the screenshot below. \r\n\r\n \r\n<img width=\"986\" height=\"591\" alt=\"image\" src=\"https://github.com/user-attachments/assets/3268991e-2141-4dc4-9dca-17c073fed1e1\" />\r\n\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2785061497",
    "pr_number": 56,
    "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
    "created_at": "2026-02-09T23:39:03+00:00",
    "commented_code": "- **MANDATORY**: Record every user response with timestamp after receiving it\n - **CRITICAL**: ALWAYS append changes to EDIT audit.md file, NEVER use tools and commands that completely overwrite its contents\n - **CRITICAL**: Using file writing tools and commands that overwrite contents of the entire audit.md and cause duplication\n-- Use ISO 8601 format for timestamps (YYYY-MM-DDTHH:MM:SSZ)\n+- When adding an entry to audit.md:\n+  1. Run a system command to get the current timestamp:\n+     - **macOS/Linux**: `date +\"%Y-%m-%dT%H:%M:%S%z\"`\n+     - **Windows (PowerShell)**: `Get-Date -Format \"yyyy-MM-ddTHH:mm:sszzz\"`",
    "repo_full_name": "awslabs/aidlc-workflows",
    "discussion_comments": [
      {
        "comment_id": "2785061497",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 56,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2785061497",
        "commented_code": "@@ -451,7 +451,12 @@ The Operations stage will eventually include:\n - **MANDATORY**: Record every user response with timestamp after receiving it\n - **CRITICAL**: ALWAYS append changes to EDIT audit.md file, NEVER use tools and commands that completely overwrite its contents\n - **CRITICAL**: Using file writing tools and commands that overwrite contents of the entire audit.md and cause duplication\n-- Use ISO 8601 format for timestamps (YYYY-MM-DDTHH:MM:SSZ)\n+- When adding an entry to audit.md:\n+  1. Run a system command to get the current timestamp:\n+     - **macOS/Linux**: `date +\"%Y-%m-%dT%H:%M:%S%z\"`\n+     - **Windows (PowerShell)**: `Get-Date -Format \"yyyy-MM-ddTHH:mm:sszzz\"`",
        "comment_created_at": "2026-02-09T23:39:03+00:00",
        "comment_author": "raj-jain-aws",
        "comment_body": "This should help. One more request - we've run into situations where the Windows users don't have access to PowerShell (security enforcement in the org). Can we offer a Windows CMD variant also?",
        "pr_file_module": null
      },
      {
        "comment_id": "2785107429",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 56,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2785061497",
        "commented_code": "@@ -451,7 +451,12 @@ The Operations stage will eventually include:\n - **MANDATORY**: Record every user response with timestamp after receiving it\n - **CRITICAL**: ALWAYS append changes to EDIT audit.md file, NEVER use tools and commands that completely overwrite its contents\n - **CRITICAL**: Using file writing tools and commands that overwrite contents of the entire audit.md and cause duplication\n-- Use ISO 8601 format for timestamps (YYYY-MM-DDTHH:MM:SSZ)\n+- When adding an entry to audit.md:\n+  1. Run a system command to get the current timestamp:\n+     - **macOS/Linux**: `date +\"%Y-%m-%dT%H:%M:%S%z\"`\n+     - **Windows (PowerShell)**: `Get-Date -Format \"yyyy-MM-ddTHH:mm:sszzz\"`",
        "comment_created_at": "2026-02-09T23:55:41+00:00",
        "comment_author": "jimini55",
        "comment_body": "@harmjeff - Could you help me on this? ",
        "pr_file_module": null
      },
      {
        "comment_id": "2785174496",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 56,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2785061497",
        "commented_code": "@@ -451,7 +451,12 @@ The Operations stage will eventually include:\n - **MANDATORY**: Record every user response with timestamp after receiving it\n - **CRITICAL**: ALWAYS append changes to EDIT audit.md file, NEVER use tools and commands that completely overwrite its contents\n - **CRITICAL**: Using file writing tools and commands that overwrite contents of the entire audit.md and cause duplication\n-- Use ISO 8601 format for timestamps (YYYY-MM-DDTHH:MM:SSZ)\n+- When adding an entry to audit.md:\n+  1. Run a system command to get the current timestamp:\n+     - **macOS/Linux**: `date +\"%Y-%m-%dT%H:%M:%S%z\"`\n+     - **Windows (PowerShell)**: `Get-Date -Format \"yyyy-MM-ddTHH:mm:sszzz\"`",
        "comment_created_at": "2026-02-10T00:21:40+00:00",
        "comment_author": "harmjeff",
        "comment_body": "`echo %date:~10,4%-%date:~4,2%-%date:~7,2%T%time:~0,2%:%time:~3,2%:%time:~6,2%%time:~9,2%`",
        "pr_file_module": null
      },
      {
        "comment_id": "2785210826",
        "repo_full_name": "awslabs/aidlc-workflows",
        "pr_number": 56,
        "pr_file": "aidlc-rules/aws-aidlc-rules/core-workflow.md",
        "discussion_id": "2785061497",
        "commented_code": "@@ -451,7 +451,12 @@ The Operations stage will eventually include:\n - **MANDATORY**: Record every user response with timestamp after receiving it\n - **CRITICAL**: ALWAYS append changes to EDIT audit.md file, NEVER use tools and commands that completely overwrite its contents\n - **CRITICAL**: Using file writing tools and commands that overwrite contents of the entire audit.md and cause duplication\n-- Use ISO 8601 format for timestamps (YYYY-MM-DDTHH:MM:SSZ)\n+- When adding an entry to audit.md:\n+  1. Run a system command to get the current timestamp:\n+     - **macOS/Linux**: `date +\"%Y-%m-%dT%H:%M:%S%z\"`\n+     - **Windows (PowerShell)**: `Get-Date -Format \"yyyy-MM-ddTHH:mm:sszzz\"`",
        "comment_created_at": "2026-02-10T00:35:41+00:00",
        "comment_author": "jimini55",
        "comment_body": "Done",
        "pr_file_module": null
      }
    ]
  }
]