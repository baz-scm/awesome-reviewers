[
  {
    "discussion_id": "2584456838",
    "pr_number": 1080,
    "pr_file": "tests/lib/test_bedrock.py",
    "created_at": "2025-12-03T10:07:59+00:00",
    "commented_code": "client = AnthropicBedrock()\n \n     assert client.aws_region == next(profile for profile in profiles if profile[\"name\"] == aws_profile)[\"region\"]\n+\n+\n+@pytest.mark.respx()\n+def test_bearer_token_env(monkeypatch: t.Any, respx_mock: MockRouter) -> None:\n+    respx_mock.post(re.compile(r\"https://bedrock-runtime\\.us-east-1\\.amazonaws\\.com/model/.*/invoke\")).mock(\n+        return_value=httpx.Response(200, json={\"foo\": \"bar\"})\n+    )\n+\n+    monkeypatch.setenv(\"AWS_BEARER_TOKEN_BEDROCK\", \"test-bearer-token\")\n+\n+    sync_client.messages.create(\n+        max_tokens=1024,\n+        messages=[\n+            {\n+                \"role\": \"user\",\n+                \"content\": \"Say hello there!\",\n+            }\n+        ],\n+        model=\"anthropic.claude-3-5-sonnet-20241022-v2:0\",\n+        extra_headers={\"Custom-Header\": \"CustomValue\"},\n+    )\n+    calls = cast(\"list[MockRequestCall]\", respx_mock.calls)\n+    assert len(calls) == 1\n+    auth_header = calls[0].request.headers.get(\"Authorization\")\n+    assert auth_header == \"Bearer test-bearer-token\"\n+\n+    # Verify that custom headers are preserved\n+    custom_header = calls[0].request.headers.get(\"Custom-Header\")\n+    assert custom_header == \"CustomValue\"",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2584456838",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1080,
        "pr_file": "tests/lib/test_bedrock.py",
        "discussion_id": "2584456838",
        "commented_code": "@@ -195,3 +195,32 @@ def test_region_infer_from_specified_profile(\n     client = AnthropicBedrock()\n \n     assert client.aws_region == next(profile for profile in profiles if profile[\"name\"] == aws_profile)[\"region\"]\n+\n+\n+@pytest.mark.respx()\n+def test_bearer_token_env(monkeypatch: t.Any, respx_mock: MockRouter) -> None:\n+    respx_mock.post(re.compile(r\"https://bedrock-runtime\\.us-east-1\\.amazonaws\\.com/model/.*/invoke\")).mock(\n+        return_value=httpx.Response(200, json={\"foo\": \"bar\"})\n+    )\n+\n+    monkeypatch.setenv(\"AWS_BEARER_TOKEN_BEDROCK\", \"test-bearer-token\")\n+\n+    sync_client.messages.create(\n+        max_tokens=1024,\n+        messages=[\n+            {\n+                \"role\": \"user\",\n+                \"content\": \"Say hello there!\",\n+            }\n+        ],\n+        model=\"anthropic.claude-3-5-sonnet-20241022-v2:0\",\n+        extra_headers={\"Custom-Header\": \"CustomValue\"},\n+    )\n+    calls = cast(\"list[MockRequestCall]\", respx_mock.calls)\n+    assert len(calls) == 1\n+    auth_header = calls[0].request.headers.get(\"Authorization\")\n+    assert auth_header == \"Bearer test-bearer-token\"\n+\n+    # Verify that custom headers are preserved\n+    custom_header = calls[0].request.headers.get(\"Custom-Header\")\n+    assert custom_header == \"CustomValue\"",
        "comment_created_at": "2025-12-03T10:07:59+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "Do we actually need to test this? If so, maybe adding an `X-` prefix would make it less confusing, as custom headers without that prefix are getting stripped out by proxy servers. I'd basically recommend just removing it and only testing that the Bearer header was passed.",
        "pr_file_module": null
      },
      {
        "comment_id": "2594457555",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1080,
        "pr_file": "tests/lib/test_bedrock.py",
        "discussion_id": "2584456838",
        "commented_code": "@@ -195,3 +195,32 @@ def test_region_infer_from_specified_profile(\n     client = AnthropicBedrock()\n \n     assert client.aws_region == next(profile for profile in profiles if profile[\"name\"] == aws_profile)[\"region\"]\n+\n+\n+@pytest.mark.respx()\n+def test_bearer_token_env(monkeypatch: t.Any, respx_mock: MockRouter) -> None:\n+    respx_mock.post(re.compile(r\"https://bedrock-runtime\\.us-east-1\\.amazonaws\\.com/model/.*/invoke\")).mock(\n+        return_value=httpx.Response(200, json={\"foo\": \"bar\"})\n+    )\n+\n+    monkeypatch.setenv(\"AWS_BEARER_TOKEN_BEDROCK\", \"test-bearer-token\")\n+\n+    sync_client.messages.create(\n+        max_tokens=1024,\n+        messages=[\n+            {\n+                \"role\": \"user\",\n+                \"content\": \"Say hello there!\",\n+            }\n+        ],\n+        model=\"anthropic.claude-3-5-sonnet-20241022-v2:0\",\n+        extra_headers={\"Custom-Header\": \"CustomValue\"},\n+    )\n+    calls = cast(\"list[MockRequestCall]\", respx_mock.calls)\n+    assert len(calls) == 1\n+    auth_header = calls[0].request.headers.get(\"Authorization\")\n+    assert auth_header == \"Bearer test-bearer-token\"\n+\n+    # Verify that custom headers are preserved\n+    custom_header = calls[0].request.headers.get(\"Custom-Header\")\n+    assert custom_header == \"CustomValue\"",
        "comment_created_at": "2025-12-06T02:59:01+00:00",
        "comment_author": "davidmhewitt",
        "comment_body": "Sure, removed.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2854666704",
    "pr_number": 1150,
    "pr_file": "src/anthropic/lib/tools/_beta_runner.py",
    "created_at": "2026-02-25T18:28:08+00:00",
    "commented_code": "message = await self._get_last_message()\n                 assert message is not None\n \n+                # Update container from response for programmatic tool calling support\n+                if message.container is not None:",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2854666704",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1150,
        "pr_file": "src/anthropic/lib/tools/_beta_runner.py",
        "discussion_id": "2854666704",
        "commented_code": "@@ -497,6 +501,10 @@ async def __run__(self) -> AsyncIterator[RunnerItemT]:\n                 message = await self._get_last_message()\n                 assert message is not None\n \n+                # Update container from response for programmatic tool calling support\n+                if message.container is not None:",
        "comment_created_at": "2026-02-25T18:28:08+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "Can we add a simple test for this case to make sure we\u2019re sending container.id back? We can use a snapshot with a real example and record it, like we do in the other tests.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2602510153",
    "pr_number": 1092,
    "pr_file": "tests/lib/utils.py",
    "created_at": "2025-12-09T12:49:04+00:00",
    "commented_code": "# e.g. `GenericModel[Location]` -> `GenericModel`\n         return self.__class__.__name__.split(\"[\", maxsplit=1)[0]\n \n-    with monkeypatch.context() as m:\n+    with pytest.MonkeyPatch.context() as m:",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2602510153",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1092,
        "pr_file": "tests/lib/utils.py",
        "discussion_id": "2602510153",
        "commented_code": "@@ -28,7 +31,7 @@ def __repr_name__(self: pydantic.BaseModel) -> str:\n         # e.g. `GenericModel[Location]` -> `GenericModel`\n         return self.__class__.__name__.split(\"[\", maxsplit=1)[0]\n \n-    with monkeypatch.context() as m:\n+    with pytest.MonkeyPatch.context() as m:",
        "comment_created_at": "2025-12-09T12:49:04+00:00",
        "comment_author": "RobertCraigie",
        "comment_body": "q: does this still work as a test-scoped patch? I did the forward `monkeypatch` setup before to make sure any patches are cleaned up properly; If this works too that would be cool.",
        "pr_file_module": null
      },
      {
        "comment_id": "2602742451",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1092,
        "pr_file": "tests/lib/utils.py",
        "discussion_id": "2602510153",
        "commented_code": "@@ -28,7 +31,7 @@ def __repr_name__(self: pydantic.BaseModel) -> str:\n         # e.g. `GenericModel[Location]` -> `GenericModel`\n         return self.__class__.__name__.split(\"[\", maxsplit=1)[0]\n \n-    with monkeypatch.context() as m:\n+    with pytest.MonkeyPatch.context() as m:",
        "comment_created_at": "2025-12-09T13:44:42+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "> I did the forward monkeypatch setup before to make sure any patches are cleaned up properly\r\n\r\nIt cleans up immediately after exiting the context manager, in both cases",
        "pr_file_module": null
      },
      {
        "comment_id": "2620793106",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1092,
        "pr_file": "tests/lib/utils.py",
        "discussion_id": "2602510153",
        "commented_code": "@@ -28,7 +31,7 @@ def __repr_name__(self: pydantic.BaseModel) -> str:\n         # e.g. `GenericModel[Location]` -> `GenericModel`\n         return self.__class__.__name__.split(\"[\", maxsplit=1)[0]\n \n-    with monkeypatch.context() as m:\n+    with pytest.MonkeyPatch.context() as m:",
        "comment_created_at": "2025-12-15T20:44:43+00:00",
        "comment_author": "RobertCraigie",
        "comment_body": "oh very nice, I missed the context manager \ud83e\udd26 ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2555457628",
    "pr_number": 1070,
    "pr_file": "tests/lib/streaming/test_extra_fields.py",
    "created_at": "2025-11-24T09:42:40+00:00",
    "commented_code": "+\"\"\"Tests for accumulating extra fields in streaming responses.\n+\n+This tests that pydantic extra fields (fields not in the schema) are properly\n+accumulated during streaming, without exposing specific field names in the SDK.\n+\"\"\"\n+\n+from __future__ import annotations\n+\n+import os\n+import asyncio\n+from typing import Any, cast\n+\n+import httpx\n+import respx\n+\n+from anthropic import Anthropic, AsyncAnthropic\n+\n+from .helpers import get_response, to_async_iter\n+\n+base_url = os.environ.get(\"TEST_API_BASE_URL\", \"http://127.0.0.1:4010\")\n+api_key = \"my-anthropic-api-key\"\n+\n+sync_client = Anthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)\n+async_client = AsyncAnthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2555457628",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1070,
        "pr_file": "tests/lib/streaming/test_extra_fields.py",
        "discussion_id": "2555457628",
        "commented_code": "@@ -0,0 +1,149 @@\n+\"\"\"Tests for accumulating extra fields in streaming responses.\n+\n+This tests that pydantic extra fields (fields not in the schema) are properly\n+accumulated during streaming, without exposing specific field names in the SDK.\n+\"\"\"\n+\n+from __future__ import annotations\n+\n+import os\n+import asyncio\n+from typing import Any, cast\n+\n+import httpx\n+import respx\n+\n+from anthropic import Anthropic, AsyncAnthropic\n+\n+from .helpers import get_response, to_async_iter\n+\n+base_url = os.environ.get(\"TEST_API_BASE_URL\", \"http://127.0.0.1:4010\")\n+api_key = \"my-anthropic-api-key\"\n+\n+sync_client = Anthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)\n+async_client = AsyncAnthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)",
        "comment_created_at": "2025-11-24T09:42:40+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "nit: we should use fixtures instead of global clients",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2559230763",
    "pr_number": 1070,
    "pr_file": "tests/lib/streaming/test_extra_fields.py",
    "created_at": "2025-11-25T09:24:40+00:00",
    "commented_code": "+\"\"\"Tests for accumulating extra fields in streaming responses.\n+\n+This tests that pydantic extra fields (fields not in the schema) are properly\n+accumulated during streaming, without exposing specific field names in the SDK.\n+\"\"\"\n+\n+from __future__ import annotations\n+\n+from typing import Any, cast\n+\n+from anthropic.types import Usage, Message, TextBlock, TextDelta\n+from anthropic._compat import PYDANTIC_V1\n+from anthropic.lib.streaming._messages import accumulate_event\n+from anthropic.types.message_delta_usage import MessageDeltaUsage\n+from anthropic.types.raw_message_delta_event import Delta, RawMessageDeltaEvent\n+from anthropic.types.raw_message_start_event import RawMessageStartEvent\n+from anthropic.types.raw_content_block_delta_event import RawContentBlockDeltaEvent\n+from anthropic.types.raw_content_block_start_event import RawContentBlockStartEvent\n+\n+\n+class TestExtraFieldsAccumulation:\n+    def test_extra_fields_accumulation(self) -> None:\n+        \"\"\"Test that extra fields are accumulated across streaming events.\"\"\"\n+        # Build message with extra field via message_start\n+        message_start = RawMessageStartEvent(\n+            type=\"message_start\",\n+            message=Message(\n+                id=\"msg_123\",\n+                type=\"message\",\n+                role=\"assistant\",\n+                content=[],\n+                model=\"claude-3-opus-latest\",\n+                stop_reason=None,\n+                stop_sequence=None,\n+                usage=Usage(input_tokens=11, output_tokens=1),\n+                # Extra field with nested structure\n+                private_field={\"nested\": {\"values\": [1, 2]}},  # type: ignore[call-arg]\n+            ),\n+        )\n+        snapshot = accumulate_event(event=message_start, current_snapshot=None)\n+\n+        # content_block_start\n+        content_block_start = RawContentBlockStartEvent(\n+            type=\"content_block_start\",\n+            index=0,\n+            content_block=TextBlock(type=\"text\", text=\"\"),\n+        )\n+        snapshot = accumulate_event(event=content_block_start, current_snapshot=snapshot)\n+\n+        # First content_block_delta with extra field\n+        delta1 = RawContentBlockDeltaEvent(\n+            type=\"content_block_delta\",\n+            index=0,\n+            delta=TextDelta(type=\"text_delta\", text=\"Hello\"),\n+            private_field={\"nested\": {\"values\": [3], \"metadata\": \"chunk1\"}},  # type: ignore[call-arg]\n+        )\n+        snapshot = accumulate_event(event=delta1, current_snapshot=snapshot)\n+\n+        # Second content_block_delta with extra field\n+        delta2 = RawContentBlockDeltaEvent(\n+            type=\"content_block_delta\",\n+            index=0,\n+            delta=TextDelta(type=\"text_delta\", text=\"!\"),\n+            private_field={\"nested\": {\"values\": [4, 5], \"metadata\": \"chunk2\"}},  # type: ignore[call-arg]\n+        )\n+        snapshot = accumulate_event(event=delta2, current_snapshot=snapshot)\n+\n+        # message_delta with extra field\n+        message_delta = RawMessageDeltaEvent(\n+            type=\"message_delta\",\n+            delta=Delta(stop_reason=\"end_turn\", stop_sequence=None),\n+            usage=MessageDeltaUsage(output_tokens=3),\n+            private_field={\"nested\": {\"values\": [6]}},  # type: ignore[call-arg]\n+        )\n+        snapshot = accumulate_event(event=message_delta, current_snapshot=snapshot)\n+\n+        # This feature requires Pydantic v2\n+        if PYDANTIC_V1:\n+            return",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2559230763",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1070,
        "pr_file": "tests/lib/streaming/test_extra_fields.py",
        "discussion_id": "2559230763",
        "commented_code": "@@ -0,0 +1,128 @@\n+\"\"\"Tests for accumulating extra fields in streaming responses.\n+\n+This tests that pydantic extra fields (fields not in the schema) are properly\n+accumulated during streaming, without exposing specific field names in the SDK.\n+\"\"\"\n+\n+from __future__ import annotations\n+\n+from typing import Any, cast\n+\n+from anthropic.types import Usage, Message, TextBlock, TextDelta\n+from anthropic._compat import PYDANTIC_V1\n+from anthropic.lib.streaming._messages import accumulate_event\n+from anthropic.types.message_delta_usage import MessageDeltaUsage\n+from anthropic.types.raw_message_delta_event import Delta, RawMessageDeltaEvent\n+from anthropic.types.raw_message_start_event import RawMessageStartEvent\n+from anthropic.types.raw_content_block_delta_event import RawContentBlockDeltaEvent\n+from anthropic.types.raw_content_block_start_event import RawContentBlockStartEvent\n+\n+\n+class TestExtraFieldsAccumulation:\n+    def test_extra_fields_accumulation(self) -> None:\n+        \"\"\"Test that extra fields are accumulated across streaming events.\"\"\"\n+        # Build message with extra field via message_start\n+        message_start = RawMessageStartEvent(\n+            type=\"message_start\",\n+            message=Message(\n+                id=\"msg_123\",\n+                type=\"message\",\n+                role=\"assistant\",\n+                content=[],\n+                model=\"claude-3-opus-latest\",\n+                stop_reason=None,\n+                stop_sequence=None,\n+                usage=Usage(input_tokens=11, output_tokens=1),\n+                # Extra field with nested structure\n+                private_field={\"nested\": {\"values\": [1, 2]}},  # type: ignore[call-arg]\n+            ),\n+        )\n+        snapshot = accumulate_event(event=message_start, current_snapshot=None)\n+\n+        # content_block_start\n+        content_block_start = RawContentBlockStartEvent(\n+            type=\"content_block_start\",\n+            index=0,\n+            content_block=TextBlock(type=\"text\", text=\"\"),\n+        )\n+        snapshot = accumulate_event(event=content_block_start, current_snapshot=snapshot)\n+\n+        # First content_block_delta with extra field\n+        delta1 = RawContentBlockDeltaEvent(\n+            type=\"content_block_delta\",\n+            index=0,\n+            delta=TextDelta(type=\"text_delta\", text=\"Hello\"),\n+            private_field={\"nested\": {\"values\": [3], \"metadata\": \"chunk1\"}},  # type: ignore[call-arg]\n+        )\n+        snapshot = accumulate_event(event=delta1, current_snapshot=snapshot)\n+\n+        # Second content_block_delta with extra field\n+        delta2 = RawContentBlockDeltaEvent(\n+            type=\"content_block_delta\",\n+            index=0,\n+            delta=TextDelta(type=\"text_delta\", text=\"!\"),\n+            private_field={\"nested\": {\"values\": [4, 5], \"metadata\": \"chunk2\"}},  # type: ignore[call-arg]\n+        )\n+        snapshot = accumulate_event(event=delta2, current_snapshot=snapshot)\n+\n+        # message_delta with extra field\n+        message_delta = RawMessageDeltaEvent(\n+            type=\"message_delta\",\n+            delta=Delta(stop_reason=\"end_turn\", stop_sequence=None),\n+            usage=MessageDeltaUsage(output_tokens=3),\n+            private_field={\"nested\": {\"values\": [6]}},  # type: ignore[call-arg]\n+        )\n+        snapshot = accumulate_event(event=message_delta, current_snapshot=snapshot)\n+\n+        # This feature requires Pydantic v2\n+        if PYDANTIC_V1:\n+            return",
        "comment_created_at": "2025-11-25T09:24:40+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "we should use `pytest.mark.skipif` if we want to skip the test for some cases, like [here](https://github.com/evanmiller-anthropic/anthropic-sdk-python/blob/829486182c91de312d80cdc5dd1032d654447e37/tests/test_models.py#L836)",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2137553389",
    "pr_number": 969,
    "pr_file": "src/anthropic/_base_client.py",
    "created_at": "2025-06-10T10:49:34+00:00",
    "commented_code": "if TCP_KEEPIDLE is not None:\n                 socket_options.append((socket.IPPROTO_TCP, TCP_KEEPIDLE, 60))\n \n-            kwargs[\"transport\"] = httpx.AsyncHTTPTransport(\n-                # note: limits is always set above\n-                limits=kwargs[\"limits\"],\n-                socket_options=socket_options,\n-            )\n+            proxy_map = {key: None if url is None else Proxy(url=url) for key, url in get_environment_proxies().items()}\n+\n+            transport_kwargs = {\n+                arg: kwargs[arg] for arg in (\"verify\", \"cert\", \"trust_env\", \"http1\", \"http2\", \"limits\") if arg in kwargs",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2137553389",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 969,
        "pr_file": "src/anthropic/_base_client.py",
        "discussion_id": "2137553389",
        "commented_code": "@@ -1367,11 +1380,23 @@ def __init__(self, **kwargs: Any) -> None:\n             if TCP_KEEPIDLE is not None:\n                 socket_options.append((socket.IPPROTO_TCP, TCP_KEEPIDLE, 60))\n \n-            kwargs[\"transport\"] = httpx.AsyncHTTPTransport(\n-                # note: limits is always set above\n-                limits=kwargs[\"limits\"],\n-                socket_options=socket_options,\n-            )\n+            proxy_map = {key: None if url is None else Proxy(url=url) for key, url in get_environment_proxies().items()}\n+\n+            transport_kwargs = {\n+                arg: kwargs[arg] for arg in (\"verify\", \"cert\", \"trust_env\", \"http1\", \"http2\", \"limits\") if arg in kwargs",
        "comment_created_at": "2025-06-10T10:49:34+00:00",
        "comment_author": "RobertCraigie",
        "comment_body": "would it be feasible to add unit tests for all of these args as well?",
        "pr_file_module": null
      },
      {
        "comment_id": "2137632746",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 969,
        "pr_file": "src/anthropic/_base_client.py",
        "discussion_id": "2137553389",
        "commented_code": "@@ -1367,11 +1380,23 @@ def __init__(self, **kwargs: Any) -> None:\n             if TCP_KEEPIDLE is not None:\n                 socket_options.append((socket.IPPROTO_TCP, TCP_KEEPIDLE, 60))\n \n-            kwargs[\"transport\"] = httpx.AsyncHTTPTransport(\n-                # note: limits is always set above\n-                limits=kwargs[\"limits\"],\n-                socket_options=socket_options,\n-            )\n+            proxy_map = {key: None if url is None else Proxy(url=url) for key, url in get_environment_proxies().items()}\n+\n+            transport_kwargs = {\n+                arg: kwargs[arg] for arg in (\"verify\", \"cert\", \"trust_env\", \"http1\", \"http2\", \"limits\") if arg in kwargs",
        "comment_created_at": "2025-06-10T11:21:46+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "I guess it's the same problem here. The transport doesn't expose any attributes, so we could write some references to the internal variables if it's worth it. But I think we can avoid that because we are just testing here whether the arguments were correctly passed to httpx.",
        "pr_file_module": null
      },
      {
        "comment_id": "2137642661",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 969,
        "pr_file": "src/anthropic/_base_client.py",
        "discussion_id": "2137553389",
        "commented_code": "@@ -1367,11 +1380,23 @@ def __init__(self, **kwargs: Any) -> None:\n             if TCP_KEEPIDLE is not None:\n                 socket_options.append((socket.IPPROTO_TCP, TCP_KEEPIDLE, 60))\n \n-            kwargs[\"transport\"] = httpx.AsyncHTTPTransport(\n-                # note: limits is always set above\n-                limits=kwargs[\"limits\"],\n-                socket_options=socket_options,\n-            )\n+            proxy_map = {key: None if url is None else Proxy(url=url) for key, url in get_environment_proxies().items()}\n+\n+            transport_kwargs = {\n+                arg: kwargs[arg] for arg in (\"verify\", \"cert\", \"trust_env\", \"http1\", \"http2\", \"limits\") if arg in kwargs",
        "comment_created_at": "2025-06-10T11:27:56+00:00",
        "comment_author": "RobertCraigie",
        "comment_body": "maybe the best tests here would just be 2 tests like this?\r\n1. you can pass the arguments without any errors happening\r\n2. mock the transport class to just make sure the arguments are passed to it?\r\n\r\nI think #2 could be nice to have but #1 would provide the most value imo",
        "pr_file_module": null
      },
      {
        "comment_id": "2137753399",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 969,
        "pr_file": "src/anthropic/_base_client.py",
        "discussion_id": "2137553389",
        "commented_code": "@@ -1367,11 +1380,23 @@ def __init__(self, **kwargs: Any) -> None:\n             if TCP_KEEPIDLE is not None:\n                 socket_options.append((socket.IPPROTO_TCP, TCP_KEEPIDLE, 60))\n \n-            kwargs[\"transport\"] = httpx.AsyncHTTPTransport(\n-                # note: limits is always set above\n-                limits=kwargs[\"limits\"],\n-                socket_options=socket_options,\n-            )\n+            proxy_map = {key: None if url is None else Proxy(url=url) for key, url in get_environment_proxies().items()}\n+\n+            transport_kwargs = {\n+                arg: kwargs[arg] for arg in (\"verify\", \"cert\", \"trust_env\", \"http1\", \"http2\", \"limits\") if arg in kwargs",
        "comment_created_at": "2025-06-10T12:22:14+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "Added a simple smoke test to ensure there are no exceptions when providing arguments to the default client. Also suppressed the deprecation warning because httpx deprecated cert and will remove it in the major release.",
        "pr_file_module": null
      }
    ]
  }
]