[
  {
    "discussion_id": "2584435865",
    "pr_number": 1080,
    "pr_file": "tests/lib/test_bedrock.py",
    "created_at": "2025-12-03T10:02:17+00:00",
    "commented_code": "client = AnthropicBedrock()\n \n     assert client.aws_region == next(profile for profile in profiles if profile[\"name\"] == aws_profile)[\"region\"]\n+\n+\n+@pytest.mark.respx()\n+def test_bearer_token_env(monkeypatch: t.Any, respx_mock: MockRouter) -> None:",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2584435865",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1080,
        "pr_file": "tests/lib/test_bedrock.py",
        "discussion_id": "2584435865",
        "commented_code": "@@ -195,3 +195,32 @@ def test_region_infer_from_specified_profile(\n     client = AnthropicBedrock()\n \n     assert client.aws_region == next(profile for profile in profiles if profile[\"name\"] == aws_profile)[\"region\"]\n+\n+\n+@pytest.mark.respx()\n+def test_bearer_token_env(monkeypatch: t.Any, respx_mock: MockRouter) -> None:",
        "comment_created_at": "2025-12-03T10:02:17+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "let's use `pytest.MonkeyPatch` instead of `t.Any`",
        "pr_file_module": null
      },
      {
        "comment_id": "2594456498",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1080,
        "pr_file": "tests/lib/test_bedrock.py",
        "discussion_id": "2584435865",
        "commented_code": "@@ -195,3 +195,32 @@ def test_region_infer_from_specified_profile(\n     client = AnthropicBedrock()\n \n     assert client.aws_region == next(profile for profile in profiles if profile[\"name\"] == aws_profile)[\"region\"]\n+\n+\n+@pytest.mark.respx()\n+def test_bearer_token_env(monkeypatch: t.Any, respx_mock: MockRouter) -> None:",
        "comment_created_at": "2025-12-06T02:56:15+00:00",
        "comment_author": "davidmhewitt",
        "comment_body": "Thanks, done!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2864647405",
    "pr_number": 1124,
    "pr_file": "src/anthropic/lib/tools/_beta_runner.py",
    "created_at": "2026-02-27T14:38:31+00:00",
    "commented_code": null,
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2864647405",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1124,
        "pr_file": "src/anthropic/lib/tools/_beta_runner.py",
        "discussion_id": "2864647405",
        "commented_code": null,
        "comment_created_at": "2026-02-27T14:38:31+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "@fede-kamel do we need to add a cast here? I think it should work with it\n",
        "pr_file_module": null
      },
      {
        "comment_id": "2865070687",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1124,
        "pr_file": "src/anthropic/lib/tools/_beta_runner.py",
        "discussion_id": "2864647405",
        "commented_code": null,
        "comment_created_at": "2026-02-27T16:08:32+00:00",
        "comment_author": "fede-kamel",
        "comment_body": "You're right, the cast was unnecessary - pyright passes without it. Removed it. Thanks for catching that\\!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2555139681",
    "pr_number": 1070,
    "pr_file": "src/anthropic/lib/streaming/_messages.py",
    "created_at": "2025-11-24T08:35:36+00:00",
    "commented_code": ")\n \n \n+def _deep_merge_extra_fields(existing: object, new: object) -> object:\n+    \"\"\"Deep merge new data into existing data, mutating containers in place.\n+\n+    - Dicts: recursively merge keys (mutates existing dict)\n+    - Lists: extend existing with new items (mutates existing list)\n+    - Other: replace with new value\n+    \"\"\"\n+    if isinstance(existing, dict) and isinstance(new, dict):\n+        existing_dict = cast(\"dict[str, object]\", existing)\n+        new_dict = cast(\"dict[str, object]\", new)",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2555139681",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1070,
        "pr_file": "src/anthropic/lib/streaming/_messages.py",
        "discussion_id": "2555139681",
        "commented_code": "@@ -401,6 +401,31 @@ def build_events(\n )\n \n \n+def _deep_merge_extra_fields(existing: object, new: object) -> object:\n+    \"\"\"Deep merge new data into existing data, mutating containers in place.\n+\n+    - Dicts: recursively merge keys (mutates existing dict)\n+    - Lists: extend existing with new items (mutates existing list)\n+    - Other: replace with new value\n+    \"\"\"\n+    if isinstance(existing, dict) and isinstance(new, dict):\n+        existing_dict = cast(\"dict[str, object]\", existing)\n+        new_dict = cast(\"dict[str, object]\", new)",
        "comment_created_at": "2025-11-24T08:35:36+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "nit: we can use `is_dict` utility methods, to avoid cast",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2555141950",
    "pr_number": 1070,
    "pr_file": "src/anthropic/lib/streaming/_messages.py",
    "created_at": "2025-11-24T08:36:08+00:00",
    "commented_code": ")\n \n \n+def _deep_merge_extra_fields(existing: object, new: object) -> object:\n+    \"\"\"Deep merge new data into existing data, mutating containers in place.\n+\n+    - Dicts: recursively merge keys (mutates existing dict)\n+    - Lists: extend existing with new items (mutates existing list)\n+    - Other: replace with new value\n+    \"\"\"\n+    if isinstance(existing, dict) and isinstance(new, dict):\n+        existing_dict = cast(\"dict[str, object]\", existing)\n+        new_dict = cast(\"dict[str, object]\", new)\n+        for key, value in new_dict.items():\n+            if key in existing_dict:\n+                existing_dict[key] = _deep_merge_extra_fields(existing_dict[key], value)\n+            else:\n+                existing_dict[key] = value\n+        return existing_dict  # Return mutated dict\n+    elif isinstance(existing, list) and isinstance(new, list):\n+        existing_list = cast(\"list[object]\", existing)\n+        new_list = cast(\"list[object]\", new)",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2555141950",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1070,
        "pr_file": "src/anthropic/lib/streaming/_messages.py",
        "discussion_id": "2555141950",
        "commented_code": "@@ -401,6 +401,31 @@ def build_events(\n )\n \n \n+def _deep_merge_extra_fields(existing: object, new: object) -> object:\n+    \"\"\"Deep merge new data into existing data, mutating containers in place.\n+\n+    - Dicts: recursively merge keys (mutates existing dict)\n+    - Lists: extend existing with new items (mutates existing list)\n+    - Other: replace with new value\n+    \"\"\"\n+    if isinstance(existing, dict) and isinstance(new, dict):\n+        existing_dict = cast(\"dict[str, object]\", existing)\n+        new_dict = cast(\"dict[str, object]\", new)\n+        for key, value in new_dict.items():\n+            if key in existing_dict:\n+                existing_dict[key] = _deep_merge_extra_fields(existing_dict[key], value)\n+            else:\n+                existing_dict[key] = value\n+        return existing_dict  # Return mutated dict\n+    elif isinstance(existing, list) and isinstance(new, list):\n+        existing_list = cast(\"list[object]\", existing)\n+        new_list = cast(\"list[object]\", new)",
        "comment_created_at": "2025-11-24T08:36:08+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "nit: same as `is_dict`, we can use utility function `is_list` to avoid typing.cast",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2555462754",
    "pr_number": 1070,
    "pr_file": "tests/lib/streaming/test_extra_fields.py",
    "created_at": "2025-11-24T09:43:49+00:00",
    "commented_code": "+\"\"\"Tests for accumulating extra fields in streaming responses.\n+\n+This tests that pydantic extra fields (fields not in the schema) are properly\n+accumulated during streaming, without exposing specific field names in the SDK.\n+\"\"\"\n+\n+from __future__ import annotations\n+\n+import os\n+import asyncio\n+from typing import Any, cast\n+\n+import httpx\n+import respx\n+\n+from anthropic import Anthropic, AsyncAnthropic\n+\n+from .helpers import get_response, to_async_iter\n+\n+base_url = os.environ.get(\"TEST_API_BASE_URL\", \"http://127.0.0.1:4010\")\n+api_key = \"my-anthropic-api-key\"\n+\n+sync_client = Anthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)\n+async_client = AsyncAnthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)\n+\n+\n+def assert_extra_fields_accumulated(message: Any) -> None:",
    "repo_full_name": "anthropics/anthropic-sdk-python",
    "discussion_comments": [
      {
        "comment_id": "2555462754",
        "repo_full_name": "anthropics/anthropic-sdk-python",
        "pr_number": 1070,
        "pr_file": "tests/lib/streaming/test_extra_fields.py",
        "discussion_id": "2555462754",
        "commented_code": "@@ -0,0 +1,149 @@\n+\"\"\"Tests for accumulating extra fields in streaming responses.\n+\n+This tests that pydantic extra fields (fields not in the schema) are properly\n+accumulated during streaming, without exposing specific field names in the SDK.\n+\"\"\"\n+\n+from __future__ import annotations\n+\n+import os\n+import asyncio\n+from typing import Any, cast\n+\n+import httpx\n+import respx\n+\n+from anthropic import Anthropic, AsyncAnthropic\n+\n+from .helpers import get_response, to_async_iter\n+\n+base_url = os.environ.get(\"TEST_API_BASE_URL\", \"http://127.0.0.1:4010\")\n+api_key = \"my-anthropic-api-key\"\n+\n+sync_client = Anthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)\n+async_client = AsyncAnthropic(base_url=base_url, api_key=api_key, _strict_response_validation=True)\n+\n+\n+def assert_extra_fields_accumulated(message: Any) -> None:",
        "comment_created_at": "2025-11-24T09:43:49+00:00",
        "comment_author": "karpetrosyan",
        "comment_body": "nit: let's have the correct type here instead of `Any`",
        "pr_file_module": null
      }
    ]
  }
]