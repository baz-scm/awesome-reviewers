[
  {
    "discussion_id": "1169706544",
    "pr_number": 26138,
    "pr_file": "src/azure-cli-core/azure/cli/core/cloud.py",
    "created_at": "2023-04-18T08:45:29+00:00",
    "commented_code": "logger = get_logger(__name__)\n \n+# This default cloud name. If you use DummyCli(random_config_dir=True), use get_cloud_config_file() instead.\n CLOUD_CONFIG_FILE = os.path.join(GLOBAL_CONFIG_DIR, 'clouds.config')",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "1169706544",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 26138,
        "pr_file": "src/azure-cli-core/azure/cli/core/cloud.py",
        "discussion_id": "1169706544",
        "commented_code": "@@ -18,8 +18,14 @@\n \n logger = get_logger(__name__)\n \n+# This default cloud name. If you use DummyCli(random_config_dir=True), use get_cloud_config_file() instead.\n CLOUD_CONFIG_FILE = os.path.join(GLOBAL_CONFIG_DIR, 'clouds.config')",
        "comment_created_at": "2023-04-18T08:45:29+00:00",
        "comment_author": "jiasli",
        "comment_body": "The original implementation is wrong. \r\n\r\n`get_default_cli` uses `GLOBAL_CONFIG_DIR` for `knack.cli.CLI.__init__`'s `config_dir` argument:\r\n\r\nhttps://github.com/Azure/azure-cli/blob/63121edc73fa103658b3184d3d9d0328f43f330a/src/azure-cli-core/azure/cli/core/__init__.py#L902-L903\r\n\r\nThe location of `clouds.config` is also built from `GLOBAL_CONFIG_DIR`.\r\n\r\nIf `knack.cli.CLI.__init__`'s `config_dir` doesn't use `GLOBAL_CONFIG_DIR`, but another location, like what has been done in #25689:\r\n\r\nhttps://github.com/Azure/azure-cli/blob/b223dd9d4379d5c633a9f4dba4941ae791531126/src/azure-cli-core/azure/cli/core/mock.py#L28-L29\r\n\r\nThen the location of `clouds.config` will still be under `GLOBAL_CONFIG_DIR`, because it directly uses `CLOUD_CONFIG_FILE`, instead of `cli_ctx.config.config_dir`. This defeats the purpose of overriding `config_dir`.\r\n\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "1170874362",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 26138,
        "pr_file": "src/azure-cli-core/azure/cli/core/cloud.py",
        "discussion_id": "1169706544",
        "commented_code": "@@ -18,8 +18,14 @@\n \n logger = get_logger(__name__)\n \n+# This default cloud name. If you use DummyCli(random_config_dir=True), use get_cloud_config_file() instead.\n CLOUD_CONFIG_FILE = os.path.join(GLOBAL_CONFIG_DIR, 'clouds.config')",
        "comment_created_at": "2023-04-19T06:43:24+00:00",
        "comment_author": "jiasli",
        "comment_body": "`profile._storage` is an `azure.cli.core._session.ACCOUNT` which is a persisted JSON. It can easily be replaced by an in-memory `dict` during tests, but `cloud.config` is an INI file, which introduces inconsistency. See https://github.com/Azure/azure-cli/pull/14436#issuecomment-1514207629",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3140753651",
    "pr_number": 33259,
    "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
    "created_at": "2026-04-24T23:21:52+00:00",
    "commented_code": "if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        Triggered when the user explicitly opts in via `--enable-hosted-system`, or\n+        implicitly by supplying the BYO VNet subnet trio (`--system-node-subnet-id` /\n+        `--node-subnet-id` / `--apiserver-subnet-id`). In either case:\n+          - `mc.hosted_system_profile.enabled` is set to True so the RP treats this\n+            as a Managed System Pool request.\n+          - `system_node_subnet_id` / `node_subnet_id` are populated when supplied.\n+          - `mc.agent_pool_profiles` is cleared. The CLI unconditionally synthesizes\n+            a default agent pool via `set_up_agentpool_profile`; on a Managed System\n+            Pool cluster the system pool is provisioned server-side from\n+            `hosted_system_profile`, so the CLI default is stale and (in the BYO case)\n+            actively conflicts with the BYO VNet.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (--enable-hosted-system SKU gate + BYO trio completeness)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+        enable_hosted_system = self.context.get_enable_hosted_system()",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "3140753651",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3140753651",
        "commented_code": "@@ -6998,12 +7178,64 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        Triggered when the user explicitly opts in via `--enable-hosted-system`, or\n+        implicitly by supplying the BYO VNet subnet trio (`--system-node-subnet-id` /\n+        `--node-subnet-id` / `--apiserver-subnet-id`). In either case:\n+          - `mc.hosted_system_profile.enabled` is set to True so the RP treats this\n+            as a Managed System Pool request.\n+          - `system_node_subnet_id` / `node_subnet_id` are populated when supplied.\n+          - `mc.agent_pool_profiles` is cleared. The CLI unconditionally synthesizes\n+            a default agent pool via `set_up_agentpool_profile`; on a Managed System\n+            Pool cluster the system pool is provisioned server-side from\n+            `hosted_system_profile`, so the CLI default is stale and (in the BYO case)\n+            actively conflicts with the BYO VNet.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (--enable-hosted-system SKU gate + BYO trio completeness)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+        enable_hosted_system = self.context.get_enable_hosted_system()",
        "comment_created_at": "2026-04-24T23:21:52+00:00",
        "comment_author": "zqingqing1",
        "comment_body": "set enable_hosted_system to true when 3 subnets being provided for automatic?",
        "pr_file_module": null
      },
      {
        "comment_id": "3140791883",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3140753651",
        "commented_code": "@@ -6998,12 +7178,64 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        Triggered when the user explicitly opts in via `--enable-hosted-system`, or\n+        implicitly by supplying the BYO VNet subnet trio (`--system-node-subnet-id` /\n+        `--node-subnet-id` / `--apiserver-subnet-id`). In either case:\n+          - `mc.hosted_system_profile.enabled` is set to True so the RP treats this\n+            as a Managed System Pool request.\n+          - `system_node_subnet_id` / `node_subnet_id` are populated when supplied.\n+          - `mc.agent_pool_profiles` is cleared. The CLI unconditionally synthesizes\n+            a default agent pool via `set_up_agentpool_profile`; on a Managed System\n+            Pool cluster the system pool is provisioned server-side from\n+            `hosted_system_profile`, so the CLI default is stale and (in the BYO case)\n+            actively conflicts with the BYO VNet.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (--enable-hosted-system SKU gate + BYO trio completeness)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+        enable_hosted_system = self.context.get_enable_hosted_system()",
        "comment_created_at": "2026-04-24T23:36:40+00:00",
        "comment_author": "wenhug",
        "comment_body": "Addressed in 0ed79d8. The implicit path is now explicit: `get_enable_hosted_system()` returns true for BYO only when all three subnet IDs (`--system-node-subnet-id`, `--node-subnet-id`, `--apiserver-subnet-id`) are present. `set_up_hosted_system_profile` then sets `hostedSystemProfile.enabled=true`. Existing validation still rejects partial subnet sets.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3135646858",
    "pr_number": 33259,
    "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
    "created_at": "2026-04-24T05:28:53+00:00",
    "commented_code": "if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        When the BYO VNet trio (`--system-node-subnet-id` / `--node-subnet-id` /\n+        `--apiserver-subnet-id`) is provided, populate\n+        `mc.hosted_system_profile.{enabled=True, system_node_subnet_id, node_subnet_id}`\n+        and clear `mc.agent_pool_profiles` because the Managed System Pool manages node\n+        pools server-side. The RP requires `enabled=True` to treat the request as BYO\n+        VNet rather than default-VNet mode.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (trio completeness + SKU gate)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+\n+        if system_node_subnet_id or node_subnet_id:\n+            if mc.hosted_system_profile is None:\n+                mc.hosted_system_profile = self.models.ManagedClusterHostedSystemProfile()\n+            # BYO VNet requires explicit enablement so the RP treats this as a BYO VNet\n+            # cluster (not default-vnet) when BYO subnets are supplied.\n+            mc.hosted_system_profile.enabled = True\n+            if system_node_subnet_id:\n+                mc.hosted_system_profile.system_node_subnet_id = system_node_subnet_id\n+            if node_subnet_id:\n+                mc.hosted_system_profile.node_subnet_id = node_subnet_id\n+            # The Managed System Pool manages node pools; drop the default agent pool so\n+            # the RP doesn't reject the request for having an unrelated default nodepool\n+            # in a VNet other than the BYO trio's VNet.\n+            if mc.agent_pool_profiles is not None:\n+                mc.agent_pool_profiles = None",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "3135646858",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3135646858",
        "commented_code": "@@ -6998,12 +7090,58 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        When the BYO VNet trio (`--system-node-subnet-id` / `--node-subnet-id` /\n+        `--apiserver-subnet-id`) is provided, populate\n+        `mc.hosted_system_profile.{enabled=True, system_node_subnet_id, node_subnet_id}`\n+        and clear `mc.agent_pool_profiles` because the Managed System Pool manages node\n+        pools server-side. The RP requires `enabled=True` to treat the request as BYO\n+        VNet rather than default-VNet mode.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (trio completeness + SKU gate)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+\n+        if system_node_subnet_id or node_subnet_id:\n+            if mc.hosted_system_profile is None:\n+                mc.hosted_system_profile = self.models.ManagedClusterHostedSystemProfile()\n+            # BYO VNet requires explicit enablement so the RP treats this as a BYO VNet\n+            # cluster (not default-vnet) when BYO subnets are supplied.\n+            mc.hosted_system_profile.enabled = True\n+            if system_node_subnet_id:\n+                mc.hosted_system_profile.system_node_subnet_id = system_node_subnet_id\n+            if node_subnet_id:\n+                mc.hosted_system_profile.node_subnet_id = node_subnet_id\n+            # The Managed System Pool manages node pools; drop the default agent pool so\n+            # the RP doesn't reject the request for having an unrelated default nodepool\n+            # in a VNet other than the BYO trio's VNet.\n+            if mc.agent_pool_profiles is not None:\n+                mc.agent_pool_profiles = None",
        "comment_created_at": "2026-04-24T05:28:53+00:00",
        "comment_author": "zqingqing1",
        "comment_body": "only remove the default pool that added by cli? for pool that customer added, keep it",
        "pr_file_module": null
      },
      {
        "comment_id": "3140791878",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3135646858",
        "commented_code": "@@ -6998,12 +7090,58 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        When the BYO VNet trio (`--system-node-subnet-id` / `--node-subnet-id` /\n+        `--apiserver-subnet-id`) is provided, populate\n+        `mc.hosted_system_profile.{enabled=True, system_node_subnet_id, node_subnet_id}`\n+        and clear `mc.agent_pool_profiles` because the Managed System Pool manages node\n+        pools server-side. The RP requires `enabled=True` to treat the request as BYO\n+        VNet rather than default-VNet mode.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (trio completeness + SKU gate)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+\n+        if system_node_subnet_id or node_subnet_id:\n+            if mc.hosted_system_profile is None:\n+                mc.hosted_system_profile = self.models.ManagedClusterHostedSystemProfile()\n+            # BYO VNet requires explicit enablement so the RP treats this as a BYO VNet\n+            # cluster (not default-vnet) when BYO subnets are supplied.\n+            mc.hosted_system_profile.enabled = True\n+            if system_node_subnet_id:\n+                mc.hosted_system_profile.system_node_subnet_id = system_node_subnet_id\n+            if node_subnet_id:\n+                mc.hosted_system_profile.node_subnet_id = node_subnet_id\n+            # The Managed System Pool manages node pools; drop the default agent pool so\n+            # the RP doesn't reject the request for having an unrelated default nodepool\n+            # in a VNet other than the BYO trio's VNet.\n+            if mc.agent_pool_profiles is not None:\n+                mc.agent_pool_profiles = None",
        "comment_created_at": "2026-04-24T23:36:40+00:00",
        "comment_author": "wenhug",
        "comment_body": "Addressed in 0ed79d8. `set_up_hosted_system_profile` now removes only the first CLI-synthesized default agent pool from the `az aks create` path and preserves any additional pools already present on `mc.agent_pool_profiles`. Added unit coverage for the single default-pool case and the extra-pool preservation case.",
        "pr_file_module": null
      },
      {
        "comment_id": "3140804887",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3135646858",
        "commented_code": "@@ -6998,12 +7090,58 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        When the BYO VNet trio (`--system-node-subnet-id` / `--node-subnet-id` /\n+        `--apiserver-subnet-id`) is provided, populate\n+        `mc.hosted_system_profile.{enabled=True, system_node_subnet_id, node_subnet_id}`\n+        and clear `mc.agent_pool_profiles` because the Managed System Pool manages node\n+        pools server-side. The RP requires `enabled=True` to treat the request as BYO\n+        VNet rather than default-VNet mode.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (trio completeness + SKU gate)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+\n+        if system_node_subnet_id or node_subnet_id:\n+            if mc.hosted_system_profile is None:\n+                mc.hosted_system_profile = self.models.ManagedClusterHostedSystemProfile()\n+            # BYO VNet requires explicit enablement so the RP treats this as a BYO VNet\n+            # cluster (not default-vnet) when BYO subnets are supplied.\n+            mc.hosted_system_profile.enabled = True\n+            if system_node_subnet_id:\n+                mc.hosted_system_profile.system_node_subnet_id = system_node_subnet_id\n+            if node_subnet_id:\n+                mc.hosted_system_profile.node_subnet_id = node_subnet_id\n+            # The Managed System Pool manages node pools; drop the default agent pool so\n+            # the RP doesn't reject the request for having an unrelated default nodepool\n+            # in a VNet other than the BYO trio's VNet.\n+            if mc.agent_pool_profiles is not None:\n+                mc.agent_pool_profiles = None",
        "comment_created_at": "2026-04-24T23:42:09+00:00",
        "comment_author": "wenhug",
        "comment_body": "Follow-up in 7fd3c16 to make this stricter: `set_up_agentpool_profile` now records the exact CLI-created default pool object, and hosted-system setup removes only that recorded object by identity. If the pool is not the recorded CLI default, nothing is removed. Added coverage for the user-pool-only case.",
        "pr_file_module": null
      },
      {
        "comment_id": "3140809702",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3135646858",
        "commented_code": "@@ -6998,12 +7090,58 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        When the BYO VNet trio (`--system-node-subnet-id` / `--node-subnet-id` /\n+        `--apiserver-subnet-id`) is provided, populate\n+        `mc.hosted_system_profile.{enabled=True, system_node_subnet_id, node_subnet_id}`\n+        and clear `mc.agent_pool_profiles` because the Managed System Pool manages node\n+        pools server-side. The RP requires `enabled=True` to treat the request as BYO\n+        VNet rather than default-VNet mode.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (trio completeness + SKU gate)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+\n+        if system_node_subnet_id or node_subnet_id:\n+            if mc.hosted_system_profile is None:\n+                mc.hosted_system_profile = self.models.ManagedClusterHostedSystemProfile()\n+            # BYO VNet requires explicit enablement so the RP treats this as a BYO VNet\n+            # cluster (not default-vnet) when BYO subnets are supplied.\n+            mc.hosted_system_profile.enabled = True\n+            if system_node_subnet_id:\n+                mc.hosted_system_profile.system_node_subnet_id = system_node_subnet_id\n+            if node_subnet_id:\n+                mc.hosted_system_profile.node_subnet_id = node_subnet_id\n+            # The Managed System Pool manages node pools; drop the default agent pool so\n+            # the RP doesn't reject the request for having an unrelated default nodepool\n+            # in a VNet other than the BYO trio's VNet.\n+            if mc.agent_pool_profiles is not None:\n+                mc.agent_pool_profiles = None",
        "comment_created_at": "2026-04-24T23:44:39+00:00",
        "comment_author": "wenhug",
        "comment_body": "Follow-up in 717c22f: moved this earlier as suggested. Hosted-system create now skips `set_up_agentpool_profile()` entirely, so the CLI default pool is never synthesized and `set_up_hosted_system_profile()` no longer removes any pools. Existing `agent_pool_profiles` are left untouched.",
        "pr_file_module": null
      },
      {
        "comment_id": "3141002761",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33259,
        "pr_file": "src/azure-cli/azure/cli/command_modules/acs/managed_cluster_decorator.py",
        "discussion_id": "3135646858",
        "commented_code": "@@ -6998,12 +7090,58 @@ def set_up_api_server_access_profile(self, mc: ManagedCluster) -> ManagedCluster\n             if api_server_access_profile is None:\n                 api_server_access_profile = self.models.ManagedClusterAPIServerAccessProfile()\n             api_server_access_profile.subnet_id = self.context.get_apiserver_subnet_id()\n+            # BYO VNet for Managed System Pool (Automatic SKU) requires apiserver VNet\n+            # integration. When the BYO subnet trio is provided, auto-enable vnet\n+            # integration so users are not forced to pass --enable-apiserver-vnet-integration\n+            # alongside the subnet IDs.\n+            if (\n+                self.context.get_system_node_subnet_id() or\n+                self.context.get_node_subnet_id()\n+            ):\n+                api_server_access_profile.enable_vnet_integration = True\n         mc.api_server_access_profile = api_server_access_profile\n \n         fqdn_subdomain = self.context.get_fqdn_subdomain()\n         mc.fqdn_subdomain = fqdn_subdomain\n         return mc\n \n+    def set_up_hosted_system_profile(self, mc: ManagedCluster) -> ManagedCluster:\n+        \"\"\"Set up hosted_system_profile on the ManagedCluster for Automatic SKU clusters.\n+\n+        When the BYO VNet trio (`--system-node-subnet-id` / `--node-subnet-id` /\n+        `--apiserver-subnet-id`) is provided, populate\n+        `mc.hosted_system_profile.{enabled=True, system_node_subnet_id, node_subnet_id}`\n+        and clear `mc.agent_pool_profiles` because the Managed System Pool manages node\n+        pools server-side. The RP requires `enabled=True` to treat the request as BYO\n+        VNet rather than default-VNet mode.\n+\n+        :return: the ManagedCluster object\n+        \"\"\"\n+        self._ensure_mc(mc)\n+\n+        # Run cross-flag validation (trio completeness + SKU gate)\n+        self.context.validate_byo_hobo_subnets()\n+\n+        system_node_subnet_id = self.context.get_system_node_subnet_id()\n+        node_subnet_id = self.context.get_node_subnet_id()\n+\n+        if system_node_subnet_id or node_subnet_id:\n+            if mc.hosted_system_profile is None:\n+                mc.hosted_system_profile = self.models.ManagedClusterHostedSystemProfile()\n+            # BYO VNet requires explicit enablement so the RP treats this as a BYO VNet\n+            # cluster (not default-vnet) when BYO subnets are supplied.\n+            mc.hosted_system_profile.enabled = True\n+            if system_node_subnet_id:\n+                mc.hosted_system_profile.system_node_subnet_id = system_node_subnet_id\n+            if node_subnet_id:\n+                mc.hosted_system_profile.node_subnet_id = node_subnet_id\n+            # The Managed System Pool manages node pools; drop the default agent pool so\n+            # the RP doesn't reject the request for having an unrelated default nodepool\n+            # in a VNet other than the BYO trio's VNet.\n+            if mc.agent_pool_profiles is not None:\n+                mc.agent_pool_profiles = None",
        "comment_created_at": "2026-04-25T01:04:52+00:00",
        "comment_author": "zqingqing1",
        "comment_body": "not right, AI removed the logic to remove default agent pool. please readdress",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2802496596",
    "pr_number": 32718,
    "pr_file": "src/azure-cli/azure/cli/command_modules/vm/_validators.py",
    "created_at": "2026-02-13T06:18:49+00:00",
    "commented_code": "# extract additional information from a managed custom image\n         res = parse_resource_id(namespace.image)\n         namespace.aux_subscriptions = [res['subscription']]\n-        compute_client = _compute_client_factory(cmd.cli_ctx, subscription_id=res['subscription'])",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "2802496596",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32718,
        "pr_file": "src/azure-cli/azure/cli/command_modules/vm/_validators.py",
        "discussion_id": "2802496596",
        "commented_code": "@@ -500,18 +508,29 @@ def _validate_vm_create_storage_profile(cmd, namespace, for_scale_set=False):\n         # extract additional information from a managed custom image\n         res = parse_resource_id(namespace.image)\n         namespace.aux_subscriptions = [res['subscription']]\n-        compute_client = _compute_client_factory(cmd.cli_ctx, subscription_id=res['subscription'])",
        "comment_created_at": "2026-02-13T06:18:49+00:00",
        "comment_author": "yanzhudd",
        "comment_body": "please check all occurrences where compute_client is used \u2014 we need to explicitly specify the subscription in each case",
        "pr_file_module": null
      }
    ]
  }
]