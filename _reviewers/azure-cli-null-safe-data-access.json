[
  {
    "discussion_id": "3470625019",
    "pr_number": 33632,
    "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
    "created_at": "2026-06-24T22:13:26+00:00",
    "commented_code": "return app\n \n \n+def _log_webapp_status_tip(name, resource_group_name, is_linux):\n+    # Per-instance runtime status (siteStatus) is a Linux App Service feature,\n+    # so only surface the tip for Linux webapps.\n+    if not is_linux:\n+        return\n+    logger.warning(\"Tip: run 'az webapp status --name %s --resource-group %s' \"\n+                   \"to see per-instance runtime status.\",\n+                   name, resource_group_name)\n+\n+\n+def _extract_webapp_status_items(result):\n+    # The siteStatus response holds per-instance status under 'properties':\n+    # a list for /siteStatus, a single object for /siteStatus/{instanceId}.\n+    # Normalize both shapes into a list for uniform formatting.\n+    if isinstance(result, dict):\n+        properties = result.get('properties')\n+        if isinstance(properties, list):",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "3470625019",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33632,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3470625019",
        "commented_code": "@@ -2460,6 +2462,81 @@ def show_app(cmd, resource_group_name, name, slot=None):\n     return app\n \n \n+def _log_webapp_status_tip(name, resource_group_name, is_linux):\n+    # Per-instance runtime status (siteStatus) is a Linux App Service feature,\n+    # so only surface the tip for Linux webapps.\n+    if not is_linux:\n+        return\n+    logger.warning(\"Tip: run 'az webapp status --name %s --resource-group %s' \"\n+                   \"to see per-instance runtime status.\",\n+                   name, resource_group_name)\n+\n+\n+def _extract_webapp_status_items(result):\n+    # The siteStatus response holds per-instance status under 'properties':\n+    # a list for /siteStatus, a single object for /siteStatus/{instanceId}.\n+    # Normalize both shapes into a list for uniform formatting.\n+    if isinstance(result, dict):\n+        properties = result.get('properties')\n+        if isinstance(properties, list):",
        "comment_created_at": "2026-06-24T22:13:26+00:00",
        "comment_author": "vageorge00",
        "comment_body": "I see there's a lot of isinstance type checks here, do we also need to check if each item in properties list is a dict? Up to you, just asking because we seem to be checking types a lot. \n\nitem.get('lastError') for item in items (line 2495) is assuming item is a dict I believe",
        "pr_file_module": null
      },
      {
        "comment_id": "3482628874",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33632,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3470625019",
        "commented_code": "@@ -2460,6 +2462,81 @@ def show_app(cmd, resource_group_name, name, slot=None):\n     return app\n \n \n+def _log_webapp_status_tip(name, resource_group_name, is_linux):\n+    # Per-instance runtime status (siteStatus) is a Linux App Service feature,\n+    # so only surface the tip for Linux webapps.\n+    if not is_linux:\n+        return\n+    logger.warning(\"Tip: run 'az webapp status --name %s --resource-group %s' \"\n+                   \"to see per-instance runtime status.\",\n+                   name, resource_group_name)\n+\n+\n+def _extract_webapp_status_items(result):\n+    # The siteStatus response holds per-instance status under 'properties':\n+    # a list for /siteStatus, a single object for /siteStatus/{instanceId}.\n+    # Normalize both shapes into a list for uniform formatting.\n+    if isinstance(result, dict):\n+        properties = result.get('properties')\n+        if isinstance(properties, list):",
        "comment_created_at": "2026-06-26T16:04:18+00:00",
        "comment_author": "HaripriyaMehta",
        "comment_body": "This is a good question - this is more for just normalizing the formats of the outputs of siteStatus and siteStatus/instanceId rather than confirming they are the correct ouput. Given that is the case, in fact, I think it would be better to remove this first check - if isinstance(result, dict): since we know that the output from ARM will be a dictionary. Thanks for bringing this up.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2802394804",
    "pr_number": 32707,
    "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
    "created_at": "2026-02-13T05:41:56+00:00",
    "commented_code": "# region VirtualMachineScaleSets Extensions\n def delete_vmss_extension(cmd, resource_group_name, vmss_name, extension_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    vmss = client.virtual_machine_scale_sets.get(resource_group_name=resource_group_name, vm_scale_set_name=vmss_name)\n+    from .operations.vmss import VMSSShow\n+    vmss = VMSSShow(cli_ctx=cmd.cli_ctx)(command_args={\n+        \"resource_group\": resource_group_name,\n+        \"vm_scale_set_name\": vmss_name,\n+    })\n     # Avoid unnecessary permission error\n-    vmss.virtual_machine_profile.storage_profile.image_reference = None\n-    # pylint: disable=no-member\n-    if not vmss.virtual_machine_profile.extension_profile:\n+    vmss[\"virtualMachineProfile\"][\"storageProfile\"][\"imageReference\"] = None",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "2802394804",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32707,
        "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
        "discussion_id": "2802394804",
        "commented_code": "@@ -5104,43 +5104,52 @@ def detach_disk_from_vmss(cmd, resource_group_name, vmss_name, lun, instance_id=\n \n # region VirtualMachineScaleSets Extensions\n def delete_vmss_extension(cmd, resource_group_name, vmss_name, extension_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    vmss = client.virtual_machine_scale_sets.get(resource_group_name=resource_group_name, vm_scale_set_name=vmss_name)\n+    from .operations.vmss import VMSSShow\n+    vmss = VMSSShow(cli_ctx=cmd.cli_ctx)(command_args={\n+        \"resource_group\": resource_group_name,\n+        \"vm_scale_set_name\": vmss_name,\n+    })\n     # Avoid unnecessary permission error\n-    vmss.virtual_machine_profile.storage_profile.image_reference = None\n-    # pylint: disable=no-member\n-    if not vmss.virtual_machine_profile.extension_profile:\n+    vmss[\"virtualMachineProfile\"][\"storageProfile\"][\"imageReference\"] = None",
        "comment_created_at": "2026-02-13T05:41:56+00:00",
        "comment_author": "yanzhudd",
        "comment_body": "we need to consider what happens if the key does not exist in the dict:\n```suggestion\n    if vmss.get(\"virtualMachineProfile\", {}).get(\"storageProfile\", None) is not None:\n        vmss[\"virtualMachineProfile\"][\"storageProfile\"][\"imageReference\"] = None\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3397781449",
    "pr_number": 33504,
    "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
    "created_at": "2026-06-11T18:52:24+00:00",
    "commented_code": "def list_function_keys(cmd, resource_group_name, name, function_name, slot=None):\n     client = web_client_factory(cmd.cli_ctx)\n     if slot:\n-        return client.web_apps.list_function_keys_slot(resource_group_name, name, function_name, slot)\n-    return client.web_apps.list_function_keys(resource_group_name, name, function_name)\n+        keys = client.web_apps.list_function_keys_slot(resource_group_name, name, function_name, slot)\n+    else:\n+        keys = client.web_apps.list_function_keys(resource_group_name, name, function_name)\n+    # SDK may return .properties as None for flat dictionary responses; fall back to raw payload\n+    return keys.properties if keys.properties is not None else dict(keys)",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "3398365119",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33504,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3397781449",
        "commented_code": "@@ -11437,8 +11437,11 @@ def update_function_key(cmd, resource_group_name, name, function_name, key_name,\n def list_function_keys(cmd, resource_group_name, name, function_name, slot=None):\n     client = web_client_factory(cmd.cli_ctx)\n     if slot:\n-        return client.web_apps.list_function_keys_slot(resource_group_name, name, function_name, slot)\n-    return client.web_apps.list_function_keys(resource_group_name, name, function_name)\n+        keys = client.web_apps.list_function_keys_slot(resource_group_name, name, function_name, slot)\n+    else:\n+        keys = client.web_apps.list_function_keys(resource_group_name, name, function_name)\n+    # SDK may return .properties as None for flat dictionary responses; fall back to raw payload\n+    return keys.properties if keys.properties is not None else dict(keys)",
        "comment_created_at": "2026-06-11T18:52:24+00:00",
        "comment_author": "harshivcodes",
        "comment_body": "Keys is always a typed StringDictionary returned by the SDK's list_function_keys(). It will always have a .properties attribute. A getattr guard would obscure the intent and mask unexpected SDK behavior that we'd want to surface rather than silently swallow.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3364416407",
    "pr_number": 33443,
    "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
    "created_at": "2026-06-05T17:51:04+00:00",
    "commented_code": "kv_cert_def = Certificate(location=location, key_vault_id=kv_id, password='',\n                               key_vault_secret_name=kv_secret_name)\n \n+    # Check if this is a Flex Consumption function app\n+    if name and is_flex_functionapp(cmd.cli_ctx, resource_group_name, name):\n+        # Build certificate envelope as dict to include loadCertificateToWebsitesSettings\n+        # (not in SDK model yet)\n+        cert_envelope = {\n+            \"location\": location,\n+            \"properties\": {\n+                \"keyVaultId\": kv_id,\n+                \"password\": \"\",\n+                \"keyVaultSecretName\": kv_secret_name\n+            }\n+        }\n+        if load_to_code is not None:\n+            cert_envelope[\"properties\"][\"loadCertificateToWebsitesSettings\"] = {\n+                \"loadToWebsite\": load_to_code\n+            }\n+        if enable_using_msi is not None:",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "3364416407",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33443,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3364416407",
        "commented_code": "@@ -6872,6 +6948,31 @@ def import_ssl_cert(cmd, resource_group_name, key_vault, key_vault_certificate_n\n     kv_cert_def = Certificate(location=location, key_vault_id=kv_id, password='',\n                               key_vault_secret_name=kv_secret_name)\n \n+    # Check if this is a Flex Consumption function app\n+    if name and is_flex_functionapp(cmd.cli_ctx, resource_group_name, name):\n+        # Build certificate envelope as dict to include loadCertificateToWebsitesSettings\n+        # (not in SDK model yet)\n+        cert_envelope = {\n+            \"location\": location,\n+            \"properties\": {\n+                \"keyVaultId\": kv_id,\n+                \"password\": \"\",\n+                \"keyVaultSecretName\": kv_secret_name\n+            }\n+        }\n+        if load_to_code is not None:\n+            cert_envelope[\"properties\"][\"loadCertificateToWebsitesSettings\"] = {\n+                \"loadToWebsite\": load_to_code\n+            }\n+        if enable_using_msi is not None:",
        "comment_created_at": "2026-06-05T17:51:04+00:00",
        "comment_author": "shreyas-gopalakrishna",
        "comment_body": "When `enable_using_msi` is set, this sends `enableKeyVaultAccessUsingMSI` to the API without checking that the app actually has a managed identity assigned. If no identity exists, the backend will likely return an opaque error.\n\nConsider checking `webapp.identity` before setting this and raising a clear error like: `'--enable-using-msi requires a managed identity. Assign one with: az functionapp identity assign -g <rg> -n <name>'`",
        "pr_file_module": null
      },
      {
        "comment_id": "3365294788",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33443,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3364416407",
        "commented_code": "@@ -6872,6 +6948,31 @@ def import_ssl_cert(cmd, resource_group_name, key_vault, key_vault_certificate_n\n     kv_cert_def = Certificate(location=location, key_vault_id=kv_id, password='',\n                               key_vault_secret_name=kv_secret_name)\n \n+    # Check if this is a Flex Consumption function app\n+    if name and is_flex_functionapp(cmd.cli_ctx, resource_group_name, name):\n+        # Build certificate envelope as dict to include loadCertificateToWebsitesSettings\n+        # (not in SDK model yet)\n+        cert_envelope = {\n+            \"location\": location,\n+            \"properties\": {\n+                \"keyVaultId\": kv_id,\n+                \"password\": \"\",\n+                \"keyVaultSecretName\": kv_secret_name\n+            }\n+        }\n+        if load_to_code is not None:\n+            cert_envelope[\"properties\"][\"loadCertificateToWebsitesSettings\"] = {\n+                \"loadToWebsite\": load_to_code\n+            }\n+        if enable_using_msi is not None:",
        "comment_created_at": "2026-06-05T20:45:41+00:00",
        "comment_author": "patelchandni",
        "comment_body": "raising ArgumentUsageError if (not webapp.identity or not webapp.identity.type) with message:\n\"--enable-using-msi requires a managed identity assigned to the function app. \"\n            \"Assign one with: az functionapp identity assign -g {} -n {}\"",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3364416411",
    "pr_number": 33443,
    "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
    "created_at": "2026-06-05T17:51:04+00:00",
    "commented_code": "if not webapp:\n         raise ResourceNotFoundError(\"'{}' app doesn't exist\".format(name))\n \n-    cert_resource_group_name = parse_resource_id(get_site_server_farm_id(webapp))['resource_group']\n-    webapp_certs = client.certificates.list_by_resource_group(cert_resource_group_name)\n+    # Check if this is a Flex Consumption function app\n+    is_flex = is_flex_functionapp(cmd.cli_ctx, resource_group_name, name)\n \n     found_cert = None\n-    # search for a cert that matches in the app service plan's RG\n-    for webapp_cert in webapp_certs:\n-        if webapp_cert.thumbprint == certificate_thumbprint:\n-            found_cert = webapp_cert\n-    # search for a cert that matches in the webapp's RG\n-    if not found_cert:\n-        webapp_certs = client.certificates.list_by_resource_group(resource_group_name)\n+\n+    # For Flex Consumption apps, search in site-scoped certificates\n+    if is_flex:\n+        site_certs = client.site_certificates.list(resource_group_name=resource_group_name, name=name)\n+        for site_cert in site_certs:\n+            if site_cert.thumbprint == certificate_thumbprint:\n+                found_cert = site_cert\n+                break\n+    # If not a Flex app, search in regular certificates\n+    else:\n+        cert_resource_group_name = parse_resource_id(get_site_server_farm_id(webapp))['resource_group']\n+        webapp_certs = client.certificates.list_by_resource_group(cert_resource_group_name)\n+        # search for a cert that matches in the app service plan's RG\n         for webapp_cert in webapp_certs:\n             if webapp_cert.thumbprint == certificate_thumbprint:\n                 found_cert = webapp_cert\n-    # search for a cert that matches in the subscription, filtering on the serverfarm\n-    if not found_cert:\n-        sub_certs = client.certificates.list(filter=f\"ServerFarmId eq '{get_site_server_farm_id(webapp)}'\")\n-        found_cert = next(iter([c for c in sub_certs if c.thumbprint == certificate_thumbprint]), None)\n+        # search for a cert that matches in the webapp's RG\n+        if not found_cert:\n+            webapp_certs = client.certificates.list_by_resource_group(resource_group_name)\n+            for webapp_cert in webapp_certs:\n+                if webapp_cert.thumbprint == certificate_thumbprint:\n+                    found_cert = webapp_cert\n+        # search for a cert that matches in the subscription, filtering on the serverfarm\n+        if not found_cert:\n+            sub_certs = client.certificates.list(filter=f\"ServerFarmId eq '{get_site_server_farm_id(webapp)}'\")\n+            found_cert = next(iter([c for c in sub_certs if c.thumbprint == certificate_thumbprint]), None)\n+\n     if found_cert:\n         if not hostname:\n             if len(found_cert.host_names) == 1 and not found_cert.host_names[0].startswith('*'):",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "3364416411",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33443,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3364416411",
        "commented_code": "@@ -6973,24 +7088,37 @@ def _update_ssl_binding(cmd, resource_group_name, name, certificate_thumbprint,\n     if not webapp:\n         raise ResourceNotFoundError(\"'{}' app doesn't exist\".format(name))\n \n-    cert_resource_group_name = parse_resource_id(get_site_server_farm_id(webapp))['resource_group']\n-    webapp_certs = client.certificates.list_by_resource_group(cert_resource_group_name)\n+    # Check if this is a Flex Consumption function app\n+    is_flex = is_flex_functionapp(cmd.cli_ctx, resource_group_name, name)\n \n     found_cert = None\n-    # search for a cert that matches in the app service plan's RG\n-    for webapp_cert in webapp_certs:\n-        if webapp_cert.thumbprint == certificate_thumbprint:\n-            found_cert = webapp_cert\n-    # search for a cert that matches in the webapp's RG\n-    if not found_cert:\n-        webapp_certs = client.certificates.list_by_resource_group(resource_group_name)\n+\n+    # For Flex Consumption apps, search in site-scoped certificates\n+    if is_flex:\n+        site_certs = client.site_certificates.list(resource_group_name=resource_group_name, name=name)\n+        for site_cert in site_certs:\n+            if site_cert.thumbprint == certificate_thumbprint:\n+                found_cert = site_cert\n+                break\n+    # If not a Flex app, search in regular certificates\n+    else:\n+        cert_resource_group_name = parse_resource_id(get_site_server_farm_id(webapp))['resource_group']\n+        webapp_certs = client.certificates.list_by_resource_group(cert_resource_group_name)\n+        # search for a cert that matches in the app service plan's RG\n         for webapp_cert in webapp_certs:\n             if webapp_cert.thumbprint == certificate_thumbprint:\n                 found_cert = webapp_cert\n-    # search for a cert that matches in the subscription, filtering on the serverfarm\n-    if not found_cert:\n-        sub_certs = client.certificates.list(filter=f\"ServerFarmId eq '{get_site_server_farm_id(webapp)}'\")\n-        found_cert = next(iter([c for c in sub_certs if c.thumbprint == certificate_thumbprint]), None)\n+        # search for a cert that matches in the webapp's RG\n+        if not found_cert:\n+            webapp_certs = client.certificates.list_by_resource_group(resource_group_name)\n+            for webapp_cert in webapp_certs:\n+                if webapp_cert.thumbprint == certificate_thumbprint:\n+                    found_cert = webapp_cert\n+        # search for a cert that matches in the subscription, filtering on the serverfarm\n+        if not found_cert:\n+            sub_certs = client.certificates.list(filter=f\"ServerFarmId eq '{get_site_server_farm_id(webapp)}'\")\n+            found_cert = next(iter([c for c in sub_certs if c.thumbprint == certificate_thumbprint]), None)\n+\n     if found_cert:\n         if not hostname:\n             if len(found_cert.host_names) == 1 and not found_cert.host_names[0].startswith('*'):",
        "comment_created_at": "2026-06-05T17:51:04+00:00",
        "comment_author": "shreyas-gopalakrishna",
        "comment_body": "This accesses `found_cert.host_names` for both Flex and non-Flex certs. If site-scoped certificate objects don't populate `host_names` the same way as resource-group-scoped certs (e.g. returns `None` or empty list), this would fail with an `AttributeError` or `TypeError`.\n\nConsider adding a guard for the Flex case - if `host_names` is `None` or empty, require `--hostname` to be specified explicitly with a clear error message.",
        "pr_file_module": null
      },
      {
        "comment_id": "3365302838",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 33443,
        "pr_file": "src/azure-cli/azure/cli/command_modules/appservice/custom.py",
        "discussion_id": "3364416411",
        "commented_code": "@@ -6973,24 +7088,37 @@ def _update_ssl_binding(cmd, resource_group_name, name, certificate_thumbprint,\n     if not webapp:\n         raise ResourceNotFoundError(\"'{}' app doesn't exist\".format(name))\n \n-    cert_resource_group_name = parse_resource_id(get_site_server_farm_id(webapp))['resource_group']\n-    webapp_certs = client.certificates.list_by_resource_group(cert_resource_group_name)\n+    # Check if this is a Flex Consumption function app\n+    is_flex = is_flex_functionapp(cmd.cli_ctx, resource_group_name, name)\n \n     found_cert = None\n-    # search for a cert that matches in the app service plan's RG\n-    for webapp_cert in webapp_certs:\n-        if webapp_cert.thumbprint == certificate_thumbprint:\n-            found_cert = webapp_cert\n-    # search for a cert that matches in the webapp's RG\n-    if not found_cert:\n-        webapp_certs = client.certificates.list_by_resource_group(resource_group_name)\n+\n+    # For Flex Consumption apps, search in site-scoped certificates\n+    if is_flex:\n+        site_certs = client.site_certificates.list(resource_group_name=resource_group_name, name=name)\n+        for site_cert in site_certs:\n+            if site_cert.thumbprint == certificate_thumbprint:\n+                found_cert = site_cert\n+                break\n+    # If not a Flex app, search in regular certificates\n+    else:\n+        cert_resource_group_name = parse_resource_id(get_site_server_farm_id(webapp))['resource_group']\n+        webapp_certs = client.certificates.list_by_resource_group(cert_resource_group_name)\n+        # search for a cert that matches in the app service plan's RG\n         for webapp_cert in webapp_certs:\n             if webapp_cert.thumbprint == certificate_thumbprint:\n                 found_cert = webapp_cert\n-    # search for a cert that matches in the subscription, filtering on the serverfarm\n-    if not found_cert:\n-        sub_certs = client.certificates.list(filter=f\"ServerFarmId eq '{get_site_server_farm_id(webapp)}'\")\n-        found_cert = next(iter([c for c in sub_certs if c.thumbprint == certificate_thumbprint]), None)\n+        # search for a cert that matches in the webapp's RG\n+        if not found_cert:\n+            webapp_certs = client.certificates.list_by_resource_group(resource_group_name)\n+            for webapp_cert in webapp_certs:\n+                if webapp_cert.thumbprint == certificate_thumbprint:\n+                    found_cert = webapp_cert\n+        # search for a cert that matches in the subscription, filtering on the serverfarm\n+        if not found_cert:\n+            sub_certs = client.certificates.list(filter=f\"ServerFarmId eq '{get_site_server_farm_id(webapp)}'\")\n+            found_cert = next(iter([c for c in sub_certs if c.thumbprint == certificate_thumbprint]), None)\n+\n     if found_cert:\n         if not hostname:\n             if len(found_cert.host_names) == 1 and not found_cert.host_names[0].startswith('*'):",
        "comment_created_at": "2026-06-05T20:47:24+00:00",
        "comment_author": "patelchandni",
        "comment_body": "for flex, if (not found_cert.host_names or len(found_cert.host_names) == 0), then raising ArgumentUsageError with message: \n \"The site-scoped certificate does not have associated host names. \"\n                    \"Please specify the hostname explicitly using --hostname.\"",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2137401156",
    "pr_number": 31566,
    "pr_file": "src/azure-cli-core/azure/cli/core/cloud.py",
    "created_at": "2025-06-10T09:41:48+00:00",
    "commented_code": "def _get_arm_endpoint(arm_dict, is_suffix=False):\n     def _get_processed_arm_endpoint(name, add_dot=False, fallback_value=None):\n         if is_suffix:\n-            return (_add_starting_dot(arm_dict['suffixes'][name]) if add_dot else arm_dict['suffixes'][name]) if name in arm_dict['suffixes'] else fallback_value\n+            return (_add_starting_dot(arm_dict['suffixes'][name]) if add_dot else arm_dict['suffixes'][name]) if 'suffixes' in arm_dict and name in arm_dict['suffixes'] else fallback_value",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "2137401156",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 31566,
        "pr_file": "src/azure-cli-core/azure/cli/core/cloud.py",
        "discussion_id": "2137401156",
        "commented_code": "@@ -246,7 +246,7 @@ def _add_starting_dot(suffix):\n def _get_arm_endpoint(arm_dict, is_suffix=False):\n     def _get_processed_arm_endpoint(name, add_dot=False, fallback_value=None):\n         if is_suffix:\n-            return (_add_starting_dot(arm_dict['suffixes'][name]) if add_dot else arm_dict['suffixes'][name]) if name in arm_dict['suffixes'] else fallback_value\n+            return (_add_starting_dot(arm_dict['suffixes'][name]) if add_dot else arm_dict['suffixes'][name]) if 'suffixes' in arm_dict and name in arm_dict['suffixes'] else fallback_value",
        "comment_created_at": "2025-06-10T09:41:48+00:00",
        "comment_author": "jiasli",
        "comment_body": "If `arm_dict` does contain `suffixes` but `arm_dict['suffixes']` is `None`, `name in arm_dict['suffixes']` will fail:\r\n\r\n```sh\r\narm_dict = {'suffixes': None}\r\nname = 'test'\r\nprint('suffixes' in arm_dict and name in arm_dict['suffixes'])\r\n```\r\n\r\nOutput:\r\n\r\n```\r\nTraceback (most recent call last):\r\n  File \"D:\\cli\\testproj\\main.py\", line 3, in <module>\r\n    print('suffixes' in arm_dict and name in arm_dict['suffixes'])\r\n                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^\r\nTypeError: argument of type 'NoneType' is not iterable\r\n```\r\n\r\nIt is hard to enumerate all possibilities for an undocumented API.\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "2137769723",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 31566,
        "pr_file": "src/azure-cli-core/azure/cli/core/cloud.py",
        "discussion_id": "2137401156",
        "commented_code": "@@ -246,7 +246,7 @@ def _add_starting_dot(suffix):\n def _get_arm_endpoint(arm_dict, is_suffix=False):\n     def _get_processed_arm_endpoint(name, add_dot=False, fallback_value=None):\n         if is_suffix:\n-            return (_add_starting_dot(arm_dict['suffixes'][name]) if add_dot else arm_dict['suffixes'][name]) if name in arm_dict['suffixes'] else fallback_value\n+            return (_add_starting_dot(arm_dict['suffixes'][name]) if add_dot else arm_dict['suffixes'][name]) if 'suffixes' in arm_dict and name in arm_dict['suffixes'] else fallback_value",
        "comment_created_at": "2025-06-10T12:30:00+00:00",
        "comment_author": "evelyn-ys",
        "comment_body": "Nice catch! Updated to use `if name in arm_dict.get('suffixes', {})` now",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2894192026",
    "pr_number": 32927,
    "pr_file": "src/azure-cli-core/azure/cli/core/aaz/_error_format.py",
    "created_at": "2026-03-06T07:03:48+00:00",
    "commented_code": "# ARM specific annotations\n         self.additional_info = [\n             TypedErrorInfo(additional_info[\"type\"], additional_info[\"info\"])\n-            for additional_info in self.get_object_prop(json_object, self.ADDITIONALINFO_LABEL, [])\n+            for additional_info in self.get_object_prop(json_object, self.ADDITIONALINFO_LABEL, []) or []",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "2894192026",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32927,
        "pr_file": "src/azure-cli-core/azure/cli/core/aaz/_error_format.py",
        "discussion_id": "2894192026",
        "commented_code": "@@ -133,7 +133,7 @@ def __init__(self, json_object):\n         # ARM specific annotations\n         self.additional_info = [\n             TypedErrorInfo(additional_info[\"type\"], additional_info[\"info\"])\n-            for additional_info in self.get_object_prop(json_object, self.ADDITIONALINFO_LABEL, [])\n+            for additional_info in self.get_object_prop(json_object, self.ADDITIONALINFO_LABEL, []) or []",
        "comment_created_at": "2026-03-06T07:03:48+00:00",
        "comment_author": "ReaNAiveD",
        "comment_body": "If ADDITIONALINFO_LABEL can be present but null, do we want additional_info to end up as None or consistently as an empty list? I don\u2019t have a preference.",
        "pr_file_module": null
      },
      {
        "comment_id": "2902628928",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32927,
        "pr_file": "src/azure-cli-core/azure/cli/core/aaz/_error_format.py",
        "discussion_id": "2894192026",
        "commented_code": "@@ -133,7 +133,7 @@ def __init__(self, json_object):\n         # ARM specific annotations\n         self.additional_info = [\n             TypedErrorInfo(additional_info[\"type\"], additional_info[\"info\"])\n-            for additional_info in self.get_object_prop(json_object, self.ADDITIONALINFO_LABEL, [])\n+            for additional_info in self.get_object_prop(json_object, self.ADDITIONALINFO_LABEL, []) or []",
        "comment_created_at": "2026-03-08T23:53:38+00:00",
        "comment_author": "necusjz",
        "comment_body": "it's a workaround to make it iterable in the list comprehension, previously there is an implicit error:\r\n```bash\r\nTypeError: 'NoneType' object is not iterable\r\n```\r\n\r\nbtw, the actual logic of `additional_info` is https://github.com/Azure/azure-cli/blob/e6e616fc91a5c67f3f27d5051c16e48928690c69/src/azure-cli-core/azure/cli/core/aaz/_error_format.py#L142-L145.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2719472665",
    "pr_number": 32685,
    "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
    "created_at": "2026-01-23T03:45:12+00:00",
    "commented_code": "def show_vmss_identity(cmd, resource_group_name, vm_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    return client.virtual_machine_scale_sets.get(resource_group_name, vm_name).identity\n+    vm = get_vmss_by_aaz(cmd, resource_group_name, vm_name)\n+\n+    identity = vm.get(\"identity\", {}) if vm else None\n+\n+    if not identity:\n+        return None\n+\n+    user_assigned = identity.get('userAssignedIdentities', {})\n+\n+    if not user_assigned:\n+        identity['userAssignedIdentities'] = None\n+    else:\n+        for user_identity in user_assigned.keys():\n+            if not user_assigned.get(user_identity).get('clientId'):\n+                user_assigned[user_identity]['clientId'] = None\n+            if not user_assigned.get(user_identity).get('principalId'):\n+                user_assigned[user_identity]['principalId'] = None\n+\n+    if not identity.get('principalId'):\n+        identity['principalId'] = None\n+\n+    if not identity.get('tenantId'):\n+        identity['tenantId'] = None\n+\n+    return identity or None",
    "repo_full_name": "Azure/azure-cli",
    "discussion_comments": [
      {
        "comment_id": "2719472665",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32685,
        "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
        "discussion_id": "2719472665",
        "commented_code": "@@ -842,8 +842,31 @@ def show_vm_identity(cmd, resource_group_name, vm_name):\n \n \n def show_vmss_identity(cmd, resource_group_name, vm_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    return client.virtual_machine_scale_sets.get(resource_group_name, vm_name).identity\n+    vm = get_vmss_by_aaz(cmd, resource_group_name, vm_name)\n+\n+    identity = vm.get(\"identity\", {}) if vm else None\n+\n+    if not identity:\n+        return None\n+\n+    user_assigned = identity.get('userAssignedIdentities', {})\n+\n+    if not user_assigned:\n+        identity['userAssignedIdentities'] = None\n+    else:\n+        for user_identity in user_assigned.keys():\n+            if not user_assigned.get(user_identity).get('clientId'):\n+                user_assigned[user_identity]['clientId'] = None\n+            if not user_assigned.get(user_identity).get('principalId'):\n+                user_assigned[user_identity]['principalId'] = None\n+\n+    if not identity.get('principalId'):\n+        identity['principalId'] = None\n+\n+    if not identity.get('tenantId'):\n+        identity['tenantId'] = None\n+\n+    return identity or None",
        "comment_created_at": "2026-01-23T03:45:12+00:00",
        "comment_author": "yanzhudd",
        "comment_body": "may I ask why we need to make these changes?",
        "pr_file_module": null
      },
      {
        "comment_id": "2719489475",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32685,
        "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
        "discussion_id": "2719472665",
        "commented_code": "@@ -842,8 +842,31 @@ def show_vm_identity(cmd, resource_group_name, vm_name):\n \n \n def show_vmss_identity(cmd, resource_group_name, vm_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    return client.virtual_machine_scale_sets.get(resource_group_name, vm_name).identity\n+    vm = get_vmss_by_aaz(cmd, resource_group_name, vm_name)\n+\n+    identity = vm.get(\"identity\", {}) if vm else None\n+\n+    if not identity:\n+        return None\n+\n+    user_assigned = identity.get('userAssignedIdentities', {})\n+\n+    if not user_assigned:\n+        identity['userAssignedIdentities'] = None\n+    else:\n+        for user_identity in user_assigned.keys():\n+            if not user_assigned.get(user_identity).get('clientId'):\n+                user_assigned[user_identity]['clientId'] = None\n+            if not user_assigned.get(user_identity).get('principalId'):\n+                user_assigned[user_identity]['principalId'] = None\n+\n+    if not identity.get('principalId'):\n+        identity['principalId'] = None\n+\n+    if not identity.get('tenantId'):\n+        identity['tenantId'] = None\n+\n+    return identity or None",
        "comment_created_at": "2026-01-23T03:56:31+00:00",
        "comment_author": "william051200",
        "comment_body": "The purpose of this change is to **match the same output structure** before migration.\r\n\r\nFor example:\r\nBefore set \"None\" to the field\r\n```\r\n{\r\n  \"systemAssignedIdentity\": \"\",\r\n  \"userAssignedIdentities\": {\r\n    \"/subscriptions/fe3bc2b7-22b0-4133-af8f-9439cb831236/resourceGroups/william-rg/providers/Microsoft.ManagedIdentity/userAssignedIdentities/id1\": {}\r\n  }\r\n}\r\n```\r\n\r\nAfter set \"None\" to the field:\r\n```\r\n{\r\n  \"systemAssignedIdentity\": \"\",\r\n  \"userAssignedIdentities\": {\r\n    \"/subscriptions/fe3bc2b7-22b0-4133-af8f-9439cb831236/resourceGroups/william-rg/providers/Microsoft.ManagedIdentity/userAssignedIdentities/id1\": {\r\n      \"clientId\": null,\r\n      \"principalId\": null\r\n    }\r\n  }\r\n}\r\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "2780785338",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32685,
        "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
        "discussion_id": "2719472665",
        "commented_code": "@@ -842,8 +842,31 @@ def show_vm_identity(cmd, resource_group_name, vm_name):\n \n \n def show_vmss_identity(cmd, resource_group_name, vm_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    return client.virtual_machine_scale_sets.get(resource_group_name, vm_name).identity\n+    vm = get_vmss_by_aaz(cmd, resource_group_name, vm_name)\n+\n+    identity = vm.get(\"identity\", {}) if vm else None\n+\n+    if not identity:\n+        return None\n+\n+    user_assigned = identity.get('userAssignedIdentities', {})\n+\n+    if not user_assigned:\n+        identity['userAssignedIdentities'] = None\n+    else:\n+        for user_identity in user_assigned.keys():\n+            if not user_assigned.get(user_identity).get('clientId'):\n+                user_assigned[user_identity]['clientId'] = None\n+            if not user_assigned.get(user_identity).get('principalId'):\n+                user_assigned[user_identity]['principalId'] = None\n+\n+    if not identity.get('principalId'):\n+        identity['principalId'] = None\n+\n+    if not identity.get('tenantId'):\n+        identity['tenantId'] = None\n+\n+    return identity or None",
        "comment_created_at": "2026-02-09T06:06:25+00:00",
        "comment_author": "yanzhudd",
        "comment_body": "we don't need to match the output to the previous one **if the values were null before**. It is known and expected behavior that we ignore the null values",
        "pr_file_module": null
      },
      {
        "comment_id": "2780788573",
        "repo_full_name": "Azure/azure-cli",
        "pr_number": 32685,
        "pr_file": "src/azure-cli/azure/cli/command_modules/vm/custom.py",
        "discussion_id": "2719472665",
        "commented_code": "@@ -842,8 +842,31 @@ def show_vm_identity(cmd, resource_group_name, vm_name):\n \n \n def show_vmss_identity(cmd, resource_group_name, vm_name):\n-    client = _compute_client_factory(cmd.cli_ctx)\n-    return client.virtual_machine_scale_sets.get(resource_group_name, vm_name).identity\n+    vm = get_vmss_by_aaz(cmd, resource_group_name, vm_name)\n+\n+    identity = vm.get(\"identity\", {}) if vm else None\n+\n+    if not identity:\n+        return None\n+\n+    user_assigned = identity.get('userAssignedIdentities', {})\n+\n+    if not user_assigned:\n+        identity['userAssignedIdentities'] = None\n+    else:\n+        for user_identity in user_assigned.keys():\n+            if not user_assigned.get(user_identity).get('clientId'):\n+                user_assigned[user_identity]['clientId'] = None\n+            if not user_assigned.get(user_identity).get('principalId'):\n+                user_assigned[user_identity]['principalId'] = None\n+\n+    if not identity.get('principalId'):\n+        identity['principalId'] = None\n+\n+    if not identity.get('tenantId'):\n+        identity['tenantId'] = None\n+\n+    return identity or None",
        "comment_created_at": "2026-02-09T06:07:46+00:00",
        "comment_author": "william051200",
        "comment_body": "Got it, let me change that",
        "pr_file_module": null
      }
    ]
  }
]