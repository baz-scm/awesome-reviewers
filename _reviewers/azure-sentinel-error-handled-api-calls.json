[
  {
    "discussion_id": "3594621888",
    "pr_number": 14689,
    "pr_file": "Solutions/Infoblox/Data Connectors/InfobloxCloudDataConnector/SharedCode/table_checkpoint_manager.py",
    "created_at": "2026-07-17T06:50:48+00:00",
    "commented_code": "+\"\"\"Azure Table Storage wrapper and table-based checkpoint manager.\"\"\"\n+\n+import logging\n+from azure.data.tables import TableClient, UpdateMode\n+from azure.core.exceptions import ResourceNotFoundError, ResourceExistsError, HttpResponseError\n+\n+\n+class ExportsTableStore:\n+\n+    def __init__(self, connection_string, table_name):\n+        self.connection_string = connection_string\n+        self.table_name = table_name\n+\n+    def create(self):\n+        with TableClient.from_connection_string(self.connection_string, self.table_name) as table_client:\n+            try:\n+                table_client.create_table()\n+                logging.info(\"Checkpoint Table created\")\n+            except ResourceExistsError:\n+                logging.warning(\"Table already exists\")\n+\n+    def post(self, pk: str, rk: str, data: dict = None):\n+        with TableClient.from_connection_string(self.connection_string, self.table_name) as table_client:\n+            entity_template = {\n+                \"PartitionKey\": pk,\n+                \"RowKey\": rk,\n+            }\n+            if data is not None:\n+                entity_template.update(data)\n+            try:\n+                table_client.create_entity(entity_template)\n+            except Exception as e:\n+                logging.warning(\"could not post entity to table\")\n+                logging.warning(e)\n+                raise e",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "3600988623",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 14689,
        "pr_file": "Solutions/Infoblox/Data Connectors/InfobloxCloudDataConnector/SharedCode/table_checkpoint_manager.py",
        "discussion_id": "3594621888",
        "commented_code": "@@ -0,0 +1,128 @@\n+\"\"\"Azure Table Storage wrapper and table-based checkpoint manager.\"\"\"\n+\n+import logging\n+from azure.data.tables import TableClient, UpdateMode\n+from azure.core.exceptions import ResourceNotFoundError, ResourceExistsError, HttpResponseError\n+\n+\n+class ExportsTableStore:\n+\n+    def __init__(self, connection_string, table_name):\n+        self.connection_string = connection_string\n+        self.table_name = table_name\n+\n+    def create(self):\n+        with TableClient.from_connection_string(self.connection_string, self.table_name) as table_client:\n+            try:\n+                table_client.create_table()\n+                logging.info(\"Checkpoint Table created\")\n+            except ResourceExistsError:\n+                logging.warning(\"Table already exists\")\n+\n+    def post(self, pk: str, rk: str, data: dict = None):\n+        with TableClient.from_connection_string(self.connection_string, self.table_name) as table_client:\n+            entity_template = {\n+                \"PartitionKey\": pk,\n+                \"RowKey\": rk,\n+            }\n+            if data is not None:\n+                entity_template.update(data)\n+            try:\n+                table_client.create_entity(entity_template)\n+            except Exception as e:\n+                logging.warning(\"could not post entity to table\")\n+                logging.warning(e)\n+                raise e",
        "comment_created_at": "2026-07-17T06:50:48+00:00",
        "comment_author": "jbhaliya-crest",
        "comment_body": "Fixed \u2014 replaced the two logging.warning(...) calls with a single logging.exception(\"could not post entity to table\") (logs at ERROR with full traceback) and changed raise e to a bare raise to preserve the original stack trace.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1334440576",
    "pr_number": 9038,
    "pr_file": "Solutions/GoogleWorkspaceReports/Data Connectors/GWorkspaceReportsAPISentinelConnector/__init__.py",
    "created_at": "2023-09-22T14:13:35+00:00",
    "commented_code": "logging.error(\"Something wrong. Exception error text: {}\".format(err))\n     return latest_timestamp\n \n+def process_result(result_obj, start_time, postactivity_list, line):\n+    if result_obj is not None:\n+        result_obj = expand_data(result_obj)\n+        sorted_data = sorted(result_obj, key=lambda x: x[\"id\"][\"time\"],reverse=False)\n+        json_string = json.dumps(result_obj)\n+        byte_ = json_string.encode(\"utf-8\")\n+        byteLength = len(byte_)\n+        mbLength = byteLength/1024/1024\n+        if(len(result_obj)) > 0 and int(mbLength) < 25 :\n+            # Sort the json based on the \"timestamp\" key\n+            body = json.dumps(result_obj)\n+            statuscode = post_data(customer_id, shared_key,body,\"GWorkspace_ReportsAPI_\"+line, len(result_obj))",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "1334440576",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 9038,
        "pr_file": "Solutions/GoogleWorkspaceReports/Data Connectors/GWorkspaceReportsAPISentinelConnector/__init__.py",
        "discussion_id": "1334440576",
        "commented_code": "@@ -249,55 +267,78 @@ def gen_chunks_with_latesttime(data,log_type):\n             logging.error(\"Something wrong. Exception error text: {}\".format(err))\n     return latest_timestamp\n \n+def process_result(result_obj, start_time, postactivity_list, line):\n+    if result_obj is not None:\n+        result_obj = expand_data(result_obj)\n+        sorted_data = sorted(result_obj, key=lambda x: x[\"id\"][\"time\"],reverse=False)\n+        json_string = json.dumps(result_obj)\n+        byte_ = json_string.encode(\"utf-8\")\n+        byteLength = len(byte_)\n+        mbLength = byteLength/1024/1024\n+        if(len(result_obj)) > 0 and int(mbLength) < 25 :\n+            # Sort the json based on the \"timestamp\" key\n+            body = json.dumps(result_obj)\n+            statuscode = post_data(customer_id, shared_key,body,\"GWorkspace_ReportsAPI_\"+line, len(result_obj))",
        "comment_created_at": "2023-09-22T14:13:35+00:00",
        "comment_author": "v-muuppugund",
        "comment_body": "Please do retry pattern for log anaytics api call in case of failure",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1334418863",
    "pr_number": 9075,
    "pr_file": "Solutions/Auth0/Data Connectors/Auth0Connector/main.py",
    "created_at": "2023-09-22T13:56:33+00:00",
    "commented_code": "self.token = None\n         self.header = None\n \n-    def get_log_events(self, config: dict) -> Tuple[str, List]:\n+    def get_log_events(self, script_start_time, config: dict) -> Tuple[str, List]:",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "1334418863",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 9075,
        "pr_file": "Solutions/Auth0/Data Connectors/Auth0Connector/main.py",
        "discussion_id": "1334418863",
        "commented_code": "@@ -77,7 +75,7 @@ def __init__(self, domain, api_path, client_id, client_secret, audience):\n         self.token = None\n         self.header = None\n \n-    def get_log_events(self, config: dict) -> Tuple[str, List]:\n+    def get_log_events(self, script_start_time, config: dict) -> Tuple[str, List]:",
        "comment_created_at": "2023-09-22T13:56:33+00:00",
        "comment_author": "v-muuppugund",
        "comment_body": "Please add try catch block in api call auth0",
        "pr_file_module": null
      },
      {
        "comment_id": "1383321915",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 9075,
        "pr_file": "Solutions/Auth0/Data Connectors/Auth0Connector/main.py",
        "discussion_id": "1334418863",
        "commented_code": "@@ -77,7 +75,7 @@ def __init__(self, domain, api_path, client_id, client_secret, audience):\n         self.token = None\n         self.header = None\n \n-    def get_log_events(self, config: dict) -> Tuple[str, List]:\n+    def get_log_events(self, script_start_time, config: dict) -> Tuple[str, List]:",
        "comment_created_at": "2023-11-06T13:20:40+00:00",
        "comment_author": "v-mchatla",
        "comment_body": "incorporated retry logic",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1386752272",
    "pr_number": 9075,
    "pr_file": "Solutions/Auth0/Data Connectors/Auth0Connector/main.py",
    "created_at": "2023-11-08T14:53:57+00:00",
    "commented_code": "self.uri = self.domain + self.api_path\n         self.token = None\n         self.header = None\n-\n-    def get_log_events(self, config: dict) -> Tuple[str, List]:\n+        self.retry = retry  \n+\n+    \"\"\"This method is used to process and post the results to Log Analytics Workspace\n+        Returns:\n+            last_log_id : last processed eventId\n+            events: last processed Events\n+    \"\"\"\n+    def get_log_events(self, script_start_time, config: dict) -> Tuple[str, List]:\n         self.token = self._get_token()\n         logging.info(f'Token provided.')\n         self.header = self._get_header()\n         last_log_id = self._get_last_log_id(config)\n         #last_log_id = \"90020230126121002244690048186607762971591195832157732866\"\n         logging.info(f'\\tLast log id extracted: {last_log_id}.')\n         if last_log_id is None:\n-            return '', []\n+            #return '', []\n+            self.update_statemarker_file(config, '', [])\n+            return\n         # first request\n         params = {'from': last_log_id, 'take': '100'}\n-        resp = requests.get(self.uri, headers=self.header, params=params)\n+        count = 0\n+        error=True\n+        while error:\n+            try:\n+                error=False\n+                resp = requests.get(self.uri, headers=self.header, params=params)\n+            except Exception as err:\n+                error = True\n+                count+=1\n+                logging.error(\"Something wrong. Exception error text: {}\".format(err))\n+                if count > self.retry:\n+                    logging.error(\"Exceeded maximum Retries\")\n+                    break\n         logging.info('\\tFirst request executed.')\n         if not resp.json():\n-            return last_log_id, []\n+            #return last_log_id, []\n+            self.update_statemarker_file(config, last_log_id, [])\n+            return\n         events = resp.json()\n         logging.info('\\t response object : {events}')\n+        events.sort(key=lambda item: item['date'], reverse=True)\n+        last_log_id = events[0]['log_id']\n+        for el in events:\n+            self.sentinel.send(el)\n+        self.sentinel.flush()\n+        logging.info('Events sent to Sentinel.')\n+        self.update_statemarker_file(config, last_log_id , events)\n+\n         if \"Link\" in resp.headers :\n             next_link = resp.headers['Link']\n             next_uri = next_link[next_link.index('<') + 1:next_link.index('>')]\n             page_num = 1\n-            while resp.json():\n-                resp = requests.get(next_uri, headers=self.header)\n+            while resp.json() and len(events)!=0:\n+                count = 0\n+                error=True\n+                while error:\n+                    try:\n+                        error=False\n+                        resp = requests.get(next_uri, headers=self.header)",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "1386752272",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 9075,
        "pr_file": "Solutions/Auth0/Data Connectors/Auth0Connector/main.py",
        "discussion_id": "1386752272",
        "commented_code": "@@ -76,45 +68,98 @@ def __init__(self, domain, api_path, client_id, client_secret, audience):\n         self.uri = self.domain + self.api_path\n         self.token = None\n         self.header = None\n-\n-    def get_log_events(self, config: dict) -> Tuple[str, List]:\n+        self.retry = retry  \n+\n+    \"\"\"This method is used to process and post the results to Log Analytics Workspace\n+        Returns:\n+            last_log_id : last processed eventId\n+            events: last processed Events\n+    \"\"\"\n+    def get_log_events(self, script_start_time, config: dict) -> Tuple[str, List]:\n         self.token = self._get_token()\n         logging.info(f'Token provided.')\n         self.header = self._get_header()\n         last_log_id = self._get_last_log_id(config)\n         #last_log_id = \"90020230126121002244690048186607762971591195832157732866\"\n         logging.info(f'\\tLast log id extracted: {last_log_id}.')\n         if last_log_id is None:\n-            return '', []\n+            #return '', []\n+            self.update_statemarker_file(config, '', [])\n+            return\n         # first request\n         params = {'from': last_log_id, 'take': '100'}\n-        resp = requests.get(self.uri, headers=self.header, params=params)\n+        count = 0\n+        error=True\n+        while error:\n+            try:\n+                error=False\n+                resp = requests.get(self.uri, headers=self.header, params=params)\n+            except Exception as err:\n+                error = True\n+                count+=1\n+                logging.error(\"Something wrong. Exception error text: {}\".format(err))\n+                if count > self.retry:\n+                    logging.error(\"Exceeded maximum Retries\")\n+                    break\n         logging.info('\\tFirst request executed.')\n         if not resp.json():\n-            return last_log_id, []\n+            #return last_log_id, []\n+            self.update_statemarker_file(config, last_log_id, [])\n+            return\n         events = resp.json()\n         logging.info('\\t response object : {events}')\n+        events.sort(key=lambda item: item['date'], reverse=True)\n+        last_log_id = events[0]['log_id']\n+        for el in events:\n+            self.sentinel.send(el)\n+        self.sentinel.flush()\n+        logging.info('Events sent to Sentinel.')\n+        self.update_statemarker_file(config, last_log_id , events)\n+\n         if \"Link\" in resp.headers :\n             next_link = resp.headers['Link']\n             next_uri = next_link[next_link.index('<') + 1:next_link.index('>')]\n             page_num = 1\n-            while resp.json():\n-                resp = requests.get(next_uri, headers=self.header)\n+            while resp.json() and len(events)!=0:\n+                count = 0\n+                error=True\n+                while error:\n+                    try:\n+                        error=False\n+                        resp = requests.get(next_uri, headers=self.header)",
        "comment_created_at": "2023-11-08T14:53:57+00:00",
        "comment_author": "v-muuppugund",
        "comment_body": "Could  you please check status code responses  and handle those in code for retry i.e. 429, 500, 502, 503, 504 and other status codes i.e. 401 ,400 etc. with different messages for those",
        "pr_file_module": null
      },
      {
        "comment_id": "1391978803",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 9075,
        "pr_file": "Solutions/Auth0/Data Connectors/Auth0Connector/main.py",
        "discussion_id": "1386752272",
        "commented_code": "@@ -76,45 +68,98 @@ def __init__(self, domain, api_path, client_id, client_secret, audience):\n         self.uri = self.domain + self.api_path\n         self.token = None\n         self.header = None\n-\n-    def get_log_events(self, config: dict) -> Tuple[str, List]:\n+        self.retry = retry  \n+\n+    \"\"\"This method is used to process and post the results to Log Analytics Workspace\n+        Returns:\n+            last_log_id : last processed eventId\n+            events: last processed Events\n+    \"\"\"\n+    def get_log_events(self, script_start_time, config: dict) -> Tuple[str, List]:\n         self.token = self._get_token()\n         logging.info(f'Token provided.')\n         self.header = self._get_header()\n         last_log_id = self._get_last_log_id(config)\n         #last_log_id = \"90020230126121002244690048186607762971591195832157732866\"\n         logging.info(f'\\tLast log id extracted: {last_log_id}.')\n         if last_log_id is None:\n-            return '', []\n+            #return '', []\n+            self.update_statemarker_file(config, '', [])\n+            return\n         # first request\n         params = {'from': last_log_id, 'take': '100'}\n-        resp = requests.get(self.uri, headers=self.header, params=params)\n+        count = 0\n+        error=True\n+        while error:\n+            try:\n+                error=False\n+                resp = requests.get(self.uri, headers=self.header, params=params)\n+            except Exception as err:\n+                error = True\n+                count+=1\n+                logging.error(\"Something wrong. Exception error text: {}\".format(err))\n+                if count > self.retry:\n+                    logging.error(\"Exceeded maximum Retries\")\n+                    break\n         logging.info('\\tFirst request executed.')\n         if not resp.json():\n-            return last_log_id, []\n+            #return last_log_id, []\n+            self.update_statemarker_file(config, last_log_id, [])\n+            return\n         events = resp.json()\n         logging.info('\\t response object : {events}')\n+        events.sort(key=lambda item: item['date'], reverse=True)\n+        last_log_id = events[0]['log_id']\n+        for el in events:\n+            self.sentinel.send(el)\n+        self.sentinel.flush()\n+        logging.info('Events sent to Sentinel.')\n+        self.update_statemarker_file(config, last_log_id , events)\n+\n         if \"Link\" in resp.headers :\n             next_link = resp.headers['Link']\n             next_uri = next_link[next_link.index('<') + 1:next_link.index('>')]\n             page_num = 1\n-            while resp.json():\n-                resp = requests.get(next_uri, headers=self.header)\n+            while resp.json() and len(events)!=0:\n+                count = 0\n+                error=True\n+                while error:\n+                    try:\n+                        error=False\n+                        resp = requests.get(next_uri, headers=self.header)",
        "comment_created_at": "2023-11-14T04:44:21+00:00",
        "comment_author": "v-mchatla",
        "comment_body": "I have added backlog item to incorporate these changes.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1111480987",
    "pr_number": 7368,
    "pr_file": "Solutions/GoogleWorkspaceReports/Data Connectors/GWorkspaceReportsAPISentinelConnector/__init__.py",
    "created_at": "2023-02-20T05:51:07+00:00",
    "commented_code": "yield chunk\n \n def gen_chunks(data,log_type):\n-    for chunk in gen_chunks_to_object(data, chunksize=2000):\n-        body = json.dumps(chunk)\n-        post_data(customer_id, shared_key,body,log_type)\n+    #for chunk in gen_chunks_to_object(data, chunksize=2000):\n+        #body = json.dumps(chunk)\n+        #post_data(customer_id, shared_key,body,log_type)\n+    chunks = [data[i:i+chunksize] for i in range(0, len(data), chunksize)]\n+    logging.info(\"Entered into the chunks mode\")\n+    latest_timestamp = \"\";\n+    i = 0\n+    for chunk in chunks:\n+        try:\n+            i = i+1\n+            logging.debug(\"Iteration chunk {}\".format(i))\n+            body = json.dumps(chunk)\n+            logging.debug(body)\n+            statuscode = post_data(customer_id, shared_key,body,log_type, len(chunk))\n+            if (statuscode >= 200 and statuscode <= 299):\n+                latest_timestamp = chunk[-1][\"id\"][\"time\"]\n+                dt = datetime.strptime(latest_timestamp, '%Y-%m-%dT%H:%M:%S.%fZ')\n+                dt += timedelta(milliseconds=1)\n+                latest_timestamp = dt.strftime('%Y-%m-%dT%H:%M:%S.%f')\n+                latest_timestamp = latest_timestamp[:-3] + 'Z'\n+                logging.info(\"Chunk Timestamp {}\".format(latest_timestamp))\n+                #logging.info(\"Logs with {} activity was processed into Azure\".format(log_type))\n+                #logging.info(\"Chunk was processed{} events\".format(chunk_count))\n+            else:\n+                logging.warn(\"There is an issue with Posting data to LA - Response code: {}\".format(statuscode))\n+        except Exception as err:\n+            logging.error(\"Something wrong. Exception error text: {}\".format(err))\n+    return latest_timestamp\n \n def main(mytimer: func.TimerRequest) -> None:\n     if mytimer.past_due:\n         logging.info('The timer is past due!')\n     logging.info('Starting program')\n     global creds\n     creds = get_credentials()\n-    start_time, end_time = generate_date()\n-    logging.info('Data processing. Period(UTC): {} - {}'.format(start_time,end_time))\n+    latest_timestamp = \"\"\n+    postactivity_list = GetDates(\"\")\n     for line in activities:\n-        result_obj = get_result(line,start_time,end_time)\n+      try:\n+        start_time,end_time = GetDates(line)\n+        #resp = json.loads(start_time)\n+        logging.info('Data processing. Period(UTC): {} - {}'.format(start_time,end_time))",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "1111480987",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 7368,
        "pr_file": "Solutions/GoogleWorkspaceReports/Data Connectors/GWorkspaceReportsAPISentinelConnector/__init__.py",
        "discussion_id": "1111480987",
        "commented_code": "@@ -158,20 +212,82 @@ def gen_chunks_to_object(data,chunksize=100):\n     yield chunk\n \n def gen_chunks(data,log_type):\n-    for chunk in gen_chunks_to_object(data, chunksize=2000):\n-        body = json.dumps(chunk)\n-        post_data(customer_id, shared_key,body,log_type)\n+    #for chunk in gen_chunks_to_object(data, chunksize=2000):\n+        #body = json.dumps(chunk)\n+        #post_data(customer_id, shared_key,body,log_type)\n+    chunks = [data[i:i+chunksize] for i in range(0, len(data), chunksize)]\n+    logging.info(\"Entered into the chunks mode\")\n+    latest_timestamp = \"\";\n+    i = 0\n+    for chunk in chunks:\n+        try:\n+            i = i+1\n+            logging.debug(\"Iteration chunk {}\".format(i))\n+            body = json.dumps(chunk)\n+            logging.debug(body)\n+            statuscode = post_data(customer_id, shared_key,body,log_type, len(chunk))\n+            if (statuscode >= 200 and statuscode <= 299):\n+                latest_timestamp = chunk[-1][\"id\"][\"time\"]\n+                dt = datetime.strptime(latest_timestamp, '%Y-%m-%dT%H:%M:%S.%fZ')\n+                dt += timedelta(milliseconds=1)\n+                latest_timestamp = dt.strftime('%Y-%m-%dT%H:%M:%S.%f')\n+                latest_timestamp = latest_timestamp[:-3] + 'Z'\n+                logging.info(\"Chunk Timestamp {}\".format(latest_timestamp))\n+                #logging.info(\"Logs with {} activity was processed into Azure\".format(log_type))\n+                #logging.info(\"Chunk was processed{} events\".format(chunk_count))\n+            else:\n+                logging.warn(\"There is an issue with Posting data to LA - Response code: {}\".format(statuscode))\n+        except Exception as err:\n+            logging.error(\"Something wrong. Exception error text: {}\".format(err))\n+    return latest_timestamp\n \n def main(mytimer: func.TimerRequest) -> None:\n     if mytimer.past_due:\n         logging.info('The timer is past due!')\n     logging.info('Starting program')\n     global creds\n     creds = get_credentials()\n-    start_time, end_time = generate_date()\n-    logging.info('Data processing. Period(UTC): {} - {}'.format(start_time,end_time))\n+    latest_timestamp = \"\"\n+    postactivity_list = GetDates(\"\")\n     for line in activities:\n-        result_obj = get_result(line,start_time,end_time)\n+      try:\n+        start_time,end_time = GetDates(line)\n+        #resp = json.loads(start_time)\n+        logging.info('Data processing. Period(UTC): {} - {}'.format(start_time,end_time))",
        "comment_created_at": "2023-02-20T05:51:07+00:00",
        "comment_author": "anki-narravula",
        "comment_body": "IF starttime >= endTime then we can exit for that activity. Otherwise API may throw error in such cases",
        "pr_file_module": null
      },
      {
        "comment_id": "1112102094",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 7368,
        "pr_file": "Solutions/GoogleWorkspaceReports/Data Connectors/GWorkspaceReportsAPISentinelConnector/__init__.py",
        "discussion_id": "1111480987",
        "commented_code": "@@ -158,20 +212,82 @@ def gen_chunks_to_object(data,chunksize=100):\n     yield chunk\n \n def gen_chunks(data,log_type):\n-    for chunk in gen_chunks_to_object(data, chunksize=2000):\n-        body = json.dumps(chunk)\n-        post_data(customer_id, shared_key,body,log_type)\n+    #for chunk in gen_chunks_to_object(data, chunksize=2000):\n+        #body = json.dumps(chunk)\n+        #post_data(customer_id, shared_key,body,log_type)\n+    chunks = [data[i:i+chunksize] for i in range(0, len(data), chunksize)]\n+    logging.info(\"Entered into the chunks mode\")\n+    latest_timestamp = \"\";\n+    i = 0\n+    for chunk in chunks:\n+        try:\n+            i = i+1\n+            logging.debug(\"Iteration chunk {}\".format(i))\n+            body = json.dumps(chunk)\n+            logging.debug(body)\n+            statuscode = post_data(customer_id, shared_key,body,log_type, len(chunk))\n+            if (statuscode >= 200 and statuscode <= 299):\n+                latest_timestamp = chunk[-1][\"id\"][\"time\"]\n+                dt = datetime.strptime(latest_timestamp, '%Y-%m-%dT%H:%M:%S.%fZ')\n+                dt += timedelta(milliseconds=1)\n+                latest_timestamp = dt.strftime('%Y-%m-%dT%H:%M:%S.%f')\n+                latest_timestamp = latest_timestamp[:-3] + 'Z'\n+                logging.info(\"Chunk Timestamp {}\".format(latest_timestamp))\n+                #logging.info(\"Logs with {} activity was processed into Azure\".format(log_type))\n+                #logging.info(\"Chunk was processed{} events\".format(chunk_count))\n+            else:\n+                logging.warn(\"There is an issue with Posting data to LA - Response code: {}\".format(statuscode))\n+        except Exception as err:\n+            logging.error(\"Something wrong. Exception error text: {}\".format(err))\n+    return latest_timestamp\n \n def main(mytimer: func.TimerRequest) -> None:\n     if mytimer.past_due:\n         logging.info('The timer is past due!')\n     logging.info('Starting program')\n     global creds\n     creds = get_credentials()\n-    start_time, end_time = generate_date()\n-    logging.info('Data processing. Period(UTC): {} - {}'.format(start_time,end_time))\n+    latest_timestamp = \"\"\n+    postactivity_list = GetDates(\"\")\n     for line in activities:\n-        result_obj = get_result(line,start_time,end_time)\n+      try:\n+        start_time,end_time = GetDates(line)\n+        #resp = json.loads(start_time)\n+        logging.info('Data processing. Period(UTC): {} - {}'.format(start_time,end_time))",
        "comment_created_at": "2023-02-20T15:35:55+00:00",
        "comment_author": "Tichandr",
        "comment_body": "handled it ",
        "pr_file_module": null
      }
    ]
  }
]