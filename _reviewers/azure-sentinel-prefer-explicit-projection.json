[
  {
    "discussion_id": "3319334234",
    "pr_number": 14221,
    "pr_file": "Parsers/ASimAlertEvent/Parsers/ASimAlertEventGoogleSecOps.yaml",
    "created_at": "2026-05-28T16:41:18+00:00",
    "commented_code": "+Parser:\n+  Title: Alert Event ASIM parser for Google SecOps Detection Alerts\n+  Version: '0.1.0'\n+  LastUpdated: May 04, 2025\n+Product:\n+  Name: Google SecOps\n+Normalization:\n+  Schema: AlertEvent\n+  Version: '0.1'\n+References:\n+- Title: ASIM Alert Event Schema\n+  Link: https://aka.ms/ASimAlertEventDoc\n+- Title: ASIM\n+  Link: https://aka.ms/AboutASIM\n+- Title: Google SecOps Detection Alerts API\n+  Link: https://cloud.google.com/chronicle/docs/reference/rest/v1alpha/projects.locations.instances.legacy/legacySearchDetections\n+Description: |\n+  This ASIM parser supports normalizing Google SecOps Detection Alerts\n+  (ingested via the DetectionAlerts_CL custom table) to the ASIM AlertEvent normalized schema.\n+ParserName: ASimAlertEventGoogleSecOps\n+EquivalentBuiltInParser: _ASim_AlertEvent_GoogleSecOps\n+ParserParams:\n+  - Name: disabled\n+    Type: bool\n+    Default: false\n+ParserQuery: |\n+  let AlertVerdictLookup = datatable(threat_verdict: string, AlertVerdict: string)\n+    [\n+    \"MALICIOUS\",  \"True Positive\",\n+    \"SUSPICIOUS\", \"True Positive\",\n+    \"BENIGN\",     \"Benign Positive\",\n+    \"UNKNOWN\",    \"Unknown\"\n+  ];\n+  let SeverityLookup = datatable(orig_severity: string, EventSeverity: string)\n+    [\n+    \"CRITICAL\",      \"High\",\n+    \"HIGH\",          \"High\",\n+    \"MEDIUM\",        \"Medium\",\n+    \"LOW\",           \"Low\",\n+    \"INFORMATIONAL\", \"Informational\",\n+    \"INFO\",          \"Informational\"\n+  ];\n+  let EventSubTypeLookup = datatable(detection_type: string, EventSubType: string)\n+    [\n+    \"RULE_DETECTION\", \"Threat\",\n+    \"GCTI_FINDING\",   \"Threat\",\n+    \"JOB_DETECTION\",  \"Suspicious Activity\"\n+  ];\n+  let DetectionMethodLookup = datatable(detection_type: string, DetectionMethod: string)\n+    [\n+    \"RULE_DETECTION\", \"Behavioral Analytics\",\n+    \"GCTI_FINDING\",   \"Threat Intelligence\",\n+    \"JOB_DETECTION\",  \"Automated Investigation\"\n+  ];\n+  let parser = (disabled: bool=false) {\n+    DetectionAlerts_CL\n+    | where not(disabled)\n+    //  Expand detection array (one row per detection element) \n+    | extend detection_array = column_ifexists(\"detection\", dynamic([]))\n+    | where isnotnull(detection_array) and array_length(detection_array) > 0\n+    | mv-expand detection = detection_array\n+    //  Top-level columns \n+    | extend\n+        detection_id   = column_ifexists(\"id\", \"\"),\n+        detection_type = column_ifexists(\"DetectionType\", \"\"),\n+        tw_start       = todatetime(column_ifexists(\"timeWindow_startTime\", \"\")),\n+        tw_end         = todatetime(column_ifexists(\"timeWindow_endTime\", \"\")),\n+        collection_arr = column_ifexists(\"collectionElements\", dynamic(null)),\n+        detect_timing  = column_ifexists(\"detectionTimingDetails\", dynamic(null)),\n+        latency_metrics = column_ifexists(\"latencyMetrics\", dynamic(null))\n+    //  Fields from expanded detection element \n+    | extend\n+        rule_name      = tostring(detection.ruleName),\n+        rule_id        = tostring(detection.ruleId),\n+        rule_version   = tostring(detection.ruleVersion),\n+        rule_type      = tostring(detection.ruleType),\n+        rule_labels    = detection.ruleLabels,\n+        detect_desc    = tostring(detection.description),\n+        orig_severity  = tostring(detection.severity),\n+        alert_state    = tostring(detection.alertState),\n+        risk_score_raw = tostring(detection.riskScore),\n+        url_back       = tostring(detection.urlBackToProduct),\n+        detect_fields  = detection.detectionFields,\n+        outcomes       = detection.outcomes,\n+        variables      = detection.variables\n+    //  Entity variables (principal / source / target) \n+    | extend\n+        DvcIpAddr   = tostring(coalesce(\n+                        variables.principal_ip.value,\n+                        variables.source_ip.value,\n+                        variables.correlation_ip.value)),\n+        Username    = tostring(coalesce(\n+                        variables.principal_user_userid.value,\n+                        variables.source_user_userid.value,\n+                        variables.user.value))\n+    //  Severity normalisation \n+    | lookup SeverityLookup on orig_severity\n+    | extend EventSeverity = coalesce(EventSeverity, \"Informational\")\n+    //  EventSubType / DetectionMethod \n+    | lookup EventSubTypeLookup on detection_type\n+    | extend EventSubType = coalesce(EventSubType, \"Threat\")\n+    | lookup DetectionMethodLookup on detection_type\n+    | extend DetectionMethod = coalesce(DetectionMethod, \"Behavioral Analytics\")\n+    //  AlertStatus \n+    | extend\n+        AlertStatus         = case(alert_state == \"ALERTING\", \"Active\", \"Closed\"),\n+        AlertOriginalStatus = alert_state\n+    //  ThreatRiskLevel \n+    | extend\n+        ThreatRiskLevel         = toint(risk_score_raw),\n+        ThreatOriginalRiskLevel = risk_score_raw\n+    //  Threat intelligence from collectionElements \n+    | extend\n+        threat_obj          = coalesce(todynamic(collection_arr)[0].references[0].entity.metadata.threat[0],todynamic(collection_arr)[1].references[0].entity.metadata.threat[0]),\n+        entity_type_raw     = coalesce(tostring(todynamic(collection_arr)[0].references[0].entity.metadata.entityType), tostring(todynamic(collection_arr)[1].references[0].entity.metadata.entityType))\n+    | extend\n+        threat_verdict_raw  = tostring(threat_obj.threatVerdict),\n+        threat_category_raw = tostring(threat_obj.category),\n+        threat_name_raw     = tostring(threat_obj.summary),\n+        threat_first_ts     = todatetime(threat_obj.firstDiscoveredTime),\n+        threat_last_ts      = todatetime(threat_obj.lastUpdatedTime),\n+        threat_url          = tostring(threat_obj.urlBackToProduct)\n+    //  AlertVerdict \n+    | extend threat_verdict = threat_verdict_raw\n+    | lookup AlertVerdictLookup on threat_verdict\n+    | extend AlertVerdict = coalesce(AlertVerdict, \"Unknown\")\n+    //  ThreatCategory \n+    | extend\n+        ThreatOriginalCategory = threat_category_raw,\n+        ThreatCategory = case(\n+            threat_category_raw has \"MALWARE\",       \"Malware\",\n+            threat_category_raw has \"RANSOMWARE\",    \"Ransomware\",\n+            threat_category_raw has \"TROJAN\",        \"Trojan\",\n+            threat_category_raw has \"VIRUS\",         \"Virus\",\n+            threat_category_raw has \"WORM\",          \"Worm\",\n+            threat_category_raw has \"ADWARE\",        \"Adware\",\n+            threat_category_raw has \"SPYWARE\",       \"Spyware\",\n+            threat_category_raw has \"ROOTKIT\",       \"Rootkit\",\n+            threat_category_raw has \"CRYPTOMINER\",   \"Cryptominer\",\n+            threat_category_raw has \"PHISHING\",      \"Phishing\",\n+            threat_category_raw has \"SPAM\",          \"Spam\",\n+            threat_category_raw has \"NETWORK_RECON\", \"Security Policy Violation\",\n+            isnotempty(threat_category_raw),         \"Unknown\",\n+                                                     \"Unknown\")\n+    //  IndicatorType \n+    | extend IndicatorType = case(\n+        entity_type_raw == \"IP_ADDRESS\",  \"Ip\",\n+        entity_type_raw == \"DOMAIN_NAME\", \"Url\",\n+        entity_type_raw == \"USER\",        \"User\",\n+        entity_type_raw == \"ASSET\",       \"Host\",\n+        entity_type_raw == \"FILE\",        \"File\",\n+        entity_type_raw == \"URL\",         \"Url\",\n+        entity_type_raw == \"PROCESS\",     \"Process\",\n+                                          \"Ip\")\n+    // Hostname helper function\n+    | extend Hostname = tostring(coalesce(\n+                variables.principal_hostname.value,\n+                variables.principal_asset_hostname.value,\n+                variables.source_hostname.value))\n+    | invoke _ASIM_ResolveDvcFQDN(\"Hostname\")\n+    //  User fields \n+    | extend UsernameType = _ASIM_GetUsernameType(Username)\n+        //  AttackTactics / AttackTechniques \n+    | mv-apply rl = rule_labels on (\n+        summarize\n+            AttackTactics = anyif(tostring(rl.value), rl.key == \"mitre_attack_tactic\"),\n+            AttackTechniques = anyif(tostring(rl.value), rl.key == \"mitre_attack_technique\")\n+    )\n+    //  Mandatory / Recommended event fields \n+    | extend\n+        EventType             = \"Alert\",\n+        EventCount            = int(1),\n+        EventStartTime        = tw_start,\n+        EventEndTime          = tw_end,\n+        EventUid              = detection_id,\n+        EventOriginalUid      = detection_id,\n+        EventProduct          = 'GoogleSecOps',\n+        EventVendor           = 'Google',\n+        EventSchema           = 'AlertEvent',\n+        EventSchemaVersion    = '0.1',\n+        EventMessage          = detect_desc,\n+        EventOriginalSeverity = orig_severity,\n+        EventOriginalSubType  = detection_type,\n+        EventReportUrl        = url_back\n+    //  Alert / Rule / Threat fields \n+    | extend\n+        AlertName               = rule_name,\n+        RuleName                = rule_name,\n+        RuleDescription         = detect_desc,\n+        ThreatName              = threat_name_raw,\n+        ThreatFirstReportedTime = threat_first_ts,\n+        ThreatLastReportedTime  = threat_last_ts,\n+        Url                     = threat_url\n+    //  AdditionalFields bag \n+    | extend AdditionalFields = bag_pack(\n+        \"ruleId\",                 rule_id,\n+        \"ruleVersion\",            rule_version,\n+        \"ruleType\",               rule_type,\n+        \"ruleLabels\",             rule_labels,\n+        \"detectionFields\",        detect_fields,\n+        \"outcomes\",               outcomes,\n+        \"variables\",              variables,\n+        \"detectionTimingDetails\", detect_timing,\n+        \"latencyMetrics\",         latency_metrics,\n+        \"collectionElements\", collection_arr)\n+    //  Aliases \n+    | extend\n+        AlertId          = EventUid,\n+        AlertDescription = EventMessage,\n+        IpAddr           = DvcIpAddr,\n+        Hostname         = DvcHostname,\n+        User             = Username,\n+        Rule             = RuleName\n+    //  Drop intermediate working columns \n+    | project-away",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "3319334234",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 14221,
        "pr_file": "Parsers/ASimAlertEvent/Parsers/ASimAlertEventGoogleSecOps.yaml",
        "discussion_id": "3319334234",
        "commented_code": "@@ -0,0 +1,223 @@\n+Parser:\n+  Title: Alert Event ASIM parser for Google SecOps Detection Alerts\n+  Version: '0.1.0'\n+  LastUpdated: May 04, 2025\n+Product:\n+  Name: Google SecOps\n+Normalization:\n+  Schema: AlertEvent\n+  Version: '0.1'\n+References:\n+- Title: ASIM Alert Event Schema\n+  Link: https://aka.ms/ASimAlertEventDoc\n+- Title: ASIM\n+  Link: https://aka.ms/AboutASIM\n+- Title: Google SecOps Detection Alerts API\n+  Link: https://cloud.google.com/chronicle/docs/reference/rest/v1alpha/projects.locations.instances.legacy/legacySearchDetections\n+Description: |\n+  This ASIM parser supports normalizing Google SecOps Detection Alerts\n+  (ingested via the DetectionAlerts_CL custom table) to the ASIM AlertEvent normalized schema.\n+ParserName: ASimAlertEventGoogleSecOps\n+EquivalentBuiltInParser: _ASim_AlertEvent_GoogleSecOps\n+ParserParams:\n+  - Name: disabled\n+    Type: bool\n+    Default: false\n+ParserQuery: |\n+  let AlertVerdictLookup = datatable(threat_verdict: string, AlertVerdict: string)\n+    [\n+    \"MALICIOUS\",  \"True Positive\",\n+    \"SUSPICIOUS\", \"True Positive\",\n+    \"BENIGN\",     \"Benign Positive\",\n+    \"UNKNOWN\",    \"Unknown\"\n+  ];\n+  let SeverityLookup = datatable(orig_severity: string, EventSeverity: string)\n+    [\n+    \"CRITICAL\",      \"High\",\n+    \"HIGH\",          \"High\",\n+    \"MEDIUM\",        \"Medium\",\n+    \"LOW\",           \"Low\",\n+    \"INFORMATIONAL\", \"Informational\",\n+    \"INFO\",          \"Informational\"\n+  ];\n+  let EventSubTypeLookup = datatable(detection_type: string, EventSubType: string)\n+    [\n+    \"RULE_DETECTION\", \"Threat\",\n+    \"GCTI_FINDING\",   \"Threat\",\n+    \"JOB_DETECTION\",  \"Suspicious Activity\"\n+  ];\n+  let DetectionMethodLookup = datatable(detection_type: string, DetectionMethod: string)\n+    [\n+    \"RULE_DETECTION\", \"Behavioral Analytics\",\n+    \"GCTI_FINDING\",   \"Threat Intelligence\",\n+    \"JOB_DETECTION\",  \"Automated Investigation\"\n+  ];\n+  let parser = (disabled: bool=false) {\n+    DetectionAlerts_CL\n+    | where not(disabled)\n+    //  Expand detection array (one row per detection element) \n+    | extend detection_array = column_ifexists(\"detection\", dynamic([]))\n+    | where isnotnull(detection_array) and array_length(detection_array) > 0\n+    | mv-expand detection = detection_array\n+    //  Top-level columns \n+    | extend\n+        detection_id   = column_ifexists(\"id\", \"\"),\n+        detection_type = column_ifexists(\"DetectionType\", \"\"),\n+        tw_start       = todatetime(column_ifexists(\"timeWindow_startTime\", \"\")),\n+        tw_end         = todatetime(column_ifexists(\"timeWindow_endTime\", \"\")),\n+        collection_arr = column_ifexists(\"collectionElements\", dynamic(null)),\n+        detect_timing  = column_ifexists(\"detectionTimingDetails\", dynamic(null)),\n+        latency_metrics = column_ifexists(\"latencyMetrics\", dynamic(null))\n+    //  Fields from expanded detection element \n+    | extend\n+        rule_name      = tostring(detection.ruleName),\n+        rule_id        = tostring(detection.ruleId),\n+        rule_version   = tostring(detection.ruleVersion),\n+        rule_type      = tostring(detection.ruleType),\n+        rule_labels    = detection.ruleLabels,\n+        detect_desc    = tostring(detection.description),\n+        orig_severity  = tostring(detection.severity),\n+        alert_state    = tostring(detection.alertState),\n+        risk_score_raw = tostring(detection.riskScore),\n+        url_back       = tostring(detection.urlBackToProduct),\n+        detect_fields  = detection.detectionFields,\n+        outcomes       = detection.outcomes,\n+        variables      = detection.variables\n+    //  Entity variables (principal / source / target) \n+    | extend\n+        DvcIpAddr   = tostring(coalesce(\n+                        variables.principal_ip.value,\n+                        variables.source_ip.value,\n+                        variables.correlation_ip.value)),\n+        Username    = tostring(coalesce(\n+                        variables.principal_user_userid.value,\n+                        variables.source_user_userid.value,\n+                        variables.user.value))\n+    //  Severity normalisation \n+    | lookup SeverityLookup on orig_severity\n+    | extend EventSeverity = coalesce(EventSeverity, \"Informational\")\n+    //  EventSubType / DetectionMethod \n+    | lookup EventSubTypeLookup on detection_type\n+    | extend EventSubType = coalesce(EventSubType, \"Threat\")\n+    | lookup DetectionMethodLookup on detection_type\n+    | extend DetectionMethod = coalesce(DetectionMethod, \"Behavioral Analytics\")\n+    //  AlertStatus \n+    | extend\n+        AlertStatus         = case(alert_state == \"ALERTING\", \"Active\", \"Closed\"),\n+        AlertOriginalStatus = alert_state\n+    //  ThreatRiskLevel \n+    | extend\n+        ThreatRiskLevel         = toint(risk_score_raw),\n+        ThreatOriginalRiskLevel = risk_score_raw\n+    //  Threat intelligence from collectionElements \n+    | extend\n+        threat_obj          = coalesce(todynamic(collection_arr)[0].references[0].entity.metadata.threat[0],todynamic(collection_arr)[1].references[0].entity.metadata.threat[0]),\n+        entity_type_raw     = coalesce(tostring(todynamic(collection_arr)[0].references[0].entity.metadata.entityType), tostring(todynamic(collection_arr)[1].references[0].entity.metadata.entityType))\n+    | extend\n+        threat_verdict_raw  = tostring(threat_obj.threatVerdict),\n+        threat_category_raw = tostring(threat_obj.category),\n+        threat_name_raw     = tostring(threat_obj.summary),\n+        threat_first_ts     = todatetime(threat_obj.firstDiscoveredTime),\n+        threat_last_ts      = todatetime(threat_obj.lastUpdatedTime),\n+        threat_url          = tostring(threat_obj.urlBackToProduct)\n+    //  AlertVerdict \n+    | extend threat_verdict = threat_verdict_raw\n+    | lookup AlertVerdictLookup on threat_verdict\n+    | extend AlertVerdict = coalesce(AlertVerdict, \"Unknown\")\n+    //  ThreatCategory \n+    | extend\n+        ThreatOriginalCategory = threat_category_raw,\n+        ThreatCategory = case(\n+            threat_category_raw has \"MALWARE\",       \"Malware\",\n+            threat_category_raw has \"RANSOMWARE\",    \"Ransomware\",\n+            threat_category_raw has \"TROJAN\",        \"Trojan\",\n+            threat_category_raw has \"VIRUS\",         \"Virus\",\n+            threat_category_raw has \"WORM\",          \"Worm\",\n+            threat_category_raw has \"ADWARE\",        \"Adware\",\n+            threat_category_raw has \"SPYWARE\",       \"Spyware\",\n+            threat_category_raw has \"ROOTKIT\",       \"Rootkit\",\n+            threat_category_raw has \"CRYPTOMINER\",   \"Cryptominer\",\n+            threat_category_raw has \"PHISHING\",      \"Phishing\",\n+            threat_category_raw has \"SPAM\",          \"Spam\",\n+            threat_category_raw has \"NETWORK_RECON\", \"Security Policy Violation\",\n+            isnotempty(threat_category_raw),         \"Unknown\",\n+                                                     \"Unknown\")\n+    //  IndicatorType \n+    | extend IndicatorType = case(\n+        entity_type_raw == \"IP_ADDRESS\",  \"Ip\",\n+        entity_type_raw == \"DOMAIN_NAME\", \"Url\",\n+        entity_type_raw == \"USER\",        \"User\",\n+        entity_type_raw == \"ASSET\",       \"Host\",\n+        entity_type_raw == \"FILE\",        \"File\",\n+        entity_type_raw == \"URL\",         \"Url\",\n+        entity_type_raw == \"PROCESS\",     \"Process\",\n+                                          \"Ip\")\n+    // Hostname helper function\n+    | extend Hostname = tostring(coalesce(\n+                variables.principal_hostname.value,\n+                variables.principal_asset_hostname.value,\n+                variables.source_hostname.value))\n+    | invoke _ASIM_ResolveDvcFQDN(\"Hostname\")\n+    //  User fields \n+    | extend UsernameType = _ASIM_GetUsernameType(Username)\n+        //  AttackTactics / AttackTechniques \n+    | mv-apply rl = rule_labels on (\n+        summarize\n+            AttackTactics = anyif(tostring(rl.value), rl.key == \"mitre_attack_tactic\"),\n+            AttackTechniques = anyif(tostring(rl.value), rl.key == \"mitre_attack_technique\")\n+    )\n+    //  Mandatory / Recommended event fields \n+    | extend\n+        EventType             = \"Alert\",\n+        EventCount            = int(1),\n+        EventStartTime        = tw_start,\n+        EventEndTime          = tw_end,\n+        EventUid              = detection_id,\n+        EventOriginalUid      = detection_id,\n+        EventProduct          = 'GoogleSecOps',\n+        EventVendor           = 'Google',\n+        EventSchema           = 'AlertEvent',\n+        EventSchemaVersion    = '0.1',\n+        EventMessage          = detect_desc,\n+        EventOriginalSeverity = orig_severity,\n+        EventOriginalSubType  = detection_type,\n+        EventReportUrl        = url_back\n+    //  Alert / Rule / Threat fields \n+    | extend\n+        AlertName               = rule_name,\n+        RuleName                = rule_name,\n+        RuleDescription         = detect_desc,\n+        ThreatName              = threat_name_raw,\n+        ThreatFirstReportedTime = threat_first_ts,\n+        ThreatLastReportedTime  = threat_last_ts,\n+        Url                     = threat_url\n+    //  AdditionalFields bag \n+    | extend AdditionalFields = bag_pack(\n+        \"ruleId\",                 rule_id,\n+        \"ruleVersion\",            rule_version,\n+        \"ruleType\",               rule_type,\n+        \"ruleLabels\",             rule_labels,\n+        \"detectionFields\",        detect_fields,\n+        \"outcomes\",               outcomes,\n+        \"variables\",              variables,\n+        \"detectionTimingDetails\", detect_timing,\n+        \"latencyMetrics\",         latency_metrics,\n+        \"collectionElements\", collection_arr)\n+    //  Aliases \n+    | extend\n+        AlertId          = EventUid,\n+        AlertDescription = EventMessage,\n+        IpAddr           = DvcIpAddr,\n+        Hostname         = DvcHostname,\n+        User             = Username,\n+        Rule             = RuleName\n+    //  Drop intermediate working columns \n+    | project-away",
        "comment_created_at": "2026-05-28T16:41:18+00:00",
        "comment_author": "yummyblabla",
        "comment_body": "I would prefer using project ASIM columns instead of project-away in case the DetectionAlerts_CL has addition of columns. It will be future-proofing",
        "pr_file_module": null
      },
      {
        "comment_id": "3361107327",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 14221,
        "pr_file": "Parsers/ASimAlertEvent/Parsers/ASimAlertEventGoogleSecOps.yaml",
        "discussion_id": "3319334234",
        "commented_code": "@@ -0,0 +1,223 @@\n+Parser:\n+  Title: Alert Event ASIM parser for Google SecOps Detection Alerts\n+  Version: '0.1.0'\n+  LastUpdated: May 04, 2025\n+Product:\n+  Name: Google SecOps\n+Normalization:\n+  Schema: AlertEvent\n+  Version: '0.1'\n+References:\n+- Title: ASIM Alert Event Schema\n+  Link: https://aka.ms/ASimAlertEventDoc\n+- Title: ASIM\n+  Link: https://aka.ms/AboutASIM\n+- Title: Google SecOps Detection Alerts API\n+  Link: https://cloud.google.com/chronicle/docs/reference/rest/v1alpha/projects.locations.instances.legacy/legacySearchDetections\n+Description: |\n+  This ASIM parser supports normalizing Google SecOps Detection Alerts\n+  (ingested via the DetectionAlerts_CL custom table) to the ASIM AlertEvent normalized schema.\n+ParserName: ASimAlertEventGoogleSecOps\n+EquivalentBuiltInParser: _ASim_AlertEvent_GoogleSecOps\n+ParserParams:\n+  - Name: disabled\n+    Type: bool\n+    Default: false\n+ParserQuery: |\n+  let AlertVerdictLookup = datatable(threat_verdict: string, AlertVerdict: string)\n+    [\n+    \"MALICIOUS\",  \"True Positive\",\n+    \"SUSPICIOUS\", \"True Positive\",\n+    \"BENIGN\",     \"Benign Positive\",\n+    \"UNKNOWN\",    \"Unknown\"\n+  ];\n+  let SeverityLookup = datatable(orig_severity: string, EventSeverity: string)\n+    [\n+    \"CRITICAL\",      \"High\",\n+    \"HIGH\",          \"High\",\n+    \"MEDIUM\",        \"Medium\",\n+    \"LOW\",           \"Low\",\n+    \"INFORMATIONAL\", \"Informational\",\n+    \"INFO\",          \"Informational\"\n+  ];\n+  let EventSubTypeLookup = datatable(detection_type: string, EventSubType: string)\n+    [\n+    \"RULE_DETECTION\", \"Threat\",\n+    \"GCTI_FINDING\",   \"Threat\",\n+    \"JOB_DETECTION\",  \"Suspicious Activity\"\n+  ];\n+  let DetectionMethodLookup = datatable(detection_type: string, DetectionMethod: string)\n+    [\n+    \"RULE_DETECTION\", \"Behavioral Analytics\",\n+    \"GCTI_FINDING\",   \"Threat Intelligence\",\n+    \"JOB_DETECTION\",  \"Automated Investigation\"\n+  ];\n+  let parser = (disabled: bool=false) {\n+    DetectionAlerts_CL\n+    | where not(disabled)\n+    //  Expand detection array (one row per detection element) \n+    | extend detection_array = column_ifexists(\"detection\", dynamic([]))\n+    | where isnotnull(detection_array) and array_length(detection_array) > 0\n+    | mv-expand detection = detection_array\n+    //  Top-level columns \n+    | extend\n+        detection_id   = column_ifexists(\"id\", \"\"),\n+        detection_type = column_ifexists(\"DetectionType\", \"\"),\n+        tw_start       = todatetime(column_ifexists(\"timeWindow_startTime\", \"\")),\n+        tw_end         = todatetime(column_ifexists(\"timeWindow_endTime\", \"\")),\n+        collection_arr = column_ifexists(\"collectionElements\", dynamic(null)),\n+        detect_timing  = column_ifexists(\"detectionTimingDetails\", dynamic(null)),\n+        latency_metrics = column_ifexists(\"latencyMetrics\", dynamic(null))\n+    //  Fields from expanded detection element \n+    | extend\n+        rule_name      = tostring(detection.ruleName),\n+        rule_id        = tostring(detection.ruleId),\n+        rule_version   = tostring(detection.ruleVersion),\n+        rule_type      = tostring(detection.ruleType),\n+        rule_labels    = detection.ruleLabels,\n+        detect_desc    = tostring(detection.description),\n+        orig_severity  = tostring(detection.severity),\n+        alert_state    = tostring(detection.alertState),\n+        risk_score_raw = tostring(detection.riskScore),\n+        url_back       = tostring(detection.urlBackToProduct),\n+        detect_fields  = detection.detectionFields,\n+        outcomes       = detection.outcomes,\n+        variables      = detection.variables\n+    //  Entity variables (principal / source / target) \n+    | extend\n+        DvcIpAddr   = tostring(coalesce(\n+                        variables.principal_ip.value,\n+                        variables.source_ip.value,\n+                        variables.correlation_ip.value)),\n+        Username    = tostring(coalesce(\n+                        variables.principal_user_userid.value,\n+                        variables.source_user_userid.value,\n+                        variables.user.value))\n+    //  Severity normalisation \n+    | lookup SeverityLookup on orig_severity\n+    | extend EventSeverity = coalesce(EventSeverity, \"Informational\")\n+    //  EventSubType / DetectionMethod \n+    | lookup EventSubTypeLookup on detection_type\n+    | extend EventSubType = coalesce(EventSubType, \"Threat\")\n+    | lookup DetectionMethodLookup on detection_type\n+    | extend DetectionMethod = coalesce(DetectionMethod, \"Behavioral Analytics\")\n+    //  AlertStatus \n+    | extend\n+        AlertStatus         = case(alert_state == \"ALERTING\", \"Active\", \"Closed\"),\n+        AlertOriginalStatus = alert_state\n+    //  ThreatRiskLevel \n+    | extend\n+        ThreatRiskLevel         = toint(risk_score_raw),\n+        ThreatOriginalRiskLevel = risk_score_raw\n+    //  Threat intelligence from collectionElements \n+    | extend\n+        threat_obj          = coalesce(todynamic(collection_arr)[0].references[0].entity.metadata.threat[0],todynamic(collection_arr)[1].references[0].entity.metadata.threat[0]),\n+        entity_type_raw     = coalesce(tostring(todynamic(collection_arr)[0].references[0].entity.metadata.entityType), tostring(todynamic(collection_arr)[1].references[0].entity.metadata.entityType))\n+    | extend\n+        threat_verdict_raw  = tostring(threat_obj.threatVerdict),\n+        threat_category_raw = tostring(threat_obj.category),\n+        threat_name_raw     = tostring(threat_obj.summary),\n+        threat_first_ts     = todatetime(threat_obj.firstDiscoveredTime),\n+        threat_last_ts      = todatetime(threat_obj.lastUpdatedTime),\n+        threat_url          = tostring(threat_obj.urlBackToProduct)\n+    //  AlertVerdict \n+    | extend threat_verdict = threat_verdict_raw\n+    | lookup AlertVerdictLookup on threat_verdict\n+    | extend AlertVerdict = coalesce(AlertVerdict, \"Unknown\")\n+    //  ThreatCategory \n+    | extend\n+        ThreatOriginalCategory = threat_category_raw,\n+        ThreatCategory = case(\n+            threat_category_raw has \"MALWARE\",       \"Malware\",\n+            threat_category_raw has \"RANSOMWARE\",    \"Ransomware\",\n+            threat_category_raw has \"TROJAN\",        \"Trojan\",\n+            threat_category_raw has \"VIRUS\",         \"Virus\",\n+            threat_category_raw has \"WORM\",          \"Worm\",\n+            threat_category_raw has \"ADWARE\",        \"Adware\",\n+            threat_category_raw has \"SPYWARE\",       \"Spyware\",\n+            threat_category_raw has \"ROOTKIT\",       \"Rootkit\",\n+            threat_category_raw has \"CRYPTOMINER\",   \"Cryptominer\",\n+            threat_category_raw has \"PHISHING\",      \"Phishing\",\n+            threat_category_raw has \"SPAM\",          \"Spam\",\n+            threat_category_raw has \"NETWORK_RECON\", \"Security Policy Violation\",\n+            isnotempty(threat_category_raw),         \"Unknown\",\n+                                                     \"Unknown\")\n+    //  IndicatorType \n+    | extend IndicatorType = case(\n+        entity_type_raw == \"IP_ADDRESS\",  \"Ip\",\n+        entity_type_raw == \"DOMAIN_NAME\", \"Url\",\n+        entity_type_raw == \"USER\",        \"User\",\n+        entity_type_raw == \"ASSET\",       \"Host\",\n+        entity_type_raw == \"FILE\",        \"File\",\n+        entity_type_raw == \"URL\",         \"Url\",\n+        entity_type_raw == \"PROCESS\",     \"Process\",\n+                                          \"Ip\")\n+    // Hostname helper function\n+    | extend Hostname = tostring(coalesce(\n+                variables.principal_hostname.value,\n+                variables.principal_asset_hostname.value,\n+                variables.source_hostname.value))\n+    | invoke _ASIM_ResolveDvcFQDN(\"Hostname\")\n+    //  User fields \n+    | extend UsernameType = _ASIM_GetUsernameType(Username)\n+        //  AttackTactics / AttackTechniques \n+    | mv-apply rl = rule_labels on (\n+        summarize\n+            AttackTactics = anyif(tostring(rl.value), rl.key == \"mitre_attack_tactic\"),\n+            AttackTechniques = anyif(tostring(rl.value), rl.key == \"mitre_attack_technique\")\n+    )\n+    //  Mandatory / Recommended event fields \n+    | extend\n+        EventType             = \"Alert\",\n+        EventCount            = int(1),\n+        EventStartTime        = tw_start,\n+        EventEndTime          = tw_end,\n+        EventUid              = detection_id,\n+        EventOriginalUid      = detection_id,\n+        EventProduct          = 'GoogleSecOps',\n+        EventVendor           = 'Google',\n+        EventSchema           = 'AlertEvent',\n+        EventSchemaVersion    = '0.1',\n+        EventMessage          = detect_desc,\n+        EventOriginalSeverity = orig_severity,\n+        EventOriginalSubType  = detection_type,\n+        EventReportUrl        = url_back\n+    //  Alert / Rule / Threat fields \n+    | extend\n+        AlertName               = rule_name,\n+        RuleName                = rule_name,\n+        RuleDescription         = detect_desc,\n+        ThreatName              = threat_name_raw,\n+        ThreatFirstReportedTime = threat_first_ts,\n+        ThreatLastReportedTime  = threat_last_ts,\n+        Url                     = threat_url\n+    //  AdditionalFields bag \n+    | extend AdditionalFields = bag_pack(\n+        \"ruleId\",                 rule_id,\n+        \"ruleVersion\",            rule_version,\n+        \"ruleType\",               rule_type,\n+        \"ruleLabels\",             rule_labels,\n+        \"detectionFields\",        detect_fields,\n+        \"outcomes\",               outcomes,\n+        \"variables\",              variables,\n+        \"detectionTimingDetails\", detect_timing,\n+        \"latencyMetrics\",         latency_metrics,\n+        \"collectionElements\", collection_arr)\n+    //  Aliases \n+    | extend\n+        AlertId          = EventUid,\n+        AlertDescription = EventMessage,\n+        IpAddr           = DvcIpAddr,\n+        Hostname         = DvcHostname,\n+        User             = Username,\n+        Rule             = RuleName\n+    //  Drop intermediate working columns \n+    | project-away",
        "comment_created_at": "2026-06-05T07:33:07+00:00",
        "comment_author": "fenil-savani",
        "comment_body": "Done",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1165232140",
    "pr_number": 7792,
    "pr_file": "Solutions/Microsoft 365 Defender/Analytic Rules/SUNSPOTHashes.yaml",
    "created_at": "2023-04-13T09:05:17+00:00",
    "commented_code": "(DeviceImageLoadEvents\n   | where InitiatingProcessSHA256 in (SUNSPOT_Hashes))\n   | extend HostCustomEntity = DeviceName, timestamp=TimeGenerated\n+  | extend HostName = tostring(split(DeviceName, '.', 0)[0]), DnsDomain = tostring(strcat_array(array_slice(split(DeviceName, '.'), 1, -1), '.'))",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "1165232140",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 7792,
        "pr_file": "Solutions/Microsoft 365 Defender/Analytic Rules/SUNSPOTHashes.yaml",
        "discussion_id": "1165232140",
        "commented_code": "@@ -31,10 +31,13 @@ query: |\n   (DeviceImageLoadEvents\n   | where InitiatingProcessSHA256 in (SUNSPOT_Hashes))\n   | extend HostCustomEntity = DeviceName, timestamp=TimeGenerated\n+  | extend HostName = tostring(split(DeviceName, '.', 0)[0]), DnsDomain = tostring(strcat_array(array_slice(split(DeviceName, '.'), 1, -1), '.'))",
        "comment_created_at": "2023-04-13T09:05:17+00:00",
        "comment_author": "rahul0216",
        "comment_body": "\"HostCustomEntity = DeviceName\" This is not required, so you can remove it.\r\n\r\nBetter You can combine these two lines like,\r\nextend timestamp=TimeGenerated, HostName = tostring(split(DeviceName, '.', 0)[0]), DnsDomain = tostring(strcat_array(array_slice(split(DeviceName, '.'), 1, -1), '.'))\r\n\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "1165257436",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 7792,
        "pr_file": "Solutions/Microsoft 365 Defender/Analytic Rules/SUNSPOTHashes.yaml",
        "discussion_id": "1165232140",
        "commented_code": "@@ -31,10 +31,13 @@ query: |\n   (DeviceImageLoadEvents\n   | where InitiatingProcessSHA256 in (SUNSPOT_Hashes))\n   | extend HostCustomEntity = DeviceName, timestamp=TimeGenerated\n+  | extend HostName = tostring(split(DeviceName, '.', 0)[0]), DnsDomain = tostring(strcat_array(array_slice(split(DeviceName, '.'), 1, -1), '.'))",
        "comment_created_at": "2023-04-13T09:22:12+00:00",
        "comment_author": "manishkumar1991",
        "comment_body": "done",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1139120619",
    "pr_number": 7582,
    "pr_file": "Solutions/Azure Active Directory/Analytic Rules/AccountCreatedandDeletedinShortTimeframe.yaml",
    "created_at": "2023-03-16T17:33:29+00:00",
    "commented_code": "| extend CreatedByUser = tostring(InitiatedBy.user.userPrincipalName), CreatedByIPAddress = tostring(InitiatedBy.user.ipAddress)",
    "repo_full_name": "Azure/Azure-Sentinel",
    "discussion_comments": [
      {
        "comment_id": "1139120619",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 7582,
        "pr_file": "Solutions/Azure Active Directory/Analytic Rules/AccountCreatedandDeletedinShortTimeframe.yaml",
        "discussion_id": "1139120619",
        "commented_code": "@@ -42,15 +42,15 @@ query: |\n   | extend CreatedByUser = tostring(InitiatedBy.user.userPrincipalName), CreatedByIPAddress = tostring(InitiatedBy.user.ipAddress)",
        "comment_created_at": "2023-03-16T17:33:29+00:00",
        "comment_author": "petebryan",
        "comment_body": "In this one we have some fixed position parameters such as `TargetResources[0]` - we should replace this with parse where statements as those positional indicators aren't reliable. In addition this query has some duplicate code we could functionize to make it easier to read",
        "pr_file_module": null
      },
      {
        "comment_id": "1139211749",
        "repo_full_name": "Azure/Azure-Sentinel",
        "pr_number": 7582,
        "pr_file": "Solutions/Azure Active Directory/Analytic Rules/AccountCreatedandDeletedinShortTimeframe.yaml",
        "discussion_id": "1139120619",
        "commented_code": "@@ -42,15 +42,15 @@ query: |\n   | extend CreatedByUser = tostring(InitiatedBy.user.userPrincipalName), CreatedByIPAddress = tostring(InitiatedBy.user.ipAddress)",
        "comment_created_at": "2023-03-16T18:32:08+00:00",
        "comment_author": "devikamehra",
        "comment_body": "Removed TargetResource[0]\r\nFor constructing a function for code readability, I will reconsider.",
        "pr_file_module": null
      }
    ]
  }
]