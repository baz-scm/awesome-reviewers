[
  {
    "discussion_id": "510545340",
    "pr_number": 135,
    "pr_file": "cloudsplaining/__init__.py",
    "created_at": "2020-10-23T01:26:44+00:00",
    "commented_code": "# pylint: disable=missing-module-docstring\n import logging\n-# import sys\n+import sys\n # Set default logging handler to avoid \"No handler found\" warnings.\n from logging import NullHandler\n \n-logging.getLogger(__name__).addHandler(NullHandler())\n+#logging.getLogger(__name__).addHandler(NullHandler())\n # Uncomment to get the full debug logs.\n # 2020-10-06 10:04:17,200 - root - DEBUG - Leveraging the bundled IAM Definition.\n # Need to figure out how to get click_log to do this for me.\n-# root = logging.getLogger()\n-# root.setLevel(logging.DEBUG)\n-# handler = logging.StreamHandler(sys.stdout)\n-# formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n-# handler.setFormatter(formatter)\n-# root.addHandler(handler)\n+logger = logging.getLogger(__name__)\n+logger.setLevel(logging.WARNING)\n+handler = logging.StreamHandler(sys.stdout)\n+formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n+handler.setFormatter(formatter)\n+logger.addHandler(handler)\n+\n+#logging.getLogger().addHandler(NullHandler())\n \n name = \"cloudsplaining\"  # pylint: disable=invalid-name\n+\n+def change_log_level(log_level):",
    "repo_full_name": "salesforce/cloudsplaining",
    "discussion_comments": [
      {
        "comment_id": "510545340",
        "repo_full_name": "salesforce/cloudsplaining",
        "pr_number": 135,
        "pr_file": "cloudsplaining/__init__.py",
        "discussion_id": "510545340",
        "commented_code": "@@ -1,18 +1,24 @@\n # pylint: disable=missing-module-docstring\n import logging\n-# import sys\n+import sys\n # Set default logging handler to avoid \"No handler found\" warnings.\n from logging import NullHandler\n \n-logging.getLogger(__name__).addHandler(NullHandler())\n+#logging.getLogger(__name__).addHandler(NullHandler())\n # Uncomment to get the full debug logs.\n # 2020-10-06 10:04:17,200 - root - DEBUG - Leveraging the bundled IAM Definition.\n # Need to figure out how to get click_log to do this for me.\n-# root = logging.getLogger()\n-# root.setLevel(logging.DEBUG)\n-# handler = logging.StreamHandler(sys.stdout)\n-# formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n-# handler.setFormatter(formatter)\n-# root.addHandler(handler)\n+logger = logging.getLogger(__name__)\n+logger.setLevel(logging.WARNING)\n+handler = logging.StreamHandler(sys.stdout)\n+formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n+handler.setFormatter(formatter)\n+logger.addHandler(handler)\n+\n+#logging.getLogger().addHandler(NullHandler())\n \n name = \"cloudsplaining\"  # pylint: disable=invalid-name\n+\n+def change_log_level(log_level):",
        "comment_created_at": "2020-10-23T01:26:44+00:00",
        "comment_author": "reetasingh",
        "comment_body": "to change the log level based on -v flag passed on command line",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "510545439",
    "pr_number": 135,
    "pr_file": "cloudsplaining/__init__.py",
    "created_at": "2020-10-23T01:27:11+00:00",
    "commented_code": "# pylint: disable=missing-module-docstring\n import logging\n-# import sys\n+import sys\n # Set default logging handler to avoid \"No handler found\" warnings.\n from logging import NullHandler\n \n-logging.getLogger(__name__).addHandler(NullHandler())\n+#logging.getLogger(__name__).addHandler(NullHandler())\n # Uncomment to get the full debug logs.\n # 2020-10-06 10:04:17,200 - root - DEBUG - Leveraging the bundled IAM Definition.\n # Need to figure out how to get click_log to do this for me.\n-# root = logging.getLogger()\n-# root.setLevel(logging.DEBUG)\n-# handler = logging.StreamHandler(sys.stdout)\n-# formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n-# handler.setFormatter(formatter)\n-# root.addHandler(handler)\n+logger = logging.getLogger(__name__)\n+logger.setLevel(logging.WARNING)",
    "repo_full_name": "salesforce/cloudsplaining",
    "discussion_comments": [
      {
        "comment_id": "510545439",
        "repo_full_name": "salesforce/cloudsplaining",
        "pr_number": 135,
        "pr_file": "cloudsplaining/__init__.py",
        "discussion_id": "510545439",
        "commented_code": "@@ -1,18 +1,24 @@\n # pylint: disable=missing-module-docstring\n import logging\n-# import sys\n+import sys\n # Set default logging handler to avoid \"No handler found\" warnings.\n from logging import NullHandler\n \n-logging.getLogger(__name__).addHandler(NullHandler())\n+#logging.getLogger(__name__).addHandler(NullHandler())\n # Uncomment to get the full debug logs.\n # 2020-10-06 10:04:17,200 - root - DEBUG - Leveraging the bundled IAM Definition.\n # Need to figure out how to get click_log to do this for me.\n-# root = logging.getLogger()\n-# root.setLevel(logging.DEBUG)\n-# handler = logging.StreamHandler(sys.stdout)\n-# formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n-# handler.setFormatter(formatter)\n-# root.addHandler(handler)\n+logger = logging.getLogger(__name__)\n+logger.setLevel(logging.WARNING)",
        "comment_created_at": "2020-10-23T01:27:11+00:00",
        "comment_author": "reetasingh",
        "comment_body": "default log level is `logging.WARNING`",
        "pr_file_module": null
      }
    ]
  }
]