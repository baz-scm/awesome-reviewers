[
  {
    "discussion_id": "1592440835",
    "pr_number": 11800,
    "pr_file": "docs/reference/docker_compose.yaml",
    "created_at": "2024-05-07T12:59:07+00:00",
    "commented_code": "Setting the `COMPOSE_IGNORE_ORPHANS` environment variable to `true` stops docker compose from detecting orphaned\n     containers for the project.\n \n+    Setting the `COMPOSE_MENU` environment variable to `false` disabled the helper menu when running `docker compose up`\n+    in detached mode. Alternatively, you can also run `docker compose up --menu=false` to disable the helper menu.",
    "repo_full_name": "docker/compose",
    "discussion_comments": [
      {
        "comment_id": "1592440835",
        "repo_full_name": "docker/compose",
        "pr_number": 11800,
        "pr_file": "docs/reference/docker_compose.yaml",
        "discussion_id": "1592440835",
        "commented_code": "@@ -118,6 +118,9 @@ long: |-\n     Setting the `COMPOSE_IGNORE_ORPHANS` environment variable to `true` stops docker compose from detecting orphaned\n     containers for the project.\n \n+    Setting the `COMPOSE_MENU` environment variable to `false` disabled the helper menu when running `docker compose up`\n+    in detached mode. Alternatively, you can also run `docker compose up --menu=false` to disable the helper menu.",
        "comment_created_at": "2024-05-07T12:59:07+00:00",
        "comment_author": "milas",
        "comment_body": "```suggestion\r\n    Setting the `COMPOSE_MENU` environment variable to `false` disables the helper menu when running `docker compose up`\r\n    in attached mode. Alternatively, you can also run `docker compose up --menu=false` to disable the helper menu.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1028489867",
    "pr_number": 9997,
    "pr_file": "docs/reference/docker_compose.yaml",
    "created_at": "2022-11-21T20:30:54+00:00",
    "commented_code": "specify the flag, Compose uses the current directory name.\n     Project name can also be set by `COMPOSE_PROJECT_NAME` environment variable.\n \n-    Most compose subcommand can be ran without a compose file, just passing\n-    project name to retrieve the relevant resources.\n+    Most compose subcommand can be run without a compose file, just passing\n+    the project name to retrieve the relevant resources.",
    "repo_full_name": "docker/compose",
    "discussion_comments": [
      {
        "comment_id": "1028489867",
        "repo_full_name": "docker/compose",
        "pr_number": 9997,
        "pr_file": "docs/reference/docker_compose.yaml",
        "discussion_id": "1028489867",
        "commented_code": "@@ -67,8 +67,8 @@ long: |-\n     specify the flag, Compose uses the current directory name.\n     Project name can also be set by `COMPOSE_PROJECT_NAME` environment variable.\n \n-    Most compose subcommand can be ran without a compose file, just passing\n-    project name to retrieve the relevant resources.\n+    Most compose subcommand can be run without a compose file, just passing\n+    the project name to retrieve the relevant resources.",
        "comment_created_at": "2022-11-21T20:30:54+00:00",
        "comment_author": "milas",
        "comment_body": "```suggestion\r\n    Many Compose subcommands can be run without a Compose file by passing\r\n    the project name.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1028490268",
    "pr_number": 9997,
    "pr_file": "docs/reference/docker_compose.yaml",
    "created_at": "2022-11-21T20:31:31+00:00",
    "commented_code": "You can set environment variables for various docker compose options, including the `-f`, `-p` and `--profiles` flags.\n \n     Setting the `COMPOSE_FILE` environment variable is equivalent to passing the `-f` flag,\n-    `COMPOSE_PROJECT_NAME` environment variable does the same for to the `-p` flag,\n-    and so does `COMPOSE_PROFILES` environment variable for to the `--profiles` flag.\n+    `COMPOSE_PROJECT_NAME` environment variable does the same to the `-p` flag,\n+    and so does `COMPOSE_PROFILES` environment variable to the `--profiles` flag.",
    "repo_full_name": "docker/compose",
    "discussion_comments": [
      {
        "comment_id": "1028490268",
        "repo_full_name": "docker/compose",
        "pr_number": 9997,
        "pr_file": "docs/reference/docker_compose.yaml",
        "discussion_id": "1028490268",
        "commented_code": "@@ -94,10 +94,10 @@ long: |-\n     You can set environment variables for various docker compose options, including the `-f`, `-p` and `--profiles` flags.\n \n     Setting the `COMPOSE_FILE` environment variable is equivalent to passing the `-f` flag,\n-    `COMPOSE_PROJECT_NAME` environment variable does the same for to the `-p` flag,\n-    and so does `COMPOSE_PROFILES` environment variable for to the `--profiles` flag.\n+    `COMPOSE_PROJECT_NAME` environment variable does the same to the `-p` flag,\n+    and so does `COMPOSE_PROFILES` environment variable to the `--profiles` flag.",
        "comment_created_at": "2022-11-21T20:31:31+00:00",
        "comment_author": "milas",
        "comment_body": "```suggestion\r\n    `COMPOSE_PROJECT_NAME` environment variable does the same as the `-p` flag,\r\n    and `COMPOSE_PROFILES` environment variable is equivalent to the `--profiles` flag.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1028490348",
    "pr_number": 9997,
    "pr_file": "docs/reference/docker_compose.yaml",
    "created_at": "2022-11-21T20:31:38+00:00",
    "commented_code": "You can set environment variables for various docker compose options, including the `-f`, `-p` and `--profiles` flags.\n \n     Setting the `COMPOSE_FILE` environment variable is equivalent to passing the `-f` flag,\n-    `COMPOSE_PROJECT_NAME` environment variable does the same for to the `-p` flag,\n-    and so does `COMPOSE_PROFILES` environment variable for to the `--profiles` flag.\n+    `COMPOSE_PROJECT_NAME` environment variable does the same to the `-p` flag,\n+    and so does `COMPOSE_PROFILES` environment variable to the `--profiles` flag.\n \n-    If flags are explicitly set on command line, associated environment variable is ignored\n+    If flags are explicitly set on the command line, the associated environment variable is ignored",
    "repo_full_name": "docker/compose",
    "discussion_comments": [
      {
        "comment_id": "1028490348",
        "repo_full_name": "docker/compose",
        "pr_number": 9997,
        "pr_file": "docs/reference/docker_compose.yaml",
        "discussion_id": "1028490348",
        "commented_code": "@@ -94,10 +94,10 @@ long: |-\n     You can set environment variables for various docker compose options, including the `-f`, `-p` and `--profiles` flags.\n \n     Setting the `COMPOSE_FILE` environment variable is equivalent to passing the `-f` flag,\n-    `COMPOSE_PROJECT_NAME` environment variable does the same for to the `-p` flag,\n-    and so does `COMPOSE_PROFILES` environment variable for to the `--profiles` flag.\n+    `COMPOSE_PROJECT_NAME` environment variable does the same to the `-p` flag,\n+    and so does `COMPOSE_PROFILES` environment variable to the `--profiles` flag.\n \n-    If flags are explicitly set on command line, associated environment variable is ignored\n+    If flags are explicitly set on the command line, the associated environment variable is ignored",
        "comment_created_at": "2022-11-21T20:31:38+00:00",
        "comment_author": "milas",
        "comment_body": "```suggestion\r\n    If a flag is set via the command line, the associated environment variable is ignored.\r\n```",
        "pr_file_module": null
      }
    ]
  }
]