[
  {
    "discussion_id": "1916077573",
    "pr_number": 2227,
    "pr_file": "dbgpt/storage/knowledge_graph/community_summary.py",
    "created_at": "2025-01-15T07:49:38+00:00",
    "commented_code": "]\n         context = \"\n\".join(summaries) if summaries else \"\"\n \n-        enable_similarity_search = self._graph_store.enable_similarity_search\n+        enable_text2gql_search = self._graph_store.enable_text2gql_search\n \n+        text2gql_query = \"\"\n         subgraph = None\n         subgraph_for_doc = None\n \n-        # Local search: extract keywords and explore subgraph\n-        triplet_graph_enabled = self._triplet_graph_enabled\n-        document_graph_enabled = self._document_graph_enabled\n-\n-        # Using subs to transfer keywords or embeddings\n-        # Using subs to transfer keywords\n-        keywords: List[str] = await self._keyword_extractor.extract(text)\n-\n-        # If enable similarity search, using subs to transfer embeddings\n-        subs: Union[List[str], List[List[float]]]\n-        if enable_similarity_search:\n-            # Embedding the question\n-            vector = await self._text_embedder.embed(text)\n-            # Embedding the keywords\n-            vectors = await self._text_embedder.batch_embed(\n-                keywords, batch_size=self._triplet_embedding_batch_size\n-            )\n-            # Using the embeddings of keywords and question\n-            vectors.append(vector)\n-            subs = vectors\n-        else:\n-            subs = keywords\n-\n-        # If enable triplet graph, using subs to search enetities\n-        # subs -> enetities\n-        if triplet_graph_enabled:\n-            subgraph = self._graph_store_apdater.explore_trigraph(\n-                subs=subs,\n-                limit=topk,\n-                topk=self._similarity_search_topk,\n-                score_threshold=self._similarity_search_score_threshold,\n-            )\n-\n-        # If enabled document graph\n-        if document_graph_enabled:\n-            # If not enable triplet graph or subgraph is None\n-            # Using subs to search chunks\n-            # subs -> chunks -> doc\n-            if subgraph is None or subgraph.vertex_count == 0:\n-                subgraph_for_doc = (\n-                    self._graph_store_apdater.explore_docgraph_without_entities(\n-                        subs=subs,\n-                        topk=self._similarity_search_topk,\n-                        score_threshold=self._similarity_search_score_threshold,\n-                        limit=self._knowledge_graph_chunk_search_top_size,\n-                    )\n+        # if enable text2gql search, use translated query to retrieve subgraph\n+        if enable_text2gql_search:\n+            intention = await self._intent_interpreter.translate(text)\n+            interaction = await self._text2cypher.translate(json.dumps(intention))\n+            try:\n+                query = interaction[\"query\"]\n+                if \"LIMIT\" not in query:\n+                    query += \" LIMIT 10\"\n+                subgraph = self._graph_store_apdater.query(query=query)\n+                text2gql_query = query\n+            except Exception as e:\n+                text2gql_query = \"\"\n+                subgraph = None\n+",
    "repo_full_name": "eosphoros-ai/DB-GPT",
    "discussion_comments": [
      {
        "comment_id": "1916077573",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 2227,
        "pr_file": "dbgpt/storage/knowledge_graph/community_summary.py",
        "discussion_id": "1916077573",
        "commented_code": "@@ -520,90 +532,108 @@ async def asimilar_search_with_scores(\n         ]\n         context = \"\\n\".join(summaries) if summaries else \"\"\n \n-        enable_similarity_search = self._graph_store.enable_similarity_search\n+        enable_text2gql_search = self._graph_store.enable_text2gql_search\n \n+        text2gql_query = \"\"\n         subgraph = None\n         subgraph_for_doc = None\n \n-        # Local search: extract keywords and explore subgraph\n-        triplet_graph_enabled = self._triplet_graph_enabled\n-        document_graph_enabled = self._document_graph_enabled\n-\n-        # Using subs to transfer keywords or embeddings\n-        # Using subs to transfer keywords\n-        keywords: List[str] = await self._keyword_extractor.extract(text)\n-\n-        # If enable similarity search, using subs to transfer embeddings\n-        subs: Union[List[str], List[List[float]]]\n-        if enable_similarity_search:\n-            # Embedding the question\n-            vector = await self._text_embedder.embed(text)\n-            # Embedding the keywords\n-            vectors = await self._text_embedder.batch_embed(\n-                keywords, batch_size=self._triplet_embedding_batch_size\n-            )\n-            # Using the embeddings of keywords and question\n-            vectors.append(vector)\n-            subs = vectors\n-        else:\n-            subs = keywords\n-\n-        # If enable triplet graph, using subs to search enetities\n-        # subs -> enetities\n-        if triplet_graph_enabled:\n-            subgraph = self._graph_store_apdater.explore_trigraph(\n-                subs=subs,\n-                limit=topk,\n-                topk=self._similarity_search_topk,\n-                score_threshold=self._similarity_search_score_threshold,\n-            )\n-\n-        # If enabled document graph\n-        if document_graph_enabled:\n-            # If not enable triplet graph or subgraph is None\n-            # Using subs to search chunks\n-            # subs -> chunks -> doc\n-            if subgraph is None or subgraph.vertex_count == 0:\n-                subgraph_for_doc = (\n-                    self._graph_store_apdater.explore_docgraph_without_entities(\n-                        subs=subs,\n-                        topk=self._similarity_search_topk,\n-                        score_threshold=self._similarity_search_score_threshold,\n-                        limit=self._knowledge_graph_chunk_search_top_size,\n-                    )\n+        # if enable text2gql search, use translated query to retrieve subgraph\n+        if enable_text2gql_search:\n+            intention = await self._intent_interpreter.translate(text)\n+            interaction = await self._text2cypher.translate(json.dumps(intention))\n+            try:\n+                query = interaction[\"query\"]\n+                if \"LIMIT\" not in query:\n+                    query += \" LIMIT 10\"\n+                subgraph = self._graph_store_apdater.query(query=query)\n+                text2gql_query = query\n+            except Exception as e:\n+                text2gql_query = \"\"\n+                subgraph = None\n+",
        "comment_created_at": "2025-01-15T07:49:38+00:00",
        "comment_author": "Appointat",
        "comment_body": "Add a warning log.\r\n\r\nAnd, you have to catch the expected exception rather than every exception.",
        "pr_file_module": null
      },
      {
        "comment_id": "1918316892",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 2227,
        "pr_file": "dbgpt/storage/knowledge_graph/community_summary.py",
        "discussion_id": "1916077573",
        "commented_code": "@@ -520,90 +532,108 @@ async def asimilar_search_with_scores(\n         ]\n         context = \"\\n\".join(summaries) if summaries else \"\"\n \n-        enable_similarity_search = self._graph_store.enable_similarity_search\n+        enable_text2gql_search = self._graph_store.enable_text2gql_search\n \n+        text2gql_query = \"\"\n         subgraph = None\n         subgraph_for_doc = None\n \n-        # Local search: extract keywords and explore subgraph\n-        triplet_graph_enabled = self._triplet_graph_enabled\n-        document_graph_enabled = self._document_graph_enabled\n-\n-        # Using subs to transfer keywords or embeddings\n-        # Using subs to transfer keywords\n-        keywords: List[str] = await self._keyword_extractor.extract(text)\n-\n-        # If enable similarity search, using subs to transfer embeddings\n-        subs: Union[List[str], List[List[float]]]\n-        if enable_similarity_search:\n-            # Embedding the question\n-            vector = await self._text_embedder.embed(text)\n-            # Embedding the keywords\n-            vectors = await self._text_embedder.batch_embed(\n-                keywords, batch_size=self._triplet_embedding_batch_size\n-            )\n-            # Using the embeddings of keywords and question\n-            vectors.append(vector)\n-            subs = vectors\n-        else:\n-            subs = keywords\n-\n-        # If enable triplet graph, using subs to search enetities\n-        # subs -> enetities\n-        if triplet_graph_enabled:\n-            subgraph = self._graph_store_apdater.explore_trigraph(\n-                subs=subs,\n-                limit=topk,\n-                topk=self._similarity_search_topk,\n-                score_threshold=self._similarity_search_score_threshold,\n-            )\n-\n-        # If enabled document graph\n-        if document_graph_enabled:\n-            # If not enable triplet graph or subgraph is None\n-            # Using subs to search chunks\n-            # subs -> chunks -> doc\n-            if subgraph is None or subgraph.vertex_count == 0:\n-                subgraph_for_doc = (\n-                    self._graph_store_apdater.explore_docgraph_without_entities(\n-                        subs=subs,\n-                        topk=self._similarity_search_topk,\n-                        score_threshold=self._similarity_search_score_threshold,\n-                        limit=self._knowledge_graph_chunk_search_top_size,\n-                    )\n+        # if enable text2gql search, use translated query to retrieve subgraph\n+        if enable_text2gql_search:\n+            intention = await self._intent_interpreter.translate(text)\n+            interaction = await self._text2cypher.translate(json.dumps(intention))\n+            try:\n+                query = interaction[\"query\"]\n+                if \"LIMIT\" not in query:\n+                    query += \" LIMIT 10\"\n+                subgraph = self._graph_store_apdater.query(query=query)\n+                text2gql_query = query\n+            except Exception as e:\n+                text2gql_query = \"\"\n+                subgraph = None\n+",
        "comment_created_at": "2025-01-16T11:50:59+00:00",
        "comment_author": "Appointat",
        "comment_body": "Add a warning log.\n\nAnd, you have to catch the expected exception rather than every exception.\n\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1916126850",
    "pr_number": 2227,
    "pr_file": "dbgpt/storage/knowledge_graph/community/tugraph_store_adapter.py",
    "created_at": "2025-01-15T08:27:38+00:00",
    "commented_code": "def query(self, query: str, **kwargs) -> MemoryGraph:\n         \"\"\"Execute a query on graph.\n \n-        white_list: List[str] = kwargs.get(\"white_list\", []), which contains the white\n-        list of properties and filters the properties that are not in the white list.\n+        white_list: List[str] = kwargs.get(\"white_list\", []),\n+        which contains the white list of properties that start with '_',\n+        filters all the properties that start with '_' and are not in the white list.\n+\n+        white_list: List of properties to keep\n+                    - If []: Keep default properties (those not starting with '_'\n+                        and not in ['id', 'name'])\n+                    - If list of strings: Keep default properties (those not starting with '_'\n+                        and not in ['id', 'name']) and properties in white_list\n         \"\"\"\n+        try:\n+            query_result = self.graph_store.conn.run(query=query)\n+        except Exception as e:\n+            raise",
    "repo_full_name": "eosphoros-ai/DB-GPT",
    "discussion_comments": [
      {
        "comment_id": "1916126850",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 2227,
        "pr_file": "dbgpt/storage/knowledge_graph/community/tugraph_store_adapter.py",
        "discussion_id": "1916126850",
        "commented_code": "@@ -890,21 +890,23 @@ def explore_docgraph_without_entities(\n     def query(self, query: str, **kwargs) -> MemoryGraph:\n         \"\"\"Execute a query on graph.\n \n-        white_list: List[str] = kwargs.get(\"white_list\", []), which contains the white\n-        list of properties and filters the properties that are not in the white list.\n+        white_list: List[str] = kwargs.get(\"white_list\", []),\n+        which contains the white list of properties that start with '_',\n+        filters all the properties that start with '_' and are not in the white list.\n+\n+        white_list: List of properties to keep\n+                    - If []: Keep default properties (those not starting with '_'\n+                        and not in ['id', 'name'])\n+                    - If list of strings: Keep default properties (those not starting with '_'\n+                        and not in ['id', 'name']) and properties in white_list\n         \"\"\"\n+        try:\n+            query_result = self.graph_store.conn.run(query=query)\n+        except Exception as e:\n+            raise",
        "comment_created_at": "2025-01-15T08:27:38+00:00",
        "comment_author": "Appointat",
        "comment_body": "The except handler raises immediatelyPylintW0706:try-except-raise, define a new exception type.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1918334419",
    "pr_number": 2227,
    "pr_file": "dbgpt/storage/knowledge_graph/community/tugraph_store_adapter.py",
    "created_at": "2025-01-16T12:03:43+00:00",
    "commented_code": "def query(self, query: str, **kwargs) -> MemoryGraph:\n         \"\"\"Execute a query on graph.\n \n-        white_list: List[str] = kwargs.get(\"white_list\", []), which contains the white\n-        list of properties and filters the properties that are not in the white list.\n+        white_list: List[str] = kwargs.get(\"white_list\", []),\n+        which contains the white list of properties that start with '_',\n+        filters all the properties that start with '_' and are not in the white list.\n+\n+        white_list: List of properties to keep\n+                    - If []: Keep default properties (those not starting with '_'\n+                        and not in ['id', 'name'])\n+                    - If list of strings: Keep default properties\n+                        (those not starting with '_' and not in ['id', 'name'])\n+                        and properties in white_list\n         \"\"\"\n-        query_result = self.graph_store.conn.run(query=query)\n-        white_list: List[str] = kwargs.get(\n-            \"white_list\",\n-            [\n-                \"id\",\n-                \"name\",\n-                \"description\",\n-                \"_document_id\",\n-                \"_chunk_id\",\n-                \"_community_id\",\n-            ],\n-        )\n-        vertices, edges = self._get_nodes_edges_from_queried_data(\n-            query_result, white_list\n-        )\n+        try:\n+            query_result = self.graph_store.conn.run(query=query)\n+        except Exception as e:\n+            logger.error(f\"Failed to execute query: {e}\")\n+            query_result = []\n+        finally:\n+            white_list: List[str] = kwargs.get(\"white_list\", [])\n+            vertices, edges = self._get_nodes_edges_from_queried_data(\n+                query_result, white_list\n+            )\n \n-        mg = MemoryGraph()\n-        for vertex in vertices:\n-            mg.upsert_vertex(vertex)\n-        for edge in edges:\n-            mg.append_edge(edge)\n-        return mg\n+            mg = MemoryGraph()\n+            for vertex in vertices:\n+                mg.upsert_vertex(vertex)\n+            for edge in edges:\n+                mg.append_edge(edge)\n+            return mg",
    "repo_full_name": "eosphoros-ai/DB-GPT",
    "discussion_comments": [
      {
        "comment_id": "1918334419",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 2227,
        "pr_file": "dbgpt/storage/knowledge_graph/community/tugraph_store_adapter.py",
        "discussion_id": "1918334419",
        "commented_code": "@@ -890,31 +890,34 @@ def explore_docgraph_without_entities(\n     def query(self, query: str, **kwargs) -> MemoryGraph:\n         \"\"\"Execute a query on graph.\n \n-        white_list: List[str] = kwargs.get(\"white_list\", []), which contains the white\n-        list of properties and filters the properties that are not in the white list.\n+        white_list: List[str] = kwargs.get(\"white_list\", []),\n+        which contains the white list of properties that start with '_',\n+        filters all the properties that start with '_' and are not in the white list.\n+\n+        white_list: List of properties to keep\n+                    - If []: Keep default properties (those not starting with '_'\n+                        and not in ['id', 'name'])\n+                    - If list of strings: Keep default properties\n+                        (those not starting with '_' and not in ['id', 'name'])\n+                        and properties in white_list\n         \"\"\"\n-        query_result = self.graph_store.conn.run(query=query)\n-        white_list: List[str] = kwargs.get(\n-            \"white_list\",\n-            [\n-                \"id\",\n-                \"name\",\n-                \"description\",\n-                \"_document_id\",\n-                \"_chunk_id\",\n-                \"_community_id\",\n-            ],\n-        )\n-        vertices, edges = self._get_nodes_edges_from_queried_data(\n-            query_result, white_list\n-        )\n+        try:\n+            query_result = self.graph_store.conn.run(query=query)\n+        except Exception as e:\n+            logger.error(f\"Failed to execute query: {e}\")\n+            query_result = []\n+        finally:\n+            white_list: List[str] = kwargs.get(\"white_list\", [])\n+            vertices, edges = self._get_nodes_edges_from_queried_data(\n+                query_result, white_list\n+            )\n \n-        mg = MemoryGraph()\n-        for vertex in vertices:\n-            mg.upsert_vertex(vertex)\n-        for edge in edges:\n-            mg.append_edge(edge)\n-        return mg\n+            mg = MemoryGraph()\n+            for vertex in vertices:\n+                mg.upsert_vertex(vertex)\n+            for edge in edges:\n+                mg.append_edge(edge)\n+            return mg",
        "comment_created_at": "2025-01-16T12:03:43+00:00",
        "comment_author": "Appointat",
        "comment_body": "'return' shadowed by the 'finally' clause.PylintW0134:return-in-finally\n\nWhat is more, do not use try catch or it will mask the error information.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1804727210",
    "pr_number": 2033,
    "pr_file": "dbgpt/storage/knowledge_graph/community/memgraph_store_adapter.py",
    "created_at": "2024-10-17T12:49:40+00:00",
    "commented_code": "+\"\"\"TuGraph Community Store Adapter.\"\"\"\n+\n+import json\n+import logging\n+from typing import AsyncGenerator, Iterator, List, Optional, Tuple\n+\n+from dbgpt.storage.graph_store.graph import (\n+    Direction,\n+    Edge,\n+    Graph,\n+    GraphElemType,\n+    MemoryGraph,\n+    Vertex,\n+)\n+from dbgpt.storage.graph_store.memgraph_store import (\n+    MemoryGraphStore,\n+    MemoryGraphStoreConfig,\n+)\n+from dbgpt.storage.knowledge_graph.community.base import Community, GraphStoreAdapter\n+\n+logger = logging.getLogger(__name__)\n+\n+\n+class MemGraphStoreAdapter(GraphStoreAdapter):\n+    \"\"\"MemGraph Community Store Adapter.\"\"\"\n+\n+    MAX_HIERARCHY_LEVEL = 3\n+\n+    def __init__(self, summary_enabled: bool = False):\n+        \"\"\"Initialize MemGraph Community Store Adapter.\"\"\"\n+        self._graph_store = MemoryGraphStore(MemoryGraphStoreConfig())\n+        self._summary_enabled = summary_enabled\n+\n+        super().__init__(self._graph_store)\n+\n+        # Create the graph\n+        self.create_graph(self._graph_store.get_config().name)\n+\n+    ############################\n+    #                          #\n+    #                          #\n+    # MemGraph Community Store #\n+    #                          #\n+    #                          #\n+    ############################\n+    async def discover_communities(self, **kwargs) -> List[str]:\n+        \"\"\"Run community discovery with leiden.\"\"\"\n+        pass\n+\n+    async def get_community(self, community_id: str) -> Community:\n+        \"\"\"Get community.\"\"\"\n+        pass\n+\n+    ##################\n+    #                #\n+    #                #\n+    # MemGraph Store #\n+    #                #\n+    #                #\n+    ##################\n+    def get_graph_config(self):\n+        \"\"\"Get the graph store config.\"\"\"\n+        return self._graph_store.get_config()\n+\n+    def get_vertex_type(self) -> str:\n+        \"\"\"Get the vertex type.\"\"\"\n+        raise NotImplementedError(\"Memory graph store does not have vertex type\")\n+\n+    def get_edge_type(self) -> str:\n+        \"\"\"Get the edge type.\"\"\"\n+        raise NotImplementedError(\"Memory graph store does not have edge type\")\n+\n+    def get_triplets(self, subj: str) -> List[Tuple[str, str]]:\n+        \"\"\"Get triplets.\"\"\"\n+        subgraph = self.explore([subj], direct=Direction.OUT, depth=1)\n+        return [(e.name, e.tid) for e in subgraph.edges()]\n+\n+    def get_document_vertex(self, doc_name: str) -> Vertex:\n+        \"\"\"Get the document vertex in the graph.\"\"\"\n+        pass",
    "repo_full_name": "eosphoros-ai/DB-GPT",
    "discussion_comments": [
      {
        "comment_id": "1804727210",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 2033,
        "pr_file": "dbgpt/storage/knowledge_graph/community/memgraph_store_adapter.py",
        "discussion_id": "1804727210",
        "commented_code": "@@ -0,0 +1,198 @@\n+\"\"\"TuGraph Community Store Adapter.\"\"\"\n+\n+import json\n+import logging\n+from typing import AsyncGenerator, Iterator, List, Optional, Tuple\n+\n+from dbgpt.storage.graph_store.graph import (\n+    Direction,\n+    Edge,\n+    Graph,\n+    GraphElemType,\n+    MemoryGraph,\n+    Vertex,\n+)\n+from dbgpt.storage.graph_store.memgraph_store import (\n+    MemoryGraphStore,\n+    MemoryGraphStoreConfig,\n+)\n+from dbgpt.storage.knowledge_graph.community.base import Community, GraphStoreAdapter\n+\n+logger = logging.getLogger(__name__)\n+\n+\n+class MemGraphStoreAdapter(GraphStoreAdapter):\n+    \"\"\"MemGraph Community Store Adapter.\"\"\"\n+\n+    MAX_HIERARCHY_LEVEL = 3\n+\n+    def __init__(self, summary_enabled: bool = False):\n+        \"\"\"Initialize MemGraph Community Store Adapter.\"\"\"\n+        self._graph_store = MemoryGraphStore(MemoryGraphStoreConfig())\n+        self._summary_enabled = summary_enabled\n+\n+        super().__init__(self._graph_store)\n+\n+        # Create the graph\n+        self.create_graph(self._graph_store.get_config().name)\n+\n+    ############################\n+    #                          #\n+    #                          #\n+    # MemGraph Community Store #\n+    #                          #\n+    #                          #\n+    ############################\n+    async def discover_communities(self, **kwargs) -> List[str]:\n+        \"\"\"Run community discovery with leiden.\"\"\"\n+        pass\n+\n+    async def get_community(self, community_id: str) -> Community:\n+        \"\"\"Get community.\"\"\"\n+        pass\n+\n+    ##################\n+    #                #\n+    #                #\n+    # MemGraph Store #\n+    #                #\n+    #                #\n+    ##################\n+    def get_graph_config(self):\n+        \"\"\"Get the graph store config.\"\"\"\n+        return self._graph_store.get_config()\n+\n+    def get_vertex_type(self) -> str:\n+        \"\"\"Get the vertex type.\"\"\"\n+        raise NotImplementedError(\"Memory graph store does not have vertex type\")\n+\n+    def get_edge_type(self) -> str:\n+        \"\"\"Get the edge type.\"\"\"\n+        raise NotImplementedError(\"Memory graph store does not have edge type\")\n+\n+    def get_triplets(self, subj: str) -> List[Tuple[str, str]]:\n+        \"\"\"Get triplets.\"\"\"\n+        subgraph = self.explore([subj], direct=Direction.OUT, depth=1)\n+        return [(e.name, e.tid) for e in subgraph.edges()]\n+\n+    def get_document_vertex(self, doc_name: str) -> Vertex:\n+        \"\"\"Get the document vertex in the graph.\"\"\"\n+        pass",
        "comment_created_at": "2024-10-17T12:49:40+00:00",
        "comment_author": "fanzhidongyzby",
        "comment_body": "Unimplemented APIs can add a todo. For unsupported ones, raise an exception directly.",
        "pr_file_module": null
      },
      {
        "comment_id": "1804959862",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 2033,
        "pr_file": "dbgpt/storage/knowledge_graph/community/memgraph_store_adapter.py",
        "discussion_id": "1804727210",
        "commented_code": "@@ -0,0 +1,198 @@\n+\"\"\"TuGraph Community Store Adapter.\"\"\"\n+\n+import json\n+import logging\n+from typing import AsyncGenerator, Iterator, List, Optional, Tuple\n+\n+from dbgpt.storage.graph_store.graph import (\n+    Direction,\n+    Edge,\n+    Graph,\n+    GraphElemType,\n+    MemoryGraph,\n+    Vertex,\n+)\n+from dbgpt.storage.graph_store.memgraph_store import (\n+    MemoryGraphStore,\n+    MemoryGraphStoreConfig,\n+)\n+from dbgpt.storage.knowledge_graph.community.base import Community, GraphStoreAdapter\n+\n+logger = logging.getLogger(__name__)\n+\n+\n+class MemGraphStoreAdapter(GraphStoreAdapter):\n+    \"\"\"MemGraph Community Store Adapter.\"\"\"\n+\n+    MAX_HIERARCHY_LEVEL = 3\n+\n+    def __init__(self, summary_enabled: bool = False):\n+        \"\"\"Initialize MemGraph Community Store Adapter.\"\"\"\n+        self._graph_store = MemoryGraphStore(MemoryGraphStoreConfig())\n+        self._summary_enabled = summary_enabled\n+\n+        super().__init__(self._graph_store)\n+\n+        # Create the graph\n+        self.create_graph(self._graph_store.get_config().name)\n+\n+    ############################\n+    #                          #\n+    #                          #\n+    # MemGraph Community Store #\n+    #                          #\n+    #                          #\n+    ############################\n+    async def discover_communities(self, **kwargs) -> List[str]:\n+        \"\"\"Run community discovery with leiden.\"\"\"\n+        pass\n+\n+    async def get_community(self, community_id: str) -> Community:\n+        \"\"\"Get community.\"\"\"\n+        pass\n+\n+    ##################\n+    #                #\n+    #                #\n+    # MemGraph Store #\n+    #                #\n+    #                #\n+    ##################\n+    def get_graph_config(self):\n+        \"\"\"Get the graph store config.\"\"\"\n+        return self._graph_store.get_config()\n+\n+    def get_vertex_type(self) -> str:\n+        \"\"\"Get the vertex type.\"\"\"\n+        raise NotImplementedError(\"Memory graph store does not have vertex type\")\n+\n+    def get_edge_type(self) -> str:\n+        \"\"\"Get the edge type.\"\"\"\n+        raise NotImplementedError(\"Memory graph store does not have edge type\")\n+\n+    def get_triplets(self, subj: str) -> List[Tuple[str, str]]:\n+        \"\"\"Get triplets.\"\"\"\n+        subgraph = self.explore([subj], direct=Direction.OUT, depth=1)\n+        return [(e.name, e.tid) for e in subgraph.edges()]\n+\n+    def get_document_vertex(self, doc_name: str) -> Vertex:\n+        \"\"\"Get the document vertex in the graph.\"\"\"\n+        pass",
        "comment_created_at": "2024-10-17T15:05:18+00:00",
        "comment_author": "Appointat",
        "comment_body": "fixed",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1621766639",
    "pr_number": 1592,
    "pr_file": "dbgpt/model/proxy/llms/zhipu.py",
    "created_at": "2024-05-31T05:52:58+00:00",
    "commented_code": "request: ModelRequest,\n         message_converter: Optional[MessageConverter] = None,\n     ) -> Iterator[ModelOutput]:\n-        import zhipuai\n \n         request = self.local_covert_message(request, message_converter)\n \n         messages = request.to_common_messages(support_system_role=False)\n \n         model = request.model or self._model\n         try:\n-            res = zhipuai.model_api.sse_invoke(\n+            response = self.client.chat.completions.create(",
    "repo_full_name": "eosphoros-ai/DB-GPT",
    "discussion_comments": [
      {
        "comment_id": "1621766639",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 1592,
        "pr_file": "dbgpt/model/proxy/llms/zhipu.py",
        "discussion_id": "1621766639",
        "commented_code": "@@ -84,26 +84,25 @@ def sync_generate_stream(\n         request: ModelRequest,\n         message_converter: Optional[MessageConverter] = None,\n     ) -> Iterator[ModelOutput]:\n-        import zhipuai\n \n         request = self.local_covert_message(request, message_converter)\n \n         messages = request.to_common_messages(support_system_role=False)\n \n         model = request.model or self._model\n         try:\n-            res = zhipuai.model_api.sse_invoke(\n+            response = self.client.chat.completions.create(",
        "comment_created_at": "2024-05-31T05:52:58+00:00",
        "comment_author": "fangyinc",
        "comment_body": "The old version of`zhipuai` doesn't work properly. So I suggest checking the version here and reporting an error to remind users to upgrade it.",
        "pr_file_module": null
      },
      {
        "comment_id": "1621836372",
        "repo_full_name": "eosphoros-ai/DB-GPT",
        "pr_number": 1592,
        "pr_file": "dbgpt/model/proxy/llms/zhipu.py",
        "discussion_id": "1621766639",
        "commented_code": "@@ -84,26 +84,25 @@ def sync_generate_stream(\n         request: ModelRequest,\n         message_converter: Optional[MessageConverter] = None,\n     ) -> Iterator[ModelOutput]:\n-        import zhipuai\n \n         request = self.local_covert_message(request, message_converter)\n \n         messages = request.to_common_messages(support_system_role=False)\n \n         model = request.model or self._model\n         try:\n-            res = zhipuai.model_api.sse_invoke(\n+            response = self.client.chat.completions.create(",
        "comment_created_at": "2024-05-31T06:56:53+00:00",
        "comment_author": "yyhhyyyyyy",
        "comment_body": "Reminder has been added.",
        "pr_file_module": null
      }
    ]
  }
]