[
  {
    "discussion_id": "2434338958",
    "pr_number": 12518,
    "pr_file": "scripts/rtl_ltr_linter.py",
    "created_at": "2025-10-16T01:39:15+00:00",
    "commented_code": "# If so, push the new context onto the stack\n         if m:\n-            stack.append(m.group(1).lower()); continue\n+            stack.append(m.group(1).lower()); #continue",
    "repo_full_name": "EbookFoundation/free-programming-books",
    "discussion_comments": [
      {
        "comment_id": "2434338958",
        "repo_full_name": "EbookFoundation/free-programming-books",
        "pr_number": 12518,
        "pr_file": "scripts/rtl_ltr_linter.py",
        "discussion_id": "2434338958",
        "commented_code": "@@ -175,14 +175,14 @@ def split_by_span(text, base_ctx):\n \n         # If so, push the new context onto the stack\n         if m:\n-            stack.append(m.group(1).lower()); continue\n+            stack.append(m.group(1).lower()); #continue",
        "comment_created_at": "2025-10-16T01:39:15+00:00",
        "comment_author": "Ajay-singh1",
        "comment_body": "Explain why are we removing continue statements?",
        "pr_file_module": null
      },
      {
        "comment_id": "2434470302",
        "repo_full_name": "EbookFoundation/free-programming-books",
        "pr_number": 12518,
        "pr_file": "scripts/rtl_ltr_linter.py",
        "discussion_id": "2434338958",
        "commented_code": "@@ -175,14 +175,14 @@ def split_by_span(text, base_ctx):\n \n         # If so, push the new context onto the stack\n         if m:\n-            stack.append(m.group(1).lower()); continue\n+            stack.append(m.group(1).lower()); #continue",
        "comment_created_at": "2025-10-16T03:40:10+00:00",
        "comment_author": "7se7en72025",
        "comment_body": "We remove the continue statements so lines with multiple tags are fully processed, preventing missed tags and keeping the context accurate.",
        "pr_file_module": null
      },
      {
        "comment_id": "2443388778",
        "repo_full_name": "EbookFoundation/free-programming-books",
        "pr_number": 12518,
        "pr_file": "scripts/rtl_ltr_linter.py",
        "discussion_id": "2434338958",
        "commented_code": "@@ -175,14 +175,14 @@ def split_by_span(text, base_ctx):\n \n         # If so, push the new context onto the stack\n         if m:\n-            stack.append(m.group(1).lower()); continue\n+            stack.append(m.group(1).lower()); #continue",
        "comment_created_at": "2025-10-19T16:34:21+00:00",
        "comment_author": "Thenlie",
        "comment_body": "Do you have an example of this bug happening?",
        "pr_file_module": null
      },
      {
        "comment_id": "2443408652",
        "repo_full_name": "EbookFoundation/free-programming-books",
        "pr_number": 12518,
        "pr_file": "scripts/rtl_ltr_linter.py",
        "discussion_id": "2434338958",
        "commented_code": "@@ -175,14 +175,14 @@ def split_by_span(text, base_ctx):\n \n         # If so, push the new context onto the stack\n         if m:\n-            stack.append(m.group(1).lower()); continue\n+            stack.append(m.group(1).lower()); #continue",
        "comment_created_at": "2025-10-19T17:27:37+00:00",
        "comment_author": "7se7en72025",
        "comment_body": "like if we dry run and keep the input as shown below,\r\nrandom Input: \"Hi<span dir='rtl'>\u0645\u0631\u062d\u0628\u0627</span>!\"\r\n\r\nIf the continue skips empty tokens, output might miss \"!\".\r\nRemoving continue, all tokens (including \"!\") are processed, so segments are correct.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2432096611",
    "pr_number": 12429,
    "pr_file": "scripts/rtl_ltr_linter.py",
    "created_at": "2025-10-15T10:46:34+00:00",
    "commented_code": "# Skip lines that start a code block (```)\n         if CODE_FENCE_START.match(line): continue\n \n-        # Check for block-level directionality changes (e.g., <div dir=\"rtl\">)\n-        m_div_open = HTML_DIR_ATTR_RE.search(line)\n-        \n-        # If an opening <div dir=\"...\" markdown=\"1\"> tag is found\n-        if m_div_open and 'markdown=\"1\"' in line:\n-            new_div_ctx = m_div_open.group(2).lower()   # Extract the new directionality context from the opening div tag\n-            block_context_stack.append(new_div_ctx)     # Push the new directionality context onto the stack\n-            continue                                    # Continue to the next line of the file\n-        \n-        # If a closing </div> tag is found and we are inside a div context\n-        # (i.e., the stack has more than just the base file_direction_ctx)\n-        if '</div>' in line and len(block_context_stack) > 1:\n-            block_context_stack.pop()   # Pop the last directionality context from the stack\n-            continue                    # Continue to the next line of the file\n-        \n+        # Corrected logic to handle all div tags on a single line\n+        div_tags = re.findall(r\"(<div[^>]*dir=['\\\"](rtl|ltr)['\\\"][^>]*>|</div>)\", line, re.IGNORECASE)\n+        for tag_tuple in div_tags:\n+            tag, direction = tag_tuple\n+            if tag.startswith('<div') and 'markdown=\"1\"' in tag:\n+                new_div_ctx = direction.lower()\n+                block_context_stack.append(new_div_ctx)\n+            elif tag == '</div>' and len(block_context_stack) > 1:\n+                block_context_stack.pop()\n+",
    "repo_full_name": "EbookFoundation/free-programming-books",
    "discussion_comments": [
      {
        "comment_id": "2432096611",
        "repo_full_name": "EbookFoundation/free-programming-books",
        "pr_number": 12429,
        "pr_file": "scripts/rtl_ltr_linter.py",
        "discussion_id": "2432096611",
        "commented_code": "@@ -247,21 +247,16 @@ def lint_file(path, cfg):\n         # Skip lines that start a code block (```)\n         if CODE_FENCE_START.match(line): continue\n \n-        # Check for block-level directionality changes (e.g., <div dir=\"rtl\">)\n-        m_div_open = HTML_DIR_ATTR_RE.search(line)\n-        \n-        # If an opening <div dir=\"...\" markdown=\"1\"> tag is found\n-        if m_div_open and 'markdown=\"1\"' in line:\n-            new_div_ctx = m_div_open.group(2).lower()   # Extract the new directionality context from the opening div tag\n-            block_context_stack.append(new_div_ctx)     # Push the new directionality context onto the stack\n-            continue                                    # Continue to the next line of the file\n-        \n-        # If a closing </div> tag is found and we are inside a div context\n-        # (i.e., the stack has more than just the base file_direction_ctx)\n-        if '</div>' in line and len(block_context_stack) > 1:\n-            block_context_stack.pop()   # Pop the last directionality context from the stack\n-            continue                    # Continue to the next line of the file\n-        \n+        # Corrected logic to handle all div tags on a single line\n+        div_tags = re.findall(r\"(<div[^>]*dir=['\\\"](rtl|ltr)['\\\"][^>]*>|</div>)\", line, re.IGNORECASE)\n+        for tag_tuple in div_tags:\n+            tag, direction = tag_tuple\n+            if tag.startswith('<div') and 'markdown=\"1\"' in tag:\n+                new_div_ctx = direction.lower()\n+                block_context_stack.append(new_div_ctx)\n+            elif tag == '</div>' and len(block_context_stack) > 1:\n+                block_context_stack.pop()\n+",
        "comment_created_at": "2025-10-15T10:46:34+00:00",
        "comment_author": "gabriele-ciccotelli",
        "comment_body": "If I may, I'd also suggest adding the following comments to the new logic:\r\n\r\n```suggestion\r\n        # Find all opening and closing <div> tags on the line to handle cases\r\n        # where there can be multiple <div> opening and closing on the same line\r\n        div_tags = re.findall(r\"(<div[^>]*dir=['\\\"](rtl|ltr)['\\\"][^>]*>|</div>)\", line, re.IGNORECASE)\r\n\r\n        # Process each found tag in order to correctly update the context stack\r\n        for tag_tuple in div_tags:\r\n            # re.findall with multiple capture groups returns a list of tuples:\r\n            # tag: The full matched tag (e.g., '<div...>' or '</div>')\r\n            # direction: The captured direction ('rtl' or 'ltr'), or empty for a closing tag\r\n            tag, direction = tag_tuple\r\n\r\n            # If it's an opening tag with 'markdown=\"1\"', push the new context\r\n            if tag.startswith('<div') and 'markdown=\"1\"' in tag:\r\n                block_context_stack.append(direction.lower())\r\n            # If it's a closing tag and we are inside a div, pop the context\r\n            elif tag == '</div>' and len(block_context_stack) > 1:\r\n                block_context_stack.pop()\r\n```\r\n\r\nThis will help ensure the code remains easy to understand and maintain in the future.",
        "pr_file_module": null
      }
    ]
  }
]