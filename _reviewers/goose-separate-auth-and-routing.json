[
  {
    "discussion_id": "3581775882",
    "pr_number": 10467,
    "pr_file": "documentation/docs/getting-started/providers.md",
    "created_at": "2026-07-14T18:54:34+00:00",
    "commented_code": "Need to connect to multiple OpenAI-compatible endpoints? [Configure custom providers](#configure-custom-provider) instead for easier switching and better organization, as well as custom naming and shareable configurations.\n :::\n \n+:::note Pointing at a LiteLLM proxy\n+You can reach a [LiteLLM](https://docs.litellm.ai/) proxy in either of two ways\u2014pick one, don't mix them:\n+\n+- Use the **OpenAI provider**: set `OPENAI_HOST` to your proxy's root (no trailing path) and `OPENAI_BASE_PATH` to the path it serves (usually `v1/chat/completions`). A `401` with `No api key passed in` or a `404` on `chat/completions` usually means `OPENAI_BASE_PATH` is wrong for your proxy.",
    "repo_full_name": "aaif-goose/goose",
    "discussion_comments": [
      {
        "comment_id": "3581814045",
        "repo_full_name": "aaif-goose/goose",
        "pr_number": 10467,
        "pr_file": "documentation/docs/getting-started/providers.md",
        "discussion_id": "3581775882",
        "commented_code": "@@ -283,12 +283,20 @@ The built-in OpenAI provider can connect to OpenAI's official API (`api.openai.c\n Need to connect to multiple OpenAI-compatible endpoints? [Configure custom providers](#configure-custom-provider) instead for easier switching and better organization, as well as custom naming and shareable configurations.\n :::\n \n+:::note Pointing at a LiteLLM proxy\n+You can reach a [LiteLLM](https://docs.litellm.ai/) proxy in either of two ways\u2014pick one, don't mix them:\n+\n+- Use the **OpenAI provider**: set `OPENAI_HOST` to your proxy's root (no trailing path) and `OPENAI_BASE_PATH` to the path it serves (usually `v1/chat/completions`). A `401` with `No api key passed in` or a `404` on `chat/completions` usually means `OPENAI_BASE_PATH` is wrong for your proxy.",
        "comment_created_at": "2026-07-14T18:54:34+00:00",
        "comment_author": "chhhee10",
        "comment_body": "Correct \u2014 fixed in b2d7b8ce2. Auth is derived solely from `OPENAI_API_KEY` (falls back to `NoAuth`), separate from `OPENAI_BASE_PATH`, so a `401 No api key passed in` is a missing-key symptom, not a path one. Reworded: `404` \u2192 base path, `401` \u2192 key not loaded, with a link to the config.yaml key note.",
        "pr_file_module": null
      }
    ]
  }
]