[
  {
    "discussion_id": "2592414387",
    "pr_number": 53,
    "pr_file": "src/heretic/evaluator.py",
    "created_at": "2025-12-05T11:46:59+00:00",
    "commented_code": "self.bad_prompts = load_prompts(settings.bad_evaluation_prompts)\n         print(f\"* [bold]{len(self.bad_prompts)}[/] prompts loaded\")\n \n-        print(\"* Counting model refusals...\")\n-        self.base_refusals = self.count_refusals()\n-        print(\n-            f\"* Initial refusals: [bold]{self.base_refusals}[/]/{len(self.bad_prompts)}\"\n+        print()\n+        print(\"Loading tagger plugin...\")\n+        self.tagger_plugin = self._load_tagger_plugin()\n+        self.model.set_requested_metadata_fields(\n+            self.tagger_plugin.required_response_metadata_fields()\n+        )\n+        print()\n+        print(\"Loading scorer plugin...\")\n+        self.scorer_plugin = self._load_scorer_plugin()\n+\n+        self.base_score = self.tag_and_score_batch()\n+        print(f\"* Initial score: [bold]{self.base_score}[/]/{len(self.bad_prompts)}\")\n+\n+    def _load_tagger_plugin(self) -> Tagger:\n+        name = self.settings.tagger_plugin\n+        tagger = None\n+\n+        if \".\" in name:\n+            # Load from arbitrary python path\n+            try:\n+                module_name, class_name = name.rsplit(\".\", 1)\n+                module = importlib.import_module(module_name)\n+                tagger = getattr(module, class_name)\n+            except (ImportError, AttributeError) as e:\n+                print(f\"[red]Error loading tagger plugin '{name}': {e}[/]\")\n+        else:\n+            try:\n+                module = importlib.import_module(\n+                    f\".tagger_plugins.{name}\", package=\"heretic\"\n+                )\n+                # Find the class defined in the module that inherits from Tagger\n+                for _, obj in inspect.getmembers(module):\n+                    if (\n+                        inspect.isclass(obj)\n+                        and issubclass(obj, Tagger)\n+                        and obj.__module__ == module.__name__\n+                    ):\n+                        tagger = obj\n+                        break\n+\n+                if tagger is None:\n+                    print(f\"[red]Error: No Tagger subclass found in plugin '{name}'[/]\")\n+                    exit()\n+            except ImportError as e:\n+                print(f\"[red]Error loading plugin '{name}': {e}[/]\")\n+                exit()\n+\n+        print(f\"* Loaded tagger plugin: [bold]{tagger.__name__}[/bold]\")\n+        self.model.set_requested_context_metadata_fields(\n+            tagger.required_context_metadata_fields()\n+        )\n+        return tagger(\n+            settings=self.settings,\n+            model=self.model,\n+            context_metadata=self.model.get_context_metadata(),\n         )\n \n-    def is_refusal(self, response: str) -> bool:\n-        # Remove emphasis (e.g. \"I *will not*...\").\n-        response = response.lower().replace(\"*\", \"\")\n-\n-        # Normalize typographic apostrophes (\"won\u2019t\" -> \"won't\").\n-        response = response.replace(\"\u2019\", \"'\")\n-\n-        # Normalize whitespace between words to a single space.\n-        response = \" \".join(response.split())\n-\n-        for marker in self.settings.refusal_markers:\n-            if marker.lower() in response:\n-                return True\n-\n-        return False\n-\n-    def count_refusals(self) -> int:\n-        responses = self.model.get_responses_batched(self.bad_prompts)\n-        refusals = [response for response in responses if self.is_refusal(response)]\n-        return len(refusals)\n+    def _load_scorer_plugin(self) -> Scorer:",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2592414387",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 53,
        "pr_file": "src/heretic/evaluator.py",
        "discussion_id": "2592414387",
        "commented_code": "@@ -30,32 +35,105 @@ def __init__(self, settings: Settings, model: Model):\n         self.bad_prompts = load_prompts(settings.bad_evaluation_prompts)\n         print(f\"* [bold]{len(self.bad_prompts)}[/] prompts loaded\")\n \n-        print(\"* Counting model refusals...\")\n-        self.base_refusals = self.count_refusals()\n-        print(\n-            f\"* Initial refusals: [bold]{self.base_refusals}[/]/{len(self.bad_prompts)}\"\n+        print()\n+        print(\"Loading tagger plugin...\")\n+        self.tagger_plugin = self._load_tagger_plugin()\n+        self.model.set_requested_metadata_fields(\n+            self.tagger_plugin.required_response_metadata_fields()\n+        )\n+        print()\n+        print(\"Loading scorer plugin...\")\n+        self.scorer_plugin = self._load_scorer_plugin()\n+\n+        self.base_score = self.tag_and_score_batch()\n+        print(f\"* Initial score: [bold]{self.base_score}[/]/{len(self.bad_prompts)}\")\n+\n+    def _load_tagger_plugin(self) -> Tagger:\n+        name = self.settings.tagger_plugin\n+        tagger = None\n+\n+        if \".\" in name:\n+            # Load from arbitrary python path\n+            try:\n+                module_name, class_name = name.rsplit(\".\", 1)\n+                module = importlib.import_module(module_name)\n+                tagger = getattr(module, class_name)\n+            except (ImportError, AttributeError) as e:\n+                print(f\"[red]Error loading tagger plugin '{name}': {e}[/]\")\n+        else:\n+            try:\n+                module = importlib.import_module(\n+                    f\".tagger_plugins.{name}\", package=\"heretic\"\n+                )\n+                # Find the class defined in the module that inherits from Tagger\n+                for _, obj in inspect.getmembers(module):\n+                    if (\n+                        inspect.isclass(obj)\n+                        and issubclass(obj, Tagger)\n+                        and obj.__module__ == module.__name__\n+                    ):\n+                        tagger = obj\n+                        break\n+\n+                if tagger is None:\n+                    print(f\"[red]Error: No Tagger subclass found in plugin '{name}'[/]\")\n+                    exit()\n+            except ImportError as e:\n+                print(f\"[red]Error loading plugin '{name}': {e}[/]\")\n+                exit()\n+\n+        print(f\"* Loaded tagger plugin: [bold]{tagger.__name__}[/bold]\")\n+        self.model.set_requested_context_metadata_fields(\n+            tagger.required_context_metadata_fields()\n+        )\n+        return tagger(\n+            settings=self.settings,\n+            model=self.model,\n+            context_metadata=self.model.get_context_metadata(),\n         )\n \n-    def is_refusal(self, response: str) -> bool:\n-        # Remove emphasis (e.g. \"I *will not*...\").\n-        response = response.lower().replace(\"*\", \"\")\n-\n-        # Normalize typographic apostrophes (\"won\u2019t\" -> \"won't\").\n-        response = response.replace(\"\u2019\", \"'\")\n-\n-        # Normalize whitespace between words to a single space.\n-        response = \" \".join(response.split())\n-\n-        for marker in self.settings.refusal_markers:\n-            if marker.lower() in response:\n-                return True\n-\n-        return False\n-\n-    def count_refusals(self) -> int:\n-        responses = self.model.get_responses_batched(self.bad_prompts)\n-        refusals = [response for response in responses if self.is_refusal(response)]\n-        return len(refusals)\n+    def _load_scorer_plugin(self) -> Scorer:",
        "comment_created_at": "2025-12-05T11:46:59+00:00",
        "comment_author": "p-e-w",
        "comment_body": "The two `_load_*_plugin` methods have a lot of code in common. The common parts should be moved to `utils.py`.\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2727588376",
    "pr_number": 53,
    "pr_file": "src/heretic/evaluator.py",
    "created_at": "2026-01-26T13:22:40+00:00",
    "commented_code": "# SPDX-License-Identifier: AGPL-3.0-or-later\n # Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n \n-import torch.nn.functional as F\n-from torch import Tensor\n+from __future__ import annotations\n \n-from .config import Settings\n+from typing import Any\n+\n+from optuna.study import StudyDirection\n+from pydantic import BaseModel\n+\n+from .config import ScorerConfig, Settings\n from .model import Model\n-from .utils import Prompt, load_prompts, print\n+from .plugin import load_plugin\n+from .scorer import Context, Score, Scorer\n+from .utils import print\n \n \n class Evaluator:\n     settings: Settings\n     model: Model\n-    good_prompts: list[Prompt]\n-    bad_prompts: list[Prompt]\n-    base_logprobs: Tensor\n-    base_refusals: int\n+    \"\"\"\n+    Manages evaluation of the model using configured scorer plugins.\n+\n+    Loads prompts, establishes baseline metrics, and runs scorers during optimization.\n+    \"\"\"\n \n     def __init__(self, settings: Settings, model: Model):\n         self.settings = settings\n         self.model = model\n+        self._scorer_configs: list[ScorerConfig] = list(settings.scorers)\n \n         print()\n-        print(\n-            f\"Loading good evaluation prompts from [bold]{settings.good_evaluation_prompts.dataset}[/]...\"\n-        )\n-        self.good_prompts = load_prompts(settings, settings.good_evaluation_prompts)\n-        print(f\"* [bold]{len(self.good_prompts)}[/] prompts loaded\")\n-\n-        print(\"* Obtaining first-token probability distributions...\")\n-        self.base_logprobs = model.get_logprobs_batched(self.good_prompts)\n-\n-        print()\n-        print(\n-            f\"Loading bad evaluation prompts from [bold]{settings.bad_evaluation_prompts.dataset}[/]...\"\n-        )\n-        self.bad_prompts = load_prompts(settings, settings.bad_evaluation_prompts)\n-        print(f\"* [bold]{len(self.bad_prompts)}[/] prompts loaded\")\n-\n-        print(\"* Counting model refusals...\")\n-        self.base_refusals = self.count_refusals()\n-        print(\n-            f\"* Initial refusals: [bold]{self.base_refusals}[/]/{len(self.bad_prompts)}\"\n-        )\n-\n-    def is_refusal(self, response: str) -> bool:\n-        # Classify empty responses as refusals to avoid optimizing for them.\n-        if not response.strip():\n-            return True\n-\n-        # Remove emphasis (e.g. \"I *will not*...\").\n-        response = response.lower().replace(\"*\", \"\")\n-\n-        # Normalize typographic apostrophes (\"won\u2019t\" -> \"won't\").\n-        response = response.replace(\"\u2019\", \"'\")\n-\n-        # Normalize whitespace between words to a single space.\n-        response = \" \".join(response.split())\n-\n-        for marker in self.settings.refusal_markers:\n-            if marker.lower() in response:\n-                return True\n-\n-        return False\n-\n-    def count_refusals(self) -> int:\n-        refusal_count = 0\n-\n-        responses = self.model.get_responses_batched(\n-            self.bad_prompts,\n-            skip_special_tokens=True,\n-        )\n-\n-        for prompt, response in zip(self.bad_prompts, responses):\n-            is_refusal = self.is_refusal(response)\n-            if is_refusal:\n-                refusal_count += 1\n-\n-            if self.settings.print_responses:\n-                print()\n-                print(f\"[bold]System prompt:[/] {prompt.system}\")\n-                print(f\"[bold]Prompt:[/] {prompt.user}\")\n-                if not response.strip():\n-                    response = \"[italic]\\\\[empty][/]\"\n-                print(\n-                    f\"[bold]Response:[/] [{'red' if is_refusal else 'green'}]{response}[/]\"\n+        print(\"Loading scorers...\")\n+        self.scorers = self._load_scorers()\n+        self._start_scorers()\n+\n+        # Establish baseline metrics (pre-abliteration)\n+        self.baseline_metrics = self.get_scores()\n+        self._print_baseline()\n+\n+    def _start_scorers(self) -> None:\n+        \"\"\"\n+        Optional scorer initialization hook.\n+        \"\"\"\n+        ctx = Context(settings=self.settings, model=self.model)\n+\n+        for scorer in self.scorers:\n+            scorer.start(ctx)\n+\n+    def _print_baseline(self) -> None:\n+        \"\"\"Print baseline metrics summary.\"\"\"\n+        for m in self.baseline_metrics:\n+            print(f\"* Baseline {m.name}: [bold]{m.display}[/]\")\n+\n+    def _get_plugin_namespace(self, namespace: str) -> dict[str, Any]:",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2727588376",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 53,
        "pr_file": "src/heretic/evaluator.py",
        "discussion_id": "2727588376",
        "commented_code": "@@ -1,125 +1,232 @@\n # SPDX-License-Identifier: AGPL-3.0-or-later\n # Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n \n-import torch.nn.functional as F\n-from torch import Tensor\n+from __future__ import annotations\n \n-from .config import Settings\n+from typing import Any\n+\n+from optuna.study import StudyDirection\n+from pydantic import BaseModel\n+\n+from .config import ScorerConfig, Settings\n from .model import Model\n-from .utils import Prompt, load_prompts, print\n+from .plugin import load_plugin\n+from .scorer import Context, Score, Scorer\n+from .utils import print\n \n \n class Evaluator:\n     settings: Settings\n     model: Model\n-    good_prompts: list[Prompt]\n-    bad_prompts: list[Prompt]\n-    base_logprobs: Tensor\n-    base_refusals: int\n+    \"\"\"\n+    Manages evaluation of the model using configured scorer plugins.\n+\n+    Loads prompts, establishes baseline metrics, and runs scorers during optimization.\n+    \"\"\"\n \n     def __init__(self, settings: Settings, model: Model):\n         self.settings = settings\n         self.model = model\n+        self._scorer_configs: list[ScorerConfig] = list(settings.scorers)\n \n         print()\n-        print(\n-            f\"Loading good evaluation prompts from [bold]{settings.good_evaluation_prompts.dataset}[/]...\"\n-        )\n-        self.good_prompts = load_prompts(settings, settings.good_evaluation_prompts)\n-        print(f\"* [bold]{len(self.good_prompts)}[/] prompts loaded\")\n-\n-        print(\"* Obtaining first-token probability distributions...\")\n-        self.base_logprobs = model.get_logprobs_batched(self.good_prompts)\n-\n-        print()\n-        print(\n-            f\"Loading bad evaluation prompts from [bold]{settings.bad_evaluation_prompts.dataset}[/]...\"\n-        )\n-        self.bad_prompts = load_prompts(settings, settings.bad_evaluation_prompts)\n-        print(f\"* [bold]{len(self.bad_prompts)}[/] prompts loaded\")\n-\n-        print(\"* Counting model refusals...\")\n-        self.base_refusals = self.count_refusals()\n-        print(\n-            f\"* Initial refusals: [bold]{self.base_refusals}[/]/{len(self.bad_prompts)}\"\n-        )\n-\n-    def is_refusal(self, response: str) -> bool:\n-        # Classify empty responses as refusals to avoid optimizing for them.\n-        if not response.strip():\n-            return True\n-\n-        # Remove emphasis (e.g. \"I *will not*...\").\n-        response = response.lower().replace(\"*\", \"\")\n-\n-        # Normalize typographic apostrophes (\"won\u2019t\" -> \"won't\").\n-        response = response.replace(\"\u2019\", \"'\")\n-\n-        # Normalize whitespace between words to a single space.\n-        response = \" \".join(response.split())\n-\n-        for marker in self.settings.refusal_markers:\n-            if marker.lower() in response:\n-                return True\n-\n-        return False\n-\n-    def count_refusals(self) -> int:\n-        refusal_count = 0\n-\n-        responses = self.model.get_responses_batched(\n-            self.bad_prompts,\n-            skip_special_tokens=True,\n-        )\n-\n-        for prompt, response in zip(self.bad_prompts, responses):\n-            is_refusal = self.is_refusal(response)\n-            if is_refusal:\n-                refusal_count += 1\n-\n-            if self.settings.print_responses:\n-                print()\n-                print(f\"[bold]System prompt:[/] {prompt.system}\")\n-                print(f\"[bold]Prompt:[/] {prompt.user}\")\n-                if not response.strip():\n-                    response = \"[italic]\\\\[empty][/]\"\n-                print(\n-                    f\"[bold]Response:[/] [{'red' if is_refusal else 'green'}]{response}[/]\"\n+        print(\"Loading scorers...\")\n+        self.scorers = self._load_scorers()\n+        self._start_scorers()\n+\n+        # Establish baseline metrics (pre-abliteration)\n+        self.baseline_metrics = self.get_scores()\n+        self._print_baseline()\n+\n+    def _start_scorers(self) -> None:\n+        \"\"\"\n+        Optional scorer initialization hook.\n+        \"\"\"\n+        ctx = Context(settings=self.settings, model=self.model)\n+\n+        for scorer in self.scorers:\n+            scorer.start(ctx)\n+\n+    def _print_baseline(self) -> None:\n+        \"\"\"Print baseline metrics summary.\"\"\"\n+        for m in self.baseline_metrics:\n+            print(f\"* Baseline {m.name}: [bold]{m.display}[/]\")\n+\n+    def _get_plugin_namespace(self, namespace: str) -> dict[str, Any]:",
        "comment_created_at": "2026-01-26T13:22:40+00:00",
        "comment_author": "p-e-w",
        "comment_body": "This is not evaluator-specific and thus doesn't belong here (I imagine the same applies to several of the other methods as well).",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2697344252",
    "pr_number": 106,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2026-01-16T07:40:38+00:00",
    "commented_code": ")\n \n     for component, parameters in trial.user_attrs[\"parameters\"].items():\n-        for name, value in asdict(parameters).items():\n+        for name, value in parameters.items():",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2697344252",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 106,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2697344252",
        "commented_code": "@@ -241,7 +241,7 @@ def get_trial_parameters(trial: Trial) -> dict[str, str]:\n     )\n \n     for component, parameters in trial.user_attrs[\"parameters\"].items():\n-        for name, value in asdict(parameters).items():\n+        for name, value in parameters.items():",
        "comment_created_at": "2026-01-16T07:40:38+00:00",
        "comment_author": "p-e-w",
        "comment_body": "What is the purpose of this change?",
        "pr_file_module": null
      },
      {
        "comment_id": "2697375741",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 106,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2697344252",
        "commented_code": "@@ -241,7 +241,7 @@ def get_trial_parameters(trial: Trial) -> dict[str, str]:\n     )\n \n     for component, parameters in trial.user_attrs[\"parameters\"].items():\n-        for name, value in asdict(parameters).items():\n+        for name, value in parameters.items():",
        "comment_created_at": "2026-01-16T07:53:05+00:00",
        "comment_author": "spikymoth",
        "comment_body": "This is the inverse of the other change - since the parameters are stored as a dict, we don't need to call `asdict()` on them.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2675317958",
    "pr_number": 103,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2026-01-09T08:38:04+00:00",
    "commented_code": "path = specification.dataset\n     split_str = specification.split\n \n-    if os.path.isdir(path):\n-        if Path(path, DATASET_STATE_JSON_FILENAME).exists():\n-            # Dataset saved with datasets.save_to_disk; needs special handling.\n-            # Path should be the subdirectory for a particular split.\n-            dataset = load_from_disk(path)\n-            assert not isinstance(dataset, DatasetDict), (\n-                \"Loading dataset dicts is not supported\"\n-            )\n-            # Parse the split instructions.\n+    # Support for plain text files (one prompt per line)\n+    if path.endswith(\".txt\") and os.path.isfile(path):",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2675317958",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 103,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2675317958",
        "commented_code": "@@ -149,38 +149,57 @@ def load_prompts(\n     path = specification.dataset\n     split_str = specification.split\n \n-    if os.path.isdir(path):\n-        if Path(path, DATASET_STATE_JSON_FILENAME).exists():\n-            # Dataset saved with datasets.save_to_disk; needs special handling.\n-            # Path should be the subdirectory for a particular split.\n-            dataset = load_from_disk(path)\n-            assert not isinstance(dataset, DatasetDict), (\n-                \"Loading dataset dicts is not supported\"\n-            )\n-            # Parse the split instructions.\n+    # Support for plain text files (one prompt per line)\n+    if path.endswith(\".txt\") and os.path.isfile(path):",
        "comment_created_at": "2026-01-09T08:38:04+00:00",
        "comment_author": "p-e-w",
        "comment_body": "```suggestion\r\n    if os.path.isfile(path):\r\n```\r\n\r\nI don't think we should require a specific extension, as plain text files are often stored without extensions on Unix systems.\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2675325212",
    "pr_number": 103,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2026-01-09T08:40:52+00:00",
    "commented_code": "path = specification.dataset\n     split_str = specification.split\n \n-    if os.path.isdir(path):\n-        if Path(path, DATASET_STATE_JSON_FILENAME).exists():\n-            # Dataset saved with datasets.save_to_disk; needs special handling.\n-            # Path should be the subdirectory for a particular split.\n-            dataset = load_from_disk(path)\n-            assert not isinstance(dataset, DatasetDict), (\n-                \"Loading dataset dicts is not supported\"\n-            )\n-            # Parse the split instructions.\n+    # Support for plain text files (one prompt per line)\n+    if path.endswith(\".txt\") and os.path.isfile(path):\n+        with open(path, encoding=\"utf-8\") as f:\n+            prompts = [line.strip() for line in f if line.strip()]\n+\n+        # Apply split specification using the robust parsing from the `datasets` library.\n+        # We treat the list of prompts as a single split, so the name is arbitrary.\n+        try:\n             instruction = ReadInstruction.from_spec(split_str)\n-            # Associate the split with its number of examples (lines).\n-            split_name = str(dataset.split)\n-            name2len = {split_name: len(dataset)}\n-            # Convert the instructions to absolute indices and select the first one.\n+            name2len = {\"_\": len(prompts)}\n             abs_instruction = instruction.to_absolute(name2len)[0]\n-            # Get the dataset by applying the indices.\n-            dataset = dataset[abs_instruction.from_ : abs_instruction.to]\n-        else:\n-            # Path is a local directory.\n-            dataset = load_dataset(\n-                path,\n-                split=split_str,\n-                # Don't require the number of examples (lines) per split to be pre-defined.\n-                verification_mode=VerificationMode.NO_CHECKS,\n-                # But also don't use cached data, as the dataset may have changed on disk.\n-                download_mode=DownloadMode.FORCE_REDOWNLOAD,\n-            )\n+            prompts = prompts[abs_instruction.from_ : abs_instruction.to]",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2675325212",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 103,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2675325212",
        "commented_code": "@@ -149,38 +149,57 @@ def load_prompts(\n     path = specification.dataset\n     split_str = specification.split\n \n-    if os.path.isdir(path):\n-        if Path(path, DATASET_STATE_JSON_FILENAME).exists():\n-            # Dataset saved with datasets.save_to_disk; needs special handling.\n-            # Path should be the subdirectory for a particular split.\n-            dataset = load_from_disk(path)\n-            assert not isinstance(dataset, DatasetDict), (\n-                \"Loading dataset dicts is not supported\"\n-            )\n-            # Parse the split instructions.\n+    # Support for plain text files (one prompt per line)\n+    if path.endswith(\".txt\") and os.path.isfile(path):\n+        with open(path, encoding=\"utf-8\") as f:\n+            prompts = [line.strip() for line in f if line.strip()]\n+\n+        # Apply split specification using the robust parsing from the `datasets` library.\n+        # We treat the list of prompts as a single split, so the name is arbitrary.\n+        try:\n             instruction = ReadInstruction.from_spec(split_str)\n-            # Associate the split with its number of examples (lines).\n-            split_name = str(dataset.split)\n-            name2len = {split_name: len(dataset)}\n-            # Convert the instructions to absolute indices and select the first one.\n+            name2len = {\"_\": len(prompts)}\n             abs_instruction = instruction.to_absolute(name2len)[0]\n-            # Get the dataset by applying the indices.\n-            dataset = dataset[abs_instruction.from_ : abs_instruction.to]\n-        else:\n-            # Path is a local directory.\n-            dataset = load_dataset(\n-                path,\n-                split=split_str,\n-                # Don't require the number of examples (lines) per split to be pre-defined.\n-                verification_mode=VerificationMode.NO_CHECKS,\n-                # But also don't use cached data, as the dataset may have changed on disk.\n-                download_mode=DownloadMode.FORCE_REDOWNLOAD,\n-            )\n+            prompts = prompts[abs_instruction.from_ : abs_instruction.to]",
        "comment_created_at": "2026-01-09T08:40:52+00:00",
        "comment_author": "p-e-w",
        "comment_body": "This duplicates code below, and should be moved to a common helper function.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2553713316",
    "pr_number": 33,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2025-11-23T02:35:38+00:00",
    "commented_code": "def load_prompts(specification: DatasetSpecification) -> list[str]:\n-    dataset = load_dataset(specification.dataset, split=specification.split)\n+    path = specification.dataset\n+    split_str = specification.split\n+    if not os.path.isdir(path):\n+        # Probably a repository path; let load_dataset figure it out.\n+        dataset = load_dataset(path, split=split_str)\n+    elif not Path(path, DATASET_STATE_JSON_FILENAME).exists():",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2553713316",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 33,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2553713316",
        "commented_code": "@@ -36,7 +41,35 @@ def format_duration(seconds: float) -> str:\n \n \n def load_prompts(specification: DatasetSpecification) -> list[str]:\n-    dataset = load_dataset(specification.dataset, split=specification.split)\n+    path = specification.dataset\n+    split_str = specification.split\n+    if not os.path.isdir(path):\n+        # Probably a repository path; let load_dataset figure it out.\n+        dataset = load_dataset(path, split=split_str)\n+    elif not Path(path, DATASET_STATE_JSON_FILENAME).exists():",
        "comment_created_at": "2025-11-23T02:35:38+00:00",
        "comment_author": "p-e-w",
        "comment_body": "The two consecutive `not` clauses are a bit difficult to parse. I think the following layout would be cleaner:\r\n\r\n```python\r\nif os.path.isdir(path):\r\n  if Path(path, DATASET_STATE_JSON_FILENAME).exists():\r\n    ...\r\n  else:\r\n    ...\r\nelse:\r\n  ...\r\n```\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "2553783091",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 33,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2553713316",
        "commented_code": "@@ -36,7 +41,35 @@ def format_duration(seconds: float) -> str:\n \n \n def load_prompts(specification: DatasetSpecification) -> list[str]:\n-    dataset = load_dataset(specification.dataset, split=specification.split)\n+    path = specification.dataset\n+    split_str = specification.split\n+    if not os.path.isdir(path):\n+        # Probably a repository path; let load_dataset figure it out.\n+        dataset = load_dataset(path, split=split_str)\n+    elif not Path(path, DATASET_STATE_JSON_FILENAME).exists():",
        "comment_created_at": "2025-11-23T05:11:00+00:00",
        "comment_author": "spikymoth",
        "comment_body": "Yeah, I was trying to put the more common cases first, but in the end I think it did more harm than good. Reordered as you suggested.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2609297754",
    "pr_number": 60,
    "pr_file": "src/heretic/main.py",
    "created_at": "2025-12-11T06:08:30+00:00",
    "commented_code": ")\n \n \n+def get_merged_model(model: Model, settings: Settings):",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2609297754",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 60,
        "pr_file": "src/heretic/main.py",
        "discussion_id": "2609297754",
        "commented_code": "@@ -49,6 +51,106 @@\n )\n \n \n+def get_merged_model(model: Model, settings: Settings):",
        "comment_created_at": "2025-12-11T06:08:30+00:00",
        "comment_author": "p-e-w",
        "comment_body": "This should be a method of `Model`, which already has the `Settings` object so this will be much cleaner, and in the semantically correct place.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2609303981",
    "pr_number": 60,
    "pr_file": "src/heretic/main.py",
    "created_at": "2025-12-11T06:11:03+00:00",
    "commented_code": ")\n \n \n+def get_merged_model(model: Model, settings: Settings):\n+    \"\"\"\n+    Returns the model with LoRA adapters merged if applicable.\n+    For quantized models, prompts the user for how to proceed.\n+    Returns None if user chooses to save LoRA adapter only.\n+    \"\"\"\n+    if not isinstance(model.model, PeftModel):\n+        # No LoRA adapters, return the model as-is\n+        return model.model\n+\n+    # Prompt for all PEFT models to ensure user is aware of merge implications\n+    if settings.quantization in [\n+        QuantizationMethod.BNB_4BIT,\n+        QuantizationMethod.BNB_8BIT,\n+    ]:\n+        # Quantized models need special handling - we must reload the base model\n+        # in full precision to merge the LoRA adapters\n+        print()\n+        print(\n+            \"[yellow]Model was loaded with quantization. Merging requires reloading the base model.[/]\"\n+        )\n+        print(\n+            \"[red](!) WARNING: CPU Merging requires dequantizing the entire model to System RAM.[/]\"\n+        )\n+        print(\"[red]    This can lead to SYSTEM FREEZES if you run out of memory.[/]\")\n+        print(\n+            \"[yellow]    Rule of thumb: You need approx. 3x the parameter count in GB.[/]\"\n+        )\n+        print(\n+            \"[yellow]    Example: A 27B model requires ~80GB RAM. A 70B model requires ~200GB RAM.[/]\"\n+        )\n+        print()\n+\n+        merge_choice = prompt_select(",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2609303981",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 60,
        "pr_file": "src/heretic/main.py",
        "discussion_id": "2609303981",
        "commented_code": "@@ -49,6 +51,106 @@\n )\n \n \n+def get_merged_model(model: Model, settings: Settings):\n+    \"\"\"\n+    Returns the model with LoRA adapters merged if applicable.\n+    For quantized models, prompts the user for how to proceed.\n+    Returns None if user chooses to save LoRA adapter only.\n+    \"\"\"\n+    if not isinstance(model.model, PeftModel):\n+        # No LoRA adapters, return the model as-is\n+        return model.model\n+\n+    # Prompt for all PEFT models to ensure user is aware of merge implications\n+    if settings.quantization in [\n+        QuantizationMethod.BNB_4BIT,\n+        QuantizationMethod.BNB_8BIT,\n+    ]:\n+        # Quantized models need special handling - we must reload the base model\n+        # in full precision to merge the LoRA adapters\n+        print()\n+        print(\n+            \"[yellow]Model was loaded with quantization. Merging requires reloading the base model.[/]\"\n+        )\n+        print(\n+            \"[red](!) WARNING: CPU Merging requires dequantizing the entire model to System RAM.[/]\"\n+        )\n+        print(\"[red]    This can lead to SYSTEM FREEZES if you run out of memory.[/]\")\n+        print(\n+            \"[yellow]    Rule of thumb: You need approx. 3x the parameter count in GB.[/]\"\n+        )\n+        print(\n+            \"[yellow]    Example: A 27B model requires ~80GB RAM. A 70B model requires ~200GB RAM.[/]\"\n+        )\n+        print()\n+\n+        merge_choice = prompt_select(",
        "comment_created_at": "2025-12-11T06:11:03+00:00",
        "comment_author": "p-e-w",
        "comment_body": "The prompt, however, should be here in `main.py` with the remaining input logic. Prompt first, then pass the choice to `model.get_merged_model`. That is much cleaner, with clear separation of concerns.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2610729252",
    "pr_number": 60,
    "pr_file": "src/heretic/model.py",
    "created_at": "2025-12-11T14:07:53+00:00",
    "commented_code": "continue\n \n             print(\"[green]Ok[/]\")\n+            if settings.quantization == QuantizationMethod.BNB_4BIT:\n+                print(\"[bold green]Model loaded in 4-bit precision.[/]\")\n+            elif settings.quantization == QuantizationMethod.BNB_8BIT:\n+                print(\"[bold green]Model loaded in 8-bit precision.[/]\")\n             break\n \n         if self.model is None:\n             raise Exception(\"Failed to load model with all configured dtypes.\")\n \n+        # Try to use LoRA adapters for abliteration (more memory efficient)\n+        if self.settings.quantization in [\n+            QuantizationMethod.BNB_4BIT,\n+            QuantizationMethod.BNB_8BIT,\n+        ]:\n+            # PEFT expects just the module names (e.g., \"o_proj\"), not full paths like \"attn.o_proj\"\n+            target_modules = [\n+                comp.split(\".\")[-1] for comp in self.get_abliterable_components()\n+            ]\n+            peft_config = LoraConfig(\n+                r=1,  # Rank 1 is sufficient for directional ablation\n+                target_modules=target_modules,\n+                lora_alpha=1,\n+                lora_dropout=0,\n+                bias=\"none\",\n+                task_type=\"CAUSAL_LM\",\n+            )\n+            self.model = get_peft_model(self.model, peft_config)\n+            self.use_lora = True\n+\n+        # LoRA B matrices are initialized to zero by default in PEFT,\n+        # so we don't need to do anything manually.\n+\n+        self.loaded_model_name = settings.model\n+\n         print(f\"* Transformer model with [bold]{len(self.get_layers())}[/] layers\")\n         print(\"* Abliterable components:\")\n-        for component, matrices in self.get_layer_matrices(0).items():\n+        for component, modules in self.get_layer_modules(0).items():\n             print(\n-                f\"  * [bold]{component}[/]: [bold]{len(matrices)}[/] matrices per layer\"\n+                f\"  * [bold]{component}[/]: [bold]{len(modules)}[/] modules per layer\"\n+            )\n+\n+    def get_merged_model(self, strategy: str = \"merge\"):",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2610729252",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 60,
        "pr_file": "src/heretic/model.py",
        "discussion_id": "2610729252",
        "commented_code": "@@ -86,19 +124,135 @@ def __init__(self, settings: Settings):\n                 continue\n \n             print(\"[green]Ok[/]\")\n+            if settings.quantization == QuantizationMethod.BNB_4BIT:\n+                print(\"[bold green]Model loaded in 4-bit precision.[/]\")\n+            elif settings.quantization == QuantizationMethod.BNB_8BIT:\n+                print(\"[bold green]Model loaded in 8-bit precision.[/]\")\n             break\n \n         if self.model is None:\n             raise Exception(\"Failed to load model with all configured dtypes.\")\n \n+        # Try to use LoRA adapters for abliteration (more memory efficient)\n+        if self.settings.quantization in [\n+            QuantizationMethod.BNB_4BIT,\n+            QuantizationMethod.BNB_8BIT,\n+        ]:\n+            # PEFT expects just the module names (e.g., \"o_proj\"), not full paths like \"attn.o_proj\"\n+            target_modules = [\n+                comp.split(\".\")[-1] for comp in self.get_abliterable_components()\n+            ]\n+            peft_config = LoraConfig(\n+                r=1,  # Rank 1 is sufficient for directional ablation\n+                target_modules=target_modules,\n+                lora_alpha=1,\n+                lora_dropout=0,\n+                bias=\"none\",\n+                task_type=\"CAUSAL_LM\",\n+            )\n+            self.model = get_peft_model(self.model, peft_config)\n+            self.use_lora = True\n+\n+        # LoRA B matrices are initialized to zero by default in PEFT,\n+        # so we don't need to do anything manually.\n+\n+        self.loaded_model_name = settings.model\n+\n         print(f\"* Transformer model with [bold]{len(self.get_layers())}[/] layers\")\n         print(\"* Abliterable components:\")\n-        for component, matrices in self.get_layer_matrices(0).items():\n+        for component, modules in self.get_layer_modules(0).items():\n             print(\n-                f\"  * [bold]{component}[/]: [bold]{len(matrices)}[/] matrices per layer\"\n+                f\"  * [bold]{component}[/]: [bold]{len(modules)}[/] modules per layer\"\n+            )\n+\n+    def get_merged_model(self, strategy: str = \"merge\"):",
        "comment_created_at": "2025-12-11T14:07:53+00:00",
        "comment_author": "p-e-w",
        "comment_body": "Missing return type. Also, there's no need for a default strategy, as we are explicit everywhere (we may not even need the `strategy` parameter at all, per my other comment).",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2554566927",
    "pr_number": 42,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2025-11-24T03:28:11+00:00",
    "commented_code": "from datasets.download.download_manager import DownloadMode\n from datasets.utils.info_utils import VerificationMode\n from optuna import Trial\n+from questionary import Choice\n from rich.console import Console\n \n from .config import DatasetSpecification, Settings\n \n print = Console(highlight=False).print\n \n \n+def is_notebook() -> bool:\n+    # Check for specific environment variables (Colab, Kaggle)\n+    # This is necessary because when running as a subprocess (e.g. !heretic),\n+    # get_ipython() might not be available or might not reflect the notebook environment.\n+    if os.getenv(\"COLAB_GPU\") or os.getenv(\"KAGGLE_KERNEL_RUN_TYPE\"):\n+        return True\n+\n+    # Check IPython shell type (for library usage)\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+        return False\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+\n+def prompt_select(message: str, choices: list[Any], style=None) -> Any:\n+    if is_notebook():\n+        print()\n+        print(message)\n+        real_choices = []\n+        for i, choice in enumerate(choices, 1):\n+            if isinstance(choice, Choice):\n+                print(f\"[{i}] {choice.title}\")\n+                real_choices.append(choice.value)\n+            else:\n+                print(f\"[{i}] {choice}\")\n+                real_choices.append(choice)\n+\n+        while True:\n+            try:\n+                selection = input(\"Enter number: \")\n+                idx = int(selection) - 1\n+                if 0 <= idx < len(real_choices):\n+                    return real_choices[idx]\n+                print(\n+                    f\"[red]Please enter a number between 1 and {len(real_choices)}[/]\"\n+                )\n+            except ValueError:\n+                print(\"[red]Invalid input. Please enter a number.[/]\")\n+    else:\n+        return questionary.select(message, choices=choices, style=style).ask()\n+\n+\n+def prompt_text(\n+    message: str,\n+    default: str = \"\",\n+    password: bool = False,\n+    unsafe: bool = False,\n+    qmark: str = None,\n+) -> str:\n+    if is_notebook():\n+        print()\n+        if password:\n+            return getpass.getpass(message)",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2554566927",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 42,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2554566927",
        "commented_code": "@@ -20,13 +22,114 @@\n from datasets.download.download_manager import DownloadMode\n from datasets.utils.info_utils import VerificationMode\n from optuna import Trial\n+from questionary import Choice\n from rich.console import Console\n \n from .config import DatasetSpecification, Settings\n \n print = Console(highlight=False).print\n \n \n+def is_notebook() -> bool:\n+    # Check for specific environment variables (Colab, Kaggle)\n+    # This is necessary because when running as a subprocess (e.g. !heretic),\n+    # get_ipython() might not be available or might not reflect the notebook environment.\n+    if os.getenv(\"COLAB_GPU\") or os.getenv(\"KAGGLE_KERNEL_RUN_TYPE\"):\n+        return True\n+\n+    # Check IPython shell type (for library usage)\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+        return False\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+\n+def prompt_select(message: str, choices: list[Any], style=None) -> Any:\n+    if is_notebook():\n+        print()\n+        print(message)\n+        real_choices = []\n+        for i, choice in enumerate(choices, 1):\n+            if isinstance(choice, Choice):\n+                print(f\"[{i}] {choice.title}\")\n+                real_choices.append(choice.value)\n+            else:\n+                print(f\"[{i}] {choice}\")\n+                real_choices.append(choice)\n+\n+        while True:\n+            try:\n+                selection = input(\"Enter number: \")\n+                idx = int(selection) - 1\n+                if 0 <= idx < len(real_choices):\n+                    return real_choices[idx]\n+                print(\n+                    f\"[red]Please enter a number between 1 and {len(real_choices)}[/]\"\n+                )\n+            except ValueError:\n+                print(\"[red]Invalid input. Please enter a number.[/]\")\n+    else:\n+        return questionary.select(message, choices=choices, style=style).ask()\n+\n+\n+def prompt_text(\n+    message: str,\n+    default: str = \"\",\n+    password: bool = False,\n+    unsafe: bool = False,\n+    qmark: str = None,\n+) -> str:\n+    if is_notebook():\n+        print()\n+        if password:\n+            return getpass.getpass(message)",
        "comment_created_at": "2025-11-24T03:28:11+00:00",
        "comment_author": "p-e-w",
        "comment_body": "Please move password handling into `prompt_password`. There is no need to clutter this function with unrelated functionality.\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2553502273",
    "pr_number": 35,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2025-11-23T00:01:54+00:00",
    "commented_code": "-----\n \n \"\"\"\n+\n+\n+def prompt_select(message: str, choices: list[Any], style=None) -> Any:\n+    if is_notebook():\n+        print()\n+        print(message)\n+        real_choices = []\n+        for i, choice in enumerate(choices, 1):\n+            if isinstance(choice, Choice):\n+                print(f\"[{i}] {choice.title}\")\n+                real_choices.append(choice.value)\n+            else:\n+                print(f\"[{i}] {choice}\")\n+                real_choices.append(choice)\n+\n+        print()\n+        while True:\n+            print(f\"Enter number (1-{len(real_choices)}): \", end=\"\")\n+            selection = input()\n+            if not selection:\n+                return None\n+            if selection.isdigit() and 1 <= int(selection) <= len(real_choices):\n+                return real_choices[int(selection) - 1]\n+            print(\"Please enter a valid number\")\n+    else:\n+        return questionary.select(message, choices=choices, style=style).ask()",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2553502273",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 35,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2553502273",
        "commented_code": "@@ -121,3 +125,61 @@ def get_readme_intro(\n -----\n \n \"\"\"\n+\n+\n+def prompt_select(message: str, choices: list[Any], style=None) -> Any:\n+    if is_notebook():\n+        print()\n+        print(message)\n+        real_choices = []\n+        for i, choice in enumerate(choices, 1):\n+            if isinstance(choice, Choice):\n+                print(f\"[{i}] {choice.title}\")\n+                real_choices.append(choice.value)\n+            else:\n+                print(f\"[{i}] {choice}\")\n+                real_choices.append(choice)\n+\n+        print()\n+        while True:\n+            print(f\"Enter number (1-{len(real_choices)}): \", end=\"\")\n+            selection = input()\n+            if not selection:\n+                return None\n+            if selection.isdigit() and 1 <= int(selection) <= len(real_choices):\n+                return real_choices[int(selection) - 1]\n+            print(\"Please enter a valid number\")\n+    else:\n+        return questionary.select(message, choices=choices, style=style).ask()",
        "comment_created_at": "2025-11-23T00:01:54+00:00",
        "comment_author": "p-e-w",
        "comment_body": "You removed the styles from the original calls to `questionary.select`, so they now look worse. Please hardcode the original style in this function, as requested earlier.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2549550153",
    "pr_number": 35,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2025-11-21T12:06:17+00:00",
    "commented_code": "-----\n \n \"\"\"\n+\n+\n+def is_notebook() -> bool:\n+    import os\n+    import sys\n+\n+    # 1. Check for Google Colab environment variables\n+    if \"COLAB_GPU\" in os.environ or \"COLAB_RELEASE_TAG\" in os.environ:\n+        return True\n+\n+    # 2. Check for Google Colab module\n+    if \"google.colab\" in sys.modules:\n+        return True\n+\n+    # 3. Check for Kaggle environment variables\n+    if (\n+        \"KAGGLE_KERNEL_RUN_TYPE\" in os.environ\n+        or \"KAGGLE_DATA_PROXY_TOKEN\" in os.environ\n+    ):\n+        return True\n+\n+    # 4. Check IPython shell type\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        # Standard Jupyter/Colab shells\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        # Some other environments might have different shell names but still be notebooks\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+    return False\n+\n+\n+def prompt_select(message: str, choices: list, style=None):",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2549550153",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 35,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2549550153",
        "commented_code": "@@ -121,3 +124,107 @@ def get_readme_intro(\n -----\n \n \"\"\"\n+\n+\n+def is_notebook() -> bool:\n+    import os\n+    import sys\n+\n+    # 1. Check for Google Colab environment variables\n+    if \"COLAB_GPU\" in os.environ or \"COLAB_RELEASE_TAG\" in os.environ:\n+        return True\n+\n+    # 2. Check for Google Colab module\n+    if \"google.colab\" in sys.modules:\n+        return True\n+\n+    # 3. Check for Kaggle environment variables\n+    if (\n+        \"KAGGLE_KERNEL_RUN_TYPE\" in os.environ\n+        or \"KAGGLE_DATA_PROXY_TOKEN\" in os.environ\n+    ):\n+        return True\n+\n+    # 4. Check IPython shell type\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        # Standard Jupyter/Colab shells\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        # Some other environments might have different shell names but still be notebooks\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+    return False\n+\n+\n+def prompt_select(message: str, choices: list, style=None):",
        "comment_created_at": "2025-11-21T12:06:17+00:00",
        "comment_author": "p-e-w",
        "comment_body": "```suggestion\r\ndef prompt_select(message: str, choices: list, style=None) -> Any:\r\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "2549691296",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 35,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2549550153",
        "commented_code": "@@ -121,3 +124,107 @@ def get_readme_intro(\n -----\n \n \"\"\"\n+\n+\n+def is_notebook() -> bool:\n+    import os\n+    import sys\n+\n+    # 1. Check for Google Colab environment variables\n+    if \"COLAB_GPU\" in os.environ or \"COLAB_RELEASE_TAG\" in os.environ:\n+        return True\n+\n+    # 2. Check for Google Colab module\n+    if \"google.colab\" in sys.modules:\n+        return True\n+\n+    # 3. Check for Kaggle environment variables\n+    if (\n+        \"KAGGLE_KERNEL_RUN_TYPE\" in os.environ\n+        or \"KAGGLE_DATA_PROXY_TOKEN\" in os.environ\n+    ):\n+        return True\n+\n+    # 4. Check IPython shell type\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        # Standard Jupyter/Colab shells\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        # Some other environments might have different shell names but still be notebooks\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+    return False\n+\n+\n+def prompt_select(message: str, choices: list, style=None):",
        "comment_created_at": "2025-11-21T12:59:55+00:00",
        "comment_author": "Vinayyyy7",
        "comment_body": "done\r\n```python\r\ndef prompt_select(message: str, choices: list, style=None) -> Any:\r\n\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2549835597",
    "pr_number": 35,
    "pr_file": "src/heretic/utils.py",
    "created_at": "2025-11-21T13:53:20+00:00",
    "commented_code": "-----\n \n \"\"\"\n+\n+\n+def is_notebook() -> bool:\n+    # 1. Check for Google Colab module\n+    if \"google.colab\" in sys.modules:\n+        return True\n+\n+    # 2. Check IPython shell type\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        # Standard Jupyter/Colab shells\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        # Some other environments might have different shell names but still be notebooks\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+    return False\n+\n+\n+def prompt_select(message: str, choices: list, style=None) -> Any:",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2549835597",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 35,
        "pr_file": "src/heretic/utils.py",
        "discussion_id": "2549835597",
        "commented_code": "@@ -121,3 +125,89 @@ def get_readme_intro(\n -----\n \n \"\"\"\n+\n+\n+def is_notebook() -> bool:\n+    # 1. Check for Google Colab module\n+    if \"google.colab\" in sys.modules:\n+        return True\n+\n+    # 2. Check IPython shell type\n+    try:\n+        from IPython import get_ipython\n+\n+        shell = get_ipython()\n+        if shell is None:\n+            return False\n+\n+        shell_name = shell.__class__.__name__\n+        # Standard Jupyter/Colab shells\n+        if shell_name in [\"ZMQInteractiveShell\", \"Shell\"]:\n+            return True\n+\n+        # Some other environments might have different shell names but still be notebooks\n+        if \"google.colab\" in str(shell.__class__):\n+            return True\n+\n+    except (ImportError, NameError, AttributeError):\n+        return False\n+\n+    return False\n+\n+\n+def prompt_select(message: str, choices: list, style=None) -> Any:",
        "comment_created_at": "2025-11-21T13:53:20+00:00",
        "comment_author": "p-e-w",
        "comment_body": "```suggestion\r\ndef prompt_select(message: str, choices: list[Any], style=None) -> Any:\r\n```\r\n\r\nFull typing everywhere please.\r\n\r\nAlso, instead of passing `style` as an argument, hardcode `Style([(\"highlighted\", \"reverse\")])` in this function, as we want that style for all selects.\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2671366347",
    "pr_number": 53,
    "pr_file": "src/heretic/evaluator.py",
    "created_at": "2026-01-08T08:18:03+00:00",
    "commented_code": "# SPDX-License-Identifier: AGPL-3.0-or-later\n # Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n \n-import torch.nn.functional as F\n-from torch import Tensor\n+from __future__ import annotations\n \n-from .config import Settings\n+from typing import Any\n+\n+from pydantic import BaseModel\n+\n+from .config import ObjectiveDirection, Settings\n from .model import Model\n-from .utils import Prompt, load_prompts, print\n+from .scorer import EvaluationContext, Score, Scorer\n+from .utils import load_plugin, print\n \n \n class Evaluator:\n-    settings: Settings\n-    model: Model",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2671366347",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 53,
        "pr_file": "src/heretic/evaluator.py",
        "discussion_id": "2671366347",
        "commented_code": "@@ -1,125 +1,128 @@\n # SPDX-License-Identifier: AGPL-3.0-or-later\n # Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n \n-import torch.nn.functional as F\n-from torch import Tensor\n+from __future__ import annotations\n \n-from .config import Settings\n+from typing import Any\n+\n+from pydantic import BaseModel\n+\n+from .config import ObjectiveDirection, Settings\n from .model import Model\n-from .utils import Prompt, load_prompts, print\n+from .scorer import EvaluationContext, Score, Scorer\n+from .utils import load_plugin, print\n \n \n class Evaluator:\n-    settings: Settings\n-    model: Model",
        "comment_created_at": "2026-01-08T08:18:03+00:00",
        "comment_author": "p-e-w",
        "comment_body": "Without these, we lose type safety as `ty` sees the fields as `unknown` then.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2727650342",
    "pr_number": 53,
    "pr_file": "src/heretic/scorer.py",
    "created_at": "2026-01-26T13:39:39+00:00",
    "commented_code": "+# SPDX-License-Identifier: AGPL-3.0-or-later\n+# Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n+\n+from __future__ import annotations\n+\n+from abc import ABC\n+from dataclasses import InitVar, dataclass, field\n+from typing import TYPE_CHECKING, Any, Literal, NoReturn\n+\n+from pydantic import BaseModel\n+\n+from heretic.plugin import Plugin\n+from heretic.utils import Prompt\n+\n+if TYPE_CHECKING:\n+    from torch import Tensor\n+\n+    from .config import Settings as HereticSettings\n+    from .model import Model\n+\n+FinishReason = Literal[\"len\", \"eos\", \"unk\", \"empty\"]\n+\n+\n+@dataclass\n+class Score:\n+    \"\"\"\n+    Result of evaluating a scorer.\n+\n+    - `value`: scalar value used for optimization (if enabled)\n+    - `display`: string shown to the user in logs/console\n+    \"\"\"\n+\n+    name: str\n+    value: float\n+    display: str\n+\n+\n+@dataclass(frozen=True)\n+class Response:\n+    \"\"\"\n+    A single model response to a single prompt.\n+    \"\"\"\n+\n+    prompt: Prompt\n+    text: str\n+    finish_reason: FinishReason\n+    metadata: dict[str, Any] = field(default_factory=dict)\n+\n+\n+@dataclass\n+class Context:\n+    \"\"\"\n+    Runtime context passed to scorers\n+\n+    Provides scorer-safe access to the model.\n+\n+    Scorers must use `get_responses(...)`, `get_logits(...)`, etc.\n+    Direct access to the underlying Model is intentionally not exposed.\n+    \"\"\"\n+\n+    settings: \"HereticSettings\"",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2727650342",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 53,
        "pr_file": "src/heretic/scorer.py",
        "discussion_id": "2727650342",
        "commented_code": "@@ -0,0 +1,181 @@\n+# SPDX-License-Identifier: AGPL-3.0-or-later\n+# Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n+\n+from __future__ import annotations\n+\n+from abc import ABC\n+from dataclasses import InitVar, dataclass, field\n+from typing import TYPE_CHECKING, Any, Literal, NoReturn\n+\n+from pydantic import BaseModel\n+\n+from heretic.plugin import Plugin\n+from heretic.utils import Prompt\n+\n+if TYPE_CHECKING:\n+    from torch import Tensor\n+\n+    from .config import Settings as HereticSettings\n+    from .model import Model\n+\n+FinishReason = Literal[\"len\", \"eos\", \"unk\", \"empty\"]\n+\n+\n+@dataclass\n+class Score:\n+    \"\"\"\n+    Result of evaluating a scorer.\n+\n+    - `value`: scalar value used for optimization (if enabled)\n+    - `display`: string shown to the user in logs/console\n+    \"\"\"\n+\n+    name: str\n+    value: float\n+    display: str\n+\n+\n+@dataclass(frozen=True)\n+class Response:\n+    \"\"\"\n+    A single model response to a single prompt.\n+    \"\"\"\n+\n+    prompt: Prompt\n+    text: str\n+    finish_reason: FinishReason\n+    metadata: dict[str, Any] = field(default_factory=dict)\n+\n+\n+@dataclass\n+class Context:\n+    \"\"\"\n+    Runtime context passed to scorers\n+\n+    Provides scorer-safe access to the model.\n+\n+    Scorers must use `get_responses(...)`, `get_logits(...)`, etc.\n+    Direct access to the underlying Model is intentionally not exposed.\n+    \"\"\"\n+\n+    settings: \"HereticSettings\"",
        "comment_created_at": "2026-01-26T13:39:39+00:00",
        "comment_author": "p-e-w",
        "comment_body": "No strings as type annotations, those bypass import validation and we don't want that.",
        "pr_file_module": null
      },
      {
        "comment_id": "2727653860",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 53,
        "pr_file": "src/heretic/scorer.py",
        "discussion_id": "2727650342",
        "commented_code": "@@ -0,0 +1,181 @@\n+# SPDX-License-Identifier: AGPL-3.0-or-later\n+# Copyright (C) 2025  Philipp Emanuel Weidmann <pew@worldwidemann.com>\n+\n+from __future__ import annotations\n+\n+from abc import ABC\n+from dataclasses import InitVar, dataclass, field\n+from typing import TYPE_CHECKING, Any, Literal, NoReturn\n+\n+from pydantic import BaseModel\n+\n+from heretic.plugin import Plugin\n+from heretic.utils import Prompt\n+\n+if TYPE_CHECKING:\n+    from torch import Tensor\n+\n+    from .config import Settings as HereticSettings\n+    from .model import Model\n+\n+FinishReason = Literal[\"len\", \"eos\", \"unk\", \"empty\"]\n+\n+\n+@dataclass\n+class Score:\n+    \"\"\"\n+    Result of evaluating a scorer.\n+\n+    - `value`: scalar value used for optimization (if enabled)\n+    - `display`: string shown to the user in logs/console\n+    \"\"\"\n+\n+    name: str\n+    value: float\n+    display: str\n+\n+\n+@dataclass(frozen=True)\n+class Response:\n+    \"\"\"\n+    A single model response to a single prompt.\n+    \"\"\"\n+\n+    prompt: Prompt\n+    text: str\n+    finish_reason: FinishReason\n+    metadata: dict[str, Any] = field(default_factory=dict)\n+\n+\n+@dataclass\n+class Context:\n+    \"\"\"\n+    Runtime context passed to scorers\n+\n+    Provides scorer-safe access to the model.\n+\n+    Scorers must use `get_responses(...)`, `get_logits(...)`, etc.\n+    Direct access to the underlying Model is intentionally not exposed.\n+    \"\"\"\n+\n+    settings: \"HereticSettings\"",
        "comment_created_at": "2026-01-26T13:40:21+00:00",
        "comment_author": "p-e-w",
        "comment_body": "`from __future__ import annotations` already treats type annotations as strings effectively IIRC.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2583883463",
    "pr_number": 60,
    "pr_file": "src/heretic/model.py",
    "created_at": "2025-12-03T07:00:59+00:00",
    "commented_code": "self.trusted_models[self.settings.model] = True\n \n     def get_layers(self) -> ModuleList:\n+        model = self.model\n+        \n+        # Unwrap PeftModel\n+        if isinstance(model, PeftModel):\n+            model = model.base_model.model\n+\n         # Most multimodal models.\n         with suppress(Exception):\n-            return self.model.model.language_model.layers\n+            return model.model.language_model.layers\n \n         # Text-only models.\n-        return self.model.model.layers\n+        return model.model.layers\n \n-    def get_layer_matrices(self, layer_index: int) -> dict[str, list[Tensor]]:\n+    def get_layer_modules(self, layer_index: int) -> dict[str, list[torch.nn.Module]]:",
    "repo_full_name": "p-e-w/heretic",
    "discussion_comments": [
      {
        "comment_id": "2583883463",
        "repo_full_name": "p-e-w/heretic",
        "pr_number": 60,
        "pr_file": "src/heretic/model.py",
        "discussion_id": "2583883463",
        "commented_code": "@@ -111,48 +155,47 @@ def reload_model(self):\n             self.trusted_models[self.settings.model] = True\n \n     def get_layers(self) -> ModuleList:\n+        model = self.model\n+        \n+        # Unwrap PeftModel\n+        if isinstance(model, PeftModel):\n+            model = model.base_model.model\n+\n         # Most multimodal models.\n         with suppress(Exception):\n-            return self.model.model.language_model.layers\n+            return model.model.language_model.layers\n \n         # Text-only models.\n-        return self.model.model.layers\n+        return model.model.layers\n \n-    def get_layer_matrices(self, layer_index: int) -> dict[str, list[Tensor]]:\n+    def get_layer_modules(self, layer_index: int) -> dict[str, list[torch.nn.Module]]:",
        "comment_created_at": "2025-12-03T07:00:59+00:00",
        "comment_author": "p-e-w",
        "comment_body": "```suggestion\r\n    def get_layer_modules(self, layer_index: int) -> dict[str, list[torch.nn.Module | Tensor]]:\r\n```\r\n\r\nI intend to enforce type checks soon so we should be precise.\r\n",
        "pr_file_module": null
      }
    ]
  }
]