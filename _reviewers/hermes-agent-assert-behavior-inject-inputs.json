[
  {
    "discussion_id": "3592738624",
    "pr_number": 64861,
    "pr_file": "ui-tui/src/app/useSessionLifecycle.ts",
    "created_at": "2026-07-16T04:57:49+00:00",
    "commented_code": "await closeSession(getUiState().sid)\n       }\n \n-      const r = await rpc<SessionCreateResponse>('session.create', { cols: colsRef.current })\n+      // Pass the launch cwd so the gateway records it as the session's\n+      // workspace (explicit_cwd). Without this, TUI sessions get cwd=NULL and\n+      // the Desktop GUI groups them under \"No workspace\" instead of the\n+      // directory the user launched `hermes --tui` from. HERMES_CWD is set by\n+      // the Python launcher (main.py:_launch_tui) and falls back to\n+      // process.cwd() so embedded- and standalone-TUI both work.\n+      const launchCwd = process.env.HERMES_CWD || process.cwd()\n+      const r = await rpc<SessionCreateResponse>('session.create', { cols: colsRef.current, cwd: launchCwd })\n \n       if (!r) {",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3592738624",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 64861,
        "pr_file": "ui-tui/src/app/useSessionLifecycle.ts",
        "discussion_id": "3592738624",
        "commented_code": "@@ -206,7 +206,14 @@ export function useSessionLifecycle(opts: UseSessionLifecycleOptions) {\n         await closeSession(getUiState().sid)\n       }\n \n-      const r = await rpc<SessionCreateResponse>('session.create', { cols: colsRef.current })\n+      // Pass the launch cwd so the gateway records it as the session's\n+      // workspace (explicit_cwd). Without this, TUI sessions get cwd=NULL and\n+      // the Desktop GUI groups them under \"No workspace\" instead of the\n+      // directory the user launched `hermes --tui` from. HERMES_CWD is set by\n+      // the Python launcher (main.py:_launch_tui) and falls back to\n+      // process.cwd() so embedded- and standalone-TUI both work.\n+      const launchCwd = process.env.HERMES_CWD || process.cwd()\n+      const r = await rpc<SessionCreateResponse>('session.create', { cols: colsRef.current, cwd: launchCwd })\n \n       if (!r) {",
        "comment_created_at": "2026-07-16T04:57:49+00:00",
        "comment_author": "teknium1",
        "comment_body": "Please add a Vitest regression that drives `startNewSession` and asserts this `session.create` payload contains `cwd: process.env.HERMES_CWD`. The server tests cover the `explicit_cwd` persistence branch, but not this client-to-server contract.",
        "pr_file_module": null
      },
      {
        "comment_id": "3595458693",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 64861,
        "pr_file": "ui-tui/src/app/useSessionLifecycle.ts",
        "discussion_id": "3592738624",
        "commented_code": "@@ -206,7 +206,14 @@ export function useSessionLifecycle(opts: UseSessionLifecycleOptions) {\n         await closeSession(getUiState().sid)\n       }\n \n-      const r = await rpc<SessionCreateResponse>('session.create', { cols: colsRef.current })\n+      // Pass the launch cwd so the gateway records it as the session's\n+      // workspace (explicit_cwd). Without this, TUI sessions get cwd=NULL and\n+      // the Desktop GUI groups them under \"No workspace\" instead of the\n+      // directory the user launched `hermes --tui` from. HERMES_CWD is set by\n+      // the Python launcher (main.py:_launch_tui) and falls back to\n+      // process.cwd() so embedded- and standalone-TUI both work.\n+      const launchCwd = process.env.HERMES_CWD || process.cwd()\n+      const r = await rpc<SessionCreateResponse>('session.create', { cols: colsRef.current, cwd: launchCwd })\n \n       if (!r) {",
        "comment_created_at": "2026-07-16T12:39:34+00:00",
        "comment_author": "Prontsevich",
        "comment_body": "Addressed in `5f9832b5e` (rebased from `4b056da41`). Added a Vitest regression that mounts `useSessionLifecycle`, invokes `newSession` (driving `startNewSession`), sets `HERMES_CWD`, and asserts the `session.create` payload includes the launch cwd. Verified after rebasing with `cd ui-tui && npm run check`: 107 test files passed, 1126 tests passed, and 1 test was skipped.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3580748869",
    "pr_number": 46740,
    "pr_file": "apps/desktop/src/hermes.ts",
    "created_at": "2026-07-14T16:18:55+00:00",
    "commented_code": "return window.hermesDesktop.api<AudioTranscriptionResponse>({\n     path: '/api/audio/transcribe',\n     method: 'POST',\n+    timeoutMs: 120_000,",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3580748869",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 46740,
        "pr_file": "apps/desktop/src/hermes.ts",
        "discussion_id": "3580748869",
        "commented_code": "@@ -730,6 +730,7 @@ export function transcribeAudio(dataUrl: string, mimeType?: string): Promise<Aud\n   return window.hermesDesktop.api<AudioTranscriptionResponse>({\n     path: '/api/audio/transcribe',\n     method: 'POST',\n+    timeoutMs: 120_000,",
        "comment_created_at": "2026-07-14T16:18:55+00:00",
        "comment_author": "teknium1",
        "comment_body": "Please add a focused `transcribeAudio()` API-wrapper test asserting this 120-second override. Current `apps/desktop/src/hermes.test.ts` covers other long REST timeouts but not `/api/audio/transcribe`.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3592659648",
    "pr_number": 65087,
    "pr_file": "apps/desktop/electron/bootstrap-platform.ts",
    "created_at": "2026-07-16T04:34:18+00:00",
    "commented_code": "}\n }\n \n+function isVirtualizedGpuEnvironment(platform = process.platform) {",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3592659648",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 65087,
        "pr_file": "apps/desktop/electron/bootstrap-platform.ts",
        "discussion_id": "3592659648",
        "commented_code": "@@ -18,6 +18,20 @@ function isWslEnvironment(env = process.env, platform = process.platform, kernel\n   }\n }\n \n+function isVirtualizedGpuEnvironment(platform = process.platform) {",
        "comment_created_at": "2026-07-16T04:34:18+00:00",
        "comment_author": "teknium1",
        "comment_body": "Please make the DMI vendor input injectable, analogous to `isWslEnvironment(..., kernelRelease)`, and add deterministic tests for QEMU, non-QEMU, read failure, and non-Linux behavior. Reading the host sysfs file directly prevents the new branch from being tested through the existing detector suite.",
        "pr_file_module": null
      },
      {
        "comment_id": "3593923983",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 65087,
        "pr_file": "apps/desktop/electron/bootstrap-platform.ts",
        "discussion_id": "3592659648",
        "commented_code": "@@ -18,6 +18,20 @@ function isWslEnvironment(env = process.env, platform = process.platform, kernel\n   }\n }\n \n+function isVirtualizedGpuEnvironment(platform = process.platform) {",
        "comment_created_at": "2026-07-16T08:44:51+00:00",
        "comment_author": "nickmimic",
        "comment_body": "Addressed the review feedback: both `sysVendor` and `productName` are now injectable (mirrors `isWslEnvironment`'s `kernelRelease` pattern), and added 15 unit tests covering QEMU detection, sysfs-read failure, and the override behavior.\r\nAlso found and fixed a gap beyond the original scope: UTM's default backend on Apple Silicon is Apple's own Virtualization.framework, not QEMU, it reports the host vendor (`Apple Inc.`) via `sys_vendor`, so the original QEMU-only check would never have matched it. Added a second detection path via DMI `product_name`. This is likely the common case for UTM-on-Apple-Silicon users, so it's a meaningful expansion of what's actually covered, not just a testability refactor.\r\nVerified in a real guest: the `virtualized-gpu (Apple Virtualization.framework detected...)` log line fires correctly. A separate pre-existing backend-startup race currently blocks full window rendering in this dev sandbox, unrelated to this fix, will file separately if needed.",
        "pr_file_module": null
      }
    ]
  }
]