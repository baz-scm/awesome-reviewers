[
  {
    "discussion_id": "3588761354",
    "pr_number": 57884,
    "pr_file": "plugins/platforms/line/adapter.py",
    "created_at": "2026-07-15T15:49:45+00:00",
    "commented_code": "\"file\": \".bin\",\n         }.get(msg_type, \".bin\")\n         try:\n+            # Audio/video/file payloads must not go through the image cache \u2014\n+            # cache_image_from_bytes validates magic bytes and refuses\n+            # non-image data (e.g. LINE voice notes arrive as .m4a).\n+            if msg_type in (\"audio\", \"video\", \"file\"):",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3588761354",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 57884,
        "pr_file": "plugins/platforms/line/adapter.py",
        "discussion_id": "3588761354",
        "commented_code": "@@ -1067,6 +1068,11 @@ async def _download_media(self, message_id: str, msg_type: str) -> Optional[str]\n             \"file\": \".bin\",\n         }.get(msg_type, \".bin\")\n         try:\n+            # Audio/video/file payloads must not go through the image cache \u2014\n+            # cache_image_from_bytes validates magic bytes and refuses\n+            # non-image data (e.g. LINE voice notes arrive as .m4a).\n+            if msg_type in (\"audio\", \"video\", \"file\"):",
        "comment_created_at": "2026-07-15T15:49:45+00:00",
        "comment_author": "teknium1",
        "comment_body": "This combines video and file with audio, but current main has dedicated `cache_video_from_bytes` and `cache_document_from_bytes` helpers. Please route each type separately; otherwise video/file are stored under the audio cache and do not preserve their intended cache classification.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3560668204",
    "pr_number": 61808,
    "pr_file": "gateway/platforms/bluebubbles.py",
    "created_at": "2026-07-10T17:06:48+00:00",
    "commented_code": "user_name=sender,\n             chat_id_alt=chat_identifier,\n         )\n+        message_guid = self._value(",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3560668204",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 61808,
        "pr_file": "gateway/platforms/bluebubbles.py",
        "discussion_id": "3560668204",
        "commented_code": "@@ -1020,16 +1205,31 @@ async def _handle_webhook(self, request):\n             user_name=sender,\n             chat_id_alt=chat_identifier,\n         )\n+        message_guid = self._value(",
        "comment_created_at": "2026-07-10T17:06:48+00:00",
        "comment_author": "teknium1",
        "comment_body": "This cache is shared by Socket.IO and `_handle_webhook()` because both call `_process_event_payload`. Default webhook mode still registers `updated-message`, so a same-GUID update now returns at line 1221 and never reaches `handle_message`, changing existing webhook behavior. Please gate this dedupe on `self.use_socketio` (or explicitly change and test the webhook contract).",
        "pr_file_module": null
      },
      {
        "comment_id": "3561015602",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 61808,
        "pr_file": "gateway/platforms/bluebubbles.py",
        "discussion_id": "3560668204",
        "commented_code": "@@ -1020,16 +1205,31 @@ async def _handle_webhook(self, request):\n             user_name=sender,\n             chat_id_alt=chat_identifier,\n         )\n+        message_guid = self._value(",
        "comment_created_at": "2026-07-10T18:02:00+00:00",
        "comment_author": "cwallace",
        "comment_body": "Good catch \u2014 agreed. This was intended as Socket.IO duplicate-event suppression, not a webhook behavior change.\n\nI updated the branch so the GUID dedupe only runs when `self.use_socketio` is true, which preserves default webhook `new-message` / `updated-message` handling. I also added a regression test covering same-GUID webhook `new-message` followed by `updated-message`, asserting both reach `handle_message`.\n\nVerification on the updated PR head `61ddfc22dbccf6addd860c20a36e16c7a1312dc9`:\n- targeted regression tests passed\n- focused suite passed: `103 tests passed, 0 failed`\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3584023710",
    "pr_number": 51414,
    "pr_file": "plugins/platforms/discord/adapter.py",
    "created_at": "2026-07-15T02:17:43+00:00",
    "commented_code": "# partition point.  Everything before this is already in the\n                 # session transcript.  (Redundant when _after_obj is set, but\n                 # needed for cold start.)\n-                if msg.author == self._client.user:\n+                # Skip this stop when full_thread mode is enabled \u2014 the user\n+                # wants the entire thread context, even messages preceding\n+                # prior bot replies.\n+                if not full_thread and msg.author == self._client.user:",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3584023710",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 51414,
        "pr_file": "plugins/platforms/discord/adapter.py",
        "discussion_id": "3584023710",
        "commented_code": "@@ -4388,7 +4425,10 @@ def _keep(msg) -> Optional[str]:\n                 # partition point.  Everything before this is already in the\n                 # session transcript.  (Redundant when _after_obj is set, but\n                 # needed for cold start.)\n-                if msg.author == self._client.user:\n+                # Skip this stop when full_thread mode is enabled \u2014 the user\n+                # wants the entire thread context, even messages preceding\n+                # prior bot replies.\n+                if not full_thread and msg.author == self._client.user:",
        "comment_created_at": "2026-07-15T02:17:43+00:00",
        "comment_author": "teknium1",
        "comment_body": "`full_thread` still uses the earlier `_last_self_message_id` cache as `channel.history(after=...)` (lines 4350-4354 and 4410). On the usual hot path, messages before the last bot response are therefore excluded before this condition runs. Disable that cache boundary in full-thread mode and add a regression test that seeds `_last_self_message_id`.",
        "pr_file_module": null
      },
      {
        "comment_id": "3599736992",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 51414,
        "pr_file": "plugins/platforms/discord/adapter.py",
        "discussion_id": "3584023710",
        "commented_code": "@@ -4388,7 +4425,10 @@ def _keep(msg) -> Optional[str]:\n                 # partition point.  Everything before this is already in the\n                 # session transcript.  (Redundant when _after_obj is set, but\n                 # needed for cold start.)\n-                if msg.author == self._client.user:\n+                # Skip this stop when full_thread mode is enabled \u2014 the user\n+                # wants the entire thread context, even messages preceding\n+                # prior bot replies.\n+                if not full_thread and msg.author == self._client.user:",
        "comment_created_at": "2026-07-17T01:06:34+00:00",
        "comment_author": "taras-polishchuk",
        "comment_body": "Valid catch \u2014 thanks. The local `break`-skip on its own was a no-op on the hot path: by the time control reached the loop, `channel.history()` had already been narrowed by `after=self._last_self_message_id[...]` (adapter.py:4350-4354 and 4410), so the messages I wanted to walk past were filtered out before the partition check ever ran. Cold start works as advertised, hot path didn't.\n\nFixed in this push:\n\n- Moved the bypass to the cache-construction site: in `_fetch_channel_context`, the `_after_obj` assignment is now wrapped in `if not full_thread:` so the cache is only consulted in partition mode. Same shape as the existing `if not full_thread and msg.author == self._client.user: break` guard, just applied at the right layer.\n- Added `test_fetch_channel_context_full_thread_ignores_last_self_cache` in `tests/gateway/test_discord_free_response.py` \u2014 seeds `_last_self_message_id[\"888\"] = \"150\"` and asserts `channel.history()` is called with `after=None` while `history_full_thread: True`. Without the fix the recorded `after` is a `discord.Object(id=150)`.\n- Added a counterpart `test_fetch_channel_context_partition_mode_still_uses_cache` so a future refactor that disables the cache globally (in either direction) gets caught \u2014 partition mode keeps the optimisation intact.\n\nEnv var fallback (`DISCORD_HISTORY_FULL_THREAD`) is kept as the ops-escape hatch, with explicit env-var precedence over YAML in the bridge \u2014 same convention as `DISCORD_HISTORY_BACKFILL` and friends.\n\nReady for re-review on this thread.\n",
        "pr_file_module": null
      },
      {
        "comment_id": "3668972580",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 51414,
        "pr_file": "plugins/platforms/discord/adapter.py",
        "discussion_id": "3584023710",
        "commented_code": "@@ -4388,7 +4425,10 @@ def _keep(msg) -> Optional[str]:\n                 # partition point.  Everything before this is already in the\n                 # session transcript.  (Redundant when _after_obj is set, but\n                 # needed for cold start.)\n-                if msg.author == self._client.user:\n+                # Skip this stop when full_thread mode is enabled \u2014 the user\n+                # wants the entire thread context, even messages preceding\n+                # prior bot replies.\n+                if not full_thread and msg.author == self._client.user:",
        "comment_created_at": "2026-07-28T20:32:18+00:00",
        "comment_author": "taras-polishchuk",
        "comment_body": "Re-ping after rebase: this concern is now resolved at the new location of the cache-construction guard. See commit `477e3f01a6` (rebased as `feat(discord): add history_full_thread option`) which still wraps the `_after_obj` assignment in `if not full_thread:`. The regression test `test_fetch_channel_context_full_thread_ignores_last_self_cache` (still passing on the rebased branch) seeds `_last_self_message_id` and asserts the cache is bypassed in full-thread mode. Re-requesting your re-review on the rebased head `723212debd`.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3655343331",
    "pr_number": 71745,
    "pr_file": "tools/browser_tool.py",
    "created_at": "2026-07-27T07:56:15+00:00",
    "commented_code": "# Failed opens and blocked redirects must not retarget follow-up clicks\n         # or snapshots to a newly-created but irrelevant session.\n         _last_active_session_key[effective_task_id] = nav_session_key\n+        _last_navigated_urls[nav_session_key] = str(final_url or \"\")",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3655343331",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 71745,
        "pr_file": "tools/browser_tool.py",
        "discussion_id": "3655343331",
        "commented_code": "@@ -2972,6 +2978,7 @@ def browser_navigate(url: str, task_id: Optional[str] = None) -> str:\n         # Failed opens and blocked redirects must not retarget follow-up clicks\n         # or snapshots to a newly-created but irrelevant session.\n         _last_active_session_key[effective_task_id] = nav_session_key\n+        _last_navigated_urls[nav_session_key] = str(final_url or \"\")",
        "comment_created_at": "2026-07-27T07:56:15+00:00",
        "comment_author": "drowchaeshew",
        "comment_body": "### tl;dr: Daemon url changes not only happens on `browser_navigate()`. \n\nConsider this scenario (cdp, browserless): \n0. There's only on tab with url `https://www.example.com`; both cdp & daemon bind to this tab; \n1. With `browser_click`, a new tab is opened - daemon binds to this newly created tab, while cdp don't. \n2. invoke `browser_console(document.title)` result in a `https:///www.example.com`. \n\n----\n\n### Suggestion?\n\nquick fix: add ` _last_navigated_urls[nav_session_key] = str(final_url or \"\")` to `browser_click()` also (though I'm not sure if a url can be obtained in that scenario...)",
        "pr_file_module": null
      },
      {
        "comment_id": "3667478366",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 71745,
        "pr_file": "tools/browser_tool.py",
        "discussion_id": "3655343331",
        "commented_code": "@@ -2972,6 +2978,7 @@ def browser_navigate(url: str, task_id: Optional[str] = None) -> str:\n         # Failed opens and blocked redirects must not retarget follow-up clicks\n         # or snapshots to a newly-created but irrelevant session.\n         _last_active_session_key[effective_task_id] = nav_session_key\n+        _last_navigated_urls[nav_session_key] = str(final_url or \"\")",
        "comment_created_at": "2026-07-28T16:40:47+00:00",
        "comment_author": "chrisyoung2005",
        "comment_body": "Good catch \u2014 confirmed, and fixed in 07be57692. Your scenario is exactly the blind spot: after a click opens a new tab, the daemon rebinds while the supervisor keeps showing the old page, whose URL still *matches* the stale record, so the URL comparison alone keeps trusting the fast path.\n\nOn the suggested quick fix: `browser_click`'s result carries no URL, so recording one there would mean probing the daemon \u2014 a subprocess call per click, and clicks are far more frequent than evals. So the commit inverts it:\n\n- `browser_click` and `browser_press(\"Enter\")` mark the session as *possibly diverged* (zero extra calls). Marked sessions skip the fast path and take the always-correct agent-browser subprocess path \u2014 worst case is the subprocess spawn we'd have paid anyway pre-fast-path, never a wrong-tab result.\n- The mark clears on the next authoritative URL: `browser_navigate`, or `browser_back` \u2014 whose result *does* report the landing URL, so it now re-records `_last_navigated_urls` too.\n- Other keys and failed clicks keep the fast path.\n\n7 new tests in `test_browser_eval_supervisor_path.py::TestPageDivergenceAfterInteractions`, including your new-tab scenario verbatim (`test_click_then_eval_falls_through_to_subprocess`); 5 fail pre-fix. Full browser suite green (555 passed).\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3590836327",
    "pr_number": 60138,
    "pr_file": "gateway/slash_commands.py",
    "created_at": "2026-07-15T21:25:09+00:00",
    "commented_code": "async_session_store: AsyncSessionStore\n \n+    def _remember_gateway_session_choices(\n+        self,\n+        session_key: str,\n+        rows: list[dict],\n+    ) -> None:\n+        \"\"\"Remember the latest numbered session rows shown in this gateway chat.\"\"\"\n+        if not session_key:\n+            return\n+        choices = [\n+            {\n+                \"id\": str(row.get(\"id\") or \"\"),\n+                \"title\": str(row.get(\"title\") or \"\"),\n+            }\n+            for row in rows[:10]\n+            if row.get(\"id\")\n+        ]\n+        latest = getattr(self, \"_latest_gateway_session_choices\", None)\n+        if not isinstance(latest, dict):\n+            latest = {}\n+            self._latest_gateway_session_choices = latest\n+        latest[session_key] = choices",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3590836327",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 60138,
        "pr_file": "gateway/slash_commands.py",
        "discussion_id": "3590836327",
        "commented_code": "@@ -89,6 +89,42 @@ class GatewaySlashCommandsMixin:\n \n     async_session_store: AsyncSessionStore\n \n+    def _remember_gateway_session_choices(\n+        self,\n+        session_key: str,\n+        rows: list[dict],\n+    ) -> None:\n+        \"\"\"Remember the latest numbered session rows shown in this gateway chat.\"\"\"\n+        if not session_key:\n+            return\n+        choices = [\n+            {\n+                \"id\": str(row.get(\"id\") or \"\"),\n+                \"title\": str(row.get(\"title\") or \"\"),\n+            }\n+            for row in rows[:10]\n+            if row.get(\"id\")\n+        ]\n+        latest = getattr(self, \"_latest_gateway_session_choices\", None)\n+        if not isinstance(latest, dict):\n+            latest = {}\n+            self._latest_gateway_session_choices = latest\n+        latest[session_key] = choices",
        "comment_created_at": "2026-07-15T21:25:09+00:00",
        "comment_author": "teknium1",
        "comment_body": "This plain dict has one retained list per distinct gateway session key and no eviction. Please cap it with an LRU or clear it through the session lifecycle; `GatewayRunner._session_sources` is a capped OrderedDict precedent.",
        "pr_file_module": null
      },
      {
        "comment_id": "3593051035",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 60138,
        "pr_file": "gateway/slash_commands.py",
        "discussion_id": "3590836327",
        "commented_code": "@@ -89,6 +89,42 @@ class GatewaySlashCommandsMixin:\n \n     async_session_store: AsyncSessionStore\n \n+    def _remember_gateway_session_choices(\n+        self,\n+        session_key: str,\n+        rows: list[dict],\n+    ) -> None:\n+        \"\"\"Remember the latest numbered session rows shown in this gateway chat.\"\"\"\n+        if not session_key:\n+            return\n+        choices = [\n+            {\n+                \"id\": str(row.get(\"id\") or \"\"),\n+                \"title\": str(row.get(\"title\") or \"\"),\n+            }\n+            for row in rows[:10]\n+            if row.get(\"id\")\n+        ]\n+        latest = getattr(self, \"_latest_gateway_session_choices\", None)\n+        if not isinstance(latest, dict):\n+            latest = {}\n+            self._latest_gateway_session_choices = latest\n+        latest[session_key] = choices",
        "comment_created_at": "2026-07-16T06:13:30+00:00",
        "comment_author": "GodsBoy",
        "comment_body": "> Please cap it with an LRU or clear it through the session lifecycle.\n\nFixed in 821269177. Numbered session choices now use a capped `OrderedDict` LRU with the same 512-entry bound as the gateway session-source cache. Reads and writes refresh recency, and coverage verifies least-recently-used eviction.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3577882832",
    "pr_number": 43525,
    "pr_file": "cron/audit.py",
    "created_at": "2026-07-14T09:52:22+00:00",
    "commented_code": "+\"\"\"Opt-in append-only audit log for cron job state changes.\"\"\"\n+\n+from __future__ import annotations\n+\n+import json\n+import logging\n+import os\n+import threading\n+from pathlib import Path\n+from typing import Any, Dict, Optional\n+\n+from hermes_constants import get_hermes_home\n+from hermes_time import now as _hermes_now\n+\n+logger = logging.getLogger(__name__)\n+\n+_TRUE_VALUES = {\"1\", \"true\", \"yes\", \"on\"}\n+_DEFAULT_MAX_MB = 10\n+_config_cache: Optional[Dict[str, Any]] = None\n+_config_lock = threading.Lock()\n+_write_lock = threading.Lock()\n+\n+\n+def _truthy(value: Any) -> bool:\n+    return str(value or \"\").strip().lower() in _TRUE_VALUES\n+\n+\n+def _load_audit_config() -> Dict[str, Any]:\n+    \"\"\"Load audit config from env/config.yaml, cached until reload.\"\"\"\n+    global _config_cache\n+    with _config_lock:\n+        if _config_cache is not None:\n+            return dict(_config_cache)\n+\n+    hermes_home = get_hermes_home()\n+    cfg: Dict[str, Any] = {\n+        \"enabled\": _truthy(os.getenv(\"HERMES_CRON_AUDIT_LOG\")),\n+        \"log_path\": str(hermes_home / \"cron\" / \"audit.log\"),\n+        \"max_mb\": _DEFAULT_MAX_MB,\n+    }\n+\n+    env_path = os.getenv(\"HERMES_CRON_AUDIT_LOG_PATH\")\n+    if env_path:\n+        cfg[\"log_path\"] = env_path\n+\n+    env_max = os.getenv(\"HERMES_CRON_AUDIT_LOG_MAX_MB\")\n+    if env_max:\n+        try:\n+            cfg[\"max_mb\"] = max(1, int(env_max))\n+        except (TypeError, ValueError):\n+            logger.debug(\"Ignoring invalid HERMES_CRON_AUDIT_LOG_MAX_MB=%r\", env_max)\n+\n+    try:\n+        from hermes_cli.config import load_config",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3577882832",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 43525,
        "pr_file": "cron/audit.py",
        "discussion_id": "3577882832",
        "commented_code": "@@ -0,0 +1,199 @@\n+\"\"\"Opt-in append-only audit log for cron job state changes.\"\"\"\n+\n+from __future__ import annotations\n+\n+import json\n+import logging\n+import os\n+import threading\n+from pathlib import Path\n+from typing import Any, Dict, Optional\n+\n+from hermes_constants import get_hermes_home\n+from hermes_time import now as _hermes_now\n+\n+logger = logging.getLogger(__name__)\n+\n+_TRUE_VALUES = {\"1\", \"true\", \"yes\", \"on\"}\n+_DEFAULT_MAX_MB = 10\n+_config_cache: Optional[Dict[str, Any]] = None\n+_config_lock = threading.Lock()\n+_write_lock = threading.Lock()\n+\n+\n+def _truthy(value: Any) -> bool:\n+    return str(value or \"\").strip().lower() in _TRUE_VALUES\n+\n+\n+def _load_audit_config() -> Dict[str, Any]:\n+    \"\"\"Load audit config from env/config.yaml, cached until reload.\"\"\"\n+    global _config_cache\n+    with _config_lock:\n+        if _config_cache is not None:\n+            return dict(_config_cache)\n+\n+    hermes_home = get_hermes_home()\n+    cfg: Dict[str, Any] = {\n+        \"enabled\": _truthy(os.getenv(\"HERMES_CRON_AUDIT_LOG\")),\n+        \"log_path\": str(hermes_home / \"cron\" / \"audit.log\"),\n+        \"max_mb\": _DEFAULT_MAX_MB,\n+    }\n+\n+    env_path = os.getenv(\"HERMES_CRON_AUDIT_LOG_PATH\")\n+    if env_path:\n+        cfg[\"log_path\"] = env_path\n+\n+    env_max = os.getenv(\"HERMES_CRON_AUDIT_LOG_MAX_MB\")\n+    if env_max:\n+        try:\n+            cfg[\"max_mb\"] = max(1, int(env_max))\n+        except (TypeError, ValueError):\n+            logger.debug(\"Ignoring invalid HERMES_CRON_AUDIT_LOG_MAX_MB=%r\", env_max)\n+\n+    try:\n+        from hermes_cli.config import load_config",
        "comment_created_at": "2026-07-14T09:52:22+00:00",
        "comment_author": "teknium1",
        "comment_body": "This cache is process-global, but dashboard cron requests scope `HERMES_HOME` per profile (`hermes_cli/web_server.py:10216-10221`). Once one profile populates it, later profiles reuse its enabled flag and log path. Cache by active home/config signature, or resolve this configuration per call, and add a two-profile regression.",
        "pr_file_module": null
      },
      {
        "comment_id": "3578694692",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 43525,
        "pr_file": "cron/audit.py",
        "discussion_id": "3577882832",
        "commented_code": "@@ -0,0 +1,199 @@\n+\"\"\"Opt-in append-only audit log for cron job state changes.\"\"\"\n+\n+from __future__ import annotations\n+\n+import json\n+import logging\n+import os\n+import threading\n+from pathlib import Path\n+from typing import Any, Dict, Optional\n+\n+from hermes_constants import get_hermes_home\n+from hermes_time import now as _hermes_now\n+\n+logger = logging.getLogger(__name__)\n+\n+_TRUE_VALUES = {\"1\", \"true\", \"yes\", \"on\"}\n+_DEFAULT_MAX_MB = 10\n+_config_cache: Optional[Dict[str, Any]] = None\n+_config_lock = threading.Lock()\n+_write_lock = threading.Lock()\n+\n+\n+def _truthy(value: Any) -> bool:\n+    return str(value or \"\").strip().lower() in _TRUE_VALUES\n+\n+\n+def _load_audit_config() -> Dict[str, Any]:\n+    \"\"\"Load audit config from env/config.yaml, cached until reload.\"\"\"\n+    global _config_cache\n+    with _config_lock:\n+        if _config_cache is not None:\n+            return dict(_config_cache)\n+\n+    hermes_home = get_hermes_home()\n+    cfg: Dict[str, Any] = {\n+        \"enabled\": _truthy(os.getenv(\"HERMES_CRON_AUDIT_LOG\")),\n+        \"log_path\": str(hermes_home / \"cron\" / \"audit.log\"),\n+        \"max_mb\": _DEFAULT_MAX_MB,\n+    }\n+\n+    env_path = os.getenv(\"HERMES_CRON_AUDIT_LOG_PATH\")\n+    if env_path:\n+        cfg[\"log_path\"] = env_path\n+\n+    env_max = os.getenv(\"HERMES_CRON_AUDIT_LOG_MAX_MB\")\n+    if env_max:\n+        try:\n+            cfg[\"max_mb\"] = max(1, int(env_max))\n+        except (TypeError, ValueError):\n+            logger.debug(\"Ignoring invalid HERMES_CRON_AUDIT_LOG_MAX_MB=%r\", env_max)\n+\n+    try:\n+        from hermes_cli.config import load_config",
        "comment_created_at": "2026-07-14T11:53:02+00:00",
        "comment_author": "capt-marbles",
        "comment_body": "Fixed in 0e04959. `_config_cache` is now keyed by the active `HERMES_HOME` (with `reload_audit_config()` clearing all profiles), so a profile with audit enabled no longer leaks its `enabled` flag / log path into another. Added a two-profile regression test (`test_cron_audit_config_is_scoped_per_profile`) that loads an enabled profile, switches `HERMES_HOME` to a disabled one *without* reloading, and asserts it recomputes as disabled \u2014 verified it fails on the old global cache.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3583764662",
    "pr_number": 51317,
    "pr_file": "tools/terminal_tool.py",
    "created_at": "2026-07-15T01:10:13+00:00",
    "commented_code": ")\n \n \n+# Attribute stored on live execution environments so a config/profile/backend\n+# change cannot accidentally reuse a sandbox/SSH shell created for a different\n+# runtime. The cache key remains the session/task id; the signature is the\n+# compatibility guard for the cached value behind that key.\n+_ENV_SIGNATURE_ATTR = \"_hermes_terminal_env_signature\"\n+\n+\n+def _normalize_signature_value(value: Any) -> str:\n+    \"\"\"Return a stable string for runtime signature values.\"\"\"\n+    if value is None:\n+        return \"\"\n+    if isinstance(value, bool):\n+        return \"true\" if value else \"false\"\n+    if isinstance(value, (list, tuple, dict)):\n+        try:\n+            return json.dumps(value, sort_keys=True, separators=(\",\", \":\"))\n+        except TypeError:\n+            return str(value)\n+    return str(value)\n+\n+\n+def _runtime_image_for_config(config: Dict[str, Any], overrides: Optional[Dict[str, Any]] = None) -> str:\n+    \"\"\"Return the selected backend image for the current config/overrides.\"\"\"\n+    overrides = overrides or {}\n+    env_type = config.get(\"env_type\") or \"local\"\n+    if env_type == \"docker\":\n+        return overrides.get(\"docker_image\") or config.get(\"docker_image\") or \"\"\n+    if env_type == \"singularity\":\n+        return overrides.get(\"singularity_image\") or config.get(\"singularity_image\") or \"\"\n+    if env_type == \"modal\":\n+        return overrides.get(\"modal_image\") or config.get(\"modal_image\") or \"\"\n+    if env_type == \"daytona\":\n+        return overrides.get(\"daytona_image\") or config.get(\"daytona_image\") or \"\"\n+    return \"\"\n+\n+\n+def _container_config_for_runtime(config: Dict[str, Any]) -> Dict[str, Any]:\n+    \"\"\"Return the complete container config passed to environment creation.\"\"\"\n+    return {\n+        \"container_cpu\": config.get(\"container_cpu\", 1),\n+        \"container_memory\": config.get(\"container_memory\", 5120),\n+        \"container_disk\": config.get(\"container_disk\", 51200),\n+        \"container_persistent\": config.get(\"container_persistent\", True),\n+        \"modal_mode\": config.get(\"modal_mode\", \"auto\"),\n+        \"docker_volumes\": config.get(\"docker_volumes\", []),\n+        \"docker_mount_cwd_to_workspace\": config.get(\"docker_mount_cwd_to_workspace\", False),\n+        \"docker_forward_env\": config.get(\"docker_forward_env\", []),\n+        \"docker_env\": config.get(\"docker_env\", {}),\n+        \"docker_run_as_host_user\": config.get(\"docker_run_as_host_user\", False),\n+        \"docker_extra_args\": config.get(\"docker_extra_args\", []),\n+        \"docker_network\": config.get(\"docker_network\", True),\n+        \"docker_persist_across_processes\": config.get(\"docker_persist_across_processes\", True),\n+        \"docker_orphan_reaper\": config.get(\"docker_orphan_reaper\", True),\n+    }\n+\n+\n+def _ssh_config_for_runtime(config: Dict[str, Any]) -> Dict[str, Any]:\n+    \"\"\"Return SSH config passed to environment creation.\"\"\"\n+    return {\n+        \"host\": config.get(\"ssh_host\", \"\"),\n+        \"user\": config.get(\"ssh_user\", \"\"),\n+        \"port\": config.get(\"ssh_port\", 22),\n+        \"key\": config.get(\"ssh_key\", \"\"),\n+        \"persistent\": config.get(\"ssh_persistent\", False),\n+    }\n+\n+\n+def _local_config_for_runtime(config: Dict[str, Any]) -> Dict[str, Any]:\n+    \"\"\"Return local backend config passed to environment creation.\"\"\"\n+    return {\n+        \"persistent\": config.get(\"local_persistent\", False),\n+    }\n+\n+\n+def _terminal_env_signature(\n+    config: Dict[str, Any],\n+    *,\n+    task_id: str = \"default\",\n+    overrides: Optional[Dict[str, Any]] = None,\n+    image: Optional[str] = None,\n+    cwd: Optional[str] = None,\n+) -> Dict[str, str]:\n+    \"\"\"Build the runtime signature for a cached terminal environment.\"\"\"\n+    overrides = overrides or {}\n+    env_type = config.get(\"env_type\") or \"local\"\n+    if image is None:\n+        image = _runtime_image_for_config(config, overrides)\n+    if cwd is None:\n+        cwd = overrides.get(\"cwd\") or config.get(\"cwd\") or \"\"\n+    fields = {\n+        \"task_id\": task_id or \"default\",\n+        \"hermes_home\": os.getenv(\"HERMES_HOME\", \"\"),\n+        \"hermes_config_path\": os.getenv(\"HERMES_CONFIG_PATH\", \"\"),\n+        \"session_profile\": os.getenv(\"HERMES_SESSION_PROFILE\", \"\"),\n+        \"env_type\": env_type,\n+        \"cwd\": cwd,\n+        \"host_cwd\": config.get(\"host_cwd\"),\n+        \"image\": image,\n+        \"modal_mode\": config.get(\"modal_mode\"),\n+        \"docker_mount_cwd_to_workspace\": config.get(\"docker_mount_cwd_to_workspace\"),\n+        \"docker_volumes\": config.get(\"docker_volumes\"),\n+        \"docker_forward_env\": config.get(\"docker_forward_env\"),\n+        \"docker_env\": config.get(\"docker_env\"),\n+        \"docker_run_as_host_user\": config.get(\"docker_run_as_host_user\"),\n+        \"docker_network\": config.get(\"docker_network\"),\n+        \"docker_extra_args\": config.get(\"docker_extra_args\"),\n+        \"docker_persist_across_processes\": config.get(\"docker_persist_across_processes\"),\n+        \"docker_orphan_reaper\": config.get(\"docker_orphan_reaper\"),\n+        \"container_cpu\": config.get(\"container_cpu\"),\n+        \"container_memory\": config.get(\"container_memory\"),\n+        \"container_disk\": config.get(\"container_disk\"),\n+        \"container_persistent\": config.get(\"container_persistent\"),\n+        \"ssh_host\": config.get(\"ssh_host\"),\n+        \"ssh_user\": config.get(\"ssh_user\"),\n+        \"ssh_port\": config.get(\"ssh_port\"),\n+        # Do not include raw key path/material in logs or signatures. Presence\n+        # distinguishes key-backed SSH from agent/password auth.\n+        \"ssh_key_present\": bool(config.get(\"ssh_key\")),",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3583764662",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 51317,
        "pr_file": "tools/terminal_tool.py",
        "discussion_id": "3583764662",
        "commented_code": "@@ -1175,6 +1175,296 @@ def resolve_task_overrides(task_id: Optional[str]) -> Dict[str, Any]:\n     )\n \n \n+# Attribute stored on live execution environments so a config/profile/backend\n+# change cannot accidentally reuse a sandbox/SSH shell created for a different\n+# runtime. The cache key remains the session/task id; the signature is the\n+# compatibility guard for the cached value behind that key.\n+_ENV_SIGNATURE_ATTR = \"_hermes_terminal_env_signature\"\n+\n+\n+def _normalize_signature_value(value: Any) -> str:\n+    \"\"\"Return a stable string for runtime signature values.\"\"\"\n+    if value is None:\n+        return \"\"\n+    if isinstance(value, bool):\n+        return \"true\" if value else \"false\"\n+    if isinstance(value, (list, tuple, dict)):\n+        try:\n+            return json.dumps(value, sort_keys=True, separators=(\",\", \":\"))\n+        except TypeError:\n+            return str(value)\n+    return str(value)\n+\n+\n+def _runtime_image_for_config(config: Dict[str, Any], overrides: Optional[Dict[str, Any]] = None) -> str:\n+    \"\"\"Return the selected backend image for the current config/overrides.\"\"\"\n+    overrides = overrides or {}\n+    env_type = config.get(\"env_type\") or \"local\"\n+    if env_type == \"docker\":\n+        return overrides.get(\"docker_image\") or config.get(\"docker_image\") or \"\"\n+    if env_type == \"singularity\":\n+        return overrides.get(\"singularity_image\") or config.get(\"singularity_image\") or \"\"\n+    if env_type == \"modal\":\n+        return overrides.get(\"modal_image\") or config.get(\"modal_image\") or \"\"\n+    if env_type == \"daytona\":\n+        return overrides.get(\"daytona_image\") or config.get(\"daytona_image\") or \"\"\n+    return \"\"\n+\n+\n+def _container_config_for_runtime(config: Dict[str, Any]) -> Dict[str, Any]:\n+    \"\"\"Return the complete container config passed to environment creation.\"\"\"\n+    return {\n+        \"container_cpu\": config.get(\"container_cpu\", 1),\n+        \"container_memory\": config.get(\"container_memory\", 5120),\n+        \"container_disk\": config.get(\"container_disk\", 51200),\n+        \"container_persistent\": config.get(\"container_persistent\", True),\n+        \"modal_mode\": config.get(\"modal_mode\", \"auto\"),\n+        \"docker_volumes\": config.get(\"docker_volumes\", []),\n+        \"docker_mount_cwd_to_workspace\": config.get(\"docker_mount_cwd_to_workspace\", False),\n+        \"docker_forward_env\": config.get(\"docker_forward_env\", []),\n+        \"docker_env\": config.get(\"docker_env\", {}),\n+        \"docker_run_as_host_user\": config.get(\"docker_run_as_host_user\", False),\n+        \"docker_extra_args\": config.get(\"docker_extra_args\", []),\n+        \"docker_network\": config.get(\"docker_network\", True),\n+        \"docker_persist_across_processes\": config.get(\"docker_persist_across_processes\", True),\n+        \"docker_orphan_reaper\": config.get(\"docker_orphan_reaper\", True),\n+    }\n+\n+\n+def _ssh_config_for_runtime(config: Dict[str, Any]) -> Dict[str, Any]:\n+    \"\"\"Return SSH config passed to environment creation.\"\"\"\n+    return {\n+        \"host\": config.get(\"ssh_host\", \"\"),\n+        \"user\": config.get(\"ssh_user\", \"\"),\n+        \"port\": config.get(\"ssh_port\", 22),\n+        \"key\": config.get(\"ssh_key\", \"\"),\n+        \"persistent\": config.get(\"ssh_persistent\", False),\n+    }\n+\n+\n+def _local_config_for_runtime(config: Dict[str, Any]) -> Dict[str, Any]:\n+    \"\"\"Return local backend config passed to environment creation.\"\"\"\n+    return {\n+        \"persistent\": config.get(\"local_persistent\", False),\n+    }\n+\n+\n+def _terminal_env_signature(\n+    config: Dict[str, Any],\n+    *,\n+    task_id: str = \"default\",\n+    overrides: Optional[Dict[str, Any]] = None,\n+    image: Optional[str] = None,\n+    cwd: Optional[str] = None,\n+) -> Dict[str, str]:\n+    \"\"\"Build the runtime signature for a cached terminal environment.\"\"\"\n+    overrides = overrides or {}\n+    env_type = config.get(\"env_type\") or \"local\"\n+    if image is None:\n+        image = _runtime_image_for_config(config, overrides)\n+    if cwd is None:\n+        cwd = overrides.get(\"cwd\") or config.get(\"cwd\") or \"\"\n+    fields = {\n+        \"task_id\": task_id or \"default\",\n+        \"hermes_home\": os.getenv(\"HERMES_HOME\", \"\"),\n+        \"hermes_config_path\": os.getenv(\"HERMES_CONFIG_PATH\", \"\"),\n+        \"session_profile\": os.getenv(\"HERMES_SESSION_PROFILE\", \"\"),\n+        \"env_type\": env_type,\n+        \"cwd\": cwd,\n+        \"host_cwd\": config.get(\"host_cwd\"),\n+        \"image\": image,\n+        \"modal_mode\": config.get(\"modal_mode\"),\n+        \"docker_mount_cwd_to_workspace\": config.get(\"docker_mount_cwd_to_workspace\"),\n+        \"docker_volumes\": config.get(\"docker_volumes\"),\n+        \"docker_forward_env\": config.get(\"docker_forward_env\"),\n+        \"docker_env\": config.get(\"docker_env\"),\n+        \"docker_run_as_host_user\": config.get(\"docker_run_as_host_user\"),\n+        \"docker_network\": config.get(\"docker_network\"),\n+        \"docker_extra_args\": config.get(\"docker_extra_args\"),\n+        \"docker_persist_across_processes\": config.get(\"docker_persist_across_processes\"),\n+        \"docker_orphan_reaper\": config.get(\"docker_orphan_reaper\"),\n+        \"container_cpu\": config.get(\"container_cpu\"),\n+        \"container_memory\": config.get(\"container_memory\"),\n+        \"container_disk\": config.get(\"container_disk\"),\n+        \"container_persistent\": config.get(\"container_persistent\"),\n+        \"ssh_host\": config.get(\"ssh_host\"),\n+        \"ssh_user\": config.get(\"ssh_user\"),\n+        \"ssh_port\": config.get(\"ssh_port\"),\n+        # Do not include raw key path/material in logs or signatures. Presence\n+        # distinguishes key-backed SSH from agent/password auth.\n+        \"ssh_key_present\": bool(config.get(\"ssh_key\")),",
        "comment_created_at": "2026-07-15T01:10:13+00:00",
        "comment_author": "teknium1",
        "comment_body": "`ssh_key_present` makes every non-empty key identical for cache comparison. `_create_environment()` passes the exact key path to `SSHEnvironment`, so key A \u2192 key B with the same host/user/port will reuse the old authenticated environment. Please compare a stable non-secret key identity and add a rotation regression.",
        "pr_file_module": null
      }
    ]
  }
]