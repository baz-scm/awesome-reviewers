[
  {
    "discussion_id": "3609240304",
    "pr_number": 65982,
    "pr_file": "cli-config.yaml.example",
    "created_at": "2026-07-18T21:38:15+00:00",
    "commented_code": "# Recommended: 20-30 for focused tasks, 50-100 for open exploration\n   max_turns: 60\n \n+  # Claude Agent SDK provider runtime (provider: claude-agent-sdk).",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3609240304",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 65982,
        "pr_file": "cli-config.yaml.example",
        "discussion_id": "3609240304",
        "commented_code": "@@ -692,6 +692,17 @@ agent:\n   # Recommended: 20-30 for focused tasks, 50-100 for open exploration\n   max_turns: 60\n \n+  # Claude Agent SDK provider runtime (provider: claude-agent-sdk).",
        "comment_created_at": "2026-07-18T21:38:15+00:00",
        "comment_author": "teknium1",
        "comment_body": "Please add matching `agent.claude_agent_sdk` defaults to `hermes_cli/config.py::DEFAULT_CONFIG` and cover configuration loading. The example file alone does not register these as canonical config options for default-driven config tooling.",
        "pr_file_module": null
      },
      {
        "comment_id": "3609524629",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 65982,
        "pr_file": "cli-config.yaml.example",
        "discussion_id": "3609240304",
        "commented_code": "@@ -692,6 +692,17 @@ agent:\n   # Recommended: 20-30 for focused tasks, 50-100 for open exploration\n   max_turns: 60\n \n+  # Claude Agent SDK provider runtime (provider: claude-agent-sdk).",
        "comment_created_at": "2026-07-19T00:52:43+00:00",
        "comment_author": "fcavalcantirj",
        "comment_body": "Done in 3e75adae8. `DEFAULT_CONFIG` now registers `agent.claude_agent_sdk` beside the other `agent.*` keys \u2014 `{streaming: false, allow_metered_key: false, append_file: \"\"}` \u2014 and a new module `tests/hermes_cli/test_claude_agent_sdk_config.py` covers it: the exact canonical defaults, the deep-merge fill-in for a pre-existing config.yaml that lacks the block, and survival of explicit user values through the merge. All four tests fail without the `DEFAULT_CONFIG` entry (verified by stashing the change and running them). The example block also gained the `append_file` key it was missing.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3649593228",
    "pr_number": 67835,
    "pr_file": "scripts/install.ps1",
    "created_at": "2026-07-25T05:54:01+00:00",
    "commented_code": "# (Hermes-Setup.exe), never via the irm|iex CLI one-liner.\n     $InstallStages += @{ Name = \"desktop\"; Title = \"Building desktop app\"; Category = \"install\"; NeedsUserInput = $false; Worker = \"Stage-Desktop\" }\n }\n+# browser is added AFTER desktop (if enabled) for two reasons:\n+#   1. Install-AgentBrowser (L355) runs `npm install -g --prefix` against a\n+#      separate prefix ($HERMES_HOME\node) -- it has no dependency on the\n+#      per-repo apps/desktop/node_modules Stage-NodeDeps / Stage-Desktop produce.\n+#      So the ordering choice is not a hard dependency, only a stable one.\n+#   2. Running after Stage-Desktop means a freshly-built Hermes.exe (if\n+#      -IncludeDesktop was passed) picks up the freshly-installed\n+#      agent-browser on its first relaunch, instead of inheriting a stale\n+#      bare-PATH agent-browser from NVM. This is the same upgrade ordering\n+#      install.sh's ensure_browser() achieves on Linux/macOS (L2550-2600).\n+#   The stage also runs for non-desktop installs (the irm|iex one-liner) --\n+#   browser tools are a general Hermes capability, not a desktop-only feature.\n+#   Soft-skip when Node is unavailable (browser tools degrade gracefully);\n+#   see the Stage-Node note at L3548-3558 for the same pattern.\n+$InstallStages += @{ Name = \"browser\"; Title = \"Installing agent-browser\"; Category = \"install\"; NeedsUserInput = $false; Worker = \"Stage-Browser\" }",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3649593228",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 67835,
        "pr_file": "scripts/install.ps1",
        "discussion_id": "3649593228",
        "commented_code": "@@ -3515,6 +3515,21 @@ if ($IncludeDesktop) {\n     # (Hermes-Setup.exe), never via the irm|iex CLI one-liner.\n     $InstallStages += @{ Name = \"desktop\"; Title = \"Building desktop app\"; Category = \"install\"; NeedsUserInput = $false; Worker = \"Stage-Desktop\" }\n }\n+# browser is added AFTER desktop (if enabled) for two reasons:\n+#   1. Install-AgentBrowser (L355) runs `npm install -g --prefix` against a\n+#      separate prefix ($HERMES_HOME\\node) -- it has no dependency on the\n+#      per-repo apps/desktop/node_modules Stage-NodeDeps / Stage-Desktop produce.\n+#      So the ordering choice is not a hard dependency, only a stable one.\n+#   2. Running after Stage-Desktop means a freshly-built Hermes.exe (if\n+#      -IncludeDesktop was passed) picks up the freshly-installed\n+#      agent-browser on its first relaunch, instead of inheriting a stale\n+#      bare-PATH agent-browser from NVM. This is the same upgrade ordering\n+#      install.sh's ensure_browser() achieves on Linux/macOS (L2550-2600).\n+#   The stage also runs for non-desktop installs (the irm|iex one-liner) --\n+#   browser tools are a general Hermes capability, not a desktop-only feature.\n+#   Soft-skip when Node is unavailable (browser tools degrade gracefully);\n+#   see the Stage-Node note at L3548-3558 for the same pattern.\n+$InstallStages += @{ Name = \"browser\"; Title = \"Installing agent-browser\"; Category = \"install\"; NeedsUserInput = $false; Worker = \"Stage-Browser\" }",
        "comment_created_at": "2026-07-25T05:54:01+00:00",
        "comment_author": "teknium1",
        "comment_body": "This stage is added to every Windows manifest without consulting persisted browser-tool configuration. `hermes_cli/tools_config.py:1988-1996` treats `agent.disabled_toolsets` as a final user override, and the PR discussion confirms this update would reinstall agent-browser after a user disabled browser tools. Please add the configuration-aware skip here or resolve the intended policy before merging.",
        "pr_file_module": null
      },
      {
        "comment_id": "3668494475",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 67835,
        "pr_file": "scripts/install.ps1",
        "discussion_id": "3649593228",
        "commented_code": "@@ -3515,6 +3515,21 @@ if ($IncludeDesktop) {\n     # (Hermes-Setup.exe), never via the irm|iex CLI one-liner.\n     $InstallStages += @{ Name = \"desktop\"; Title = \"Building desktop app\"; Category = \"install\"; NeedsUserInput = $false; Worker = \"Stage-Desktop\" }\n }\n+# browser is added AFTER desktop (if enabled) for two reasons:\n+#   1. Install-AgentBrowser (L355) runs `npm install -g --prefix` against a\n+#      separate prefix ($HERMES_HOME\\node) -- it has no dependency on the\n+#      per-repo apps/desktop/node_modules Stage-NodeDeps / Stage-Desktop produce.\n+#      So the ordering choice is not a hard dependency, only a stable one.\n+#   2. Running after Stage-Desktop means a freshly-built Hermes.exe (if\n+#      -IncludeDesktop was passed) picks up the freshly-installed\n+#      agent-browser on its first relaunch, instead of inheriting a stale\n+#      bare-PATH agent-browser from NVM. This is the same upgrade ordering\n+#      install.sh's ensure_browser() achieves on Linux/macOS (L2550-2600).\n+#   The stage also runs for non-desktop installs (the irm|iex one-liner) --\n+#   browser tools are a general Hermes capability, not a desktop-only feature.\n+#   Soft-skip when Node is unavailable (browser tools degrade gracefully);\n+#   see the Stage-Node note at L3548-3558 for the same pattern.\n+$InstallStages += @{ Name = \"browser\"; Title = \"Installing agent-browser\"; Category = \"install\"; NeedsUserInput = $false; Worker = \"Stage-Browser\" }",
        "comment_created_at": "2026-07-28T19:13:55+00:00",
        "comment_author": "LiteSoul",
        "comment_body": "Resolved in `cc6c08e83` (amended from `4bb103263` \u2014 same logical content, rebased to incorporate the behavioral test rewrite): Stage-Browser now consults `agent.disabled_toolsets` from config.yaml before installing. A new `Get-HermesConfigDisabledToolsets` helper (scripts/install.ps1, after `Write-BrowserEnv`) reads `$HermesHome\\config.yaml` and returns the suppressed toolset names as a hashtable. If `browser` is in that set, the worker sets `$script:_StageSkippedReason` and returns \u2014 surfacing `skipped=true, ok=true` in the JSON frame, matching Stage-Node's soft-skip pattern. This mirrors the runtime resolver's behavior (`hermes_cli/tools_config.py:2146-2154` \u2014 `agent.disabled_toolsets` applied as a final user override that runs last).\n\nThe helper uses a small line-oriented state machine rather than a YAML parser \u2014 PowerShell has no built-in YAML module, and importing one would widen the install bootstrap's dependency footprint for a single scalar key. It handles both inline (`[browser, memory]`) and block-sequence (`- browser`) YAML forms. When config.yaml is absent (fresh install), the function returns an empty set and the stage proceeds \u2014 the guard only applies during Update runs where config.yaml already exists.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3583383321",
    "pr_number": 50684,
    "pr_file": "cli-config.yaml.example",
    "created_at": "2026-07-14T23:44:11+00:00",
    "commented_code": "local:\n     model: \"base\"              # tiny | base | small | medium | large-v3 | turbo\n     # language: \"\"             # auto-detect; set to \"en\", \"es\", \"fr\", etc. to force\n+    # initial_prompt: \"\"       # Optional faster-whisper prompt, e.g. bias Chinese output to simplified Chinese",
    "repo_full_name": "NousResearch/hermes-agent",
    "discussion_comments": [
      {
        "comment_id": "3583383321",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 50684,
        "pr_file": "cli-config.yaml.example",
        "discussion_id": "3583383321",
        "commented_code": "@@ -1006,6 +1006,7 @@ stt:\n   local:\n     model: \"base\"              # tiny | base | small | medium | large-v3 | turbo\n     # language: \"\"             # auto-detect; set to \"en\", \"es\", \"fr\", etc. to force\n+    # initial_prompt: \"\"       # Optional faster-whisper prompt, e.g. bias Chinese output to simplified Chinese",
        "comment_created_at": "2026-07-14T23:44:11+00:00",
        "comment_author": "teknium1",
        "comment_body": "Please also add `initial_prompt: \"\"` to `DEFAULT_CONFIG[\"stt\"][\"local\"]` in `hermes_cli/config.py`; this example documents a public configuration option that the runtime schema currently does not declare.",
        "pr_file_module": null
      },
      {
        "comment_id": "3608344347",
        "repo_full_name": "NousResearch/hermes-agent",
        "pr_number": 50684,
        "pr_file": "cli-config.yaml.example",
        "discussion_id": "3583383321",
        "commented_code": "@@ -1006,6 +1006,7 @@ stt:\n   local:\n     model: \"base\"              # tiny | base | small | medium | large-v3 | turbo\n     # language: \"\"             # auto-detect; set to \"en\", \"es\", \"fr\", etc. to force\n+    # initial_prompt: \"\"       # Optional faster-whisper prompt, e.g. bias Chinese output to simplified Chinese",
        "comment_created_at": "2026-07-18T12:18:31+00:00",
        "comment_author": "BlackishGreen33",
        "comment_body": "Addressed in 55b5594d4. Added `stt.local.initial_prompt: \"\"` to `DEFAULT_CONFIG` and a regression test that writes a real `config.yaml`, loads it through `_load_stt_config()` / `load_config()`, and verifies the user model override plus the merged prompt default.\\n\\nVerification: 30 transcription tests and 158 config/STT tests passed; focused Ruff and `py_compile` also passed.",
        "pr_file_module": null
      }
    ]
  }
]