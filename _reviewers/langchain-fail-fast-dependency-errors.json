[
  {
    "discussion_id": "2649875554",
    "pr_number": 34514,
    "pr_file": "libs/langchain_v1/langchain/agents/factory.py",
    "created_at": "2025-12-28T19:08:06+00:00",
    "commented_code": "return result\n \n \n+def _resolve_middleware_dependencies(\n+    middleware: Sequence[AgentMiddleware[StateT_co, ContextT]],\n+) -> list[AgentMiddleware[StateT_co, ContextT]]:\n+    \"\"\"Resolve middleware dependencies and return ordered list with auto-instantiated deps.\n+\n+    This function:\n+    1. Collects all dependencies from middleware instances\n+    2. Auto-instantiates missing class dependencies with default parameters\n+    3. Uses provided instance dependencies as-is\n+    4. Performs topological sort to ensure dependencies come before dependents\n+    5. Preserves original order for middleware without dependency constraints\n+    6. Detects and raises errors for circular dependencies\n+\n+    Args:\n+        middleware: Original sequence of middleware instances.\n+\n+    Returns:\n+        Ordered list of middleware with dependencies resolved and auto-instantiated.\n+\n+    Raises:\n+        ValueError: If circular dependencies are detected.\n+\n+    Example:\n+        ```python\n+        # MyMiddleware depends on ModelRetryMiddleware class\n+        middleware = [MyMiddleware(depends_on=(ModelRetryMiddleware,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [ModelRetryMiddleware(), MyMiddleware(...)]\n+\n+        # Or with an instance\n+        retry = ModelRetryMiddleware(max_retries=5)\n+        middleware = [MyMiddleware(depends_on=(retry,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [retry, MyMiddleware(...)]\n+        ```\n+    \"\"\"\n+    # If no middleware has dependencies, return as-is\n+    has_dependencies = any(getattr(m, \"depends_on\", ()) for m in middleware)\n+    if not has_dependencies:\n+        return list(middleware)\n+\n+    # Build a map: class -> instance for all middleware\n+    # For original middleware, use the first instance of each class\n+    instance_by_class: dict[type[AgentMiddleware[Any, Any]], AgentMiddleware[Any, Any]] = {}\n+    original_classes: set[type[AgentMiddleware[Any, Any]]] = set()\n+\n+    for m in middleware:\n+        cls = type(m)\n+        original_classes.add(cls)\n+        # Use the first instance of each class\n+        if cls not in instance_by_class:\n+            instance_by_class[cls] = m\n+\n+    # First pass: collect all instance dependencies (these take priority)\n+    def collect_instance_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect instance dependencies first (they take priority).\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if not isinstance(dep, type):\n+                # It's an instance\n+                dep_class = type(dep)\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Store the instance (overwrite if already present from original)\n+                instance_by_class[dep_class] = dep\n+\n+                # Recursively collect instance dependencies\n+                collect_instance_dependencies(dep, visited | {dep_class})\n+\n+    # Collect instance dependencies for all original middleware\n+    for m in middleware:\n+        collect_instance_dependencies(m, {type(m)})\n+\n+    # Second pass: collect class dependencies and auto-instantiate\n+    def collect_class_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect class dependencies and auto-instantiate if needed.\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if isinstance(dep, type):\n+                # It's a class\n+                dep_class = dep\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Instantiate if not already present\n+                if dep_class not in instance_by_class:\n+                    try:\n+                        instance_by_class[dep_class] = dep_class()\n+                    except Exception as e:\n+                        # Log and skip if instantiation fails\n+                        import logging\n+\n+                        logging.getLogger(__name__).debug(\n+                            \"Failed to auto-instantiate middleware %s: %s\", dep_class.__name__, e\n+                        )\n+                        continue\n+\n+                # Recursively collect dependencies\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})\n+            else:\n+                # It's an instance - already handled in first pass\n+                dep_class = type(dep)\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2649875554",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34514,
        "pr_file": "libs/langchain_v1/langchain/agents/factory.py",
        "discussion_id": "2649875554",
        "commented_code": "@@ -538,6 +538,178 @@ async def call_inner(req: ToolCallRequest) -> ToolMessage | Command:\n     return result\n \n \n+def _resolve_middleware_dependencies(\n+    middleware: Sequence[AgentMiddleware[StateT_co, ContextT]],\n+) -> list[AgentMiddleware[StateT_co, ContextT]]:\n+    \"\"\"Resolve middleware dependencies and return ordered list with auto-instantiated deps.\n+\n+    This function:\n+    1. Collects all dependencies from middleware instances\n+    2. Auto-instantiates missing class dependencies with default parameters\n+    3. Uses provided instance dependencies as-is\n+    4. Performs topological sort to ensure dependencies come before dependents\n+    5. Preserves original order for middleware without dependency constraints\n+    6. Detects and raises errors for circular dependencies\n+\n+    Args:\n+        middleware: Original sequence of middleware instances.\n+\n+    Returns:\n+        Ordered list of middleware with dependencies resolved and auto-instantiated.\n+\n+    Raises:\n+        ValueError: If circular dependencies are detected.\n+\n+    Example:\n+        ```python\n+        # MyMiddleware depends on ModelRetryMiddleware class\n+        middleware = [MyMiddleware(depends_on=(ModelRetryMiddleware,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [ModelRetryMiddleware(), MyMiddleware(...)]\n+\n+        # Or with an instance\n+        retry = ModelRetryMiddleware(max_retries=5)\n+        middleware = [MyMiddleware(depends_on=(retry,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [retry, MyMiddleware(...)]\n+        ```\n+    \"\"\"\n+    # If no middleware has dependencies, return as-is\n+    has_dependencies = any(getattr(m, \"depends_on\", ()) for m in middleware)\n+    if not has_dependencies:\n+        return list(middleware)\n+\n+    # Build a map: class -> instance for all middleware\n+    # For original middleware, use the first instance of each class\n+    instance_by_class: dict[type[AgentMiddleware[Any, Any]], AgentMiddleware[Any, Any]] = {}\n+    original_classes: set[type[AgentMiddleware[Any, Any]]] = set()\n+\n+    for m in middleware:\n+        cls = type(m)\n+        original_classes.add(cls)\n+        # Use the first instance of each class\n+        if cls not in instance_by_class:\n+            instance_by_class[cls] = m\n+\n+    # First pass: collect all instance dependencies (these take priority)\n+    def collect_instance_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect instance dependencies first (they take priority).\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if not isinstance(dep, type):\n+                # It's an instance\n+                dep_class = type(dep)\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Store the instance (overwrite if already present from original)\n+                instance_by_class[dep_class] = dep\n+\n+                # Recursively collect instance dependencies\n+                collect_instance_dependencies(dep, visited | {dep_class})\n+\n+    # Collect instance dependencies for all original middleware\n+    for m in middleware:\n+        collect_instance_dependencies(m, {type(m)})\n+\n+    # Second pass: collect class dependencies and auto-instantiate\n+    def collect_class_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect class dependencies and auto-instantiate if needed.\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if isinstance(dep, type):\n+                # It's a class\n+                dep_class = dep\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Instantiate if not already present\n+                if dep_class not in instance_by_class:\n+                    try:\n+                        instance_by_class[dep_class] = dep_class()\n+                    except Exception as e:\n+                        # Log and skip if instantiation fails\n+                        import logging\n+\n+                        logging.getLogger(__name__).debug(\n+                            \"Failed to auto-instantiate middleware %s: %s\", dep_class.__name__, e\n+                        )\n+                        continue\n+\n+                # Recursively collect dependencies\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})\n+            else:\n+                # It's an instance - already handled in first pass\n+                dep_class = type(dep)\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})",
        "comment_created_at": "2025-12-28T19:08:06+00:00",
        "comment_author": "diffray-bot",
        "comment_body": "\ud83d\udd34 **CRITICAL** - KeyError on accessing uninstantiated middleware dependency\n**Agent:** [bugs](https://diffray.ai/agents/bugs)\n\n**Category:** bug\n\n**Description:**\nWhen middleware auto-instantiation fails (line 639), the exception is caught and code executes 'continue' at line 647. However, line 650 unconditionally accesses instance_by_class[dep_class], causing KeyError if instantiation failed since the class was never added to the map.\n\n**Suggestion:**\nAfter the except block continues, the code at line 650 must check if dep_class exists in instance_by_class before accessing it, or raise a clear error explaining instantiation failed.\n\n**Confidence:** 95%\n**Rule:** `bug_empty_catch`\n<sub>Review ID: `c9898485-5a76-4c92-ac22-7c83e35a50c8`</sub>\n<sub>Rate it \ud83d\udc4d or \ud83d\udc4e to improve future reviews | Powered by <a href=\"https://diffray.ai?utm_source=github-private-issue\">diffray</a></sub>",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2649875555",
    "pr_number": 34514,
    "pr_file": "libs/langchain_v1/langchain/agents/factory.py",
    "created_at": "2025-12-28T19:08:06+00:00",
    "commented_code": "return result\n \n \n+def _resolve_middleware_dependencies(\n+    middleware: Sequence[AgentMiddleware[StateT_co, ContextT]],\n+) -> list[AgentMiddleware[StateT_co, ContextT]]:\n+    \"\"\"Resolve middleware dependencies and return ordered list with auto-instantiated deps.\n+\n+    This function:\n+    1. Collects all dependencies from middleware instances\n+    2. Auto-instantiates missing class dependencies with default parameters\n+    3. Uses provided instance dependencies as-is\n+    4. Performs topological sort to ensure dependencies come before dependents\n+    5. Preserves original order for middleware without dependency constraints\n+    6. Detects and raises errors for circular dependencies\n+\n+    Args:\n+        middleware: Original sequence of middleware instances.\n+\n+    Returns:\n+        Ordered list of middleware with dependencies resolved and auto-instantiated.\n+\n+    Raises:\n+        ValueError: If circular dependencies are detected.\n+\n+    Example:\n+        ```python\n+        # MyMiddleware depends on ModelRetryMiddleware class\n+        middleware = [MyMiddleware(depends_on=(ModelRetryMiddleware,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [ModelRetryMiddleware(), MyMiddleware(...)]\n+\n+        # Or with an instance\n+        retry = ModelRetryMiddleware(max_retries=5)\n+        middleware = [MyMiddleware(depends_on=(retry,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [retry, MyMiddleware(...)]\n+        ```\n+    \"\"\"\n+    # If no middleware has dependencies, return as-is\n+    has_dependencies = any(getattr(m, \"depends_on\", ()) for m in middleware)\n+    if not has_dependencies:\n+        return list(middleware)\n+\n+    # Build a map: class -> instance for all middleware\n+    # For original middleware, use the first instance of each class\n+    instance_by_class: dict[type[AgentMiddleware[Any, Any]], AgentMiddleware[Any, Any]] = {}\n+    original_classes: set[type[AgentMiddleware[Any, Any]]] = set()\n+\n+    for m in middleware:\n+        cls = type(m)\n+        original_classes.add(cls)\n+        # Use the first instance of each class\n+        if cls not in instance_by_class:\n+            instance_by_class[cls] = m\n+\n+    # First pass: collect all instance dependencies (these take priority)\n+    def collect_instance_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect instance dependencies first (they take priority).\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if not isinstance(dep, type):\n+                # It's an instance\n+                dep_class = type(dep)\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Store the instance (overwrite if already present from original)\n+                instance_by_class[dep_class] = dep\n+\n+                # Recursively collect instance dependencies\n+                collect_instance_dependencies(dep, visited | {dep_class})\n+\n+    # Collect instance dependencies for all original middleware\n+    for m in middleware:\n+        collect_instance_dependencies(m, {type(m)})\n+\n+    # Second pass: collect class dependencies and auto-instantiate\n+    def collect_class_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect class dependencies and auto-instantiate if needed.\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if isinstance(dep, type):\n+                # It's a class\n+                dep_class = dep\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Instantiate if not already present\n+                if dep_class not in instance_by_class:\n+                    try:\n+                        instance_by_class[dep_class] = dep_class()\n+                    except Exception as e:\n+                        # Log and skip if instantiation fails\n+                        import logging\n+\n+                        logging.getLogger(__name__).debug(\n+                            \"Failed to auto-instantiate middleware %s: %s\", dep_class.__name__, e\n+                        )\n+                        continue",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2649875555",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34514,
        "pr_file": "libs/langchain_v1/langchain/agents/factory.py",
        "discussion_id": "2649875555",
        "commented_code": "@@ -538,6 +538,178 @@ async def call_inner(req: ToolCallRequest) -> ToolMessage | Command:\n     return result\n \n \n+def _resolve_middleware_dependencies(\n+    middleware: Sequence[AgentMiddleware[StateT_co, ContextT]],\n+) -> list[AgentMiddleware[StateT_co, ContextT]]:\n+    \"\"\"Resolve middleware dependencies and return ordered list with auto-instantiated deps.\n+\n+    This function:\n+    1. Collects all dependencies from middleware instances\n+    2. Auto-instantiates missing class dependencies with default parameters\n+    3. Uses provided instance dependencies as-is\n+    4. Performs topological sort to ensure dependencies come before dependents\n+    5. Preserves original order for middleware without dependency constraints\n+    6. Detects and raises errors for circular dependencies\n+\n+    Args:\n+        middleware: Original sequence of middleware instances.\n+\n+    Returns:\n+        Ordered list of middleware with dependencies resolved and auto-instantiated.\n+\n+    Raises:\n+        ValueError: If circular dependencies are detected.\n+\n+    Example:\n+        ```python\n+        # MyMiddleware depends on ModelRetryMiddleware class\n+        middleware = [MyMiddleware(depends_on=(ModelRetryMiddleware,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [ModelRetryMiddleware(), MyMiddleware(...)]\n+\n+        # Or with an instance\n+        retry = ModelRetryMiddleware(max_retries=5)\n+        middleware = [MyMiddleware(depends_on=(retry,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [retry, MyMiddleware(...)]\n+        ```\n+    \"\"\"\n+    # If no middleware has dependencies, return as-is\n+    has_dependencies = any(getattr(m, \"depends_on\", ()) for m in middleware)\n+    if not has_dependencies:\n+        return list(middleware)\n+\n+    # Build a map: class -> instance for all middleware\n+    # For original middleware, use the first instance of each class\n+    instance_by_class: dict[type[AgentMiddleware[Any, Any]], AgentMiddleware[Any, Any]] = {}\n+    original_classes: set[type[AgentMiddleware[Any, Any]]] = set()\n+\n+    for m in middleware:\n+        cls = type(m)\n+        original_classes.add(cls)\n+        # Use the first instance of each class\n+        if cls not in instance_by_class:\n+            instance_by_class[cls] = m\n+\n+    # First pass: collect all instance dependencies (these take priority)\n+    def collect_instance_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect instance dependencies first (they take priority).\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if not isinstance(dep, type):\n+                # It's an instance\n+                dep_class = type(dep)\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Store the instance (overwrite if already present from original)\n+                instance_by_class[dep_class] = dep\n+\n+                # Recursively collect instance dependencies\n+                collect_instance_dependencies(dep, visited | {dep_class})\n+\n+    # Collect instance dependencies for all original middleware\n+    for m in middleware:\n+        collect_instance_dependencies(m, {type(m)})\n+\n+    # Second pass: collect class dependencies and auto-instantiate\n+    def collect_class_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect class dependencies and auto-instantiate if needed.\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if isinstance(dep, type):\n+                # It's a class\n+                dep_class = dep\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Instantiate if not already present\n+                if dep_class not in instance_by_class:\n+                    try:\n+                        instance_by_class[dep_class] = dep_class()\n+                    except Exception as e:\n+                        # Log and skip if instantiation fails\n+                        import logging\n+\n+                        logging.getLogger(__name__).debug(\n+                            \"Failed to auto-instantiate middleware %s: %s\", dep_class.__name__, e\n+                        )\n+                        continue",
        "comment_created_at": "2025-12-28T19:08:06+00:00",
        "comment_author": "diffray-bot",
        "comment_body": "\ud83d\udfe0 **HIGH** - Overly broad exception handling masks instantiation errors\n**Agent:** python\n\n**Category:** bug\n\n**Description:**\nThe except clause at line 640 catches 'Exception' broadly, masking serious errors like TypeError or AttributeError. Error is logged at DEBUG level (line 644), making it invisible in production. Silent continuation delays actual error until KeyError at line 650.\n\n**Suggestion:**\nCatch specific exception types expected during instantiation (e.g., 'except TypeError'). Change log level to 'warning' or 'error' for production visibility. Consider failing fast with a clear message.\n\n**Confidence:** 82%\n**Rule:** `py_add_specific_exception_handling`\n<sub>Review ID: `c9898485-5a76-4c92-ac22-7c83e35a50c8`</sub>\n<sub>Rate it \ud83d\udc4d or \ud83d\udc4e to improve future reviews | Powered by <a href=\"https://diffray.ai?utm_source=github-private-issue\">diffray</a></sub>",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2649875561",
    "pr_number": 34514,
    "pr_file": "libs/langchain_v1/langchain/agents/middleware/types.py",
    "created_at": "2025-12-28T19:08:06+00:00",
    "commented_code": "tools: Sequence[BaseTool]\n     \"\"\"Additional tools registered by the middleware.\"\"\"\n \n+    depends_on: Sequence[type[AgentMiddleware] | AgentMiddleware] = ()\n+    \"\"\"Middleware classes or instances that must be executed before this middleware.\n+\n+    Can be either:\n+    - Middleware classes (will be auto-instantiated with default parameters)\n+    - Middleware instances (will be used as-is)\n+\n+    Dependencies will be automatically added to the middleware list if not already\n+    present, and will be ordered to execute before this middleware.\n+    \"\"\"\n+",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2649875561",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34514,
        "pr_file": "libs/langchain_v1/langchain/agents/middleware/types.py",
        "discussion_id": "2649875561",
        "commented_code": "@@ -341,6 +341,17 @@ class AgentMiddleware(Generic[StateT, ContextT]):\n     tools: Sequence[BaseTool]\n     \"\"\"Additional tools registered by the middleware.\"\"\"\n \n+    depends_on: Sequence[type[AgentMiddleware] | AgentMiddleware] = ()\n+    \"\"\"Middleware classes or instances that must be executed before this middleware.\n+\n+    Can be either:\n+    - Middleware classes (will be auto-instantiated with default parameters)\n+    - Middleware instances (will be used as-is)\n+\n+    Dependencies will be automatically added to the middleware list if not already\n+    present, and will be ordered to execute before this middleware.\n+    \"\"\"\n+",
        "comment_created_at": "2025-12-28T19:08:06+00:00",
        "comment_author": "diffray-bot",
        "comment_body": "\ud83d\udfe1 **MEDIUM** - depends_on attribute lacks input validation\n**Agent:** [architecture](https://diffray.ai/agents/architecture)\n\n**Category:** architecture\n\n**Description:**\nThe AgentMiddleware.depends_on attribute accepts Sequence[type[AgentMiddleware] | AgentMiddleware] with no validation. Errors only surface during dependency resolution in factory.py, far from the source, violating fail-fast principle.\n\n**Suggestion:**\nAdd validation in AgentMiddleware.__init__() to ensure depends_on contains only valid AgentMiddleware subclasses or instances. Raise ValueError with context if validation fails.\n\n**Confidence:** 62%\n**Rule:** `arch_srp_violation`\n<sub>Review ID: `c9898485-5a76-4c92-ac22-7c83e35a50c8`</sub>\n<sub>Rate it \ud83d\udc4d or \ud83d\udc4e to improve future reviews | Powered by <a href=\"https://diffray.ai?utm_source=github-private-issue\">diffray</a></sub>",
        "pr_file_module": null
      }
    ]
  }
]