[
  {
    "discussion_id": "3070033513",
    "pr_number": 36401,
    "pr_file": "libs/partners/openrouter/langchain_openrouter/chat_models.py",
    "created_at": "2026-04-12T19:08:05+00:00",
    "commented_code": "return content\n \n \n+def _merge_reasoning_details(\n+    details: list[dict[str, Any]],\n+) -> list[dict[str, Any]]:\n+    \"\"\"Merge fragmented ``reasoning_details`` from streaming chunk concatenation.\n+\n+    During streaming, ``AIMessageChunk.__add__`` list-concatenates\n+    ``reasoning_details`` in ``additional_kwargs``, fragmenting a single\n+    entry into many.  When serialized back to the API via\n+    ``_convert_message_to_dict``, these fragments cause\n+    ``BadRequestResponseError`` on multi-turn conversations.\n+\n+    This function merges consecutive entries that share the same ``type``\n+    **and** ``index`` (both must be present).  Entries without ``index``\n+    or with distinct ``index`` values are preserved as separate items,\n+    since non-streaming responses can legitimately contain multiple entries.\n+\n+    Metadata fields (for example ``format`` and ``signature``) use last-wins\n+    semantics, which preserves stable provider metadata without concatenating\n+    repeated strings across fragments.\n+\n+    A single-entry list passes through unchanged.\n+    \"\"\"\n+    if not isinstance(details, list) or len(details) <= 1:\n+        return details\n+\n+    merged: list[dict[str, Any]] = []\n+    i = 0\n+    while i < len(details):\n+        entry = details[i]\n+        if not isinstance(entry, dict):\n+            merged.append(entry)\n+            i += 1\n+            continue\n+\n+        entry_type = entry.get(\"type\", \"\")\n+        entry_index = entry.get(\"index\")\n+        text_key = (\n+            \"text\" if \"text\" in entry else \"content\" if \"content\" in entry else None\n+        )\n+\n+        # Only merge if entry has both a text field and an index.\n+        # Without index we cannot distinguish fragments from distinct entries.\n+        if text_key is None or entry_index is None:\n+            merged.append(entry)\n+            i += 1\n+            continue\n+\n+        # Merge consecutive fragments (same type + same index)\n+        texts = [entry.get(text_key, \"\")]\n+        base = {k: v for k, v in entry.items() if k != text_key}\n+        i += 1\n+        while i < len(details):\n+            nxt = details[i]\n+            if (\n+                isinstance(nxt, dict)\n+                and nxt.get(\"type\") == entry_type\n+                and nxt.get(\"index\") == entry_index\n+            ):\n+                texts.append(nxt.get(text_key, \"\") or \"\")\n+                # Preserve metadata from later fragments (e.g. signature)\n+                base.update(\n+                    {k: v for k, v in nxt.items() if k != text_key and v is not None}\n+                )\n+                i += 1",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "3070033513",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 36401,
        "pr_file": "libs/partners/openrouter/langchain_openrouter/chat_models.py",
        "discussion_id": "3070033513",
        "commented_code": "@@ -1124,6 +1124,79 @@ def _format_message_content(content: Any) -> Any:\n     return content\n \n \n+def _merge_reasoning_details(\n+    details: list[dict[str, Any]],\n+) -> list[dict[str, Any]]:\n+    \"\"\"Merge fragmented ``reasoning_details`` from streaming chunk concatenation.\n+\n+    During streaming, ``AIMessageChunk.__add__`` list-concatenates\n+    ``reasoning_details`` in ``additional_kwargs``, fragmenting a single\n+    entry into many.  When serialized back to the API via\n+    ``_convert_message_to_dict``, these fragments cause\n+    ``BadRequestResponseError`` on multi-turn conversations.\n+\n+    This function merges consecutive entries that share the same ``type``\n+    **and** ``index`` (both must be present).  Entries without ``index``\n+    or with distinct ``index`` values are preserved as separate items,\n+    since non-streaming responses can legitimately contain multiple entries.\n+\n+    Metadata fields (for example ``format`` and ``signature``) use last-wins\n+    semantics, which preserves stable provider metadata without concatenating\n+    repeated strings across fragments.\n+\n+    A single-entry list passes through unchanged.\n+    \"\"\"\n+    if not isinstance(details, list) or len(details) <= 1:\n+        return details\n+\n+    merged: list[dict[str, Any]] = []\n+    i = 0\n+    while i < len(details):\n+        entry = details[i]\n+        if not isinstance(entry, dict):\n+            merged.append(entry)\n+            i += 1\n+            continue\n+\n+        entry_type = entry.get(\"type\", \"\")\n+        entry_index = entry.get(\"index\")\n+        text_key = (\n+            \"text\" if \"text\" in entry else \"content\" if \"content\" in entry else None\n+        )\n+\n+        # Only merge if entry has both a text field and an index.\n+        # Without index we cannot distinguish fragments from distinct entries.\n+        if text_key is None or entry_index is None:\n+            merged.append(entry)\n+            i += 1\n+            continue\n+\n+        # Merge consecutive fragments (same type + same index)\n+        texts = [entry.get(text_key, \"\")]\n+        base = {k: v for k, v in entry.items() if k != text_key}\n+        i += 1\n+        while i < len(details):\n+            nxt = details[i]\n+            if (\n+                isinstance(nxt, dict)\n+                and nxt.get(\"type\") == entry_type\n+                and nxt.get(\"index\") == entry_index\n+            ):\n+                texts.append(nxt.get(text_key, \"\") or \"\")\n+                # Preserve metadata from later fragments (e.g. signature)\n+                base.update(\n+                    {k: v for k, v in nxt.items() if k != text_key and v is not None}\n+                )\n+                i += 1",
        "comment_created_at": "2026-04-12T19:08:05+00:00",
        "comment_author": "torontodeveloper",
        "comment_body": "can this be decomposed into another function or so otherwise with 2 while loops, i already lost focus to understand what exactly is it doing. ur cal?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2649875562",
    "pr_number": 34514,
    "pr_file": "libs/langchain_v1/langchain/agents/factory.py",
    "created_at": "2025-12-28T19:08:06+00:00",
    "commented_code": "return result\n \n \n+def _resolve_middleware_dependencies(\n+    middleware: Sequence[AgentMiddleware[StateT_co, ContextT]],\n+) -> list[AgentMiddleware[StateT_co, ContextT]]:\n+    \"\"\"Resolve middleware dependencies and return ordered list with auto-instantiated deps.\n+\n+    This function:\n+    1. Collects all dependencies from middleware instances\n+    2. Auto-instantiates missing class dependencies with default parameters\n+    3. Uses provided instance dependencies as-is\n+    4. Performs topological sort to ensure dependencies come before dependents\n+    5. Preserves original order for middleware without dependency constraints\n+    6. Detects and raises errors for circular dependencies\n+\n+    Args:\n+        middleware: Original sequence of middleware instances.\n+\n+    Returns:\n+        Ordered list of middleware with dependencies resolved and auto-instantiated.\n+\n+    Raises:\n+        ValueError: If circular dependencies are detected.\n+\n+    Example:\n+        ```python\n+        # MyMiddleware depends on ModelRetryMiddleware class\n+        middleware = [MyMiddleware(depends_on=(ModelRetryMiddleware,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [ModelRetryMiddleware(), MyMiddleware(...)]\n+\n+        # Or with an instance\n+        retry = ModelRetryMiddleware(max_retries=5)\n+        middleware = [MyMiddleware(depends_on=(retry,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [retry, MyMiddleware(...)]\n+        ```\n+    \"\"\"\n+    # If no middleware has dependencies, return as-is\n+    has_dependencies = any(getattr(m, \"depends_on\", ()) for m in middleware)\n+    if not has_dependencies:\n+        return list(middleware)\n+\n+    # Build a map: class -> instance for all middleware\n+    # For original middleware, use the first instance of each class\n+    instance_by_class: dict[type[AgentMiddleware[Any, Any]], AgentMiddleware[Any, Any]] = {}\n+    original_classes: set[type[AgentMiddleware[Any, Any]]] = set()\n+\n+    for m in middleware:\n+        cls = type(m)\n+        original_classes.add(cls)\n+        # Use the first instance of each class\n+        if cls not in instance_by_class:\n+            instance_by_class[cls] = m\n+\n+    # First pass: collect all instance dependencies (these take priority)\n+    def collect_instance_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect instance dependencies first (they take priority).\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if not isinstance(dep, type):\n+                # It's an instance\n+                dep_class = type(dep)\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Store the instance (overwrite if already present from original)\n+                instance_by_class[dep_class] = dep\n+\n+                # Recursively collect instance dependencies\n+                collect_instance_dependencies(dep, visited | {dep_class})\n+\n+    # Collect instance dependencies for all original middleware\n+    for m in middleware:\n+        collect_instance_dependencies(m, {type(m)})\n+\n+    # Second pass: collect class dependencies and auto-instantiate\n+    def collect_class_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect class dependencies and auto-instantiate if needed.\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if isinstance(dep, type):\n+                # It's a class\n+                dep_class = dep\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Instantiate if not already present\n+                if dep_class not in instance_by_class:\n+                    try:\n+                        instance_by_class[dep_class] = dep_class()\n+                    except Exception as e:\n+                        # Log and skip if instantiation fails\n+                        import logging\n+\n+                        logging.getLogger(__name__).debug(\n+                            \"Failed to auto-instantiate middleware %s: %s\", dep_class.__name__, e\n+                        )\n+                        continue\n+\n+                # Recursively collect dependencies\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})\n+            else:\n+                # It's an instance - already handled in first pass\n+                dep_class = type(dep)\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})\n+\n+    # Collect class dependencies for all original middleware\n+    for m in middleware:\n+        collect_class_dependencies(m, {type(m)})\n+\n+    # Topological sort using Kahn's algorithm\n+    # Build dependency graph: class -> set of classes it depends on\n+    dep_graph: dict[type[AgentMiddleware[Any, Any]], set[type[AgentMiddleware[Any, Any]]]] = {}\n+    for cls, instance in instance_by_class.items():\n+        deps = getattr(instance, \"depends_on\", ())\n+        dep_classes = set()\n+        for dep in deps:\n+            dep_class = type(dep) if not isinstance(dep, type) else dep\n+            dep_classes.add(dep_class)\n+        dep_graph[cls] = dep_classes\n+    # Calculate in-degree for each class\n+    in_degree: dict[type[AgentMiddleware[Any, Any]], int] = {\n+        cls: len(dep_graph.get(cls, set())) for cls in instance_by_class\n+    }\n+\n+    # Find all nodes with in-degree 0\n+    queue: list[type[AgentMiddleware[Any, Any]]] = [\n+        cls for cls in instance_by_class if in_degree[cls] == 0\n+    ]\n+    # Sort queue for stability\n+    queue.sort(key=lambda c: c.__name__)\n+\n+    sorted_classes: list[type[AgentMiddleware[Any, Any]]] = []\n+\n+    while queue:\n+        # Remove a node with in-degree 0\n+        current = queue.pop(0)\n+        sorted_classes.append(current)\n+\n+        # For each node that depends on current, decrease its in-degree\n+        for cls, deps in dep_graph.items():\n+            if current in deps:\n+                in_degree[cls] -= 1\n+                if in_degree[cls] == 0:\n+                    queue.append(cls)\n+                    # Keep queue sorted for stability\n+                    queue.sort(key=lambda c: c.__name__)\n+\n+    # Check for circular dependencies\n+    if len(sorted_classes) != len(instance_by_class):\n+        msg = \"Circular dependency detected in middleware\"\n+        raise ValueError(msg)\n+\n+    # Build result list\n+    result: list[AgentMiddleware[StateT_co, ContextT]] = [\n+        instance_by_class[cls] for cls in sorted_classes\n+    ]\n+\n+    return result\n+\n+\n def create_agent(",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2649875562",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34514,
        "pr_file": "libs/langchain_v1/langchain/agents/factory.py",
        "discussion_id": "2649875562",
        "commented_code": "@@ -538,6 +538,178 @@ async def call_inner(req: ToolCallRequest) -> ToolMessage | Command:\n     return result\n \n \n+def _resolve_middleware_dependencies(\n+    middleware: Sequence[AgentMiddleware[StateT_co, ContextT]],\n+) -> list[AgentMiddleware[StateT_co, ContextT]]:\n+    \"\"\"Resolve middleware dependencies and return ordered list with auto-instantiated deps.\n+\n+    This function:\n+    1. Collects all dependencies from middleware instances\n+    2. Auto-instantiates missing class dependencies with default parameters\n+    3. Uses provided instance dependencies as-is\n+    4. Performs topological sort to ensure dependencies come before dependents\n+    5. Preserves original order for middleware without dependency constraints\n+    6. Detects and raises errors for circular dependencies\n+\n+    Args:\n+        middleware: Original sequence of middleware instances.\n+\n+    Returns:\n+        Ordered list of middleware with dependencies resolved and auto-instantiated.\n+\n+    Raises:\n+        ValueError: If circular dependencies are detected.\n+\n+    Example:\n+        ```python\n+        # MyMiddleware depends on ModelRetryMiddleware class\n+        middleware = [MyMiddleware(depends_on=(ModelRetryMiddleware,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [ModelRetryMiddleware(), MyMiddleware(...)]\n+\n+        # Or with an instance\n+        retry = ModelRetryMiddleware(max_retries=5)\n+        middleware = [MyMiddleware(depends_on=(retry,))]\n+        resolved = _resolve_middleware_dependencies(middleware)\n+        # Result: [retry, MyMiddleware(...)]\n+        ```\n+    \"\"\"\n+    # If no middleware has dependencies, return as-is\n+    has_dependencies = any(getattr(m, \"depends_on\", ()) for m in middleware)\n+    if not has_dependencies:\n+        return list(middleware)\n+\n+    # Build a map: class -> instance for all middleware\n+    # For original middleware, use the first instance of each class\n+    instance_by_class: dict[type[AgentMiddleware[Any, Any]], AgentMiddleware[Any, Any]] = {}\n+    original_classes: set[type[AgentMiddleware[Any, Any]]] = set()\n+\n+    for m in middleware:\n+        cls = type(m)\n+        original_classes.add(cls)\n+        # Use the first instance of each class\n+        if cls not in instance_by_class:\n+            instance_by_class[cls] = m\n+\n+    # First pass: collect all instance dependencies (these take priority)\n+    def collect_instance_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect instance dependencies first (they take priority).\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if not isinstance(dep, type):\n+                # It's an instance\n+                dep_class = type(dep)\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Store the instance (overwrite if already present from original)\n+                instance_by_class[dep_class] = dep\n+\n+                # Recursively collect instance dependencies\n+                collect_instance_dependencies(dep, visited | {dep_class})\n+\n+    # Collect instance dependencies for all original middleware\n+    for m in middleware:\n+        collect_instance_dependencies(m, {type(m)})\n+\n+    # Second pass: collect class dependencies and auto-instantiate\n+    def collect_class_dependencies(\n+        instance: AgentMiddleware[Any, Any], visited: set[type[AgentMiddleware[Any, Any]]]\n+    ) -> None:\n+        \"\"\"Collect class dependencies and auto-instantiate if needed.\"\"\"\n+        deps = getattr(instance, \"depends_on\", ())\n+\n+        for dep in deps:\n+            if isinstance(dep, type):\n+                # It's a class\n+                dep_class = dep\n+                # Check for circular dependencies\n+                if dep_class in visited:\n+                    msg = f\"Circular dependency detected in middleware: {dep_class.__name__}\"\n+                    raise ValueError(msg)\n+\n+                # Instantiate if not already present\n+                if dep_class not in instance_by_class:\n+                    try:\n+                        instance_by_class[dep_class] = dep_class()\n+                    except Exception as e:\n+                        # Log and skip if instantiation fails\n+                        import logging\n+\n+                        logging.getLogger(__name__).debug(\n+                            \"Failed to auto-instantiate middleware %s: %s\", dep_class.__name__, e\n+                        )\n+                        continue\n+\n+                # Recursively collect dependencies\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})\n+            else:\n+                # It's an instance - already handled in first pass\n+                dep_class = type(dep)\n+                dep_instance = instance_by_class[dep_class]\n+                collect_class_dependencies(dep_instance, visited | {dep_class})\n+\n+    # Collect class dependencies for all original middleware\n+    for m in middleware:\n+        collect_class_dependencies(m, {type(m)})\n+\n+    # Topological sort using Kahn's algorithm\n+    # Build dependency graph: class -> set of classes it depends on\n+    dep_graph: dict[type[AgentMiddleware[Any, Any]], set[type[AgentMiddleware[Any, Any]]]] = {}\n+    for cls, instance in instance_by_class.items():\n+        deps = getattr(instance, \"depends_on\", ())\n+        dep_classes = set()\n+        for dep in deps:\n+            dep_class = type(dep) if not isinstance(dep, type) else dep\n+            dep_classes.add(dep_class)\n+        dep_graph[cls] = dep_classes\n+    # Calculate in-degree for each class\n+    in_degree: dict[type[AgentMiddleware[Any, Any]], int] = {\n+        cls: len(dep_graph.get(cls, set())) for cls in instance_by_class\n+    }\n+\n+    # Find all nodes with in-degree 0\n+    queue: list[type[AgentMiddleware[Any, Any]]] = [\n+        cls for cls in instance_by_class if in_degree[cls] == 0\n+    ]\n+    # Sort queue for stability\n+    queue.sort(key=lambda c: c.__name__)\n+\n+    sorted_classes: list[type[AgentMiddleware[Any, Any]]] = []\n+\n+    while queue:\n+        # Remove a node with in-degree 0\n+        current = queue.pop(0)\n+        sorted_classes.append(current)\n+\n+        # For each node that depends on current, decrease its in-degree\n+        for cls, deps in dep_graph.items():\n+            if current in deps:\n+                in_degree[cls] -= 1\n+                if in_degree[cls] == 0:\n+                    queue.append(cls)\n+                    # Keep queue sorted for stability\n+                    queue.sort(key=lambda c: c.__name__)\n+\n+    # Check for circular dependencies\n+    if len(sorted_classes) != len(instance_by_class):\n+        msg = \"Circular dependency detected in middleware\"\n+        raise ValueError(msg)\n+\n+    # Build result list\n+    result: list[AgentMiddleware[StateT_co, ContextT]] = [\n+        instance_by_class[cls] for cls in sorted_classes\n+    ]\n+\n+    return result\n+\n+\n def create_agent(",
        "comment_created_at": "2025-12-28T19:08:06+00:00",
        "comment_author": "diffray-bot",
        "comment_body": "\ud83d\udfe0 **HIGH** - Excessively long function with high cyclomatic complexity\n**Agent:** refactoring\n\n**Category:** quality\n\n**Description:**\nThe create_agent function spans 945 lines (from line 713 to 1657) with deeply nested conditionals for middleware hooks, tools, response formats, and graph edges. This makes testing and maintenance difficult.\n\n**Suggestion:**\nExtract middleware collection, tool setup, edge configuration, and graph building into separate helper functions. Consider creating a GraphBuilder class to encapsulate graph construction logic.\n\n**Why this matters:** Long chains couple your code to object internal structure, making refactoring difficult.\n\n**Confidence:** 85%\n**Rule:** `quality_law_of_demeter`\n<sub>Review ID: `c9898485-5a76-4c92-ac22-7c83e35a50c8`</sub>\n<sub>Rate it \ud83d\udc4d or \ud83d\udc4e to improve future reviews | Powered by <a href=\"https://diffray.ai?utm_source=github-private-issue\">diffray</a></sub>",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2649875563",
    "pr_number": 34514,
    "pr_file": "libs/langchain_v1/langchain/agents/middleware/tool_emulator.py",
    "created_at": "2025-12-28T19:08:06+00:00",
    "commented_code": "# Assume BaseTool with .name attribute\n                     self.tools_to_emulate.add(tool.name)\n \n+        self.depends_on = depends_on\n+\n         # Initialize emulator model\n         if model is None:\n             self.model = init_chat_model(\"anthropic:claude-sonnet-4-5-20250929\", temperature=1)",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2649875563",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34514,
        "pr_file": "libs/langchain_v1/langchain/agents/middleware/tool_emulator.py",
        "discussion_id": "2649875563",
        "commented_code": "@@ -98,6 +101,8 @@ def __init__(\n                     # Assume BaseTool with .name attribute\n                     self.tools_to_emulate.add(tool.name)\n \n+        self.depends_on = depends_on\n+\n         # Initialize emulator model\n         if model is None:\n             self.model = init_chat_model(\"anthropic:claude-sonnet-4-5-20250929\", temperature=1)",
        "comment_created_at": "2025-12-28T19:08:06+00:00",
        "comment_author": "diffray-bot",
        "comment_body": "\ud83d\udd35 **LOW** - Duplicated emulation logic in sync and async methods\n**Agent:** refactoring\n\n**Category:** quality\n\n**Description:**\nThe wrap_tool_call (lines 114-162) and awrap_tool_call (lines 164-214) methods contain nearly identical code for checking if a tool should be emulated and building the emulation prompt.\n\n**Suggestion:**\nExtract the tool emulation decision and prompt building into helper methods like `_should_emulate(tool_name)` and `_build_emulation_prompt(tool_name, tool_args, tool_description)` that both sync and async implementations can use.\n\n**Why this matters:** Makes code harder to change safely.\n\n**Confidence:** 70%\n**Rule:** `quality_commented_code_blocks`\n<sub>Review ID: `c9898485-5a76-4c92-ac22-7c83e35a50c8`</sub>\n<sub>Rate it \ud83d\udc4d or \ud83d\udc4e to improve future reviews | Powered by <a href=\"https://diffray.ai?utm_source=github-private-issue\">diffray</a></sub>",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2649875564",
    "pr_number": 34514,
    "pr_file": "libs/langchain_v1/langchain/agents/middleware/file_search.py",
    "created_at": "2025-12-28T19:08:06+00:00",
    "commented_code": "root_path: str,",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2649875564",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34514,
        "pr_file": "libs/langchain_v1/langchain/agents/middleware/file_search.py",
        "discussion_id": "2649875564",
        "commented_code": "@@ -115,6 +115,7 @@ def __init__(\n         root_path: str,",
        "comment_created_at": "2025-12-28T19:08:06+00:00",
        "comment_author": "diffray-bot",
        "comment_body": "\ud83d\udfe1 **MEDIUM** - Long __init__ with complete tool implementations inside\n**Agent:** refactoring\n\n**Category:** quality\n\n**Description:**\nThe __init__ method of FilesystemFileSearchMiddleware contains 127 lines with two complete @tool function definitions (glob_search and grep_search) implemented as closures inside the constructor.\n\n**Suggestion:**\nExtract tool definitions into private methods (e.g., _create_glob_tool(), _create_grep_tool()) called from __init__, or define them as standalone functions/methods that accept the middleware instance.\n\n**Why this matters:** Long chains couple your code to object internal structure, making refactoring difficult.\n\n**Confidence:** 68%\n**Rule:** `quality_law_of_demeter`\n<sub>Review ID: `c9898485-5a76-4c92-ac22-7c83e35a50c8`</sub>\n<sub>Rate it \ud83d\udc4d or \ud83d\udc4e to improve future reviews | Powered by <a href=\"https://diffray.ai?utm_source=github-private-issue\">diffray</a></sub>",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3069854135",
    "pr_number": 35680,
    "pr_file": "libs/core/tests/unit_tests/utils/test_function_calling.py",
    "created_at": "2026-04-12T17:31:16+00:00",
    "commented_code": "}\n     with pytest.raises(ValueError, match=\"my_field\"):\n         convert_to_openai_function(schema_without_title)\n+\n+\n+class TestParseGoogleDocstring:\n+    \"\"\"Tests for _parse_google_docstring continuation-line handling.\"\"\"\n+\n+    def test_continuation_line_with_colon(self) -> None:\n+        \"\"\"Continuation lines containing colons should not be treated as new args.\"\"\"\n+        from langchain_core.utils.function_calling import _parse_google_docstring",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "3069854135",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 35680,
        "pr_file": "libs/core/tests/unit_tests/utils/test_function_calling.py",
        "discussion_id": "3069854135",
        "commented_code": "@@ -1242,3 +1242,74 @@ def test_convert_to_openai_function_json_schema_missing_title_includes_schema()\n     }\n     with pytest.raises(ValueError, match=\"my_field\"):\n         convert_to_openai_function(schema_without_title)\n+\n+\n+class TestParseGoogleDocstring:\n+    \"\"\"Tests for _parse_google_docstring continuation-line handling.\"\"\"\n+\n+    def test_continuation_line_with_colon(self) -> None:\n+        \"\"\"Continuation lines containing colons should not be treated as new args.\"\"\"\n+        from langchain_core.utils.function_calling import _parse_google_docstring",
        "comment_created_at": "2026-04-12T17:31:16+00:00",
        "comment_author": "torontodeveloper",
        "comment_body": "i dislike seeing imports in the middle of the file and I belive PEP8 talks against this too. But i have seen lot of python developers put imports in middle of the file\r\nhttps://peps.python.org/pep-0008/#imports\r\n\r\n<img width=\"1903\" height=\"890\" alt=\"Image\" src=\"https://github.com/user-attachments/assets/2dab4494-9482-4006-92d1-0b6a966bb0f7\" />\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2577783802",
    "pr_number": 34120,
    "pr_file": "libs/partners/anthropic/tests/integration_tests/test_chat_models.py",
    "created_at": "2025-12-01T16:28:05+00:00",
    "commented_code": "assert calculator_tool.get(\"defer_loading\") is True\n \n \n+def test_tool_search_with_langchain_tools_extras() -> None:\n+    \"\"\"Test tool search with LangChain tools using extras parameter.\"\"\"\n+    from langchain_core.tools import tool",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2577783802",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34120,
        "pr_file": "libs/partners/anthropic/tests/integration_tests/test_chat_models.py",
        "discussion_id": "2577783802",
        "commented_code": "@@ -2163,6 +2163,90 @@ def test_tool_search_with_deferred_tools() -> None:\n     assert calculator_tool.get(\"defer_loading\") is True\n \n \n+def test_tool_search_with_langchain_tools_extras() -> None:\n+    \"\"\"Test tool search with LangChain tools using extras parameter.\"\"\"\n+    from langchain_core.tools import tool",
        "comment_created_at": "2025-12-01T16:28:05+00:00",
        "comment_author": "ccurme",
        "comment_body": "why not import top level?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2621332341",
    "pr_number": 34379,
    "pr_file": "libs/partners/openai/tests/unit_tests/test_token_counter.py",
    "created_at": "2025-12-16T01:01:43+00:00",
    "commented_code": "+def test_token_counter_handles_function_call():\n+    from langchain_openai import ChatOpenAI\n+    from langchain_core.messages import HumanMessage",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2621332341",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34379,
        "pr_file": "libs/partners/openai/tests/unit_tests/test_token_counter.py",
        "discussion_id": "2621332341",
        "commented_code": "@@ -0,0 +1,22 @@\n+def test_token_counter_handles_function_call():\n+    from langchain_openai import ChatOpenAI\n+    from langchain_core.messages import HumanMessage",
        "comment_created_at": "2025-12-16T01:01:43+00:00",
        "comment_author": "mdrxy",
        "comment_body": "Should be at top-level",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2677751305",
    "pr_number": 34675,
    "pr_file": "libs/core/tests/unit_tests/prompts/test_chat.py",
    "created_at": "2026-01-09T22:06:42+00:00",
    "commented_code": "\"kwargs\": {\n                                     \"input_variables\": [],\n                                     \"template_format\": \"mustache\",\n-                                    \"template\": \"Note: This may be just one example of many possible correct ways for the student to respond.\n\",  # noqa: E501\n+                                    \"template\": (\n+                                        \"Note: This may be just one example of many \"\n+                                        \"possible correct ways for the student to \"\n+                                        \"respond.\n\"\n+                                    ),\n                                     \"additional_content_fields\": {\n-                                        \"text\": \"Note: This may be just one example of many possible correct ways for the student to respond.\n\",  # noqa: E501\n+                                        \"text\": (\n+                                            \"Note: This may be just one example of \"\n+                                            \"many possible correct ways for the \"\n+                                            \"student to respond.\n\",",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "2677751305",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34675,
        "pr_file": "libs/core/tests/unit_tests/prompts/test_chat.py",
        "discussion_id": "2677751305",
        "commented_code": "@@ -1600,9 +1606,17 @@ def test_rendering_prompt_with_conditionals_no_empty_text_blocks() -> None:\n                                 \"kwargs\": {\n                                     \"input_variables\": [],\n                                     \"template_format\": \"mustache\",\n-                                    \"template\": \"Note: This may be just one example of many possible correct ways for the student to respond.\\n\",  # noqa: E501\n+                                    \"template\": (\n+                                        \"Note: This may be just one example of many \"\n+                                        \"possible correct ways for the student to \"\n+                                        \"respond.\\n\"\n+                                    ),\n                                     \"additional_content_fields\": {\n-                                        \"text\": \"Note: This may be just one example of many possible correct ways for the student to respond.\\n\",  # noqa: E501\n+                                        \"text\": (\n+                                            \"Note: This may be just one example of \"\n+                                            \"many possible correct ways for the \"\n+                                            \"student to respond.\\n\",",
        "comment_created_at": "2026-01-09T22:06:42+00:00",
        "comment_author": "mdrxy",
        "comment_body": "this becomes a tuple i think due to the trailing comma?",
        "pr_file_module": null
      },
      {
        "comment_id": "2678016060",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 34675,
        "pr_file": "libs/core/tests/unit_tests/prompts/test_chat.py",
        "discussion_id": "2677751305",
        "commented_code": "@@ -1600,9 +1606,17 @@ def test_rendering_prompt_with_conditionals_no_empty_text_blocks() -> None:\n                                 \"kwargs\": {\n                                     \"input_variables\": [],\n                                     \"template_format\": \"mustache\",\n-                                    \"template\": \"Note: This may be just one example of many possible correct ways for the student to respond.\\n\",  # noqa: E501\n+                                    \"template\": (\n+                                        \"Note: This may be just one example of many \"\n+                                        \"possible correct ways for the student to \"\n+                                        \"respond.\\n\"\n+                                    ),\n                                     \"additional_content_fields\": {\n-                                        \"text\": \"Note: This may be just one example of many possible correct ways for the student to respond.\\n\",  # noqa: E501\n+                                        \"text\": (\n+                                            \"Note: This may be just one example of \"\n+                                            \"many possible correct ways for the \"\n+                                            \"student to respond.\\n\",",
        "comment_created_at": "2026-01-10T00:38:59+00:00",
        "comment_author": "cbornet",
        "comment_body": "Thanks for fixing",
        "pr_file_module": null
      }
    ]
  }
]