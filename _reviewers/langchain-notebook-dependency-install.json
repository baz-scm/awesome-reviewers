[
  {
    "discussion_id": "1751087472",
    "pr_number": 26234,
    "pr_file": "docs/docs/tutorials/chatbot.ipynb",
    "created_at": "2024-09-10T00:28:07+00:00",
    "commented_code": "\"metadata\": {},\n    \"outputs\": [],\n    \"source\": [\n-    \"# ! pip install langchain_community\"\n+    \"pip install langchain_community\"",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "1751087472",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 26234,
        "pr_file": "docs/docs/tutorials/chatbot.ipynb",
        "discussion_id": "1751087472",
        "commented_code": "@@ -254,14 +254,14 @@\n    \"metadata\": {},\n    \"outputs\": [],\n    \"source\": [\n-    \"# ! pip install langchain_community\"\n+    \"pip install langchain_community\"",
        "comment_created_at": "2024-09-10T00:28:07+00:00",
        "comment_author": "efriis",
        "comment_body": "```suggestion\r\n    \"%pip install langchain_community\"\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1751087778",
    "pr_number": 26234,
    "pr_file": "docs/docs/tutorials/chatbot.ipynb",
    "created_at": "2024-09-10T00:28:41+00:00",
    "commented_code": "\"metadata\": {},\n    \"outputs\": [],\n    \"source\": [\n-    \"# ! pip install langchain_community\"\n+    \"%pip install langchain_community\"\n    ]\n   },\n   {\n    \"cell_type\": \"markdown\",\n    \"metadata\": {},\n    \"source\": [\n-    \"After that, we can import the relevant classes and set up our chain which wraps the model and adds in this message history. A key part here is the function we pass into as the `get_session_history`. This function is expected to take in a `session_id` and return a Message History object. This `session_id` is used to distinguish between separate conversations, and should be passed in as part of the config when calling the new chain (we'll show how to do that).\"\n+    \"After that, we can import the relevant classes and set up our chain which wraps the model and adds to this message history. A key part here is the function we pass into as the `get_session_history`. This function is expected to take in a `session_id` and return a Message History object. This `session_id` is used to distinguish between separate conversations, and should be passed in as part of the config when calling the new chain (we'll show how to do that).\"",
    "repo_full_name": "langchain-ai/langchain",
    "discussion_comments": [
      {
        "comment_id": "1751087778",
        "repo_full_name": "langchain-ai/langchain",
        "pr_number": 26234,
        "pr_file": "docs/docs/tutorials/chatbot.ipynb",
        "discussion_id": "1751087778",
        "commented_code": "@@ -254,14 +254,14 @@\n    \"metadata\": {},\n    \"outputs\": [],\n    \"source\": [\n-    \"# ! pip install langchain_community\"\n+    \"%pip install langchain_community\"\n    ]\n   },\n   {\n    \"cell_type\": \"markdown\",\n    \"metadata\": {},\n    \"source\": [\n-    \"After that, we can import the relevant classes and set up our chain which wraps the model and adds in this message history. A key part here is the function we pass into as the `get_session_history`. This function is expected to take in a `session_id` and return a Message History object. This `session_id` is used to distinguish between separate conversations, and should be passed in as part of the config when calling the new chain (we'll show how to do that).\"\n+    \"After that, we can import the relevant classes and set up our chain which wraps the model and adds to this message history. A key part here is the function we pass into as the `get_session_history`. This function is expected to take in a `session_id` and return a Message History object. This `session_id` is used to distinguish between separate conversations, and should be passed in as part of the config when calling the new chain (we'll show how to do that).\"",
        "comment_created_at": "2024-09-10T00:28:41+00:00",
        "comment_author": "efriis",
        "comment_body": "```suggestion\r\n    \"After that, we can import the relevant classes and set up our chain which wraps the model and adds in this message history. A key part here is the function we pass into as the `get_session_history`. This function is expected to take in a `session_id` and return a Message History object. This `session_id` is used to distinguish between separate conversations, and should be passed in as part of the config when calling the new chain (we'll show how to do that).\"\r\n```",
        "pr_file_module": null
      }
    ]
  }
]