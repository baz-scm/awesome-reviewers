[
  {
    "discussion_id": "1936207686",
    "pr_number": 43318,
    "pr_file": "packages/mui-material/src/Modal/useModal.ts",
    "created_at": "2025-01-30T20:00:40+00:00",
    "commented_code": "};\n \n   const handleOpen = useEventCallback(() => {\n-    const resolvedContainer = getContainer(container) || getDoc().body;\n+    /**\n+     * Resolving this could be simplified (mountNodeRef should take priority when it's set)\n+     * but this will also work because the logic matches {@link Portal}.\n+     */\n+    const resolvedContainer = disablePortal\n+      ? ((mountNodeRef.current ?? modalRef.current)?.parentElement ?? getDoc().body)\n+      : getContainer(container) || getDoc().body;",
    "repo_full_name": "mui/material-ui",
    "discussion_comments": [
      {
        "comment_id": "1936207686",
        "repo_full_name": "mui/material-ui",
        "pr_number": 43318,
        "pr_file": "packages/mui-material/src/Modal/useModal.ts",
        "discussion_id": "1936207686",
        "commented_code": "@@ -83,7 +85,13 @@ function useModal(parameters: UseModalParameters): UseModalReturnValue {\n   };\n \n   const handleOpen = useEventCallback(() => {\n-    const resolvedContainer = getContainer(container) || getDoc().body;\n+    /**\n+     * Resolving this could be simplified (mountNodeRef should take priority when it's set)\n+     * but this will also work because the logic matches {@link Portal}.\n+     */\n+    const resolvedContainer = disablePortal\n+      ? ((mountNodeRef.current ?? modalRef.current)?.parentElement ?? getDoc().body)\n+      : getContainer(container) || getDoc().body;",
        "comment_created_at": "2025-01-30T20:00:40+00:00",
        "comment_author": "DiegoAndai",
        "comment_body": "If providing the `container` prop was required when using `disablePortal`, this could be simplified to \r\n\r\n```suggestion\r\nconst resolvedContainer = getContainer(container) || getDoc().body;\r\n```\r\n\r\nRight?",
        "pr_file_module": null
      },
      {
        "comment_id": "2145141652",
        "repo_full_name": "mui/material-ui",
        "pr_number": 43318,
        "pr_file": "packages/mui-material/src/Modal/useModal.ts",
        "discussion_id": "1936207686",
        "commented_code": "@@ -83,7 +85,13 @@ function useModal(parameters: UseModalParameters): UseModalReturnValue {\n   };\n \n   const handleOpen = useEventCallback(() => {\n-    const resolvedContainer = getContainer(container) || getDoc().body;\n+    /**\n+     * Resolving this could be simplified (mountNodeRef should take priority when it's set)\n+     * but this will also work because the logic matches {@link Portal}.\n+     */\n+    const resolvedContainer = disablePortal\n+      ? ((mountNodeRef.current ?? modalRef.current)?.parentElement ?? getDoc().body)\n+      : getContainer(container) || getDoc().body;",
        "comment_created_at": "2025-06-13T13:46:33+00:00",
        "comment_author": "Gr3q",
        "comment_body": "But it's not required and it shouldn't be, because if you don't use Portal how could you mount it to a different container?\r\n\r\nThat is also the reason `disablePortal` takes priority over `container` here.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1133541447",
    "pr_number": 35653,
    "pr_file": "packages/mui-joy/src/Autocomplete/AutocompleteProps.ts",
    "created_at": "2023-03-13T07:43:31+00:00",
    "commented_code": "* @default 'No options'\n      */\n     noOptionsText?: React.ReactNode;\n+    /**\n+     * Callback fired when scroll bar reaches bottom of listbox.\n+     */\n+    onScrollToBottom?: (event: React.SyntheticEvent) => void;",
    "repo_full_name": "mui/material-ui",
    "discussion_comments": [
      {
        "comment_id": "1133541447",
        "repo_full_name": "mui/material-ui",
        "pr_number": 35653,
        "pr_file": "packages/mui-joy/src/Autocomplete/AutocompleteProps.ts",
        "discussion_id": "1133541447",
        "commented_code": "@@ -269,6 +269,10 @@ type AutocompleteOwnProps<\n      * @default 'No options'\n      */\n     noOptionsText?: React.ReactNode;\n+    /**\n+     * Callback fired when scroll bar reaches bottom of listbox.\n+     */\n+    onScrollToBottom?: (event: React.SyntheticEvent) => void;",
        "comment_created_at": "2023-03-13T07:43:31+00:00",
        "comment_author": "ZeeshanTamboli",
        "comment_body": "No need to define it again here since `useAutocomplete` types already has it and Joy UI and Material UI extends the hook's types.\r\n```suggestion\r\n```",
        "pr_file_module": null
      }
    ]
  }
]