[
  {
    "discussion_id": "3581373002",
    "pr_number": 3981,
    "pr_file": "src/timestream-for-influxdb-mcp-server/kiro_power/steering/influxdb2/gotchas.md",
    "created_at": "2026-07-14T17:49:04+00:00",
    "commented_code": "+# Common Gotchas & Pain Points for Timestream for InfluxDB v2\n+\n+## Critical \u2014 Will Cause Outages If Ignored\n+",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3581373002",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 3981,
        "pr_file": "src/timestream-for-influxdb-mcp-server/kiro_power/steering/influxdb2/gotchas.md",
        "discussion_id": "3581373002",
        "commented_code": "@@ -0,0 +1,57 @@\n+# Common Gotchas & Pain Points for Timestream for InfluxDB v2\n+\n+## Critical \u2014 Will Cause Outages If Ignored\n+",
        "comment_created_at": "2026-07-14T17:49:04+00:00",
        "comment_author": "mhchengaws",
        "comment_body": "Worth adding a gotcha which instances are patched during the default or specified maintenance window and could experience downtime while patching. Recommend setting the maintenance window to a low-traffic period.",
        "pr_file_module": null
      },
      {
        "comment_id": "3582433777",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 3981,
        "pr_file": "src/timestream-for-influxdb-mcp-server/kiro_power/steering/influxdb2/gotchas.md",
        "discussion_id": "3581373002",
        "commented_code": "@@ -0,0 +1,57 @@\n+# Common Gotchas & Pain Points for Timestream for InfluxDB v2\n+\n+## Critical \u2014 Will Cause Outages If Ignored\n+",
        "comment_created_at": "2026-07-14T20:33:42+00:00",
        "comment_author": "trevorbonas",
        "comment_body": "Added.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3691203268",
    "pr_number": 3981,
    "pr_file": "src/timestream-for-influxdb-mcp-server/kiro_power/steering/influxdb3/ingestion.md",
    "created_at": "2026-07-31T14:42:03+00:00",
    "commented_code": "+# InfluxDB v3 Data Ingestion & Writing\n+\n+## Write Endpoints\n+\n+```\n+POST /api/v3/write_lp?db=<database>&precision=<auto|nanosecond|microsecond|millisecond|second>\n+Authorization: Bearer <token>\n+Content-Type: text/plain\n+```\n+\n+- No `org` parameter \u2014 V3 has no organizations.\n+- `db` is the database name (required).\n+- `accept_partial` (boolean, default true) \u2014 when true, valid points succeed even if some fail.\n+- Default port is **8181** (not 8086 like V2).\n+- Auth uses `Bearer` prefix (not `Token`).\n+\n+### Backward-compatible endpoint\n+\n+V3 also supports the V2 write endpoint for migration:\n+\n+```\n+POST /api/v2/write?bucket=<database>&precision=<auto|nanosecond|microsecond|millisecond|second>\n+Authorization: Bearer <token>\n+Content-Type: text/plain\n+```\n+\n+- `bucket` maps to the V3 database name.\n+- `org` parameter is accepted but **ignored**.\n+- Allows existing V2 client code (including `influxdb-client` Python library) to write to V3 without changes \u2014 just update the URL, port, and token prefix.\n+\n+## Batching\n+\n+**Always batch writes.**\n+\n+- Target **5,000+ points per request** minimum\n+- Optimal batch size: 5,000\u201310,000 points\n+- Max recommended: ~50,000 points per request (beyond this, HTTP timeouts become a risk)\n+- For high-throughput workloads, tune `ingestQueryInstances` in the parameter group (see Tuning section below) or scale vertically by updating the cluster size with the `--db-instance-type` option (with the AWS CLI).",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3691203268",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 3981,
        "pr_file": "src/timestream-for-influxdb-mcp-server/kiro_power/steering/influxdb3/ingestion.md",
        "discussion_id": "3691203268",
        "commented_code": "@@ -0,0 +1,201 @@\n+# InfluxDB v3 Data Ingestion & Writing\n+\n+## Write Endpoints\n+\n+```\n+POST /api/v3/write_lp?db=<database>&precision=<auto|nanosecond|microsecond|millisecond|second>\n+Authorization: Bearer <token>\n+Content-Type: text/plain\n+```\n+\n+- No `org` parameter \u2014 V3 has no organizations.\n+- `db` is the database name (required).\n+- `accept_partial` (boolean, default true) \u2014 when true, valid points succeed even if some fail.\n+- Default port is **8181** (not 8086 like V2).\n+- Auth uses `Bearer` prefix (not `Token`).\n+\n+### Backward-compatible endpoint\n+\n+V3 also supports the V2 write endpoint for migration:\n+\n+```\n+POST /api/v2/write?bucket=<database>&precision=<auto|nanosecond|microsecond|millisecond|second>\n+Authorization: Bearer <token>\n+Content-Type: text/plain\n+```\n+\n+- `bucket` maps to the V3 database name.\n+- `org` parameter is accepted but **ignored**.\n+- Allows existing V2 client code (including `influxdb-client` Python library) to write to V3 without changes \u2014 just update the URL, port, and token prefix.\n+\n+## Batching\n+\n+**Always batch writes.**\n+\n+- Target **5,000+ points per request** minimum\n+- Optimal batch size: 5,000\u201310,000 points\n+- Max recommended: ~50,000 points per request (beyond this, HTTP timeouts become a risk)\n+- For high-throughput workloads, tune `ingestQueryInstances` in the parameter group (see Tuning section below) or scale vertically by updating the cluster size with the `--db-instance-type` option (with the AWS CLI).",
        "comment_created_at": "2026-07-31T14:42:03+00:00",
        "comment_author": "scottschreckengaust",
        "comment_body": "_(non-blocking)_ **Enterprise-only tuning lever offered without an inline Core caveat.** `ingestQueryInstances` is Enterprise-only (Core is single-node \u2014 `parameters.md:95`); a Core reader here can't use it and should be pointed to `--db-instance-type` vertical scaling (which you do mention). Also the tuning table at line 196 shows \"Default 2\", but `parameters.md:101` marks this field `[required]` with no default \u2014 please reconcile.",
        "pr_file_module": null
      },
      {
        "comment_id": "3693776774",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 3981,
        "pr_file": "src/timestream-for-influxdb-mcp-server/kiro_power/steering/influxdb3/ingestion.md",
        "discussion_id": "3691203268",
        "commented_code": "@@ -0,0 +1,201 @@\n+# InfluxDB v3 Data Ingestion & Writing\n+\n+## Write Endpoints\n+\n+```\n+POST /api/v3/write_lp?db=<database>&precision=<auto|nanosecond|microsecond|millisecond|second>\n+Authorization: Bearer <token>\n+Content-Type: text/plain\n+```\n+\n+- No `org` parameter \u2014 V3 has no organizations.\n+- `db` is the database name (required).\n+- `accept_partial` (boolean, default true) \u2014 when true, valid points succeed even if some fail.\n+- Default port is **8181** (not 8086 like V2).\n+- Auth uses `Bearer` prefix (not `Token`).\n+\n+### Backward-compatible endpoint\n+\n+V3 also supports the V2 write endpoint for migration:\n+\n+```\n+POST /api/v2/write?bucket=<database>&precision=<auto|nanosecond|microsecond|millisecond|second>\n+Authorization: Bearer <token>\n+Content-Type: text/plain\n+```\n+\n+- `bucket` maps to the V3 database name.\n+- `org` parameter is accepted but **ignored**.\n+- Allows existing V2 client code (including `influxdb-client` Python library) to write to V3 without changes \u2014 just update the URL, port, and token prefix.\n+\n+## Batching\n+\n+**Always batch writes.**\n+\n+- Target **5,000+ points per request** minimum\n+- Optimal batch size: 5,000\u201310,000 points\n+- Max recommended: ~50,000 points per request (beyond this, HTTP timeouts become a risk)\n+- For high-throughput workloads, tune `ingestQueryInstances` in the parameter group (see Tuning section below) or scale vertically by updating the cluster size with the `--db-instance-type` option (with the AWS CLI).",
        "comment_created_at": "2026-07-31T22:47:10+00:00",
        "comment_author": "trevorbonas",
        "comment_body": "I've added a note that this is only for Enterprise and that for Core vertical scaling should be considered. I've updated the table in parameters.md with defaults (from service-owned parameter group).",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2620932704",
    "pr_number": 1862,
    "pr_file": "src/valkey-mcp-server/README.md",
    "created_at": "2025-12-15T21:36:47+00:00",
    "commented_code": "## Configuration\n \n-The server can be configured using the following environment variables:\n-\n-| Name | Description | Default Value |\n-|------|-------------|---------------|\n-| `VALKEY_HOST` | ElastiCache Primary Endpoint or MemoryDB Cluster Endpoint or Valkey IP or hostname | `\"127.0.0.1\"` |\n-| `VALKEY_PORT` | Valkey port | `6379` |\n-| `VALKEY_USERNAME` | Default database username | `None` |\n-| `VALKEY_PWD` | Default database password | `\"\"` |\n-| `VALKEY_USE_SSL` | Enables or disables SSL/TLS | `False` |\n-| `VALKEY_CA_PATH` | CA certificate for verifying server | `None` |\n-| `VALKEY_SSL_KEYFILE` | Client's private key file | `None` |\n-| `VALKEY_SSL_CERTFILE` | Client's certificate file | `None` |\n-| `VALKEY_CERT_REQS` | Server certificate verification | `\"required\"` |\n-| `VALKEY_CA_CERTS` | Path to trusted CA certificates | `None` |\n-| `VALKEY_CLUSTER_MODE` | Enable Valkey Cluster mode | `False` |\n+### Valkey Connection\n+The Valkey server can be configured using the following environment variables:\n+\n+| Name | Description                                                                                                                    | Default Value |\n+|------|--------------------------------------------------------------------------------------------------------------------------------|---------------|\n+| `VALKEY_HOST` | ElastiCache Primary Endpoint or MemoryDB Cluster Endpoint or Valkey IP or hostname                                             | `\"127.0.0.1\"` |\n+| `VALKEY_PORT` | Valkey port                                                                                                                    | `6379`        |\n+| `VALKEY_USERNAME` | Default database username                                                                                                      | `None`        |\n+| `VALKEY_PWD` | Default database password                                                                                                      | `\"\"`          |\n+| `VALKEY_USE_SSL` | Enables or disables SSL/TLS                                                                                                    | `False`       |\n+| `VALKEY_CA_PATH` | CA certificate for verifying server                                                                                            | `None`        |\n+| `VALKEY_SSL_KEYFILE` | Client's private key file                                                                                                      | `None`        |\n+| `VALKEY_SSL_CERTFILE` | Client's certificate file                                                                                                      | `None`        |\n+| `VALKEY_CERT_REQS` | Server certificate verification                                                                                                | `\"required\"`  |\n+| `VALKEY_CA_CERTS` | Path to trusted CA certificates                                                                                                | `None`        |\n+| `VALKEY_CLUSTER_MODE` | Enable Valkey Cluster mode                                                                                                     | `False`       |\n+| `VALKEY_VECTOR_INDEX_TYPE` | Default vector index structure type to use when vector indices are created in calls to add documents (either `FLAT` or `HNSW`) | `FLAT`        |\n+\n+### Embeddings Provider\n+Semantic search requires integration with an embeddings provider.\n+The following environment variables are used to configure this:\n+\n+#### Common Embedding Configuration\n+The first step is to configure the embeddings provider, which is used to generate embeddings for semantic search.\n+This defaults to using the Bedrock embeddings provider, but can be configured to use any of the following:\n+\n+| Name | Description                                                                    | Default Value                  |\n+|------|--------------------------------------------------------------------------------|--------------------------------|\n+| `EMBEDDING_PROVIDER` | Embeddings provider to use (i.e. 'Ollama', 'Bedrock', or 'OpenAI')                         | `\"bedrock\"`                    |\n+\n+#### Bedrock\n+The following environment variables are used to configure the Bedrock embeddings provider, including a number\n+of optional configuration options.  Credentials for the Bedrock service must be provided using the `AWS_ACCESS_KEY_ID` and\n+`AWS_SECRET_ACCESS_KEY` environment variables or via `AWS_PROFILE`.",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "2620932704",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 1862,
        "pr_file": "src/valkey-mcp-server/README.md",
        "discussion_id": "2620932704",
        "commented_code": "@@ -193,21 +193,68 @@ To run in readonly mode with Docker:\n \n ## Configuration\n \n-The server can be configured using the following environment variables:\n-\n-| Name | Description | Default Value |\n-|------|-------------|---------------|\n-| `VALKEY_HOST` | ElastiCache Primary Endpoint or MemoryDB Cluster Endpoint or Valkey IP or hostname | `\"127.0.0.1\"` |\n-| `VALKEY_PORT` | Valkey port | `6379` |\n-| `VALKEY_USERNAME` | Default database username | `None` |\n-| `VALKEY_PWD` | Default database password | `\"\"` |\n-| `VALKEY_USE_SSL` | Enables or disables SSL/TLS | `False` |\n-| `VALKEY_CA_PATH` | CA certificate for verifying server | `None` |\n-| `VALKEY_SSL_KEYFILE` | Client's private key file | `None` |\n-| `VALKEY_SSL_CERTFILE` | Client's certificate file | `None` |\n-| `VALKEY_CERT_REQS` | Server certificate verification | `\"required\"` |\n-| `VALKEY_CA_CERTS` | Path to trusted CA certificates | `None` |\n-| `VALKEY_CLUSTER_MODE` | Enable Valkey Cluster mode | `False` |\n+### Valkey Connection\n+The Valkey server can be configured using the following environment variables:\n+\n+| Name | Description                                                                                                                    | Default Value |\n+|------|--------------------------------------------------------------------------------------------------------------------------------|---------------|\n+| `VALKEY_HOST` | ElastiCache Primary Endpoint or MemoryDB Cluster Endpoint or Valkey IP or hostname                                             | `\"127.0.0.1\"` |\n+| `VALKEY_PORT` | Valkey port                                                                                                                    | `6379`        |\n+| `VALKEY_USERNAME` | Default database username                                                                                                      | `None`        |\n+| `VALKEY_PWD` | Default database password                                                                                                      | `\"\"`          |\n+| `VALKEY_USE_SSL` | Enables or disables SSL/TLS                                                                                                    | `False`       |\n+| `VALKEY_CA_PATH` | CA certificate for verifying server                                                                                            | `None`        |\n+| `VALKEY_SSL_KEYFILE` | Client's private key file                                                                                                      | `None`        |\n+| `VALKEY_SSL_CERTFILE` | Client's certificate file                                                                                                      | `None`        |\n+| `VALKEY_CERT_REQS` | Server certificate verification                                                                                                | `\"required\"`  |\n+| `VALKEY_CA_CERTS` | Path to trusted CA certificates                                                                                                | `None`        |\n+| `VALKEY_CLUSTER_MODE` | Enable Valkey Cluster mode                                                                                                     | `False`       |\n+| `VALKEY_VECTOR_INDEX_TYPE` | Default vector index structure type to use when vector indices are created in calls to add documents (either `FLAT` or `HNSW`) | `FLAT`        |\n+\n+### Embeddings Provider\n+Semantic search requires integration with an embeddings provider.\n+The following environment variables are used to configure this:\n+\n+#### Common Embedding Configuration\n+The first step is to configure the embeddings provider, which is used to generate embeddings for semantic search.\n+This defaults to using the Bedrock embeddings provider, but can be configured to use any of the following:\n+\n+| Name | Description                                                                    | Default Value                  |\n+|------|--------------------------------------------------------------------------------|--------------------------------|\n+| `EMBEDDING_PROVIDER` | Embeddings provider to use (i.e. 'Ollama', 'Bedrock', or 'OpenAI')                         | `\"bedrock\"`                    |\n+\n+#### Bedrock\n+The following environment variables are used to configure the Bedrock embeddings provider, including a number\n+of optional configuration options.  Credentials for the Bedrock service must be provided using the `AWS_ACCESS_KEY_ID` and\n+`AWS_SECRET_ACCESS_KEY` environment variables or via `AWS_PROFILE`.",
        "comment_created_at": "2025-12-15T21:36:47+00:00",
        "comment_author": "seaofawareness",
        "comment_body": "Allow AWS_SESSION_TOKEN",
        "pr_file_module": null
      }
    ]
  }
]