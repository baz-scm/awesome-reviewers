[
  {
    "discussion_id": "3606562781",
    "pr_number": 4136,
    "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/tools/procurement_preferences_tools.py",
    "created_at": "2026-07-17T22:26:04+00:00",
    "commented_code": "+# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.\n+#\n+# Licensed under the Apache License, Version 2.0 (the \"License\");\n+# you may not use this file except in compliance with the License.\n+# You may obtain a copy of the License at\n+#\n+#     http://www.apache.org/licenses/LICENSE-2.0\n+#\n+# Unless required by applicable law or agreed to in writing, software\n+# distributed under the License is distributed on an \"AS IS\" BASIS,\n+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+# See the License for the specific language governing permissions and\n+# limitations under the License.\n+\n+\"\"\"AWS Procurement Portal Preference tools for the BCM MCP server.\n+\n+Exposes a single read-only ``procurement-preferences`` tool that routes by\n+``operation`` across the AWS Procurement Portal Preferences read APIs\n+(mirroring the ``invoicing`` tool). The rich tool description is the primary\n+vehicle that gives the agent semantic context about every request parameter and\n+response field.\n+\"\"\"\n+\n+from ..utilities.aws_service_base import format_response\n+from .procurement_preferences_operations import (\n+    get_procurement_portal_preference as _get_procurement_portal_preference,\n+)\n+from .procurement_preferences_operations import (\n+    list_procurement_portal_preferences as _list_procurement_portal_preferences,\n+)\n+from fastmcp import Context, FastMCP\n+from typing import Any, Dict, Optional\n+\n+\n+procurement_preferences_server = FastMCP(\n+    name='procurement-preferences-tools',\n+    instructions='Read-only tools for AWS Procurement Portal Preferences (SAP/Coupa, e-invoice delivery)',\n+)\n+\n+\n+async def _procurement_preferences(\n+    ctx: Context,\n+    operation: str,\n+    procurement_portal_preference_arn: Optional[str] = None,\n+    max_results: Optional[int] = None,\n+    next_token: Optional[str] = None,\n+    max_pages: Optional[int] = None,\n+) -> Dict[str, Any]:\n+    \"\"\"Route an AWS Procurement Portal Preference ``operation`` to its handler.\n+\n+    Kept separate from the FastMCP-decorated wrapper so the routing can be unit\n+    tested directly (decorated tools cannot be invoked as plain functions).\n+\n+    Args:\n+        ctx: The MCP context object.\n+        operation: The operation (``\"list_procurement_portal_preferences\"`` or\n+            ``\"get_procurement_portal_preference\"``).\n+        procurement_portal_preference_arn: ARN of a single preference\n+            (get_procurement_portal_preference).\n+        max_results: Maximum results per page (1-100).\n+        next_token: Pagination token from a previous response.\n+        max_pages: Maximum pages to auto-paginate through (default: all).\n+\n+    Returns:\n+        The operation's response, or a standardized error for an unknown\n+        operation.\n+    \"\"\"\n+    await ctx.info(f'Procurement portal preference operation: {operation}')\n+\n+    if operation == 'list_procurement_portal_preferences':\n+        return await _list_procurement_portal_preferences(\n+            ctx,\n+            max_results=max_results,\n+            next_token=next_token,\n+            max_pages=max_pages,\n+        )\n+\n+    if operation == 'get_procurement_portal_preference':\n+        if procurement_portal_preference_arn is None:\n+            return format_response(\n+                'error',\n+                {\n+                    'message': (\n+                        'procurement_portal_preference_arn is required for '\n+                        'get_procurement_portal_preference.'\n+                    )\n+                },\n+            )\n+        return await _get_procurement_portal_preference(\n+            ctx,\n+            procurement_portal_preference_arn=procurement_portal_preference_arn,\n+        )\n+\n+    return format_response(\n+        'error',\n+        {\n+            'message': (\n+                f\"Unsupported operation: '{operation}'. Supported operations: \"\n+                'list_procurement_portal_preferences, get_procurement_portal_preference.'\n+            )\n+        },\n+    )\n+\n+\n+@procurement_preferences_server.tool(\n+    name='procurement-preferences',\n+    description=\"\"\"Access AWS Procurement Portal Preferences \u2014 read-only. These are the procurement portal connections (e.g. SAP Business Network, Coupa) and e-invoice delivery / purchase-order retrieval settings configured for the account. Choose an action with the required `operation` parameter.\n+\n+## OPERATIONS\n+\n+1) list_procurement_portal_preferences - list configured procurement portal preferences\n+   Required: operation=\"list_procurement_portal_preferences\"\n+   Pagination: max_results caps items per page (1-100); max_pages caps pages fetched (default: all). The `pagination` block reports total_results, pages_fetched, has_more, and next_token.\n+   Returns: `data.procurement_portal_preferences`, each including the preference ARN, portal type/connection details, status, EinvoiceDeliveryPreference, and CreateDate / LastUpdateDate (ISO 8601 UTC).",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3606562781",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4136,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/tools/procurement_preferences_tools.py",
        "discussion_id": "3606562781",
        "commented_code": "@@ -0,0 +1,159 @@\n+# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.\n+#\n+# Licensed under the Apache License, Version 2.0 (the \"License\");\n+# you may not use this file except in compliance with the License.\n+# You may obtain a copy of the License at\n+#\n+#     http://www.apache.org/licenses/LICENSE-2.0\n+#\n+# Unless required by applicable law or agreed to in writing, software\n+# distributed under the License is distributed on an \"AS IS\" BASIS,\n+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+# See the License for the specific language governing permissions and\n+# limitations under the License.\n+\n+\"\"\"AWS Procurement Portal Preference tools for the BCM MCP server.\n+\n+Exposes a single read-only ``procurement-preferences`` tool that routes by\n+``operation`` across the AWS Procurement Portal Preferences read APIs\n+(mirroring the ``invoicing`` tool). The rich tool description is the primary\n+vehicle that gives the agent semantic context about every request parameter and\n+response field.\n+\"\"\"\n+\n+from ..utilities.aws_service_base import format_response\n+from .procurement_preferences_operations import (\n+    get_procurement_portal_preference as _get_procurement_portal_preference,\n+)\n+from .procurement_preferences_operations import (\n+    list_procurement_portal_preferences as _list_procurement_portal_preferences,\n+)\n+from fastmcp import Context, FastMCP\n+from typing import Any, Dict, Optional\n+\n+\n+procurement_preferences_server = FastMCP(\n+    name='procurement-preferences-tools',\n+    instructions='Read-only tools for AWS Procurement Portal Preferences (SAP/Coupa, e-invoice delivery)',\n+)\n+\n+\n+async def _procurement_preferences(\n+    ctx: Context,\n+    operation: str,\n+    procurement_portal_preference_arn: Optional[str] = None,\n+    max_results: Optional[int] = None,\n+    next_token: Optional[str] = None,\n+    max_pages: Optional[int] = None,\n+) -> Dict[str, Any]:\n+    \"\"\"Route an AWS Procurement Portal Preference ``operation`` to its handler.\n+\n+    Kept separate from the FastMCP-decorated wrapper so the routing can be unit\n+    tested directly (decorated tools cannot be invoked as plain functions).\n+\n+    Args:\n+        ctx: The MCP context object.\n+        operation: The operation (``\"list_procurement_portal_preferences\"`` or\n+            ``\"get_procurement_portal_preference\"``).\n+        procurement_portal_preference_arn: ARN of a single preference\n+            (get_procurement_portal_preference).\n+        max_results: Maximum results per page (1-100).\n+        next_token: Pagination token from a previous response.\n+        max_pages: Maximum pages to auto-paginate through (default: all).\n+\n+    Returns:\n+        The operation's response, or a standardized error for an unknown\n+        operation.\n+    \"\"\"\n+    await ctx.info(f'Procurement portal preference operation: {operation}')\n+\n+    if operation == 'list_procurement_portal_preferences':\n+        return await _list_procurement_portal_preferences(\n+            ctx,\n+            max_results=max_results,\n+            next_token=next_token,\n+            max_pages=max_pages,\n+        )\n+\n+    if operation == 'get_procurement_portal_preference':\n+        if procurement_portal_preference_arn is None:\n+            return format_response(\n+                'error',\n+                {\n+                    'message': (\n+                        'procurement_portal_preference_arn is required for '\n+                        'get_procurement_portal_preference.'\n+                    )\n+                },\n+            )\n+        return await _get_procurement_portal_preference(\n+            ctx,\n+            procurement_portal_preference_arn=procurement_portal_preference_arn,\n+        )\n+\n+    return format_response(\n+        'error',\n+        {\n+            'message': (\n+                f\"Unsupported operation: '{operation}'. Supported operations: \"\n+                'list_procurement_portal_preferences, get_procurement_portal_preference.'\n+            )\n+        },\n+    )\n+\n+\n+@procurement_preferences_server.tool(\n+    name='procurement-preferences',\n+    description=\"\"\"Access AWS Procurement Portal Preferences \u2014 read-only. These are the procurement portal connections (e.g. SAP Business Network, Coupa) and e-invoice delivery / purchase-order retrieval settings configured for the account. Choose an action with the required `operation` parameter.\n+\n+## OPERATIONS\n+\n+1) list_procurement_portal_preferences - list configured procurement portal preferences\n+   Required: operation=\"list_procurement_portal_preferences\"\n+   Pagination: max_results caps items per page (1-100); max_pages caps pages fetched (default: all). The `pagination` block reports total_results, pages_fetched, has_more, and next_token.\n+   Returns: `data.procurement_portal_preferences`, each including the preference ARN, portal type/connection details, status, EinvoiceDeliveryPreference, and CreateDate / LastUpdateDate (ISO 8601 UTC).",
        "comment_created_at": "2026-07-17T22:26:04+00:00",
        "comment_author": "wangyuhere",
        "comment_body": "nit: API will return `ProcurementPortalPreferences` instead of  `data.procurement_portal_preferences` right? ",
        "pr_file_module": null
      },
      {
        "comment_id": "3616148332",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4136,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/tools/procurement_preferences_tools.py",
        "discussion_id": "3606562781",
        "commented_code": "@@ -0,0 +1,159 @@\n+# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.\n+#\n+# Licensed under the Apache License, Version 2.0 (the \"License\");\n+# you may not use this file except in compliance with the License.\n+# You may obtain a copy of the License at\n+#\n+#     http://www.apache.org/licenses/LICENSE-2.0\n+#\n+# Unless required by applicable law or agreed to in writing, software\n+# distributed under the License is distributed on an \"AS IS\" BASIS,\n+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+# See the License for the specific language governing permissions and\n+# limitations under the License.\n+\n+\"\"\"AWS Procurement Portal Preference tools for the BCM MCP server.\n+\n+Exposes a single read-only ``procurement-preferences`` tool that routes by\n+``operation`` across the AWS Procurement Portal Preferences read APIs\n+(mirroring the ``invoicing`` tool). The rich tool description is the primary\n+vehicle that gives the agent semantic context about every request parameter and\n+response field.\n+\"\"\"\n+\n+from ..utilities.aws_service_base import format_response\n+from .procurement_preferences_operations import (\n+    get_procurement_portal_preference as _get_procurement_portal_preference,\n+)\n+from .procurement_preferences_operations import (\n+    list_procurement_portal_preferences as _list_procurement_portal_preferences,\n+)\n+from fastmcp import Context, FastMCP\n+from typing import Any, Dict, Optional\n+\n+\n+procurement_preferences_server = FastMCP(\n+    name='procurement-preferences-tools',\n+    instructions='Read-only tools for AWS Procurement Portal Preferences (SAP/Coupa, e-invoice delivery)',\n+)\n+\n+\n+async def _procurement_preferences(\n+    ctx: Context,\n+    operation: str,\n+    procurement_portal_preference_arn: Optional[str] = None,\n+    max_results: Optional[int] = None,\n+    next_token: Optional[str] = None,\n+    max_pages: Optional[int] = None,\n+) -> Dict[str, Any]:\n+    \"\"\"Route an AWS Procurement Portal Preference ``operation`` to its handler.\n+\n+    Kept separate from the FastMCP-decorated wrapper so the routing can be unit\n+    tested directly (decorated tools cannot be invoked as plain functions).\n+\n+    Args:\n+        ctx: The MCP context object.\n+        operation: The operation (``\"list_procurement_portal_preferences\"`` or\n+            ``\"get_procurement_portal_preference\"``).\n+        procurement_portal_preference_arn: ARN of a single preference\n+            (get_procurement_portal_preference).\n+        max_results: Maximum results per page (1-100).\n+        next_token: Pagination token from a previous response.\n+        max_pages: Maximum pages to auto-paginate through (default: all).\n+\n+    Returns:\n+        The operation's response, or a standardized error for an unknown\n+        operation.\n+    \"\"\"\n+    await ctx.info(f'Procurement portal preference operation: {operation}')\n+\n+    if operation == 'list_procurement_portal_preferences':\n+        return await _list_procurement_portal_preferences(\n+            ctx,\n+            max_results=max_results,\n+            next_token=next_token,\n+            max_pages=max_pages,\n+        )\n+\n+    if operation == 'get_procurement_portal_preference':\n+        if procurement_portal_preference_arn is None:\n+            return format_response(\n+                'error',\n+                {\n+                    'message': (\n+                        'procurement_portal_preference_arn is required for '\n+                        'get_procurement_portal_preference.'\n+                    )\n+                },\n+            )\n+        return await _get_procurement_portal_preference(\n+            ctx,\n+            procurement_portal_preference_arn=procurement_portal_preference_arn,\n+        )\n+\n+    return format_response(\n+        'error',\n+        {\n+            'message': (\n+                f\"Unsupported operation: '{operation}'. Supported operations: \"\n+                'list_procurement_portal_preferences, get_procurement_portal_preference.'\n+            )\n+        },\n+    )\n+\n+\n+@procurement_preferences_server.tool(\n+    name='procurement-preferences',\n+    description=\"\"\"Access AWS Procurement Portal Preferences \u2014 read-only. These are the procurement portal connections (e.g. SAP Business Network, Coupa) and e-invoice delivery / purchase-order retrieval settings configured for the account. Choose an action with the required `operation` parameter.\n+\n+## OPERATIONS\n+\n+1) list_procurement_portal_preferences - list configured procurement portal preferences\n+   Required: operation=\"list_procurement_portal_preferences\"\n+   Pagination: max_results caps items per page (1-100); max_pages caps pages fetched (default: all). The `pagination` block reports total_results, pages_fetched, has_more, and next_token.\n+   Returns: `data.procurement_portal_preferences`, each including the preference ARN, portal type/connection details, status, EinvoiceDeliveryPreference, and CreateDate / LastUpdateDate (ISO 8601 UTC).",
        "comment_created_at": "2026-07-20T17:17:43+00:00",
        "comment_author": "nvvijayatwork",
        "comment_body": "The tool intentionally re-keys the raw `ProcurementPortalPreferences` into `data.procurement_portal_preferences` (snake_case) for consistency with the sibling tools (`data.invoice_summaries`, `data.invoice_units`); the per-item fields stay PascalCase as the API returns them. Can switch to raw passthrough if you'd rather! Let me know.\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "3618088132",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4136,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/tools/procurement_preferences_tools.py",
        "discussion_id": "3606562781",
        "commented_code": "@@ -0,0 +1,159 @@\n+# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.\n+#\n+# Licensed under the Apache License, Version 2.0 (the \"License\");\n+# you may not use this file except in compliance with the License.\n+# You may obtain a copy of the License at\n+#\n+#     http://www.apache.org/licenses/LICENSE-2.0\n+#\n+# Unless required by applicable law or agreed to in writing, software\n+# distributed under the License is distributed on an \"AS IS\" BASIS,\n+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+# See the License for the specific language governing permissions and\n+# limitations under the License.\n+\n+\"\"\"AWS Procurement Portal Preference tools for the BCM MCP server.\n+\n+Exposes a single read-only ``procurement-preferences`` tool that routes by\n+``operation`` across the AWS Procurement Portal Preferences read APIs\n+(mirroring the ``invoicing`` tool). The rich tool description is the primary\n+vehicle that gives the agent semantic context about every request parameter and\n+response field.\n+\"\"\"\n+\n+from ..utilities.aws_service_base import format_response\n+from .procurement_preferences_operations import (\n+    get_procurement_portal_preference as _get_procurement_portal_preference,\n+)\n+from .procurement_preferences_operations import (\n+    list_procurement_portal_preferences as _list_procurement_portal_preferences,\n+)\n+from fastmcp import Context, FastMCP\n+from typing import Any, Dict, Optional\n+\n+\n+procurement_preferences_server = FastMCP(\n+    name='procurement-preferences-tools',\n+    instructions='Read-only tools for AWS Procurement Portal Preferences (SAP/Coupa, e-invoice delivery)',\n+)\n+\n+\n+async def _procurement_preferences(\n+    ctx: Context,\n+    operation: str,\n+    procurement_portal_preference_arn: Optional[str] = None,\n+    max_results: Optional[int] = None,\n+    next_token: Optional[str] = None,\n+    max_pages: Optional[int] = None,\n+) -> Dict[str, Any]:\n+    \"\"\"Route an AWS Procurement Portal Preference ``operation`` to its handler.\n+\n+    Kept separate from the FastMCP-decorated wrapper so the routing can be unit\n+    tested directly (decorated tools cannot be invoked as plain functions).\n+\n+    Args:\n+        ctx: The MCP context object.\n+        operation: The operation (``\"list_procurement_portal_preferences\"`` or\n+            ``\"get_procurement_portal_preference\"``).\n+        procurement_portal_preference_arn: ARN of a single preference\n+            (get_procurement_portal_preference).\n+        max_results: Maximum results per page (1-100).\n+        next_token: Pagination token from a previous response.\n+        max_pages: Maximum pages to auto-paginate through (default: all).\n+\n+    Returns:\n+        The operation's response, or a standardized error for an unknown\n+        operation.\n+    \"\"\"\n+    await ctx.info(f'Procurement portal preference operation: {operation}')\n+\n+    if operation == 'list_procurement_portal_preferences':\n+        return await _list_procurement_portal_preferences(\n+            ctx,\n+            max_results=max_results,\n+            next_token=next_token,\n+            max_pages=max_pages,\n+        )\n+\n+    if operation == 'get_procurement_portal_preference':\n+        if procurement_portal_preference_arn is None:\n+            return format_response(\n+                'error',\n+                {\n+                    'message': (\n+                        'procurement_portal_preference_arn is required for '\n+                        'get_procurement_portal_preference.'\n+                    )\n+                },\n+            )\n+        return await _get_procurement_portal_preference(\n+            ctx,\n+            procurement_portal_preference_arn=procurement_portal_preference_arn,\n+        )\n+\n+    return format_response(\n+        'error',\n+        {\n+            'message': (\n+                f\"Unsupported operation: '{operation}'. Supported operations: \"\n+                'list_procurement_portal_preferences, get_procurement_portal_preference.'\n+            )\n+        },\n+    )\n+\n+\n+@procurement_preferences_server.tool(\n+    name='procurement-preferences',\n+    description=\"\"\"Access AWS Procurement Portal Preferences \u2014 read-only. These are the procurement portal connections (e.g. SAP Business Network, Coupa) and e-invoice delivery / purchase-order retrieval settings configured for the account. Choose an action with the required `operation` parameter.\n+\n+## OPERATIONS\n+\n+1) list_procurement_portal_preferences - list configured procurement portal preferences\n+   Required: operation=\"list_procurement_portal_preferences\"\n+   Pagination: max_results caps items per page (1-100); max_pages caps pages fetched (default: all). The `pagination` block reports total_results, pages_fetched, has_more, and next_token.\n+   Returns: `data.procurement_portal_preferences`, each including the preference ARN, portal type/connection details, status, EinvoiceDeliveryPreference, and CreateDate / LastUpdateDate (ISO 8601 UTC).",
        "comment_created_at": "2026-07-20T22:58:14+00:00",
        "comment_author": "wangyuhere",
        "comment_body": "OK, I see.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3589551337",
    "pr_number": 4127,
    "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/models.py",
    "created_at": "2026-07-15T17:46:47+00:00",
    "commented_code": "database_acl: Optional[str] = Field(\n         None, description='Access control information (for internal use)'\n     )\n-    database_options: Optional[str] = Field(None, description='The properties of the database')\n+    # SHOW DATABASES names this column 'parameters'.",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3589551337",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4127,
        "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/models.py",
        "discussion_id": "3589551337",
        "commented_code": "@@ -51,14 +87,17 @@ class RedshiftDatabase(BaseModel):\n     database_acl: Optional[str] = Field(\n         None, description='Access control information (for internal use)'\n     )\n-    database_options: Optional[str] = Field(None, description='The properties of the database')\n+    # SHOW DATABASES names this column 'parameters'.",
        "comment_created_at": "2026-07-15T17:46:47+00:00",
        "comment_author": "grayhemp",
        "comment_body": "Shall we rename it in the model and all downstream to comply with SHOW?",
        "pr_file_module": null
      },
      {
        "comment_id": "3592904111",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4127,
        "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/models.py",
        "discussion_id": "3589551337",
        "commented_code": "@@ -51,14 +87,17 @@ class RedshiftDatabase(BaseModel):\n     database_acl: Optional[str] = Field(\n         None, description='Access control information (for internal use)'\n     )\n-    database_options: Optional[str] = Field(None, description='The properties of the database')\n+    # SHOW DATABASES names this column 'parameters'.",
        "comment_created_at": "2026-07-16T05:41:58+00:00",
        "comment_author": "saeedma8",
        "comment_body": "Renamed to parameters to match the SHOW column, dropped the alias, and updated all downstream.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3150893990",
    "pr_number": 2955,
    "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/review/executor.py",
    "created_at": "2026-04-28T00:32:48+00:00",
    "commented_code": "+# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3150893990",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 2955,
        "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/review/executor.py",
        "discussion_id": "3150893990",
        "commented_code": "@@ -0,0 +1,130 @@\n+# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.",
        "comment_created_at": "2026-04-28T00:32:48+00:00",
        "comment_author": "grayhemp",
        "comment_body": "\"pipeline\" - it's a bit confusing. Pipeline is an established SWE pattern. I suggest executor.py as it better describes what's inside.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3560482666",
    "pr_number": 4109,
    "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/redshift.py",
    "created_at": "2026-07-10T16:34:18+00:00",
    "commented_code": "CLIENT_READ_TIMEOUT,\n     CLIENT_RETRIES,\n     CLIENT_USER_AGENT_NAME,\n+    COLUMNS_SQL,\n+    DATABASES_SQL,\n     QUERY_POLL_INTERVAL,\n     QUERY_TIMEOUT,\n+    SCHEMAS_SQL,\n     SESSION_KEEPALIVE,\n-    SVV_ALL_COLUMNS_QUERY,\n-    SVV_ALL_SCHEMAS_QUERY,\n-    SVV_ALL_TABLES_QUERY,\n-    SVV_REDSHIFT_DATABASES_QUERY,\n+    TABLES_SQL,\n )\n from awslabs.redshift_mcp_server.sql_guard import assert_executable\n from botocore.config import Config\n from loguru import logger\n+from sqlglot import exp\n+\n+\n+def _sql_string_literal(value: str) -> str:",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3560482666",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4109,
        "pr_file": "src/redshift-mcp-server/awslabs/redshift_mcp_server/redshift.py",
        "discussion_id": "3560482666",
        "commented_code": "@@ -24,17 +24,23 @@\n     CLIENT_READ_TIMEOUT,\n     CLIENT_RETRIES,\n     CLIENT_USER_AGENT_NAME,\n+    COLUMNS_SQL,\n+    DATABASES_SQL,\n     QUERY_POLL_INTERVAL,\n     QUERY_TIMEOUT,\n+    SCHEMAS_SQL,\n     SESSION_KEEPALIVE,\n-    SVV_ALL_COLUMNS_QUERY,\n-    SVV_ALL_SCHEMAS_QUERY,\n-    SVV_ALL_TABLES_QUERY,\n-    SVV_REDSHIFT_DATABASES_QUERY,\n+    TABLES_SQL,\n )\n from awslabs.redshift_mcp_server.sql_guard import assert_executable\n from botocore.config import Config\n from loguru import logger\n+from sqlglot import exp\n+\n+\n+def _sql_string_literal(value: str) -> str:",
        "comment_created_at": "2026-07-10T16:34:18+00:00",
        "comment_author": "grayhemp",
        "comment_body": "_sql_string_literal() should be _sql_identifier() and should use exp.to_identifier instead of exp.Literal.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2954747723",
    "pr_number": 2627,
    "pr_file": "src/amazon-neptune-mcp-server/awslabs/amazon_neptune_mcp_server/server.py",
    "created_at": "2026-03-18T16:33:55+00:00",
    "commented_code": "'t',\n         )\n \n-        _graph = NeptuneServer(endpoint, use_https=use_https)\n+        endpoint_url = os.environ.get('NEPTUNE_ANALYTICS_ENDPOINT_URL', None)\n+\n+        _graph = NeptuneServer(endpoint, use_https=use_https, endpoint_url=endpoint_url)",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "2954747723",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 2627,
        "pr_file": "src/amazon-neptune-mcp-server/awslabs/amazon_neptune_mcp_server/server.py",
        "discussion_id": "2954747723",
        "commented_code": "@@ -65,7 +65,9 @@ def get_graph():\n             't',\n         )\n \n-        _graph = NeptuneServer(endpoint, use_https=use_https)\n+        endpoint_url = os.environ.get('NEPTUNE_ANALYTICS_ENDPOINT_URL', None)\n+\n+        _graph = NeptuneServer(endpoint, use_https=use_https, endpoint_url=endpoint_url)",
        "comment_created_at": "2026-03-18T16:33:55+00:00",
        "comment_author": "cornerwings",
        "comment_body": "I think endpoint/endpoint_url are bit confusing, perhaps endpoint_override? Even better would be to extend the approach of how endpoint is being specified for neptune. So for example, instead of `neptune-graph://<graphId>`, we can also support `neptune-graph://<endpoint>`. GraphId is guaranteed to conform to this regex (`^g-[a-z0-9]{10}$`), we can try to match with this and if not treat it as endpoint. This avoids the need for additional environment variable.",
        "pr_file_module": null
      },
      {
        "comment_id": "2954875307",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 2627,
        "pr_file": "src/amazon-neptune-mcp-server/awslabs/amazon_neptune_mcp_server/server.py",
        "discussion_id": "2954747723",
        "commented_code": "@@ -65,7 +65,9 @@ def get_graph():\n             't',\n         )\n \n-        _graph = NeptuneServer(endpoint, use_https=use_https)\n+        endpoint_url = os.environ.get('NEPTUNE_ANALYTICS_ENDPOINT_URL', None)\n+\n+        _graph = NeptuneServer(endpoint, use_https=use_https, endpoint_url=endpoint_url)",
        "comment_created_at": "2026-03-18T16:54:10+00:00",
        "comment_author": "phanindhra876",
        "comment_body": "Using two endpoint fields is confusing. We can extract the graphId from endpoint field used already. For an endpoint of form `neptune-graph://g-123457890.us-east-1.neptune-graph.amazonaws.com`, we can split it into graphId `g-1234567890` and endpoint_url(boto3 parameter name) `https://us-east-1.neptune-graph.amazonaws.com`.",
        "pr_file_module": null
      },
      {
        "comment_id": "2955216598",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 2627,
        "pr_file": "src/amazon-neptune-mcp-server/awslabs/amazon_neptune_mcp_server/server.py",
        "discussion_id": "2954747723",
        "commented_code": "@@ -65,7 +65,9 @@ def get_graph():\n             't',\n         )\n \n-        _graph = NeptuneServer(endpoint, use_https=use_https)\n+        endpoint_url = os.environ.get('NEPTUNE_ANALYTICS_ENDPOINT_URL', None)\n+\n+        _graph = NeptuneServer(endpoint, use_https=use_https, endpoint_url=endpoint_url)",
        "comment_created_at": "2026-03-18T17:51:02+00:00",
        "comment_author": "thompsonbry",
        "comment_body": "refactored per suggestions",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3591744082",
    "pr_number": 4095,
    "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/utilities/sql_utils.py",
    "created_at": "2026-07-16T00:40:40+00:00",
    "commented_code": "'aws_pricing_get_products': 'pricing_products',\n     }\n \n-    return converters.get(operation_name)\n+    if operation_name in converters:\n+        return converters[operation_name]\n+\n+    # Compute Optimizer Automation list operations all return a uniform\n+    # {<list_key>: [...], count, next_token} shape; store one row per item so\n+    # the offloaded data stays queryable instead of a single JSON blob.\n+    if operation_name.startswith('compute_optimizer_automation_'):\n+        return 'records'\n+\n+    return None\n+\n+\n+def _extract_record_list(response: Dict[str, Any]) -> List[Dict[str, Any]]:\n+    \"\"\"Pull the item list out of a {<list_key>: [...], count, next_token} response.\n+\n+    Returns the first list-valued field (the item collection), ignoring the\n+    scalar pagination fields (`count`, `next_token`). Returns an empty list if\n+    the response carries no list field.\n+\n+    Args:\n+        response: A uniform list response.\n+\n+    Returns:\n+        The list of item dicts, or an empty list.\n+    \"\"\"\n+    for value in response.values():\n+        if isinstance(value, list):\n+            return value\n+    return []\n+\n+\n+def _record_columns(items: List[Dict[str, Any]]) -> List[str]:\n+    \"\"\"Derive the ordered column set for a record list from the union of item keys.\n+\n+    Keys are collected in first-seen order across all items so items with\n+    differing optional fields (e.g. rules with vs. without `criteria`) all fit\n+    one table. Non-dict items fall back to a single `value` column.\n+\n+    Column names are interpolated into the CREATE/INSERT SQL, so each is validated\n+    as a safe identifier (letters, numbers, underscores) \u2014 mirroring\n+    validate_table_name \u2014 to keep the module's injection-prevention model intact.\n+\n+    Args:\n+        items: The list of items to store.\n+\n+    Returns:\n+        The ordered list of column names (defaults to ['value'] if none found).\n+\n+    Raises:\n+        ValueError: If an item key is not a safe SQL identifier.\n+    \"\"\"\n+    columns: List[str] = []\n+    for item in items:\n+        if isinstance(item, dict):\n+            for key in item:\n+                if key not in columns:\n+                    if not isinstance(key, str) or not re.match(r'^[a-zA-Z0-9_]+$', key):\n+                        raise ValueError(f'Invalid column name for SQL table: {key!r}')",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3591744082",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4095,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/utilities/sql_utils.py",
        "discussion_id": "3591744082",
        "commented_code": "@@ -359,7 +359,66 @@ def _get_specialized_converter(operation_name: str) -> Optional[str]:\n         'aws_pricing_get_products': 'pricing_products',\n     }\n \n-    return converters.get(operation_name)\n+    if operation_name in converters:\n+        return converters[operation_name]\n+\n+    # Compute Optimizer Automation list operations all return a uniform\n+    # {<list_key>: [...], count, next_token} shape; store one row per item so\n+    # the offloaded data stays queryable instead of a single JSON blob.\n+    if operation_name.startswith('compute_optimizer_automation_'):\n+        return 'records'\n+\n+    return None\n+\n+\n+def _extract_record_list(response: Dict[str, Any]) -> List[Dict[str, Any]]:\n+    \"\"\"Pull the item list out of a {<list_key>: [...], count, next_token} response.\n+\n+    Returns the first list-valued field (the item collection), ignoring the\n+    scalar pagination fields (`count`, `next_token`). Returns an empty list if\n+    the response carries no list field.\n+\n+    Args:\n+        response: A uniform list response.\n+\n+    Returns:\n+        The list of item dicts, or an empty list.\n+    \"\"\"\n+    for value in response.values():\n+        if isinstance(value, list):\n+            return value\n+    return []\n+\n+\n+def _record_columns(items: List[Dict[str, Any]]) -> List[str]:\n+    \"\"\"Derive the ordered column set for a record list from the union of item keys.\n+\n+    Keys are collected in first-seen order across all items so items with\n+    differing optional fields (e.g. rules with vs. without `criteria`) all fit\n+    one table. Non-dict items fall back to a single `value` column.\n+\n+    Column names are interpolated into the CREATE/INSERT SQL, so each is validated\n+    as a safe identifier (letters, numbers, underscores) \u2014 mirroring\n+    validate_table_name \u2014 to keep the module's injection-prevention model intact.\n+\n+    Args:\n+        items: The list of items to store.\n+\n+    Returns:\n+        The ordered list of column names (defaults to ['value'] if none found).\n+\n+    Raises:\n+        ValueError: If an item key is not a safe SQL identifier.\n+    \"\"\"\n+    columns: List[str] = []\n+    for item in items:\n+        if isinstance(item, dict):\n+            for key in item:\n+                if key not in columns:\n+                    if not isinstance(key, str) or not re.match(r'^[a-zA-Z0-9_]+$', key):\n+                        raise ValueError(f'Invalid column name for SQL table: {key!r}')",
        "comment_created_at": "2026-07-16T00:40:40+00:00",
        "comment_author": "wangyuhere",
        "comment_body": "Do we also need to handle the reserved SQL keyword here?",
        "pr_file_module": null
      },
      {
        "comment_id": "3591843633",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4095,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/utilities/sql_utils.py",
        "discussion_id": "3591744082",
        "commented_code": "@@ -359,7 +359,66 @@ def _get_specialized_converter(operation_name: str) -> Optional[str]:\n         'aws_pricing_get_products': 'pricing_products',\n     }\n \n-    return converters.get(operation_name)\n+    if operation_name in converters:\n+        return converters[operation_name]\n+\n+    # Compute Optimizer Automation list operations all return a uniform\n+    # {<list_key>: [...], count, next_token} shape; store one row per item so\n+    # the offloaded data stays queryable instead of a single JSON blob.\n+    if operation_name.startswith('compute_optimizer_automation_'):\n+        return 'records'\n+\n+    return None\n+\n+\n+def _extract_record_list(response: Dict[str, Any]) -> List[Dict[str, Any]]:\n+    \"\"\"Pull the item list out of a {<list_key>: [...], count, next_token} response.\n+\n+    Returns the first list-valued field (the item collection), ignoring the\n+    scalar pagination fields (`count`, `next_token`). Returns an empty list if\n+    the response carries no list field.\n+\n+    Args:\n+        response: A uniform list response.\n+\n+    Returns:\n+        The list of item dicts, or an empty list.\n+    \"\"\"\n+    for value in response.values():\n+        if isinstance(value, list):\n+            return value\n+    return []\n+\n+\n+def _record_columns(items: List[Dict[str, Any]]) -> List[str]:\n+    \"\"\"Derive the ordered column set for a record list from the union of item keys.\n+\n+    Keys are collected in first-seen order across all items so items with\n+    differing optional fields (e.g. rules with vs. without `criteria`) all fit\n+    one table. Non-dict items fall back to a single `value` column.\n+\n+    Column names are interpolated into the CREATE/INSERT SQL, so each is validated\n+    as a safe identifier (letters, numbers, underscores) \u2014 mirroring\n+    validate_table_name \u2014 to keep the module's injection-prevention model intact.\n+\n+    Args:\n+        items: The list of items to store.\n+\n+    Returns:\n+        The ordered list of column names (defaults to ['value'] if none found).\n+\n+    Raises:\n+        ValueError: If an item key is not a safe SQL identifier.\n+    \"\"\"\n+    columns: List[str] = []\n+    for item in items:\n+        if isinstance(item, dict):\n+            for key in item:\n+                if key not in columns:\n+                    if not isinstance(key, str) or not re.match(r'^[a-zA-Z0-9_]+$', key):\n+                        raise ValueError(f'Invalid column name for SQL table: {key!r}')",
        "comment_created_at": "2026-07-16T01:04:19+00:00",
        "comment_author": "mattkelsey",
        "comment_body": "These values are based on API names, so an API would have to be named `select` or `group`, etc to hit this case.\r\n\r\nNot a problem for Automation APIs but in the probably a good idea to handle them regardless, will update.\r\n\r\nedit: Fixed!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3591723358",
    "pr_number": 4095,
    "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/utilities/sql_utils.py",
    "created_at": "2026-07-16T00:36:19+00:00",
    "commented_code": "'aws_pricing_get_products': 'pricing_products',\n     }\n \n-    return converters.get(operation_name)\n+    if operation_name in converters:\n+        return converters[operation_name]\n+\n+    # Compute Optimizer Automation list operations all return a uniform\n+    # {<list_key>: [...], count, next_token} shape; store one row per item so\n+    # the offloaded data stays queryable instead of a single JSON blob.\n+    if operation_name.startswith('compute_optimizer_automation_'):\n+        return 'records'",
    "repo_full_name": "awslabs/mcp",
    "discussion_comments": [
      {
        "comment_id": "3591723358",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4095,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/utilities/sql_utils.py",
        "discussion_id": "3591723358",
        "commented_code": "@@ -359,7 +359,66 @@ def _get_specialized_converter(operation_name: str) -> Optional[str]:\n         'aws_pricing_get_products': 'pricing_products',\n     }\n \n-    return converters.get(operation_name)\n+    if operation_name in converters:\n+        return converters[operation_name]\n+\n+    # Compute Optimizer Automation list operations all return a uniform\n+    # {<list_key>: [...], count, next_token} shape; store one row per item so\n+    # the offloaded data stays queryable instead of a single JSON blob.\n+    if operation_name.startswith('compute_optimizer_automation_'):\n+        return 'records'",
        "comment_created_at": "2026-07-16T00:36:19+00:00",
        "comment_author": "wangyuhere",
        "comment_body": "How reliable it is? Is it better to list each operation name explicitly?",
        "pr_file_module": null
      },
      {
        "comment_id": "3591779899",
        "repo_full_name": "awslabs/mcp",
        "pr_number": 4095,
        "pr_file": "src/billing-cost-management-mcp-server/awslabs/billing_cost_management_mcp_server/utilities/sql_utils.py",
        "discussion_id": "3591723358",
        "commented_code": "@@ -359,7 +359,66 @@ def _get_specialized_converter(operation_name: str) -> Optional[str]:\n         'aws_pricing_get_products': 'pricing_products',\n     }\n \n-    return converters.get(operation_name)\n+    if operation_name in converters:\n+        return converters[operation_name]\n+\n+    # Compute Optimizer Automation list operations all return a uniform\n+    # {<list_key>: [...], count, next_token} shape; store one row per item so\n+    # the offloaded data stays queryable instead of a single JSON blob.\n+    if operation_name.startswith('compute_optimizer_automation_'):\n+        return 'records'",
        "comment_created_at": "2026-07-16T00:49:52+00:00",
        "comment_author": "mattkelsey",
        "comment_body": "compute_optimizer_automation operation names are literals, so reliable as long as caller uses correctly\n\nMy intent is to allow any future list APIs to pass through this without needing another update",
        "pr_file_module": null
      }
    ]
  }
]