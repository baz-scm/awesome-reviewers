[
  {
    "discussion_id": "522529275",
    "pr_number": 2307,
    "pr_file": "src/simpleocv.cpp",
    "created_at": "2020-11-13T00:37:45+00:00",
    "commented_code": "#if NCNN_SIMPLEOCV\n \n #include <stdio.h>\n-\n+#if NCNN_SIMPLEOCV_STB\n+#define STB_IMAGE_IMPLEMENTATION\n+#include \"stb_image.h\"\n+#define STB_IMAGE_WRITE_IMPLEMENTATION\n+#include \"stb_image_write.h\"\n+#endif\n namespace cv {\n \n Mat imread(const std::string& path, int flags)\n {\n     (void)flags;\n-\n+    Mat m;\n     // read pgm/ppm\n-    FILE* fp = fopen(path.c_str(), \"rb\");\n-    if (!fp)\n-        return Mat();\n+    if (path.find(\".pgm\") != std::string::npos || path.find(\".ppm\") != std::string::npos)\n+    {\n+        FILE* fp = fopen(path.c_str(), \"rb\");\n+        if (!fp)\n+            return Mat();\n \n-    Mat m;\n+        Mat m;\n \n-    char magic[3];\n-    int w, h;\n-    int nscan = fscanf(fp, \"%2s\n%d %d\n255\n\", magic, &w, &h);\n-    if (nscan == 3 && magic[0] == 'P' && (magic[1] == '5' || magic[1] == '6'))\n-    {\n-        if (magic[1] == '5')\n+        char magic[3];\n+        int w, h;\n+        int nscan = fscanf(fp, \"%2s\n%d %d\n255\n\", magic, &w, &h);\n+        if (nscan == 3 && magic[0] == 'P' && (magic[1] == '5' || magic[1] == '6'))\n         {\n-            m.create(h, w, CV_8UC1);\n-        }\n-        else if (magic[1] == '6')\n-        {\n-            m.create(h, w, CV_8UC3);\n-        }\n-        if (m.empty())\n-        {\n-            fclose(fp);\n-            return Mat();\n+            if (magic[1] == '5')\n+            {\n+                m.create(h, w, CV_8UC1);\n+            }\n+            else if (magic[1] == '6')\n+            {\n+                m.create(h, w, CV_8UC3);\n+            }\n+            if (m.empty())\n+            {\n+                fclose(fp);\n+                return Mat();\n+            }\n+\n+            fread(m.data, 1, m.total(), fp);\n         }\n \n-        fread(m.data, 1, m.total(), fp);\n+        fclose(fp);\n+    }\n+    else\n+    {\n+#if NCNN_SIMPLEOCV_STB\n+        int w, h, c;\n+        unsigned char* data = stbi_load(path.c_str(), &w, &h, &c, 0);\n+        ncnn::Mat in = ncnn::Mat::from_pixels(data, ncnn::Mat::PIXEL_RGB, w, h);\n+        m.create(h, w, CV_8UC3);\n+        in.to_pixels(m.data, ncnn::Mat::PIXEL_RGB2BGR);\n+        stbi_image_free(data);\n+#endif\n     }\n-\n-    fclose(fp);\n \n     return m;\n }\n \n void imwrite(const std::string& path, const Mat& m)\n {\n     // write pgm/ppm\n-    FILE* fp = fopen(path.c_str(), \"wb\");\n-    if (!fp)\n-        return;\n-\n-    if (m.channels() == 1)\n+    if (path.find(\".pgm\") != std::string::npos || path.find(\".ppm\") != std::string::npos)\n     {\n-        fprintf(fp, \"P5\n%d %d\n255\n\", m.cols, m.rows);\n+        FILE* fp = fopen(path.c_str(), \"wb\");\n+        if (!fp)\n+            return;\n+\n+        if (m.channels() == 1)\n+        {\n+            fprintf(fp, \"P5\n%d %d\n255\n\", m.cols, m.rows);\n+        }\n+        else if (m.channels() == 3)\n+        {\n+            fprintf(fp, \"P6\n%d %d\n255\n\", m.cols, m.rows);\n+        }\n+\n+        fwrite(m.data, 1, m.total(), fp);\n+\n+        fclose(fp);\n     }\n-    else if (m.channels() == 3)\n+    else\n     {\n-        fprintf(fp, \"P6\n%d %d\n255\n\", m.cols, m.rows);\n+#if NCNN_SIMPLEOCV_STB\n+        if (path.find(\".jpg\") != std::string::npos)",
    "repo_full_name": "Tencent/ncnn",
    "discussion_comments": [
      {
        "comment_id": "522529275",
        "repo_full_name": "Tencent/ncnn",
        "pr_number": 2307,
        "pr_file": "src/simpleocv.cpp",
        "discussion_id": "522529275",
        "commented_code": "@@ -17,66 +17,99 @@\n #if NCNN_SIMPLEOCV\n \n #include <stdio.h>\n-\n+#if NCNN_SIMPLEOCV_STB\n+#define STB_IMAGE_IMPLEMENTATION\n+#include \"stb_image.h\"\n+#define STB_IMAGE_WRITE_IMPLEMENTATION\n+#include \"stb_image_write.h\"\n+#endif\n namespace cv {\n \n Mat imread(const std::string& path, int flags)\n {\n     (void)flags;\n-\n+    Mat m;\n     // read pgm/ppm\n-    FILE* fp = fopen(path.c_str(), \"rb\");\n-    if (!fp)\n-        return Mat();\n+    if (path.find(\".pgm\") != std::string::npos || path.find(\".ppm\") != std::string::npos)\n+    {\n+        FILE* fp = fopen(path.c_str(), \"rb\");\n+        if (!fp)\n+            return Mat();\n \n-    Mat m;\n+        Mat m;\n \n-    char magic[3];\n-    int w, h;\n-    int nscan = fscanf(fp, \"%2s\\n%d %d\\n255\\n\", magic, &w, &h);\n-    if (nscan == 3 && magic[0] == 'P' && (magic[1] == '5' || magic[1] == '6'))\n-    {\n-        if (magic[1] == '5')\n+        char magic[3];\n+        int w, h;\n+        int nscan = fscanf(fp, \"%2s\\n%d %d\\n255\\n\", magic, &w, &h);\n+        if (nscan == 3 && magic[0] == 'P' && (magic[1] == '5' || magic[1] == '6'))\n         {\n-            m.create(h, w, CV_8UC1);\n-        }\n-        else if (magic[1] == '6')\n-        {\n-            m.create(h, w, CV_8UC3);\n-        }\n-        if (m.empty())\n-        {\n-            fclose(fp);\n-            return Mat();\n+            if (magic[1] == '5')\n+            {\n+                m.create(h, w, CV_8UC1);\n+            }\n+            else if (magic[1] == '6')\n+            {\n+                m.create(h, w, CV_8UC3);\n+            }\n+            if (m.empty())\n+            {\n+                fclose(fp);\n+                return Mat();\n+            }\n+\n+            fread(m.data, 1, m.total(), fp);\n         }\n \n-        fread(m.data, 1, m.total(), fp);\n+        fclose(fp);\n+    }\n+    else\n+    {\n+#if NCNN_SIMPLEOCV_STB\n+        int w, h, c;\n+        unsigned char* data = stbi_load(path.c_str(), &w, &h, &c, 0);\n+        ncnn::Mat in = ncnn::Mat::from_pixels(data, ncnn::Mat::PIXEL_RGB, w, h);\n+        m.create(h, w, CV_8UC3);\n+        in.to_pixels(m.data, ncnn::Mat::PIXEL_RGB2BGR);\n+        stbi_image_free(data);\n+#endif\n     }\n-\n-    fclose(fp);\n \n     return m;\n }\n \n void imwrite(const std::string& path, const Mat& m)\n {\n     // write pgm/ppm\n-    FILE* fp = fopen(path.c_str(), \"wb\");\n-    if (!fp)\n-        return;\n-\n-    if (m.channels() == 1)\n+    if (path.find(\".pgm\") != std::string::npos || path.find(\".ppm\") != std::string::npos)\n     {\n-        fprintf(fp, \"P5\\n%d %d\\n255\\n\", m.cols, m.rows);\n+        FILE* fp = fopen(path.c_str(), \"wb\");\n+        if (!fp)\n+            return;\n+\n+        if (m.channels() == 1)\n+        {\n+            fprintf(fp, \"P5\\n%d %d\\n255\\n\", m.cols, m.rows);\n+        }\n+        else if (m.channels() == 3)\n+        {\n+            fprintf(fp, \"P6\\n%d %d\\n255\\n\", m.cols, m.rows);\n+        }\n+\n+        fwrite(m.data, 1, m.total(), fp);\n+\n+        fclose(fp);\n     }\n-    else if (m.channels() == 3)\n+    else\n     {\n-        fprintf(fp, \"P6\\n%d %d\\n255\\n\", m.cols, m.rows);\n+#if NCNN_SIMPLEOCV_STB\n+        if (path.find(\".jpg\") != std::string::npos)",
        "comment_created_at": "2020-11-13T00:37:45+00:00",
        "comment_author": "zchrissirhcz",
        "comment_body": "lena.jpg.png \u8fd9\u6837\u7684\u6587\u4ef6\u540d\u8be5\u7528\u4ec0\u4e48\u65b9\u5f0f\u7f16\u89e3\u7801\u5462\uff1f",
        "pr_file_module": null
      },
      {
        "comment_id": "522531102",
        "repo_full_name": "Tencent/ncnn",
        "pr_number": 2307,
        "pr_file": "src/simpleocv.cpp",
        "discussion_id": "522529275",
        "commented_code": "@@ -17,66 +17,99 @@\n #if NCNN_SIMPLEOCV\n \n #include <stdio.h>\n-\n+#if NCNN_SIMPLEOCV_STB\n+#define STB_IMAGE_IMPLEMENTATION\n+#include \"stb_image.h\"\n+#define STB_IMAGE_WRITE_IMPLEMENTATION\n+#include \"stb_image_write.h\"\n+#endif\n namespace cv {\n \n Mat imread(const std::string& path, int flags)\n {\n     (void)flags;\n-\n+    Mat m;\n     // read pgm/ppm\n-    FILE* fp = fopen(path.c_str(), \"rb\");\n-    if (!fp)\n-        return Mat();\n+    if (path.find(\".pgm\") != std::string::npos || path.find(\".ppm\") != std::string::npos)\n+    {\n+        FILE* fp = fopen(path.c_str(), \"rb\");\n+        if (!fp)\n+            return Mat();\n \n-    Mat m;\n+        Mat m;\n \n-    char magic[3];\n-    int w, h;\n-    int nscan = fscanf(fp, \"%2s\\n%d %d\\n255\\n\", magic, &w, &h);\n-    if (nscan == 3 && magic[0] == 'P' && (magic[1] == '5' || magic[1] == '6'))\n-    {\n-        if (magic[1] == '5')\n+        char magic[3];\n+        int w, h;\n+        int nscan = fscanf(fp, \"%2s\\n%d %d\\n255\\n\", magic, &w, &h);\n+        if (nscan == 3 && magic[0] == 'P' && (magic[1] == '5' || magic[1] == '6'))\n         {\n-            m.create(h, w, CV_8UC1);\n-        }\n-        else if (magic[1] == '6')\n-        {\n-            m.create(h, w, CV_8UC3);\n-        }\n-        if (m.empty())\n-        {\n-            fclose(fp);\n-            return Mat();\n+            if (magic[1] == '5')\n+            {\n+                m.create(h, w, CV_8UC1);\n+            }\n+            else if (magic[1] == '6')\n+            {\n+                m.create(h, w, CV_8UC3);\n+            }\n+            if (m.empty())\n+            {\n+                fclose(fp);\n+                return Mat();\n+            }\n+\n+            fread(m.data, 1, m.total(), fp);\n         }\n \n-        fread(m.data, 1, m.total(), fp);\n+        fclose(fp);\n+    }\n+    else\n+    {\n+#if NCNN_SIMPLEOCV_STB\n+        int w, h, c;\n+        unsigned char* data = stbi_load(path.c_str(), &w, &h, &c, 0);\n+        ncnn::Mat in = ncnn::Mat::from_pixels(data, ncnn::Mat::PIXEL_RGB, w, h);\n+        m.create(h, w, CV_8UC3);\n+        in.to_pixels(m.data, ncnn::Mat::PIXEL_RGB2BGR);\n+        stbi_image_free(data);\n+#endif\n     }\n-\n-    fclose(fp);\n \n     return m;\n }\n \n void imwrite(const std::string& path, const Mat& m)\n {\n     // write pgm/ppm\n-    FILE* fp = fopen(path.c_str(), \"wb\");\n-    if (!fp)\n-        return;\n-\n-    if (m.channels() == 1)\n+    if (path.find(\".pgm\") != std::string::npos || path.find(\".ppm\") != std::string::npos)\n     {\n-        fprintf(fp, \"P5\\n%d %d\\n255\\n\", m.cols, m.rows);\n+        FILE* fp = fopen(path.c_str(), \"wb\");\n+        if (!fp)\n+            return;\n+\n+        if (m.channels() == 1)\n+        {\n+            fprintf(fp, \"P5\\n%d %d\\n255\\n\", m.cols, m.rows);\n+        }\n+        else if (m.channels() == 3)\n+        {\n+            fprintf(fp, \"P6\\n%d %d\\n255\\n\", m.cols, m.rows);\n+        }\n+\n+        fwrite(m.data, 1, m.total(), fp);\n+\n+        fclose(fp);\n     }\n-    else if (m.channels() == 3)\n+    else\n     {\n-        fprintf(fp, \"P6\\n%d %d\\n255\\n\", m.cols, m.rows);\n+#if NCNN_SIMPLEOCV_STB\n+        if (path.find(\".jpg\") != std::string::npos)",
        "comment_created_at": "2020-11-13T00:43:41+00:00",
        "comment_author": "ncnnnnn",
        "comment_body": "\u5224\u65ad\u6700\u540e\u56db\u4e2a\u5427\u3002\r\n\u540e\u7f00\u540d\u5224\u65ad\u4e0d\u5927\u5408\u7406\uff0c\u5e94\u8be5\u8bfb\u6587\u4ef6\u5934\u3002\u3002\r\n\u4f30\u8ba1up\u4e5f\u4e0d\u4f1a\u5141\u8bb8\u585e\u592a\u591a\u7b2c\u4e09\u65b9\u5e93\uff0c\u81ea\u5df1\u5f04\u6ca1\u8fd9\u80fd\u529b\u3002\u3002\u3002\r\n\r\n\u554acv\u8dd1example\u7684\u3002\u3002\u6ee1\u8bd5\u4e00\u8bd5prpr\u3002 \uff1a\uff09",
        "pr_file_module": null
      }
    ]
  }
]