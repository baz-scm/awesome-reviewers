[
  {
    "discussion_id": "3164123725",
    "pr_number": 3170,
    "pr_file": "src/openai/lib/aws.py",
    "created_at": "2026-04-29T20:58:31+00:00",
    "commented_code": "+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "3164123725",
        "repo_full_name": "openai/openai-python",
        "pr_number": 3170,
        "pr_file": "src/openai/lib/aws.py",
        "discussion_id": "3164123725",
        "commented_code": "@@ -0,0 +1,345 @@\n+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")",
        "comment_created_at": "2026-04-29T20:58:31+00:00",
        "comment_author": "3coins",
        "comment_body": "Should we make these **OpenAIError**, so all credential errors raise the same error?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3164124188",
    "pr_number": 3170,
    "pr_file": "src/openai/lib/aws.py",
    "created_at": "2026-04-29T20:58:37+00:00",
    "commented_code": "+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")\n+    resolved_region = resolved_region or \"\"\n+\n+    # Resolve base_url \u2014 fall back to region-derived endpoint\n+    if base_url is None:\n+        if not resolved_region:\n+            raise ValueError(\"Must provide base_url, or set region / AWS_REGION / AWS_DEFAULT_REGION\")",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "3164124188",
        "repo_full_name": "openai/openai-python",
        "pr_number": 3170,
        "pr_file": "src/openai/lib/aws.py",
        "discussion_id": "3164124188",
        "commented_code": "@@ -0,0 +1,345 @@\n+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")\n+    resolved_region = resolved_region or \"\"\n+\n+    # Resolve base_url \u2014 fall back to region-derived endpoint\n+    if base_url is None:\n+        if not resolved_region:\n+            raise ValueError(\"Must provide base_url, or set region / AWS_REGION / AWS_DEFAULT_REGION\")",
        "comment_created_at": "2026-04-29T20:58:37+00:00",
        "comment_author": "3coins",
        "comment_body": "Should we make these **OpenAIError**, so all credential errors raise the same error?",
        "pr_file_module": null
      },
      {
        "comment_id": "3185065577",
        "repo_full_name": "openai/openai-python",
        "pr_number": 3170,
        "pr_file": "src/openai/lib/aws.py",
        "discussion_id": "3164124188",
        "commented_code": "@@ -0,0 +1,345 @@\n+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")\n+    resolved_region = resolved_region or \"\"\n+\n+    # Resolve base_url \u2014 fall back to region-derived endpoint\n+    if base_url is None:\n+        if not resolved_region:\n+            raise ValueError(\"Must provide base_url, or set region / AWS_REGION / AWS_DEFAULT_REGION\")",
        "comment_created_at": "2026-05-04T23:10:35+00:00",
        "comment_author": "prashanthkvs",
        "comment_body": "Updated.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3164206051",
    "pr_number": 3170,
    "pr_file": "src/openai/lib/aws.py",
    "created_at": "2026-04-29T21:13:56+00:00",
    "commented_code": "+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")\n+    resolved_region = resolved_region or \"\"\n+\n+    # Resolve base_url \u2014 fall back to region-derived endpoint\n+    if base_url is None:\n+        if not resolved_region:\n+            raise ValueError(\"Must provide base_url, or set region / AWS_REGION / AWS_DEFAULT_REGION\")\n+        base_url = f\"https://bedrock-mantle.{resolved_region}.api.aws/v1\"\n+\n+    # Resolve botocore credentials if needed\n+    botocore_credentials: Any = None\n+    if use_sigv4 and credential_provider is None:\n+        botocore_credentials = _get_default_credentials()",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "3164206051",
        "repo_full_name": "openai/openai-python",
        "pr_number": 3170,
        "pr_file": "src/openai/lib/aws.py",
        "discussion_id": "3164206051",
        "commented_code": "@@ -0,0 +1,345 @@\n+from __future__ import annotations\n+\n+import os\n+import inspect\n+from typing import Any, Union, Mapping, Callable, Awaitable\n+from typing_extensions import Self, override\n+\n+import httpx\n+\n+from .._types import NOT_GIVEN, Timeout, NotGiven\n+from .._client import OpenAI, AsyncOpenAI\n+from .._exceptions import OpenAIError\n+from .._base_client import DEFAULT_MAX_RETRIES\n+\n+# Sentinel API key used when SigV4 mode is active, so the base OpenAI\n+# constructor (which requires a non-None api_key) is satisfied.\n+API_KEY_SENTINEL = \"<bedrock-mantle-sigv4>\"\n+\n+# A credential provider is a callable that returns a botocore-compatible\n+# credentials object (with access_key, secret_key, token attributes).\n+CredentialProvider = Callable[[], Any]\n+AsyncCredentialProvider = Callable[[], \"Union[Any, Awaitable[Any]]\"]\n+\n+\n+def _ensure_botocore() -> None:\n+    \"\"\"Raise OpenAIError if botocore is not installed.\"\"\"\n+    try:\n+        import botocore  # type: ignore[import-untyped]  # noqa: F401  # pyright: ignore[reportMissingTypeStubs, reportUnusedImport]\n+    except ImportError as err:\n+        raise OpenAIError(\n+            \"botocore must be installed for SigV4 authentication. Install it with: pip install 'openai[aws]'\"\n+        ) from err\n+\n+\n+def _get_default_credentials() -> Any:\n+    \"\"\"Resolve the botocore credentials object via the default credential chain.\n+\n+    Returns the unfrozen botocore credentials object so that RefreshableCredentials\n+    (e.g. from IAM roles, EC2 instance profiles, ECS task roles) can auto-refresh\n+    on subsequent access. Raises OpenAIError if botocore is not installed or\n+    credentials cannot be resolved.\n+    \"\"\"\n+    _ensure_botocore()\n+    import botocore.session  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    session = botocore.session.get_session()  # pyright: ignore[reportUnknownMemberType]\n+    creds = session.get_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if creds is None:\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    # Validate that credentials are usable by checking a frozen snapshot,\n+    # but return the unfrozen object so RefreshableCredentials can auto-refresh.\n+    frozen = creds.get_frozen_credentials()  # pyright: ignore[reportUnknownVariableType, reportUnknownMemberType]\n+    if not frozen.access_key or not frozen.secret_key:  # pyright: ignore[reportUnknownMemberType]\n+        raise OpenAIError(\"Could not resolve AWS credentials from the default botocore credential chain.\")\n+    return creds  # pyright: ignore[reportUnknownVariableType]\n+\n+\n+def _sign_httpx_request(\n+    request: httpx.Request,\n+    credentials: Any,\n+    region: str,\n+    service: str = \"bedrock\",\n+) -> None:\n+    \"\"\"Sign an httpx.Request in-place using botocore's SigV4Auth.\"\"\"\n+    import botocore.auth  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+    import botocore.awsrequest  # type: ignore[import-untyped]  # pyright: ignore[reportMissingTypeStubs]\n+\n+    # Exclude httpx transport-level headers that cause SigV4 signature mismatch\n+    _HEADERS_TO_EXCLUDE = {\"connection\", \"accept-encoding\"}\n+    clean_headers = {k: v for k, v in request.headers.items() if k.lower() not in _HEADERS_TO_EXCLUDE}\n+\n+    # Convert httpx.Request \u2192 botocore.awsrequest.AWSRequest\n+    aws_request = botocore.awsrequest.AWSRequest(\n+        method=request.method,\n+        url=str(request.url),\n+        headers=clean_headers,\n+        data=request.content,\n+    )\n+\n+    signer = botocore.auth.SigV4Auth(credentials, service, region)\n+    signer.add_auth(aws_request)  # pyright: ignore[reportUnknownMemberType]\n+\n+    # Copy signed headers back into the httpx request\n+    for key, value in aws_request.headers.items():\n+        request.headers[key] = value\n+\n+\n+# ---------------------------------------------------------------------------\n+# Shared init / credential resolution logic\n+# ---------------------------------------------------------------------------\n+\n+\n+def _resolve_bedrock_mantle_config(\n+    *,\n+    api_key: str | None,\n+    credential_provider: Any | None,\n+    region: str | None,\n+    base_url: str | None,\n+) -> tuple[bool, Any | None, str, str, Any | None]:\n+    \"\"\"Shared constructor logic for both sync and async clients.\n+\n+    Returns (use_sigv4, credential_provider, region, base_url, botocore_credentials).\n+    \"\"\"\n+    # Normalize: treat the sentinel as \"no api_key provided\"\n+    if api_key == API_KEY_SENTINEL:\n+        api_key = None\n+\n+    if api_key is not None and credential_provider is not None:\n+        raise OpenAIError(\"api_key and credential_provider are mutually exclusive\")\n+\n+    # Determine auth mode\n+    if api_key is not None:\n+        use_sigv4 = False\n+        credential_provider = None\n+    else:\n+        use_sigv4 = True\n+\n+    # Resolve region (needed for SigV4 and base_url fallback)\n+    resolved_region = region or os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\")\n+    if use_sigv4 and not resolved_region:\n+        raise ValueError(\"Must provide region or set AWS_REGION / AWS_DEFAULT_REGION environment variable\")\n+    resolved_region = resolved_region or \"\"\n+\n+    # Resolve base_url \u2014 fall back to region-derived endpoint\n+    if base_url is None:\n+        if not resolved_region:\n+            raise ValueError(\"Must provide base_url, or set region / AWS_REGION / AWS_DEFAULT_REGION\")\n+        base_url = f\"https://bedrock-mantle.{resolved_region}.api.aws/v1\"\n+\n+    # Resolve botocore credentials if needed\n+    botocore_credentials: Any = None\n+    if use_sigv4 and credential_provider is None:\n+        botocore_credentials = _get_default_credentials()",
        "comment_created_at": "2026-04-29T21:13:56+00:00",
        "comment_author": "3coins",
        "comment_body": "The PR description says \u201cCredentials are resolved at request time,\u201d but credentials are actually resolved before the first request. This means that client construction can fail or block if AWS credentials are temporarily unavailable, metadata credentials are slow or a user only expected credential lookup when making a request. \n\nShould we rather do credential lookup inside _prepare_request() and constructor only verifies botocore is available? The same behavior might apply to _resolve_credentials_async() and tests to verify them.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2872454416",
    "pr_number": 2917,
    "pr_file": "src/openai/_exceptions.py",
    "created_at": "2026-03-02T13:40:36+00:00",
    "commented_code": ")\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "2872521428",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/_exceptions.py",
        "discussion_id": "2872454416",
        "commented_code": "@@ -157,5 +158,24 @@ def __init__(self) -> None:\n         )\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.",
        "comment_created_at": "2026-03-02T13:40:36+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Good catch \u2014 fixed in 81f3e5b. Updated the docstring to cover both Pydantic `BaseModel` and dataclass-like types validated via `pydantic.TypeAdapter`.",
        "pr_file_module": null
      },
      {
        "comment_id": "2873081001",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/_exceptions.py",
        "discussion_id": "2872454416",
        "commented_code": "@@ -157,5 +158,24 @@ def __init__(self) -> None:\n         )\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.",
        "comment_created_at": "2026-03-02T15:29:08+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Addressed in commit 24438b0c: `ContentFormatError` now carries `expected_response_format`, no longer manually sets `__cause__`, avoids embedding raw response content in its message, and docstrings were updated to reflect both BaseModel and TypeAdapter-supported formats.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2872454390",
    "pr_number": 2917,
    "pr_file": "src/openai/_exceptions.py",
    "created_at": "2026-03-02T13:40:36+00:00",
    "commented_code": ")\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.\n+    \"\"\"\n+\n+    raw_content: str\n+    \"\"\"The raw content string returned by the API that failed to parse.\"\"\"\n+\n+    def __init__(self, *, raw_content: str, error: Exception) -> None:\n+        super().__init__(\n+            f\"Could not parse response content as the response did not match the expected format: {error}. \"\n+            f\"Raw content: {raw_content!r}\"\n+        )\n+        self.raw_content = raw_content\n+        self.__cause__ = error",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "2872521443",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/_exceptions.py",
        "discussion_id": "2872454390",
        "commented_code": "@@ -157,5 +158,24 @@ def __init__(self) -> None:\n         )\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.\n+    \"\"\"\n+\n+    raw_content: str\n+    \"\"\"The raw content string returned by the API that failed to parse.\"\"\"\n+\n+    def __init__(self, *, raw_content: str, error: Exception) -> None:\n+        super().__init__(\n+            f\"Could not parse response content as the response did not match the expected format: {error}. \"\n+            f\"Raw content: {raw_content!r}\"\n+        )\n+        self.raw_content = raw_content\n+        self.__cause__ = error",
        "comment_created_at": "2026-03-02T13:40:36+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Good catch \u2014 fixed in 81f3e5b. Removed the redundant `self.__cause__` assignment since `raise ... from exc` already handles exception chaining. The original exception is now stored on a dedicated `.error` attribute for explicit programmatic access.",
        "pr_file_module": null
      },
      {
        "comment_id": "2873080803",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/_exceptions.py",
        "discussion_id": "2872454390",
        "commented_code": "@@ -157,5 +158,24 @@ def __init__(self) -> None:\n         )\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.\n+    \"\"\"\n+\n+    raw_content: str\n+    \"\"\"The raw content string returned by the API that failed to parse.\"\"\"\n+\n+    def __init__(self, *, raw_content: str, error: Exception) -> None:\n+        super().__init__(\n+            f\"Could not parse response content as the response did not match the expected format: {error}. \"\n+            f\"Raw content: {raw_content!r}\"\n+        )\n+        self.raw_content = raw_content\n+        self.__cause__ = error",
        "comment_created_at": "2026-03-02T15:29:05+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Addressed in commit 24438b0c: `ContentFormatError` now carries `expected_response_format`, no longer manually sets `__cause__`, avoids embedding raw response content in its message, and docstrings were updated to reflect both BaseModel and TypeAdapter-supported formats.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2872454337",
    "pr_number": 2917,
    "pr_file": "src/openai/_exceptions.py",
    "created_at": "2026-03-02T13:40:36+00:00",
    "commented_code": ")\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.\n+    \"\"\"\n+\n+    raw_content: str\n+    \"\"\"The raw content string returned by the API that failed to parse.\"\"\"\n+\n+    def __init__(self, *, raw_content: str, error: Exception) -> None:\n+        super().__init__(\n+            f\"Could not parse response content as the response did not match the expected format: {error}. \"\n+            f\"Raw content: {raw_content!r}\"",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "2872521446",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/_exceptions.py",
        "discussion_id": "2872454337",
        "commented_code": "@@ -157,5 +158,24 @@ def __init__(self) -> None:\n         )\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.\n+    \"\"\"\n+\n+    raw_content: str\n+    \"\"\"The raw content string returned by the API that failed to parse.\"\"\"\n+\n+    def __init__(self, *, raw_content: str, error: Exception) -> None:\n+        super().__init__(\n+            f\"Could not parse response content as the response did not match the expected format: {error}. \"\n+            f\"Raw content: {raw_content!r}\"",
        "comment_created_at": "2026-03-02T13:40:36+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Agreed \u2014 fixed in 81f3e5b. The error message now truncates `raw_content` to 500 characters with a `... (truncated)` suffix. The full content remains available via the `.raw_content` attribute for programmatic access.",
        "pr_file_module": null
      },
      {
        "comment_id": "2873080522",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/_exceptions.py",
        "discussion_id": "2872454337",
        "commented_code": "@@ -157,5 +158,24 @@ def __init__(self) -> None:\n         )\n \n \n+class ContentFormatError(OpenAIError):\n+    \"\"\"Raised when the API returns content that cannot be parsed into the expected response format.\n+\n+    This typically happens when the model returns malformed or truncated JSON that\n+    does not match the expected pydantic model schema.\n+    \"\"\"\n+\n+    raw_content: str\n+    \"\"\"The raw content string returned by the API that failed to parse.\"\"\"\n+\n+    def __init__(self, *, raw_content: str, error: Exception) -> None:\n+        super().__init__(\n+            f\"Could not parse response content as the response did not match the expected format: {error}. \"\n+            f\"Raw content: {raw_content!r}\"",
        "comment_created_at": "2026-03-02T15:29:03+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Addressed in commit 24438b0c: `ContentFormatError` now carries `expected_response_format`, no longer manually sets `__cause__`, avoids embedding raw response content in its message, and docstrings were updated to reflect both BaseModel and TypeAdapter-supported formats.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2872454251",
    "pr_number": 2917,
    "pr_file": "src/openai/lib/_parsing/_completions.py",
    "created_at": "2026-03-02T13:40:37+00:00",
    "commented_code": "def _parse_content(response_format: type[ResponseFormatT], content: str) -> ResponseFormatT:\n-    if is_basemodel_type(response_format):\n-        return cast(ResponseFormatT, model_parse_json(response_format, content))\n+    try:\n+        if is_basemodel_type(response_format):\n+            return cast(ResponseFormatT, model_parse_json(response_format, content))\n \n-    if is_dataclass_like_type(response_format):\n-        if PYDANTIC_V1:\n-            raise TypeError(f\"Non BaseModel types are only supported with Pydantic v2 - {response_format}\")\n+        if is_dataclass_like_type(response_format):\n+            if PYDANTIC_V1:\n+                raise TypeError(f\"Non BaseModel types are only supported with Pydantic v2 - {response_format}\")\n \n-        return pydantic.TypeAdapter(response_format).validate_json(content)\n+            return pydantic.TypeAdapter(response_format).validate_json(content)\n+    except (pydantic.ValidationError, json.JSONDecodeError) as exc:\n+        raise ContentFormatError(raw_content=content, error=exc) from exc",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "2872521457",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/lib/_parsing/_completions.py",
        "discussion_id": "2872454251",
        "commented_code": "@@ -241,14 +241,17 @@ def is_parseable_tool(input_tool: ChatCompletionToolUnionParam) -> bool:\n \n \n def _parse_content(response_format: type[ResponseFormatT], content: str) -> ResponseFormatT:\n-    if is_basemodel_type(response_format):\n-        return cast(ResponseFormatT, model_parse_json(response_format, content))\n+    try:\n+        if is_basemodel_type(response_format):\n+            return cast(ResponseFormatT, model_parse_json(response_format, content))\n \n-    if is_dataclass_like_type(response_format):\n-        if PYDANTIC_V1:\n-            raise TypeError(f\"Non BaseModel types are only supported with Pydantic v2 - {response_format}\")\n+        if is_dataclass_like_type(response_format):\n+            if PYDANTIC_V1:\n+                raise TypeError(f\"Non BaseModel types are only supported with Pydantic v2 - {response_format}\")\n \n-        return pydantic.TypeAdapter(response_format).validate_json(content)\n+            return pydantic.TypeAdapter(response_format).validate_json(content)\n+    except (pydantic.ValidationError, json.JSONDecodeError) as exc:\n+        raise ContentFormatError(raw_content=content, error=exc) from exc",
        "comment_created_at": "2026-03-02T13:40:37+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Good point. The error message already includes the pydantic `ValidationError` (or `JSONDecodeError`) which describes the expected schema and what didn't match, and the `raw_content` attribute gives direct access to the original content. Adding the `response_format` type name would add marginal value given that context \u2014 the caller already knows which type they passed. Keeping it simple for now, but open to revisiting if real-world usage shows it's needed.",
        "pr_file_module": null
      },
      {
        "comment_id": "2873080225",
        "repo_full_name": "openai/openai-python",
        "pr_number": 2917,
        "pr_file": "src/openai/lib/_parsing/_completions.py",
        "discussion_id": "2872454251",
        "commented_code": "@@ -241,14 +241,17 @@ def is_parseable_tool(input_tool: ChatCompletionToolUnionParam) -> bool:\n \n \n def _parse_content(response_format: type[ResponseFormatT], content: str) -> ResponseFormatT:\n-    if is_basemodel_type(response_format):\n-        return cast(ResponseFormatT, model_parse_json(response_format, content))\n+    try:\n+        if is_basemodel_type(response_format):\n+            return cast(ResponseFormatT, model_parse_json(response_format, content))\n \n-    if is_dataclass_like_type(response_format):\n-        if PYDANTIC_V1:\n-            raise TypeError(f\"Non BaseModel types are only supported with Pydantic v2 - {response_format}\")\n+        if is_dataclass_like_type(response_format):\n+            if PYDANTIC_V1:\n+                raise TypeError(f\"Non BaseModel types are only supported with Pydantic v2 - {response_format}\")\n \n-        return pydantic.TypeAdapter(response_format).validate_json(content)\n+            return pydantic.TypeAdapter(response_format).validate_json(content)\n+    except (pydantic.ValidationError, json.JSONDecodeError) as exc:\n+        raise ContentFormatError(raw_content=content, error=exc) from exc",
        "comment_created_at": "2026-03-02T15:29:00+00:00",
        "comment_author": "giulio-leone",
        "comment_body": "Addressed in commit 24438b0c: `ContentFormatError` now carries `expected_response_format`, no longer manually sets `__cause__`, avoids embedding raw response content in its message, and docstrings were updated to reflect both BaseModel and TypeAdapter-supported formats.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1220084236",
    "pr_number": 439,
    "pr_file": "openai/api_requestor.py",
    "created_at": "2023-06-06T17:57:33+00:00",
    "commented_code": "str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if predicate(response):\n+            message, code = (None, None) if 'error' not in response.data else (\n+                response.data['error']['message'] if 'message' in response.data['error'] else None,\n+                response.data['error']['code'] if 'code' in response.data['error'] else None\n+            )\n+            message = message if message is not None else 'Operation failed'\n+            raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        while not until(response):",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "1220084236",
        "repo_full_name": "openai/openai-python",
        "pr_number": 439,
        "pr_file": "openai/api_requestor.py",
        "discussion_id": "1220084236",
        "commented_code": "@@ -149,6 +151,63 @@ def format_app_info(cls, info):\n             str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if predicate(response):\n+            message, code = (None, None) if 'error' not in response.data else (\n+                response.data['error']['message'] if 'message' in response.data['error'] else None,\n+                response.data['error']['code'] if 'code' in response.data['error'] else None\n+            )\n+            message = message if message is not None else 'Operation failed'\n+            raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        while not until(response):",
        "comment_created_at": "2023-06-06T17:57:33+00:00",
        "comment_author": "jhallard",
        "comment_body": "There's a chance for an infinite loop here unless the Azure APIs guarantee that the `status` of a request always goes into either `succeeded` or `failed`. If you confirm that guarantee then this is fine",
        "pr_file_module": null
      },
      {
        "comment_id": "1220093152",
        "repo_full_name": "openai/openai-python",
        "pr_number": 439,
        "pr_file": "openai/api_requestor.py",
        "discussion_id": "1220084236",
        "commented_code": "@@ -149,6 +151,63 @@ def format_app_info(cls, info):\n             str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if predicate(response):\n+            message, code = (None, None) if 'error' not in response.data else (\n+                response.data['error']['message'] if 'message' in response.data['error'] else None,\n+                response.data['error']['code'] if 'code' in response.data['error'] else None\n+            )\n+            message = message if message is not None else 'Operation failed'\n+            raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        while not until(response):",
        "comment_created_at": "2023-06-06T18:06:54+00:00",
        "comment_author": "johanste",
        "comment_body": "We should add a timeout to the API/loop - but I can confirm that all long running/polling Azure APIs are supposed to end up in one of (potentially) three states (also known as terminal states in Azure) - succeeded, failed or cancelled (the latter is only for cancellable APIs).",
        "pr_file_module": null
      },
      {
        "comment_id": "1220193110",
        "repo_full_name": "openai/openai-python",
        "pr_number": 439,
        "pr_file": "openai/api_requestor.py",
        "discussion_id": "1220084236",
        "commented_code": "@@ -149,6 +151,63 @@ def format_app_info(cls, info):\n             str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if predicate(response):\n+            message, code = (None, None) if 'error' not in response.data else (\n+                response.data['error']['message'] if 'message' in response.data['error'] else None,\n+                response.data['error']['code'] if 'code' in response.data['error'] else None\n+            )\n+            message = message if message is not None else 'Operation failed'\n+            raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        while not until(response):",
        "comment_created_at": "2023-06-06T19:21:07+00:00",
        "comment_author": "glecaros",
        "comment_body": "added a timeout (using the value already defined in api_requestor.py)",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1220308687",
    "pr_number": 439,
    "pr_file": "openai/api_requestor.py",
    "created_at": "2023-06-06T20:36:54+00:00",
    "commented_code": "str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if not predicate(response):\n+            return\n+        error_data = response.data['error']\n+        message = error_data.get('message', 'Operation failed')\n+        code = error_data.get('code')\n+        raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        start_time = time.time()\n+        while not until(response) and time.time() - start_time < TIMEOUT_SECS:",
    "repo_full_name": "openai/openai-python",
    "discussion_comments": [
      {
        "comment_id": "1220308687",
        "repo_full_name": "openai/openai-python",
        "pr_number": 439,
        "pr_file": "openai/api_requestor.py",
        "discussion_id": "1220308687",
        "commented_code": "@@ -149,6 +151,64 @@ def format_app_info(cls, info):\n             str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if not predicate(response):\n+            return\n+        error_data = response.data['error']\n+        message = error_data.get('message', 'Operation failed')\n+        code = error_data.get('code')\n+        raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        start_time = time.time()\n+        while not until(response) and time.time() - start_time < TIMEOUT_SECS:",
        "comment_created_at": "2023-06-06T20:36:54+00:00",
        "comment_author": "jhallard",
        "comment_body": "I'm pushing back a bit but these are mostly nits, feel free to steamroll them if you have stronger guarantees about the API response than I know about. But, if the timeout fails and we break the loop, the following line to index into `response.data['result']` would raise a `KeyError`. ",
        "pr_file_module": null
      },
      {
        "comment_id": "1220364647",
        "repo_full_name": "openai/openai-python",
        "pr_number": 439,
        "pr_file": "openai/api_requestor.py",
        "discussion_id": "1220308687",
        "commented_code": "@@ -149,6 +151,64 @@ def format_app_info(cls, info):\n             str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if not predicate(response):\n+            return\n+        error_data = response.data['error']\n+        message = error_data.get('message', 'Operation failed')\n+        code = error_data.get('code')\n+        raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        start_time = time.time()\n+        while not until(response) and time.time() - start_time < TIMEOUT_SECS:",
        "comment_created_at": "2023-06-06T21:06:52+00:00",
        "comment_author": "glecaros",
        "comment_body": "update it to raise an exception.",
        "pr_file_module": null
      },
      {
        "comment_id": "1220365105",
        "repo_full_name": "openai/openai-python",
        "pr_number": 439,
        "pr_file": "openai/api_requestor.py",
        "discussion_id": "1220308687",
        "commented_code": "@@ -149,6 +151,64 @@ def format_app_info(cls, info):\n             str += \" (%s)\" % (info[\"url\"],)\n         return str\n \n+    def _check_polling_response(self, response: OpenAIResponse, predicate: Callable[[OpenAIResponse], bool]):\n+        if not predicate(response):\n+            return\n+        error_data = response.data['error']\n+        message = error_data.get('message', 'Operation failed')\n+        code = error_data.get('code')\n+        raise error.OpenAIError(message=message, code=code)\n+\n+    def _poll(\n+        self,\n+        method,\n+        url,\n+        until,\n+        failed,\n+        params = None,\n+        headers = None,\n+        interval = None,\n+        delay = None\n+    ) -> Tuple[Iterator[OpenAIResponse], bool, str]:\n+        if delay:\n+            time.sleep(delay)\n+\n+        response, b, api_key = self.request(method, url, params, headers)\n+        self._check_polling_response(response, failed)\n+        start_time = time.time()\n+        while not until(response) and time.time() - start_time < TIMEOUT_SECS:",
        "comment_created_at": "2023-06-06T21:07:12+00:00",
        "comment_author": "glecaros",
        "comment_body": "I appreciate the nits in any case, thank you so much for the review!",
        "pr_file_module": null
      }
    ]
  }
]