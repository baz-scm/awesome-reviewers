[
  {
    "discussion_id": "2696319181",
    "pr_number": 761,
    "pr_file": "packages/coding-agent/src/core/agent-session.ts",
    "created_at": "2026-01-15T23:24:29+00:00",
    "commented_code": "}\n \t\t}\n \n+\t\t// Emit input event for extension interception (before template expansion)\n+\t\tlet currentText = text;\n+\t\tlet currentImages = options?.images;\n+\t\tif (this._extensionRunner?.hasHandlers(\"input\")) {\n+\t\t\tconst inputResult = await this._extensionRunner.emitInput(\n+\t\t\t\tcurrentText,\n+\t\t\t\tcurrentImages,\n+\t\t\t\toptions?._source ?? \"interactive\",\n+\t\t\t);\n+\t\t\tif (inputResult.action === \"handled\") {\n+\t\t\t\tif (inputResult.response) {\n+\t\t\t\t\tthis._extensionRunner.hasUI()\n+\t\t\t\t\t\t? this._extensionRunner.getUIContext().notify(inputResult.response, \"info\")\n+\t\t\t\t\t\t: console.log(inputResult.response);",
    "repo_full_name": "earendil-works/pi",
    "discussion_comments": [
      {
        "comment_id": "2696319181",
        "repo_full_name": "earendil-works/pi",
        "pr_number": 761,
        "pr_file": "packages/coding-agent/src/core/agent-session.ts",
        "discussion_id": "2696319181",
        "commented_code": "@@ -564,8 +566,33 @@ export class AgentSession {\n \t\t\t}\n \t\t}\n \n+\t\t// Emit input event for extension interception (before template expansion)\n+\t\tlet currentText = text;\n+\t\tlet currentImages = options?.images;\n+\t\tif (this._extensionRunner?.hasHandlers(\"input\")) {\n+\t\t\tconst inputResult = await this._extensionRunner.emitInput(\n+\t\t\t\tcurrentText,\n+\t\t\t\tcurrentImages,\n+\t\t\t\toptions?._source ?? \"interactive\",\n+\t\t\t);\n+\t\t\tif (inputResult.action === \"handled\") {\n+\t\t\t\tif (inputResult.response) {\n+\t\t\t\t\tthis._extensionRunner.hasUI()\n+\t\t\t\t\t\t? this._extensionRunner.getUIContext().notify(inputResult.response, \"info\")\n+\t\t\t\t\t\t: console.log(inputResult.response);",
        "comment_created_at": "2026-01-15T23:24:29+00:00",
        "comment_author": "badlogic",
        "comment_body": "Oopsies :)",
        "pr_file_module": null
      },
      {
        "comment_id": "2696442153",
        "repo_full_name": "earendil-works/pi",
        "pr_number": 761,
        "pr_file": "packages/coding-agent/src/core/agent-session.ts",
        "discussion_id": "2696319181",
        "commented_code": "@@ -564,8 +566,33 @@ export class AgentSession {\n \t\t\t}\n \t\t}\n \n+\t\t// Emit input event for extension interception (before template expansion)\n+\t\tlet currentText = text;\n+\t\tlet currentImages = options?.images;\n+\t\tif (this._extensionRunner?.hasHandlers(\"input\")) {\n+\t\t\tconst inputResult = await this._extensionRunner.emitInput(\n+\t\t\t\tcurrentText,\n+\t\t\t\tcurrentImages,\n+\t\t\t\toptions?._source ?? \"interactive\",\n+\t\t\t);\n+\t\t\tif (inputResult.action === \"handled\") {\n+\t\t\t\tif (inputResult.response) {\n+\t\t\t\t\tthis._extensionRunner.hasUI()\n+\t\t\t\t\t\t? this._extensionRunner.getUIContext().notify(inputResult.response, \"info\")\n+\t\t\t\t\t\t: console.log(inputResult.response);",
        "comment_created_at": "2026-01-16T00:31:10+00:00",
        "comment_author": "badlogic",
        "comment_body": "What I meant was that we should NEVER console.log anywhere. Breaks tui, breaks other shit.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2646056426",
    "pr_number": 302,
    "pr_file": "packages/ai/src/providers/anthropic.ts",
    "created_at": "2025-12-24T17:00:09+00:00",
    "commented_code": "});\n \t\t\t\t} else if (block.type === \"thinking\") {\n \t\t\t\t\tif (block.thinking.trim().length === 0) continue;\n-\t\t\t\t\t// If thinking signature is missing/empty (e.g., from aborted stream),\n-\t\t\t\t\t// convert to text block to avoid API rejection\n+\t\t\t\t\t// Unsigned thinking blocks (from aborted streams) are skipped by default.\n+\t\t\t\t\t// This matches Vercel AI SDK behavior which emits \"unsupported reasoning metadata\" warning.\n+\t\t\t\t\t// Preserving as literal <thinking> tags causes Claude to mimic them in responses.\n \t\t\t\t\tif (!block.thinkingSignature || block.thinkingSignature.trim().length === 0) {\n-\t\t\t\t\t\tblocks.push({\n-\t\t\t\t\t\t\ttype: \"text\",\n-\t\t\t\t\t\t\ttext: sanitizeSurrogates(`<thinking>\n${block.thinking}\n</thinking>`),\n-\t\t\t\t\t\t});\n-\t\t\t\t\t} else {\n-\t\t\t\t\t\tblocks.push({\n-\t\t\t\t\t\t\ttype: \"thinking\",\n-\t\t\t\t\t\t\tthinking: sanitizeSurrogates(block.thinking),\n-\t\t\t\t\t\t\tsignature: block.thinkingSignature,\n-\t\t\t\t\t\t});\n+\t\t\t\t\t\tif (options?.preserveUnsignedThinking) {\n+\t\t\t\t\t\t\tblocks.push({\n+\t\t\t\t\t\t\t\ttype: \"text\",\n+\t\t\t\t\t\t\t\ttext: sanitizeSurrogates(`<thinking>\n${block.thinking}\n</thinking>`),\n+\t\t\t\t\t\t\t});\n+\t\t\t\t\t\t} else {\n+\t\t\t\t\t\t\tconsole.warn(\"[pi-ai] Skipping unsigned thinking block (aborted stream).\");",
    "repo_full_name": "earendil-works/pi",
    "discussion_comments": [
      {
        "comment_id": "2646056426",
        "repo_full_name": "earendil-works/pi",
        "pr_number": 302,
        "pr_file": "packages/ai/src/providers/anthropic.ts",
        "discussion_id": "2646056426",
        "commented_code": "@@ -462,20 +467,25 @@ function convertMessages(messages: Message[], model: Model<\"anthropic-messages\">\n \t\t\t\t\t});\n \t\t\t\t} else if (block.type === \"thinking\") {\n \t\t\t\t\tif (block.thinking.trim().length === 0) continue;\n-\t\t\t\t\t// If thinking signature is missing/empty (e.g., from aborted stream),\n-\t\t\t\t\t// convert to text block to avoid API rejection\n+\t\t\t\t\t// Unsigned thinking blocks (from aborted streams) are skipped by default.\n+\t\t\t\t\t// This matches Vercel AI SDK behavior which emits \"unsupported reasoning metadata\" warning.\n+\t\t\t\t\t// Preserving as literal <thinking> tags causes Claude to mimic them in responses.\n \t\t\t\t\tif (!block.thinkingSignature || block.thinkingSignature.trim().length === 0) {\n-\t\t\t\t\t\tblocks.push({\n-\t\t\t\t\t\t\ttype: \"text\",\n-\t\t\t\t\t\t\ttext: sanitizeSurrogates(`<thinking>\\n${block.thinking}\\n</thinking>`),\n-\t\t\t\t\t\t});\n-\t\t\t\t\t} else {\n-\t\t\t\t\t\tblocks.push({\n-\t\t\t\t\t\t\ttype: \"thinking\",\n-\t\t\t\t\t\t\tthinking: sanitizeSurrogates(block.thinking),\n-\t\t\t\t\t\t\tsignature: block.thinkingSignature,\n-\t\t\t\t\t\t});\n+\t\t\t\t\t\tif (options?.preserveUnsignedThinking) {\n+\t\t\t\t\t\t\tblocks.push({\n+\t\t\t\t\t\t\t\ttype: \"text\",\n+\t\t\t\t\t\t\t\ttext: sanitizeSurrogates(`<thinking>\\n${block.thinking}\\n</thinking>`),\n+\t\t\t\t\t\t\t});\n+\t\t\t\t\t\t} else {\n+\t\t\t\t\t\t\tconsole.warn(\"[pi-ai] Skipping unsigned thinking block (aborted stream).\");",
        "comment_created_at": "2025-12-24T17:00:09+00:00",
        "comment_author": "badlogic",
        "comment_body": "That breaks the TUI hard. No code should ever directly log to the console.",
        "pr_file_module": null
      }
    ]
  }
]