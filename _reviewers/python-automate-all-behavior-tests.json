[
  {
    "discussion_id": "3170419021",
    "pr_number": 14609,
    "pr_file": "sorts/topological_sort.py",
    "created_at": "2026-04-30T19:38:54+00:00",
    "commented_code": null,
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3170419021",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14609,
        "pr_file": "sorts/topological_sort.py",
        "discussion_id": "3170419021",
        "commented_code": null,
        "comment_created_at": "2026-04-30T19:38:54+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "Consider adding a small assertion-based test instead of only printing the result. That would make it easier to verify the corrected topological order automatically.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3170507378",
    "pr_number": 14595,
    "pr_file": "backtracking/all_permutations.py",
    "created_at": "2026-04-30T19:56:47+00:00",
    "commented_code": "remove the comment to take an input from the user\n \n print(\"Enter the elements\")\n-sequence = list(map(int, input().split()))\n+MAX_SEQUENCE_LENGTH = 8\n+user_input = list(map(int, input().split()))\n+if len(user_input) > MAX_SEQUENCE_LENGTH:\n+    raise ValueError(f\"Input sequence too long (max {MAX_SEQUENCE_LENGTH} elements).\")",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3170507378",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14595,
        "pr_file": "backtracking/all_permutations.py",
        "discussion_id": "3170507378",
        "commented_code": "@@ -78,7 +78,11 @@ def create_state_space_tree(\n remove the comment to take an input from the user\n \n print(\"Enter the elements\")\n-sequence = list(map(int, input().split()))\n+MAX_SEQUENCE_LENGTH = 8\n+user_input = list(map(int, input().split()))\n+if len(user_input) > MAX_SEQUENCE_LENGTH:\n+    raise ValueError(f\"Input sequence too long (max {MAX_SEQUENCE_LENGTH} elements).\")",
        "comment_created_at": "2026-04-30T19:56:47+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "Consider adding a test or doctest for the new length limit so this behavior is verified automatically and does not regress later.",
        "pr_file_module": null
      },
      {
        "comment_id": "3176313597",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14595,
        "pr_file": "backtracking/all_permutations.py",
        "discussion_id": "3170507378",
        "commented_code": "@@ -78,7 +78,11 @@ def create_state_space_tree(\n remove the comment to take an input from the user\n \n print(\"Enter the elements\")\n-sequence = list(map(int, input().split()))\n+MAX_SEQUENCE_LENGTH = 8\n+user_input = list(map(int, input().split()))\n+if len(user_input) > MAX_SEQUENCE_LENGTH:\n+    raise ValueError(f\"Input sequence too long (max {MAX_SEQUENCE_LENGTH} elements).\")",
        "comment_created_at": "2026-05-02T07:26:21+00:00",
        "comment_author": "orbisai0security",
        "comment_body": "fixed",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3156118964",
    "pr_number": 13900,
    "pr_file": "searches/binary_search.py",
    "created_at": "2026-04-28T17:42:26+00:00",
    "commented_code": "2\n     \"\"\"\n     if hi < 0:\n-        hi = len(sorted_collection)\n+        hi = len(sorted_collection) + hi + 1  # in case of negetive indexing used for hi",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3156118964",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 13900,
        "pr_file": "searches/binary_search.py",
        "discussion_id": "3156118964",
        "commented_code": "@@ -45,7 +45,7 @@ def bisect_left(\n     2\n     \"\"\"\n     if hi < 0:\n-        hi = len(sorted_collection)\n+        hi = len(sorted_collection) + hi + 1  # in case of negetive indexing used for hi",
        "comment_created_at": "2026-04-28T17:42:26+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "Worth adding a doctest that demonstrates the negative index behavior \nto verify this fix works as expected and prevent regressions:\n\n>>> binary_search([1, 2, 3, 4, 5], 4, 0, -1)\n3\n\nThis would make the edge case explicitly testable.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3143495168",
    "pr_number": 14581,
    "pr_file": "searches/sentinel_linear_search.py",
    "created_at": "2026-04-26T12:47:27+00:00",
    "commented_code": "\"\"\"\n-This is pure Python implementation of sentinel linear search algorithm\n-\n-For doctests run following command:\n-python -m doctest -v sentinel_linear_search.py\n-or\n-python3 -m doctest -v sentinel_linear_search.py\n-\n-For manual testing run:\n-python sentinel_linear_search.py\n+Sentinel Linear Search Algorithm\n+A variation of linear search that reduces comparisons.\n+Reference: https://en.wikipedia.org/wiki/Linear_search#With_a_sentinel\n \"\"\"\n \n \n-def sentinel_linear_search(sequence, target):\n-    \"\"\"Pure implementation of sentinel linear search algorithm in Python\n-\n-    :param sequence: some sequence with comparable items\n-    :param target: item value to search\n-    :return: index of found item or None if item is not found\n-\n-    Examples:\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 0)\n-    0\n-\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 15)\n-    4\n-\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 5)\n-    1\n-\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 6)\n-\n+def sentinel_linear_search(arr: list[int], target: int) -> int:\n     \"\"\"\n-    sequence.append(target)\n+    Search for target using sentinel method, return index or -1 if not found.\n+\n+    >>> sentinel_linear_search([1, 2, 3, 4, 5], 3)\n+    2\n+    >>> sentinel_linear_search([1, 2, 3, 4, 5], 6)\n+    -1\n+    >>> sentinel_linear_search([], 1)\n+    -1\n+    \"\"\"\n+    n = len(arr)\n+    if n == 0:\n+        return -1\n \n-    index = 0\n-    while sequence[index] != target:\n-        index += 1\n+    last = arr[n - 1]\n+    arr[n - 1] = target\n+    i = 0\n \n-    sequence.pop()\n+    while arr[i] != target:\n+        i += 1\n \n-    if index == len(sequence):\n-        return None\n+    arr[n - 1] = last\n \n-    return index\n+    if i < n - 1 or arr[n - 1] == target:\n+        return i\n+    return -1\n \n \n if __name__ == \"__main__\":\n-    user_input = input(\"Enter numbers separated by comma:\n\").strip()\n-    sequence = [int(item) for item in user_input.split(\",\")]\n-\n-    target_input = input(\"Enter a single number to be found in the list:\n\")\n-    target = int(target_input)\n-    result = sentinel_linear_search(sequence, target)\n-    if result is not None:\n-        print(f\"{target} found at positions: {result}\")\n-    else:\n-        print(\"Not found\")\n+    import doctest\n+",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3143495168",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14581,
        "pr_file": "searches/sentinel_linear_search.py",
        "discussion_id": "3143495168",
        "commented_code": "@@ -1,58 +1,40 @@\n \"\"\"\n-This is pure Python implementation of sentinel linear search algorithm\n-\n-For doctests run following command:\n-python -m doctest -v sentinel_linear_search.py\n-or\n-python3 -m doctest -v sentinel_linear_search.py\n-\n-For manual testing run:\n-python sentinel_linear_search.py\n+Sentinel Linear Search Algorithm\n+A variation of linear search that reduces comparisons.\n+Reference: https://en.wikipedia.org/wiki/Linear_search#With_a_sentinel\n \"\"\"\n \n \n-def sentinel_linear_search(sequence, target):\n-    \"\"\"Pure implementation of sentinel linear search algorithm in Python\n-\n-    :param sequence: some sequence with comparable items\n-    :param target: item value to search\n-    :return: index of found item or None if item is not found\n-\n-    Examples:\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 0)\n-    0\n-\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 15)\n-    4\n-\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 5)\n-    1\n-\n-    >>> sentinel_linear_search([0, 5, 7, 10, 15], 6)\n-\n+def sentinel_linear_search(arr: list[int], target: int) -> int:\n     \"\"\"\n-    sequence.append(target)\n+    Search for target using sentinel method, return index or -1 if not found.\n+\n+    >>> sentinel_linear_search([1, 2, 3, 4, 5], 3)\n+    2\n+    >>> sentinel_linear_search([1, 2, 3, 4, 5], 6)\n+    -1\n+    >>> sentinel_linear_search([], 1)\n+    -1\n+    \"\"\"\n+    n = len(arr)\n+    if n == 0:\n+        return -1\n \n-    index = 0\n-    while sequence[index] != target:\n-        index += 1\n+    last = arr[n - 1]\n+    arr[n - 1] = target\n+    i = 0\n \n-    sequence.pop()\n+    while arr[i] != target:\n+        i += 1\n \n-    if index == len(sequence):\n-        return None\n+    arr[n - 1] = last\n \n-    return index\n+    if i < n - 1 or arr[n - 1] == target:\n+        return i\n+    return -1\n \n \n if __name__ == \"__main__\":\n-    user_input = input(\"Enter numbers separated by comma:\\n\").strip()\n-    sequence = [int(item) for item in user_input.split(\",\")]\n-\n-    target_input = input(\"Enter a single number to be found in the list:\\n\")\n-    target = int(target_input)\n-    result = sentinel_linear_search(sequence, target)\n-    if result is not None:\n-        print(f\"{target} found at positions: {result}\")\n-    else:\n-        print(\"Not found\")\n+    import doctest\n+",
        "comment_created_at": "2026-04-26T12:47:27+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "The __main__ block is missing a print statement after doctest.testmod() \nto confirm tests passed. Small addition but consistent with the repo's \nother implementations:\n\n    if __name__ == \"__main__\":\n        import doctest\n        doctest.testmod()\n        print(\"All tests passed.\")",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2024180701",
    "pr_number": 12648,
    "pr_file": "control_algorithms/adrc.py",
    "created_at": "2025-04-02T06:54:16+00:00",
    "commented_code": "+\"\"\"\n+Active Disturbance Rejection Control (ADRC) is a robust control strategy\n+that estimates and compensates for disturbances in real-time without needing\n+an explicit mathematical model of the system.\n+\n+It consists of:\n+1. Tracking Differentiator (TD) - Smooths the reference signal\n+2. Extended State Observer (ESO) - Estimates system states and disturbances\n+3. Nonlinear State Error Feedback (NLSEF) - Generates the control signal\n+\n+Refer - https://en.wikipedia.org/wiki/Active_disturbance_rejection_control\n+\"\"\"\n+\n+\n+class ADRC:\n+    def __init__(self, beta1: float, beta2: float, beta3: float, setpoint: float = 0.0):\n+        \"\"\"\n+        Initialize the ADRC controller.\n+\n+        :param beta1: Gain for error correction in ESO\n+        :param beta2: Gain for disturbance estimation in ESO\n+        :param beta3: Gain for acceleration estimation in ESO\n+        :param setpoint: Desired target value\n+        \"\"\"\n+        self.beta1 = beta1\n+        self.beta2 = beta2\n+        self.beta3 = beta3\n+        self.setpoint = setpoint\n+\n+        self.z1 = 0.0  # Estimated system output\n+        self.z2 = 0.0  # Estimated system velocity\n+        self.z3 = 0.0  # Estimated total disturbance\n+\n+    def compute(self, measured_value: float, dt: float) -> float:\n+        \"\"\"\n+        Compute the control signal based on error estimation and disturbance rejection.\n+\n+        :param measured_value: The current process variable\n+        :param dt: Time difference since the last update\n+        :return: Control output\n+        \"\"\"\n+\n+        # Extended State Observer (ESO) Update\n+        self.z1 += dt * (self.z2 - self.beta1 * (self.z1 - measured_value))\n+        self.z2 += dt * (self.z3 - self.beta2 * (self.z1 - measured_value))\n+        self.z3 -= self.beta3 * (self.z1 - measured_value)\n+\n+        # Control Law (Nonlinear State Error Feedback - NLSEF)\n+        control_output = self.z2 - self.z3\n+        return control_output\n+\n+    def reset(self):\n+        \"\"\"Reset the estimated states.\"\"\"",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "2024180701",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12648,
        "pr_file": "control_algorithms/adrc.py",
        "discussion_id": "2024180701",
        "commented_code": "@@ -0,0 +1,62 @@\n+\"\"\"\n+Active Disturbance Rejection Control (ADRC) is a robust control strategy\n+that estimates and compensates for disturbances in real-time without needing\n+an explicit mathematical model of the system.\n+\n+It consists of:\n+1. Tracking Differentiator (TD) - Smooths the reference signal\n+2. Extended State Observer (ESO) - Estimates system states and disturbances\n+3. Nonlinear State Error Feedback (NLSEF) - Generates the control signal\n+\n+Refer - https://en.wikipedia.org/wiki/Active_disturbance_rejection_control\n+\"\"\"\n+\n+\n+class ADRC:\n+    def __init__(self, beta1: float, beta2: float, beta3: float, setpoint: float = 0.0):\n+        \"\"\"\n+        Initialize the ADRC controller.\n+\n+        :param beta1: Gain for error correction in ESO\n+        :param beta2: Gain for disturbance estimation in ESO\n+        :param beta3: Gain for acceleration estimation in ESO\n+        :param setpoint: Desired target value\n+        \"\"\"\n+        self.beta1 = beta1\n+        self.beta2 = beta2\n+        self.beta3 = beta3\n+        self.setpoint = setpoint\n+\n+        self.z1 = 0.0  # Estimated system output\n+        self.z2 = 0.0  # Estimated system velocity\n+        self.z3 = 0.0  # Estimated total disturbance\n+\n+    def compute(self, measured_value: float, dt: float) -> float:\n+        \"\"\"\n+        Compute the control signal based on error estimation and disturbance rejection.\n+\n+        :param measured_value: The current process variable\n+        :param dt: Time difference since the last update\n+        :return: Control output\n+        \"\"\"\n+\n+        # Extended State Observer (ESO) Update\n+        self.z1 += dt * (self.z2 - self.beta1 * (self.z1 - measured_value))\n+        self.z2 += dt * (self.z3 - self.beta2 * (self.z1 - measured_value))\n+        self.z3 -= self.beta3 * (self.z1 - measured_value)\n+\n+        # Control Law (Nonlinear State Error Feedback - NLSEF)\n+        control_output = self.z2 - self.z3\n+        return control_output\n+\n+    def reset(self):\n+        \"\"\"Reset the estimated states.\"\"\"",
        "comment_created_at": "2025-04-02T06:54:16+00:00",
        "comment_author": "cclauss",
        "comment_body": "As discussed in CONTRIBUTING.md, all methods need one or more doctests.\r\n\r\n```suggestion\r\n        \"\"\"Reset the estimated states.\r\n\r\n        >>> adrc = ADRC(0.0, 0.0, 0.0)\r\n        >>> adrc.z1, adrc.z2, adrc.z3 = 1.1, 2.2, 3.3\r\n        >>> adrc.reset()\r\n        >>> adrc.z1, adrc.z2, adrc.z3\r\n        (0.0, 0.0, 0.0)\r\n        \"\"\"\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2054752782",
    "pr_number": 12678,
    "pr_file": "data_structures/hashing/hash_map.py",
    "created_at": "2025-04-22T19:46:05+00:00",
    "commented_code": "Traceback (most recent call last):\n         ...\n         KeyError: 4\n+\n+        # Test resize down when sparse\n+        ## Setup: resize up\n+        >>> hm = HashMap(100, capacity_factor=0.75)",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "2054752782",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12678,
        "pr_file": "data_structures/hashing/hash_map.py",
        "discussion_id": "2054752782",
        "commented_code": "@@ -228,6 +228,25 @@ def __delitem__(self, key: KEY) -> None:\n         Traceback (most recent call last):\n         ...\n         KeyError: 4\n+\n+        # Test resize down when sparse\n+        ## Setup: resize up\n+        >>> hm = HashMap(100, capacity_factor=0.75)",
        "comment_created_at": "2025-04-22T19:46:05+00:00",
        "comment_author": "Cjkjvfnby",
        "comment_body": "Can we go with size 4?  In that case, we could avoid the loop.  ",
        "pr_file_module": null
      },
      {
        "comment_id": "2057794073",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12678,
        "pr_file": "data_structures/hashing/hash_map.py",
        "discussion_id": "2054752782",
        "commented_code": "@@ -228,6 +228,25 @@ def __delitem__(self, key: KEY) -> None:\n         Traceback (most recent call last):\n         ...\n         KeyError: 4\n+\n+        # Test resize down when sparse\n+        ## Setup: resize up\n+        >>> hm = HashMap(100, capacity_factor=0.75)",
        "comment_created_at": "2025-04-24T08:03:18+00:00",
        "comment_author": "isidroas",
        "comment_body": "Something like this?\r\n\r\n```python\r\n# Test resize down when sparse\r\n## Setup: resize up\r\n>>> hm = HashMap(4, capacity_factor=0.75)\r\n>>> len(hm._buckets)\r\n4\r\n>>> hm[0] = 0\r\n>>> hm[1] = 1\r\n>>> hm[2] = 2\r\n>>> len(hm._buckets)\r\n4\r\n>>> hm[3] = 3\r\n>>> len(hm._buckets)\r\n8\r\n\r\n## Resize down\r\n>>> del hm[3]\r\n>>> len(hm._buckets)\r\n8\r\n>>> del hm[2]\r\n>>> len(hm._buckets)\r\n4\r\n```\r\n\r\nI've checked that it passes, but I don't know which one I prefer",
        "pr_file_module": null
      },
      {
        "comment_id": "2059330672",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12678,
        "pr_file": "data_structures/hashing/hash_map.py",
        "discussion_id": "2054752782",
        "commented_code": "@@ -228,6 +228,25 @@ def __delitem__(self, key: KEY) -> None:\n         Traceback (most recent call last):\n         ...\n         KeyError: 4\n+\n+        # Test resize down when sparse\n+        ## Setup: resize up\n+        >>> hm = HashMap(100, capacity_factor=0.75)",
        "comment_created_at": "2025-04-24T22:31:05+00:00",
        "comment_author": "Cjkjvfnby",
        "comment_body": "I'd prefer to keep tests as linear as possible.",
        "pr_file_module": null
      },
      {
        "comment_id": "2059332330",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12678,
        "pr_file": "data_structures/hashing/hash_map.py",
        "discussion_id": "2054752782",
        "commented_code": "@@ -228,6 +228,25 @@ def __delitem__(self, key: KEY) -> None:\n         Traceback (most recent call last):\n         ...\n         KeyError: 4\n+\n+        # Test resize down when sparse\n+        ## Setup: resize up\n+        >>> hm = HashMap(100, capacity_factor=0.75)",
        "comment_created_at": "2025-04-24T22:31:33+00:00",
        "comment_author": "Cjkjvfnby",
        "comment_body": "Let's wait for the 3d opinion. ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1409822277",
    "pr_number": 11185,
    "pr_file": "greedy_methods/kruskal.py",
    "created_at": "2023-11-29T20:20:13+00:00",
    "commented_code": "+\"\"\"\n+https://en.wikipedia.org/wiki/Kruskal%27s_algorithm\n+\"\"\"\n+\n+import doctest\n+from collections.abc import Iterable\n+from dataclasses import dataclass\n+\n+\n+@dataclass\n+class Edge:\n+    \"\"\"\n+    Represents an edge in a graph.\n+\n+    Attributes:\n+        start (str): The starting vertex of the edge.\n+        end (str): The ending vertex of the edge.\n+        weight (int): The weight of the edge.\n+    \"\"\"\n+\n+    start: str\n+    end: str\n+    weight: int\n+\n+\n+class DisjointSet:\n+    \"\"\"\n+    Implements a disjoint-set (union-find) data structure.\n+\n+    This data structure is useful for tracking the partitioning of a set into disjoint,\n+    non-overlapping subsets. It offers efficient operations for union and finding the\n+    representative of a set.\n+\n+    Attributes:\n+        parent (dict[str, str]): Maps each element to its parent element.\n+        rank (dict[str, int]): Maps each element to its rank in the tree.\n+\n+    Methods:\n+        find(item): Finds the representative of the set containing 'item'.\n+        union(set1, set2): Merges the sets containing 'set1' and 'set2'.\n+\n+    >>> ds = DisjointSet([\"A\", \"B\", \"C\"])\n+    >>> ds.find(\"A\")\n+    'A'\n+    >>> ds.union(\"A\", \"B\")\n+    >>> ds.find(\"B\")\n+    'A'\n+    >>> ds.union(\"B\", \"C\")\n+    >>> ds.find(\"C\")\n+    'A'",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1409822277",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11185,
        "pr_file": "greedy_methods/kruskal.py",
        "discussion_id": "1409822277",
        "commented_code": "@@ -0,0 +1,120 @@\n+\"\"\"\n+https://en.wikipedia.org/wiki/Kruskal%27s_algorithm\n+\"\"\"\n+\n+import doctest\n+from collections.abc import Iterable\n+from dataclasses import dataclass\n+\n+\n+@dataclass\n+class Edge:\n+    \"\"\"\n+    Represents an edge in a graph.\n+\n+    Attributes:\n+        start (str): The starting vertex of the edge.\n+        end (str): The ending vertex of the edge.\n+        weight (int): The weight of the edge.\n+    \"\"\"\n+\n+    start: str\n+    end: str\n+    weight: int\n+\n+\n+class DisjointSet:\n+    \"\"\"\n+    Implements a disjoint-set (union-find) data structure.\n+\n+    This data structure is useful for tracking the partitioning of a set into disjoint,\n+    non-overlapping subsets. It offers efficient operations for union and finding the\n+    representative of a set.\n+\n+    Attributes:\n+        parent (dict[str, str]): Maps each element to its parent element.\n+        rank (dict[str, int]): Maps each element to its rank in the tree.\n+\n+    Methods:\n+        find(item): Finds the representative of the set containing 'item'.\n+        union(set1, set2): Merges the sets containing 'set1' and 'set2'.\n+\n+    >>> ds = DisjointSet([\"A\", \"B\", \"C\"])\n+    >>> ds.find(\"A\")\n+    'A'\n+    >>> ds.union(\"A\", \"B\")\n+    >>> ds.find(\"B\")\n+    'A'\n+    >>> ds.union(\"B\", \"C\")\n+    >>> ds.find(\"C\")\n+    'A'",
        "comment_created_at": "2023-11-29T20:20:13+00:00",
        "comment_author": "cclauss",
        "comment_body": "Having all three answers be `'A'` does not raise confidence.\r\n\r\nLet's use `disjoint_set` instead of `ds` as our variable name to make our code more self-documenting.\r\n\r\nPlease add tests where `disjoint_set = DisjointSet([])` and `disjoint_set = DisjointSet([1, 2, 3])`.\r\n\r\nLet's add tests that fail like `>>> disjoint_set.find(\"D\")`",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1899504504",
    "pr_number": 12437,
    "pr_file": "conversions/rec_to_pol.py",
    "created_at": "2024-12-30T12:14:52+00:00",
    "commented_code": "+import doctest\n+import math\n+\n+\n+def rec_to_pol(real: float, img: float) -> tuple:\n+    \"\"\"\n+    https://en.wikipedia.org/wiki/Polar_coordinate_system\n+\n+    >>> rec_to_pol(5,-5)\n+    (7.07, -45.0)\n+    >>> rec_to_pol(-1,1)\n+    (1.41, 135.0)\n+    >>> rec_to_pol(-1,-1)\n+    (1.41, -135.0)\n+    >>> rec_to_pol(1e-10,1e-10)\n+    (0.0, 45.0)\n+    >>> rec_to_pol(-1e-10,1e-10)\n+    (0.0, 135.0)\n+    >>> rec_to_pol(9.75,5.93)\n+    (11.41, 31.31)\n+    >>> rec_to_pol(10000,99999)\n+    (100497.76, 84.29)\n+\n+    \"\"\"\n+\n+    mod = round(math.sqrt((real**2) + (img**2)), 2)\n+\n+    ang = round(math.degrees(math.atan2(img, real)), 2)\n+    return (mod, ang)\n+\n+\n+if __name__ == \"__main__\":",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1899504504",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12437,
        "pr_file": "conversions/rec_to_pol.py",
        "discussion_id": "1899504504",
        "commented_code": "@@ -0,0 +1,33 @@\n+import doctest\n+import math\n+\n+\n+def rec_to_pol(real: float, img: float) -> tuple:\n+    \"\"\"\n+    https://en.wikipedia.org/wiki/Polar_coordinate_system\n+\n+    >>> rec_to_pol(5,-5)\n+    (7.07, -45.0)\n+    >>> rec_to_pol(-1,1)\n+    (1.41, 135.0)\n+    >>> rec_to_pol(-1,-1)\n+    (1.41, -135.0)\n+    >>> rec_to_pol(1e-10,1e-10)\n+    (0.0, 45.0)\n+    >>> rec_to_pol(-1e-10,1e-10)\n+    (0.0, 135.0)\n+    >>> rec_to_pol(9.75,5.93)\n+    (11.41, 31.31)\n+    >>> rec_to_pol(10000,99999)\n+    (100497.76, 84.29)\n+\n+    \"\"\"\n+\n+    mod = round(math.sqrt((real**2) + (img**2)), 2)\n+\n+    ang = round(math.degrees(math.atan2(img, real)), 2)\n+    return (mod, ang)\n+\n+\n+if __name__ == \"__main__\":",
        "comment_created_at": "2024-12-30T12:14:52+00:00",
        "comment_author": "cclauss",
        "comment_body": "```suggestion\r\nif __name__ == \"__main__\":\r\n    import doctest\r\n\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1342166662",
    "pr_number": 9316,
    "pr_file": "divide_and_conquer/heaps_algorithm_iterative.py",
    "created_at": "2023-10-01T17:45:25+00:00",
    "commented_code": "[(-1, 1), (1, -1)]\n     >>> heaps([1, 2, 3])\n     [(1, 2, 3), (2, 1, 3), (3, 1, 2), (1, 3, 2), (2, 3, 1), (3, 2, 1)]\n-    >>> from itertools import permutations\n-    >>> sorted(heaps([1,2,3])) == sorted(permutations([1,2,3]))\n-    True\n-    >>> all(sorted(heaps(x)) == sorted(permutations(x))\n-    ...     for x in ([], [0], [-1, 1], [1, 2, 3]))\n-    True",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1342166662",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 9316,
        "pr_file": "divide_and_conquer/heaps_algorithm_iterative.py",
        "discussion_id": "1342166662",
        "commented_code": "@@ -19,38 +20,27 @@ def heaps(arr: list) -> list:\n     [(-1, 1), (1, -1)]\n     >>> heaps([1, 2, 3])\n     [(1, 2, 3), (2, 1, 3), (3, 1, 2), (1, 3, 2), (2, 3, 1), (3, 2, 1)]\n-    >>> from itertools import permutations\n-    >>> sorted(heaps([1,2,3])) == sorted(permutations([1,2,3]))\n-    True\n-    >>> all(sorted(heaps(x)) == sorted(permutations(x))\n-    ...     for x in ([], [0], [-1, 1], [1, 2, 3]))\n-    True",
        "comment_created_at": "2023-10-01T17:45:25+00:00",
        "comment_author": "cclauss",
        "comment_body": "Removing tests is not a good idea.\r\n\r\nhttps://docs.python.org/3/library/doctest.html",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1343839392",
    "pr_number": 9582,
    "pr_file": "boolean_algebra/sop_pos_generator.py",
    "created_at": "2023-10-03T10:07:54+00:00",
    "commented_code": "+\"\"\"\n+Wiki: \n+https://en.wikipedia.org/wiki/Canonical_normal_form\n+Other resources: \n+https://www.electronics-tutorials.ws/boolean/sum-of-product.html\n+https://www.electronics-tutorials.ws/boolean/product-of-sum.html\n+\"\"\"\n+\n+def generate_sop(variables, min_terms):\n+    \"\"\"\n+    Generates a Sum of Products (SOP) expression.\n+\n+    Args:\n+        variables (list): A list of variables.\n+        min_terms (list): A list of minterms.\n+\n+    Returns:\n+        str: The SOP expression.\n+\n+    Examples:\n+        >>> generate_sop(['A', 'B', 'C'], [0, 1, 3])\n+        '(A & !B & C) | (A & B & C) | (A & B & !C)'\n+\n+        >>> generate_sop(['X', 'Y'], [0, 2])\n+        '(X & !Y) | (!X & Y)'\n+\n+    \"\"\"\n+    sop_terms = []\n+    \n+\n+    # Create a function to convert a minterm to a term\n+    def minterm_to_term(minterm):\n+        term = []\n+        minterm = int(minterm)  # Convert minterm to an integer\n+        for i, var in enumerate(variables):\n+            # Check if the i-th bit of the minterm is 0\n+            if (minterm >> i) & 1 == 0:\n+                term.append(f\"!{var}\")  # Append negated variable if bit is 0\n+            else:\n+                term.append(var)       # Append variable if bit is 1\n+        return \" & \".join(term)       # Combine variables with AND operator\n+\n+    for minterm in min_terms:\n+        term = minterm_to_term(minterm)\n+        sop_terms.append(f\"({term})\")  # Enclose each term in parentheses\n+\n+    sop_expression = \" | \".join(sop_terms)  # Combine terms with OR operator\n+    return sop_expression\n+\n+def generate_pos(variables, max_terms):\n+    \"\"\"\n+    Generates a Product of Sums (POS) expression.\n+\n+    Args:\n+        variables (list): A list of variables.\n+        max_terms (list): A list of maxterms.\n+\n+    Returns:\n+        str: The POS expression.\n+\n+    Examples:\n+        >>> generate_pos(['A', 'B'], [0, 2])\n+        '(A | !B) & (!A | B)'\n+\n+        >>> generate_pos(['X', 'Y'], [1, 3])\n+        '(!X & Y) & (X & !Y)'\n+\n+    \"\"\"\n+    pos_terms = []\n+\n+    # Create a function to convert a maxterm to a term\n+    def maxterm_to_term(maxterm):",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1343852894",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 9582,
        "pr_file": "boolean_algebra/sop_pos_generator.py",
        "discussion_id": "1343839392",
        "commented_code": "@@ -0,0 +1,102 @@\n+\"\"\"\n+Wiki: \n+https://en.wikipedia.org/wiki/Canonical_normal_form\n+Other resources: \n+https://www.electronics-tutorials.ws/boolean/sum-of-product.html\n+https://www.electronics-tutorials.ws/boolean/product-of-sum.html\n+\"\"\"\n+\n+def generate_sop(variables, min_terms):\n+    \"\"\"\n+    Generates a Sum of Products (SOP) expression.\n+\n+    Args:\n+        variables (list): A list of variables.\n+        min_terms (list): A list of minterms.\n+\n+    Returns:\n+        str: The SOP expression.\n+\n+    Examples:\n+        >>> generate_sop(['A', 'B', 'C'], [0, 1, 3])\n+        '(A & !B & C) | (A & B & C) | (A & B & !C)'\n+\n+        >>> generate_sop(['X', 'Y'], [0, 2])\n+        '(X & !Y) | (!X & Y)'\n+\n+    \"\"\"\n+    sop_terms = []\n+    \n+\n+    # Create a function to convert a minterm to a term\n+    def minterm_to_term(minterm):\n+        term = []\n+        minterm = int(minterm)  # Convert minterm to an integer\n+        for i, var in enumerate(variables):\n+            # Check if the i-th bit of the minterm is 0\n+            if (minterm >> i) & 1 == 0:\n+                term.append(f\"!{var}\")  # Append negated variable if bit is 0\n+            else:\n+                term.append(var)       # Append variable if bit is 1\n+        return \" & \".join(term)       # Combine variables with AND operator\n+\n+    for minterm in min_terms:\n+        term = minterm_to_term(minterm)\n+        sop_terms.append(f\"({term})\")  # Enclose each term in parentheses\n+\n+    sop_expression = \" | \".join(sop_terms)  # Combine terms with OR operator\n+    return sop_expression\n+\n+def generate_pos(variables, max_terms):\n+    \"\"\"\n+    Generates a Product of Sums (POS) expression.\n+\n+    Args:\n+        variables (list): A list of variables.\n+        max_terms (list): A list of maxterms.\n+\n+    Returns:\n+        str: The POS expression.\n+\n+    Examples:\n+        >>> generate_pos(['A', 'B'], [0, 2])\n+        '(A | !B) & (!A | B)'\n+\n+        >>> generate_pos(['X', 'Y'], [1, 3])\n+        '(!X & Y) & (X & !Y)'\n+\n+    \"\"\"\n+    pos_terms = []\n+\n+    # Create a function to convert a maxterm to a term\n+    def maxterm_to_term(maxterm):",
        "comment_created_at": "2023-10-03T10:07:54+00:00",
        "comment_author": "cclauss",
        "comment_body": "We discourage nested functions because the doctests in their docstrings never get run.",
        "pr_file_module": null
      }
    ]
  }
]