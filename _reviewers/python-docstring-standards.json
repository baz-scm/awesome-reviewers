[
  {
    "discussion_id": "3185927710",
    "pr_number": 14623,
    "pr_file": "physics/snells_law.py",
    "created_at": "2026-05-05T03:47:13+00:00",
    "commented_code": "+import math",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3185927710",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14623,
        "pr_file": "physics/snells_law.py",
        "discussion_id": "3185927710",
        "commented_code": "@@ -0,0 +1,66 @@\n+import math",
        "comment_created_at": "2026-05-05T03:47:13+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "The file is missing a module-level docstring. The repo's convention\nis to have a module docstring before imports. Consider adding:\n\n\"\"\"\nSnell's Law \u2014 Refraction Angle Calculation.\n\nCalculates the angle of refraction when light passes between two\nmedia using Snell's Law: n1 * sin(theta1) = n2 * sin(theta2).\n\nReference: https://en.wikipedia.org/wiki/Snell%27s_law\n\"\"\"",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3173522364",
    "pr_number": 14602,
    "pr_file": "maths/laplace_transformation.py",
    "created_at": "2026-05-01T14:19:29+00:00",
    "commented_code": "+\"\"\"\n+This module provides a numerical implementation of the Laplace Transform.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3173522364",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14602,
        "pr_file": "maths/laplace_transformation.py",
        "discussion_id": "3173522364",
        "commented_code": "@@ -0,0 +1,66 @@\n+\"\"\"\n+This module provides a numerical implementation of the Laplace Transform.",
        "comment_created_at": "2026-05-01T14:19:29+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "The module docstring is minimal \u2014 just one line and a Wikipedia link. \nConsider expanding it to match the quality of the function docstring:\n\n\"\"\"\nLaplace Transform \u2014 Numerical Implementation.\n\nComputes the numerical Laplace Transform using the trapezoidal \nintegration rule. Supports real-valued, non-negative Laplace \nparameters only.\n\nReference: https://en.wikipedia.org/wiki/Laplace_transform\n\"\"\"",
        "pr_file_module": null
      },
      {
        "comment_id": "3183420485",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14602,
        "pr_file": "maths/laplace_transformation.py",
        "discussion_id": "3173522364",
        "commented_code": "@@ -0,0 +1,66 @@\n+\"\"\"\n+This module provides a numerical implementation of the Laplace Transform.",
        "comment_created_at": "2026-05-04T17:53:28+00:00",
        "comment_author": "Tushar-R-Tyagi",
        "comment_body": "Thanks for this suggestion. I have updated the module just like you recommended.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3170539265",
    "pr_number": 14580,
    "pr_file": "physics/orbital_period.py",
    "created_at": "2026-04-30T20:02:08+00:00",
    "commented_code": "+\"\"\"\n+The time period of a satellite is the time taken by a satellite to\n+complete one full orbit around a celestial body.\n+\n+T = 2\u03c0 * sqrt((R + h)^3 / (G * M))\n+\n+G is the Universal Gravitational Constant and equals 6.674 X 10^-11 N m\u00b2/kg\u00b2\n+M is the mass of the celestial body (kg)\n+R is the radius of the celestial body (m)\n+h is the height of the satellite above the surface (m)\n+\n+Reference: https://en.wikipedia.org/wiki/Orbital_period\n+\"\"\"\n+\n+\n+def time_period_of_satellite(mass: float, radius: float, height: float) -> float:\n+    \"\"\"\n+    Calculate the time period of a satellite orbiting a celestial body\n+\n+    >>> time_period_of_satellite(5.972e24, 6.371e6, 3.5e5)",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3170539265",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14580,
        "pr_file": "physics/orbital_period.py",
        "discussion_id": "3170539265",
        "commented_code": "@@ -0,0 +1,56 @@\n+\"\"\"\n+The time period of a satellite is the time taken by a satellite to\n+complete one full orbit around a celestial body.\n+\n+T = 2\u03c0 * sqrt((R + h)^3 / (G * M))\n+\n+G is the Universal Gravitational Constant and equals 6.674 X 10^-11 N m\u00b2/kg\u00b2\n+M is the mass of the celestial body (kg)\n+R is the radius of the celestial body (m)\n+h is the height of the satellite above the surface (m)\n+\n+Reference: https://en.wikipedia.org/wiki/Orbital_period\n+\"\"\"\n+\n+\n+def time_period_of_satellite(mass: float, radius: float, height: float) -> float:\n+    \"\"\"\n+    Calculate the time period of a satellite orbiting a celestial body\n+\n+    >>> time_period_of_satellite(5.972e24, 6.371e6, 3.5e5)",
        "comment_created_at": "2026-04-30T20:02:08+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "Nice use of realistic Earth values \ud83d\udc4d Consider explicitly mentioning units (kg, meters) in the docstring examples to avoid ambiguity for users.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3170442547",
    "pr_number": 14554,
    "pr_file": "maths/first_fundamental_form.py",
    "created_at": "2026-04-30T19:44:00+00:00",
    "commented_code": "+\"\"\"\n+Calculates the First Fundamental Form of a parametric surface.\n+\n+The first fundamental form allows the measurement of lengths, angles,\n+and areas on a surface defined by parametric equations r(u, v).\n+\n+Reference:\n+- https://en.wikipedia.org/wiki/First_fundamental_form\n+\n+Author: Chahat Sandhu\n+GitHub: https://github.com/singhc7\n+\"\"\"\n+\n+import sympy as sp",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "3170442547",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 14554,
        "pr_file": "maths/first_fundamental_form.py",
        "discussion_id": "3170442547",
        "commented_code": "@@ -0,0 +1,75 @@\n+\"\"\"\n+Calculates the First Fundamental Form of a parametric surface.\n+\n+The first fundamental form allows the measurement of lengths, angles,\n+and areas on a surface defined by parametric equations r(u, v).\n+\n+Reference:\n+- https://en.wikipedia.org/wiki/First_fundamental_form\n+\n+Author: Chahat Sandhu\n+GitHub: https://github.com/singhc7\n+\"\"\"\n+\n+import sympy as sp",
        "comment_created_at": "2026-04-30T19:44:00+00:00",
        "comment_author": "jgohel9902",
        "comment_body": "`sympy` is a heavy dependency. Consider mentioning this in the module docstring or adding a note about installation requirements (e.g., `pip install sympy`) for better usability.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2615889844",
    "pr_number": 13946,
    "pr_file": "searches/binary_search.py",
    "created_at": "2025-12-13T00:28:05+00:00",
    "commented_code": "return -1\n \n \n+def binary_search_with_duplicates(sorted_collection: list[int], item: int) -> list[int]:\n+    \"\"\"Pure implementation of a binary search algorithm in Python that supports\n+    duplicates.\n+\n+    Resources used:\n+    https://stackoverflow.com/questions/13197552/using-binary-search-with-sorted-array-with-duplicates\n+\n+    The collection must be sorted in ascending order; otherwise the result will be\n+    unpredictable. If the target appears multiple times, this function returns a\n+    list of **all** indexes where the target occurs. If the target is not found,\n+    it returns an empty list.\n+\n+    :param sorted_collection: some ascending sorted collection with comparable items\n+    :param item: item value to search for\n+    :return: a list of indexes where the item is found (empty list if not found)\n+\n+    Examples:\n+    >>> binary_search_with_duplicates([0, 5, 7, 10, 15], 0)\n+    [0]\n+    >>> binary_search_with_duplicates([0, 5, 7, 10, 15], 15)\n+    [4]\n+    >>> binary_search_with_duplicates([1, 2, 2, 2, 3], 2)\n+    [1, 2, 3]\n+    >>> binary_search_with_duplicates([1, 2, 2, 2, 3], 4)\n+    []\n+    \"\"\"\n+    if list(sorted_collection) != sorted(sorted_collection):\n+        raise ValueError(\"sorted_collection must be sorted in ascending order\")\n+\n+    def lower_bound(sorted_collection: list[int], item: int) -> int:\n+        \"\"\"\n+        Returns the index of the first element greater than or equal to the item.\n+\n+        Args:\n+            sorted_collection: The sorted list to search.\n+            item: The item to find the lower bound for.\n+\n+        Returns:\n+            int: The index where the item can be inserted while maintaining order.\n+        \"\"\"\n+        left = 0\n+        right = len(sorted_collection)\n+        while left < right:\n+            midpoint = left + (right - left) // 2\n+            current_item = sorted_collection[midpoint]\n+            if current_item < item:\n+                left = midpoint + 1\n+            else:\n+                right = midpoint\n+        return left\n+\n+    def upper_bound(sorted_collection: list[int], item: int) -> int:\n+        \"\"\"\n+        Returns the index of the first element strictly greater than the item.\n+\n+        Args:\n+            sorted_collection: The sorted list to search.\n+            item: The item to find the upper bound for.\n+\n+        Returns:\n+            int: The index where the item can be inserted after all existing instances.\n+        \"\"\"\n+        left = 0\n+        right = len(sorted_collection)\n+        while left < right:\n+            midpoint = left + (right - left) // 2\n+            current_item = sorted_collection[midpoint]\n+            if current_item <= item:\n+                left = midpoint + 1\n+            else:\n+                right = midpoint\n+        return left",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "2615900343",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 13946,
        "pr_file": "searches/binary_search.py",
        "discussion_id": "2615889844",
        "commented_code": "@@ -243,6 +243,87 @@ def binary_search_std_lib(sorted_collection: list[int], item: int) -> int:\n     return -1\n \n \n+def binary_search_with_duplicates(sorted_collection: list[int], item: int) -> list[int]:\n+    \"\"\"Pure implementation of a binary search algorithm in Python that supports\n+    duplicates.\n+\n+    Resources used:\n+    https://stackoverflow.com/questions/13197552/using-binary-search-with-sorted-array-with-duplicates\n+\n+    The collection must be sorted in ascending order; otherwise the result will be\n+    unpredictable. If the target appears multiple times, this function returns a\n+    list of **all** indexes where the target occurs. If the target is not found,\n+    it returns an empty list.\n+\n+    :param sorted_collection: some ascending sorted collection with comparable items\n+    :param item: item value to search for\n+    :return: a list of indexes where the item is found (empty list if not found)\n+\n+    Examples:\n+    >>> binary_search_with_duplicates([0, 5, 7, 10, 15], 0)\n+    [0]\n+    >>> binary_search_with_duplicates([0, 5, 7, 10, 15], 15)\n+    [4]\n+    >>> binary_search_with_duplicates([1, 2, 2, 2, 3], 2)\n+    [1, 2, 3]\n+    >>> binary_search_with_duplicates([1, 2, 2, 2, 3], 4)\n+    []\n+    \"\"\"\n+    if list(sorted_collection) != sorted(sorted_collection):\n+        raise ValueError(\"sorted_collection must be sorted in ascending order\")\n+\n+    def lower_bound(sorted_collection: list[int], item: int) -> int:\n+        \"\"\"\n+        Returns the index of the first element greater than or equal to the item.\n+\n+        Args:\n+            sorted_collection: The sorted list to search.\n+            item: The item to find the lower bound for.\n+\n+        Returns:\n+            int: The index where the item can be inserted while maintaining order.\n+        \"\"\"\n+        left = 0\n+        right = len(sorted_collection)\n+        while left < right:\n+            midpoint = left + (right - left) // 2\n+            current_item = sorted_collection[midpoint]\n+            if current_item < item:\n+                left = midpoint + 1\n+            else:\n+                right = midpoint\n+        return left\n+\n+    def upper_bound(sorted_collection: list[int], item: int) -> int:\n+        \"\"\"\n+        Returns the index of the first element strictly greater than the item.\n+\n+        Args:\n+            sorted_collection: The sorted list to search.\n+            item: The item to find the upper bound for.\n+\n+        Returns:\n+            int: The index where the item can be inserted after all existing instances.\n+        \"\"\"\n+        left = 0\n+        right = len(sorted_collection)\n+        while left < right:\n+            midpoint = left + (right - left) // 2\n+            current_item = sorted_collection[midpoint]\n+            if current_item <= item:\n+                left = midpoint + 1\n+            else:\n+                right = midpoint\n+        return left",
        "comment_created_at": "2025-12-13T00:28:05+00:00",
        "comment_author": "shuhao-alan-fan",
        "comment_body": "Changing to Sphinx-style format for consistency ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1781733144",
    "pr_number": 11589,
    "pr_file": "backtracking/coloring.py",
    "created_at": "2024-09-30T21:04:43+00:00",
    "commented_code": "neighbours: list[int], colored_vertices: list[int], color: int\n ) -> bool:\n     \"\"\"\n+    This function checks if a given vertex can be assigned the specified color",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1781733144",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11589,
        "pr_file": "backtracking/coloring.py",
        "discussion_id": "1781733144",
        "commented_code": "@@ -11,22 +11,46 @@ def valid_coloring(\n     neighbours: list[int], colored_vertices: list[int], color: int\n ) -> bool:\n     \"\"\"\n+    This function checks if a given vertex can be assigned the specified color",
        "comment_created_at": "2024-09-30T21:04:43+00:00",
        "comment_author": "cclauss",
        "comment_body": "```suggestion\r\n    Check if a given vertex can be assigned the specified color\r\n```\r\nhttps://docs.astral.sh/ruff/rules/docstring-starts-with-this/",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1781734868",
    "pr_number": 11589,
    "pr_file": "backtracking/coloring.py",
    "created_at": "2024-09-30T21:06:47+00:00",
    "commented_code": "neighbours: list[int], colored_vertices: list[int], color: int\n ) -> bool:\n     \"\"\"\n+    This function checks if a given vertex can be assigned the specified color\n+    without violating the graph coloring constraints (i.e., no two adjacent vertices\n+    have the same color).\n+\n+    Procedure:\n     For each neighbour check if the coloring constraint is satisfied\n     If any of the neighbours fail the constraint return False\n     If all neighbours validate the constraint return True\n \n-    >>> neighbours = [0,1,0,1,0]\n+    Parameters:\n+    neighbours (list[int]): The list representing which vertices",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1781734868",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11589,
        "pr_file": "backtracking/coloring.py",
        "discussion_id": "1781734868",
        "commented_code": "@@ -11,22 +11,46 @@ def valid_coloring(\n     neighbours: list[int], colored_vertices: list[int], color: int\n ) -> bool:\n     \"\"\"\n+    This function checks if a given vertex can be assigned the specified color\n+    without violating the graph coloring constraints (i.e., no two adjacent vertices\n+    have the same color).\n+\n+    Procedure:\n     For each neighbour check if the coloring constraint is satisfied\n     If any of the neighbours fail the constraint return False\n     If all neighbours validate the constraint return True\n \n-    >>> neighbours = [0,1,0,1,0]\n+    Parameters:\n+    neighbours (list[int]): The list representing which vertices",
        "comment_created_at": "2024-09-30T21:06:47+00:00",
        "comment_author": "cclauss",
        "comment_body": "```suggestion\r\n    neighbours: The list representing which vertices\r\n```\r\nPlease do not repeat the data types which are already stated in the function signature above and tested by mypy.  Readers will become confused if one is changed and the other is not.",
        "pr_file_module": null
      },
      {
        "comment_id": "1782046467",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11589,
        "pr_file": "backtracking/coloring.py",
        "discussion_id": "1781734868",
        "commented_code": "@@ -11,22 +11,46 @@ def valid_coloring(\n     neighbours: list[int], colored_vertices: list[int], color: int\n ) -> bool:\n     \"\"\"\n+    This function checks if a given vertex can be assigned the specified color\n+    without violating the graph coloring constraints (i.e., no two adjacent vertices\n+    have the same color).\n+\n+    Procedure:\n     For each neighbour check if the coloring constraint is satisfied\n     If any of the neighbours fail the constraint return False\n     If all neighbours validate the constraint return True\n \n-    >>> neighbours = [0,1,0,1,0]\n+    Parameters:\n+    neighbours (list[int]): The list representing which vertices",
        "comment_created_at": "2024-10-01T02:51:45+00:00",
        "comment_author": "Hardvan",
        "comment_body": "Got it, thanks for the feedback!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1342320121",
    "pr_number": 9317,
    "pr_file": "other/coordinate_compression.py",
    "created_at": "2023-10-02T06:32:45+00:00",
    "commented_code": "+\"\"\"\n+Assumption:\n+    - The values to compress are assumed to be comparable,\n+    - values can be sorted and compared with '<' and '>' operators.\n+\"\"\"\n+\n+\n+class CoordinateCompressor:\n+    \"\"\"\n+    A class for coordinate compression.\n+\n+    This class allows you to compress and decompress a list of values.\n+\n+    Mapping:\n+    In addition to compression and decompression, this class maintains a mapping\n+    between original values and their compressed counterparts using two data\n+    structures: a dictionary `coordinate_map` and a list `reverse_map`.\n+\n+    - `coordinate_map`: A dictionary that maps original values to their compressed\n+      coordinates. Keys are original values, and values are compressed coordinates.\n+\n+    - `reverse_map`: A list used for reverse mapping, where each index corresponds\n+      to a compressed coordinate, and the value at that index is the original value.\n+\n+    Example mapping:\n+\n+    Original: 10, Compressed: 0\n+    Original: 52, Compressed: 1\n+    Original: 83, Compressed: 2\n+    Original: 100, Compressed: 3\n+\n+    This mapping allows for efficient compression and decompression of values within\n+    the list.\n+    \"\"\"\n+\n+    def __init__(self, arr: list[int | float | str]) -> None:\n+        \"\"\"\n+        Initialize the CoordinateCompressor with a list.\n+\n+        Args:\n+        arr (list): The list of values to be compressed.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1342320121",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 9317,
        "pr_file": "other/coordinate_compression.py",
        "discussion_id": "1342320121",
        "commented_code": "@@ -0,0 +1,141 @@\n+\"\"\"\n+Assumption:\n+    - The values to compress are assumed to be comparable,\n+    - values can be sorted and compared with '<' and '>' operators.\n+\"\"\"\n+\n+\n+class CoordinateCompressor:\n+    \"\"\"\n+    A class for coordinate compression.\n+\n+    This class allows you to compress and decompress a list of values.\n+\n+    Mapping:\n+    In addition to compression and decompression, this class maintains a mapping\n+    between original values and their compressed counterparts using two data\n+    structures: a dictionary `coordinate_map` and a list `reverse_map`.\n+\n+    - `coordinate_map`: A dictionary that maps original values to their compressed\n+      coordinates. Keys are original values, and values are compressed coordinates.\n+\n+    - `reverse_map`: A list used for reverse mapping, where each index corresponds\n+      to a compressed coordinate, and the value at that index is the original value.\n+\n+    Example mapping:\n+\n+    Original: 10, Compressed: 0\n+    Original: 52, Compressed: 1\n+    Original: 83, Compressed: 2\n+    Original: 100, Compressed: 3\n+\n+    This mapping allows for efficient compression and decompression of values within\n+    the list.\n+    \"\"\"\n+\n+    def __init__(self, arr: list[int | float | str]) -> None:\n+        \"\"\"\n+        Initialize the CoordinateCompressor with a list.\n+\n+        Args:\n+        arr (list): The list of values to be compressed.",
        "comment_created_at": "2023-10-02T06:32:45+00:00",
        "comment_author": "cclauss",
        "comment_body": "Mypy can check the data types in the function signature but not in the comments.\r\n\r\nPlease do not repeat the data types in both places because readers will be confused if one changes but the other does not.\r\n```suggestion\r\n        arr: The list of values to be compressed.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1899605476",
    "pr_number": 12491,
    "pr_file": "computer_vision/intensity_based_segmentation.py",
    "created_at": "2024-12-30T15:03:31+00:00",
    "commented_code": "+# Source: \"https://www.ijcse.com/docs/IJCSE11-02-03-117.pdf\"\r\n+\r\n+# Importing necessary libraries\r\n+import numpy as np\r\n+from PIL import Image\r\n+import matplotlib.pyplot as plt\r\n+\r\n+\r\n+def segment_image(image: np.ndarray, thresholds: list[int]) -> np.ndarray:\r\n+    \"\"\"\r\n+    Performs image segmentation based on intensity thresholds.\r\n+\r\n+    Args:\r\n+        image (np.ndarray): Input grayscale image as a 2D array.\r\n+        thresholds (list[int]): Intensity thresholds to define segments.\r\n+\r\n+    Returns:\r\n+        np.ndarray: A labeled 2D array where each region corresponds to a threshold range.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1899605476",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12491,
        "pr_file": "computer_vision/intensity_based_segmentation.py",
        "discussion_id": "1899605476",
        "commented_code": "@@ -0,0 +1,62 @@\n+# Source: \"https://www.ijcse.com/docs/IJCSE11-02-03-117.pdf\"\r\n+\r\n+# Importing necessary libraries\r\n+import numpy as np\r\n+from PIL import Image\r\n+import matplotlib.pyplot as plt\r\n+\r\n+\r\n+def segment_image(image: np.ndarray, thresholds: list[int]) -> np.ndarray:\r\n+    \"\"\"\r\n+    Performs image segmentation based on intensity thresholds.\r\n+\r\n+    Args:\r\n+        image (np.ndarray): Input grayscale image as a 2D array.\r\n+        thresholds (list[int]): Intensity thresholds to define segments.\r\n+\r\n+    Returns:\r\n+        np.ndarray: A labeled 2D array where each region corresponds to a threshold range.\r",
        "comment_created_at": "2024-12-30T15:03:31+00:00",
        "comment_author": "cclauss",
        "comment_body": "Please do not repeat the datatypes that are in the function signature and verified by `mypy`. Duplicates are problematic when one is changed and the other is not, and the reader does not know which one to trust.\r\n```suggestion\r\n        image: Input grayscale image as a 2D array.\r\n        thresholds: Intensity thresholds to define segments.\r\n\r\n    Returns:\r\n        A labeled 2D array where each region corresponds to a threshold range.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1375728250",
    "pr_number": 11080,
    "pr_file": "fuzzy_logic/fuzzy_operations.py",
    "created_at": "2023-10-30T06:35:35+00:00",
    "commented_code": "\"\"\"\n         Calculate the complement (negation) of this fuzzy set.\n         Returns:\n-            FuzzySet: A new fuzzy set representing the complement.\n+        FuzzySet: A new fuzzy set representing the complement.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1375728250",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11080,
        "pr_file": "fuzzy_logic/fuzzy_operations.py",
        "discussion_id": "1375728250",
        "commented_code": "@@ -78,7 +86,7 @@ def complement(self) -> FuzzySet:\n         \"\"\"\n         Calculate the complement (negation) of this fuzzy set.\n         Returns:\n-            FuzzySet: A new fuzzy set representing the complement.\n+        FuzzySet: A new fuzzy set representing the complement.",
        "comment_created_at": "2023-10-30T06:35:35+00:00",
        "comment_author": "cclauss",
        "comment_body": "Please do not repeat the data types that are already in the function signature.  The reason is that mypy checks the types in the function signature but not in the docstring and readers will get confused if one is changed and the other is not.\r\n```suggestion\r\n            A new fuzzy set representing the complement.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1782116475",
    "pr_number": 11597,
    "pr_file": "computer_vision/haralick_descriptors.py",
    "created_at": "2024-10-01T05:07:21+00:00",
    "commented_code": null,
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1782116475",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11597,
        "pr_file": "computer_vision/haralick_descriptors.py",
        "discussion_id": "1782116475",
        "commented_code": null,
        "comment_created_at": "2024-10-01T05:07:21+00:00",
        "comment_author": "tianyizheng02",
        "comment_body": "Do not delete the docstrings. Not only are they the documentation for the functions in the file, but they also contain all the unit testing for the code.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1783556747",
    "pr_number": 11636,
    "pr_file": "maths/fibonacci.py",
    "created_at": "2024-10-01T21:33:43+00:00",
    "commented_code": "return [round(phi**i / sqrt_5) for i in range(n + 1)]\n \n \n+def fib_matrix(n: int) -> int:\n+    # https://www.nayuki.io/page/fast-fibonacci-algorithms#:~:text=\n+    # Summary:%20The%20two%20fast%20Fibonacci%20algorithms%20are%20matrix",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1783556747",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11636,
        "pr_file": "maths/fibonacci.py",
        "discussion_id": "1783556747",
        "commented_code": "@@ -230,6 +232,40 @@ def fib_binet(n: int) -> list[int]:\n     return [round(phi**i / sqrt_5) for i in range(n + 1)]\n \n \n+def fib_matrix(n: int) -> int:\n+    # https://www.nayuki.io/page/fast-fibonacci-algorithms#:~:text=\n+    # Summary:%20The%20two%20fast%20Fibonacci%20algorithms%20are%20matrix",
        "comment_created_at": "2024-10-01T21:33:43+00:00",
        "comment_author": "tianyizheng02",
        "comment_body": "1. Could you make this into a docstring so that it serves as documentation as well?\r\n2. This function needs unit tests, which should also go in the docstring.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1785442421",
    "pr_number": 11685,
    "pr_file": "data_structures/stacks/next_greater_element.py",
    "created_at": "2024-10-03T00:07:49+00:00",
    "commented_code": "def next_greatest_element_slow(arr: list[float]) -> list[float]:\n     \"\"\"\n-    Get the Next Greatest Element (NGE) for all elements in a list.\n-    Maximum element present after the current one which is also greater than the\n-    current one.\n+    Get the Next Greatest Element (NGE) for each element in the array\n+    by checking all subsequent elements to find the next greater one.\n+\n+    This is a brute-force implementation, and it has a time complexity\n+    of O(n^2), where n is the size of the array.\n+\n+    Args:\n+        arr (list[float]): List of numbers for which the NGE is calculated.\n+\n+    Returns:\n+        list[float]: List containing the next greatest elements. If no\n+        greater element is found, -1 is placed in the result.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1785442421",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11685,
        "pr_file": "data_structures/stacks/next_greater_element.py",
        "discussion_id": "1785442421",
        "commented_code": "@@ -6,9 +6,20 @@\n \n def next_greatest_element_slow(arr: list[float]) -> list[float]:\n     \"\"\"\n-    Get the Next Greatest Element (NGE) for all elements in a list.\n-    Maximum element present after the current one which is also greater than the\n-    current one.\n+    Get the Next Greatest Element (NGE) for each element in the array\n+    by checking all subsequent elements to find the next greater one.\n+\n+    This is a brute-force implementation, and it has a time complexity\n+    of O(n^2), where n is the size of the array.\n+\n+    Args:\n+        arr (list[float]): List of numbers for which the NGE is calculated.\n+\n+    Returns:\n+        list[float]: List containing the next greatest elements. If no\n+        greater element is found, -1 is placed in the result.",
        "comment_created_at": "2024-10-03T00:07:49+00:00",
        "comment_author": "tianyizheng02",
        "comment_body": "```suggestion\r\n    Args:\r\n        arr: List of numbers for which the NGE is calculated.\r\n\r\n    Returns:\r\n        List containing the next greatest elements. If no\r\n        greater element is found, -1 is placed in the result.\r\n```\r\nIf the type of the variable ever changes for any reason, we'd need to remember to update the type hint in two different places. As such, in this repo we prefer to avoid specifying the variable types in the docstrings.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1404851876",
    "pr_number": 11171,
    "pr_file": "strings/levenshtein_distance.py",
    "created_at": "2023-11-25T11:22:08+00:00",
    "commented_code": "\"\"\"\n-This is a Python implementation of the levenshtein distance.\n-Levenshtein distance is a string metric for measuring the\n-difference between two sequences.\n+This is an optimized Python implementation of the Levenshtein distance algorithm.\n+Levenshtein distance is a metric for measuring differences between sequences.\n \n-For doctests run following command:\n-python -m doctest -v levenshtein-distance.py\n+For doctests, run the following command:\n+python -m doctest -v levenshtein_distance.py\n or\n-python3 -m doctest -v levenshtein-distance.py\n+python3 -m doctest -v levenshtein_distance.py\n \n-For manual testing run:\n-python levenshtein-distance.py\n+For manual testing, run:\n+python levenshtein_distance.py\n \"\"\"\n \n \n-def levenshtein_distance(first_word: str, second_word: str) -> int:\n-    \"\"\"Implementation of the levenshtein distance in Python.\n-    :param first_word: the first word to measure the difference.\n-    :param second_word: the second word to measure the difference.\n-    :return: the levenshtein distance between the two words.\n+def levenshtein_distance_optimized(first_word: str, second_word: str) -> int:\n+    \"\"\"\n+    Compute the Levenshtein distance between two words (strings).\n+\n+    The function is optimized for efficiency by modifying rows in place.\n+\n+    Parameters:\n+    first_word (str): The first word to measure the difference.\n+    second_word (str): The second word to measure the difference.\n+\n+    Returns:\n+    int: The Levenshtein distance between the two words.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1404851876",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11171,
        "pr_file": "strings/levenshtein_distance.py",
        "discussion_id": "1404851876",
        "commented_code": "@@ -1,70 +1,71 @@\n \"\"\"\n-This is a Python implementation of the levenshtein distance.\n-Levenshtein distance is a string metric for measuring the\n-difference between two sequences.\n+This is an optimized Python implementation of the Levenshtein distance algorithm.\n+Levenshtein distance is a metric for measuring differences between sequences.\n \n-For doctests run following command:\n-python -m doctest -v levenshtein-distance.py\n+For doctests, run the following command:\n+python -m doctest -v levenshtein_distance.py\n or\n-python3 -m doctest -v levenshtein-distance.py\n+python3 -m doctest -v levenshtein_distance.py\n \n-For manual testing run:\n-python levenshtein-distance.py\n+For manual testing, run:\n+python levenshtein_distance.py\n \"\"\"\n \n \n-def levenshtein_distance(first_word: str, second_word: str) -> int:\n-    \"\"\"Implementation of the levenshtein distance in Python.\n-    :param first_word: the first word to measure the difference.\n-    :param second_word: the second word to measure the difference.\n-    :return: the levenshtein distance between the two words.\n+def levenshtein_distance_optimized(first_word: str, second_word: str) -> int:\n+    \"\"\"\n+    Compute the Levenshtein distance between two words (strings).\n+\n+    The function is optimized for efficiency by modifying rows in place.\n+\n+    Parameters:\n+    first_word (str): The first word to measure the difference.\n+    second_word (str): The second word to measure the difference.\n+\n+    Returns:\n+    int: The Levenshtein distance between the two words.",
        "comment_created_at": "2023-11-25T11:22:08+00:00",
        "comment_author": "cclauss",
        "comment_body": "This is a standard way to document parameters and return values.  Please do not repeat the datatypes that are in the function signature because mypy validates them and readers will be confused if one is change and the other is not.\r\n```suggestion\r\n    :param first_word: the first word to measure the difference.\r\n    :param second_word: the second word to measure the difference.\r\n    :return: the Levenshtein distance between the two words.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1389865216",
    "pr_number": 11149,
    "pr_file": "data_structures/stacks/stack.py",
    "created_at": "2023-11-10T20:07:22+00:00",
    "commented_code": "return self.stack[-1]\n \n     def is_empty(self) -> bool:\n-        \"\"\"Check if a stack is empty.\"\"\"\n+        \"\"\"\n+        Check if a stack is empty.\n+\n+        >>> S = Stack()\n+        >>> S.is_empty()\n+        True\n+\n+        >>> S = Stack()\n+        >>> S.push(10)\n+        >>> S.is_empty()\n+        False\n+        \"\"\"\n         return not bool(self.stack)\n \n     def is_full(self) -> bool:\n+        \"\"\"\n+        >>> S = Stack()\n+        >>> S.is_full()\n+        False\n+\n+        >>> S = Stack(1)\n+        >>> S.push(10)\n+        >>> S.is_full()\n+        True\n+        \"\"\"\n         return self.size() == self.limit\n \n     def size(self) -> int:\n-        \"\"\"Return the size of the stack.\"\"\"\n+        \"\"\"\n+        Return the size of the stack.\n+\n+        >>> S = Stack(3)\n+        >>> S.size()\n+        0\n+\n+        >>> S = Stack(3)\n+        >>> S.push(10)\n+        >>> S.size()\n+        1\n+\n+        >>> S = Stack(3)\n+        >>> S.push(10)\n+        >>> S.push(20)\n+        >>> S.size()\n+        2\n+        \"\"\"\n         return len(self.stack)\n \n     def __contains__(self, item: T) -> bool:\n-        \"\"\"Check if item is in stack\"\"\"\n+        \"\"\"\n+        Check if item is in stack\n+\n+        >>> S = Stack(3)\n+        >>> S.push(10)\n+        >>> S.__contains__(10)",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1389865216",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11149,
        "pr_file": "data_structures/stacks/stack.py",
        "discussion_id": "1389865216",
        "commented_code": "@@ -65,18 +93,68 @@ def peek(self) -> T:\n         return self.stack[-1]\n \n     def is_empty(self) -> bool:\n-        \"\"\"Check if a stack is empty.\"\"\"\n+        \"\"\"\n+        Check if a stack is empty.\n+\n+        >>> S = Stack()\n+        >>> S.is_empty()\n+        True\n+\n+        >>> S = Stack()\n+        >>> S.push(10)\n+        >>> S.is_empty()\n+        False\n+        \"\"\"\n         return not bool(self.stack)\n \n     def is_full(self) -> bool:\n+        \"\"\"\n+        >>> S = Stack()\n+        >>> S.is_full()\n+        False\n+\n+        >>> S = Stack(1)\n+        >>> S.push(10)\n+        >>> S.is_full()\n+        True\n+        \"\"\"\n         return self.size() == self.limit\n \n     def size(self) -> int:\n-        \"\"\"Return the size of the stack.\"\"\"\n+        \"\"\"\n+        Return the size of the stack.\n+\n+        >>> S = Stack(3)\n+        >>> S.size()\n+        0\n+\n+        >>> S = Stack(3)\n+        >>> S.push(10)\n+        >>> S.size()\n+        1\n+\n+        >>> S = Stack(3)\n+        >>> S.push(10)\n+        >>> S.push(20)\n+        >>> S.size()\n+        2\n+        \"\"\"\n         return len(self.stack)\n \n     def __contains__(self, item: T) -> bool:\n-        \"\"\"Check if item is in stack\"\"\"\n+        \"\"\"\n+        Check if item is in stack\n+\n+        >>> S = Stack(3)\n+        >>> S.push(10)\n+        >>> S.__contains__(10)",
        "comment_created_at": "2023-11-10T20:07:22+00:00",
        "comment_author": "tianyizheng02",
        "comment_body": "```suggestion\r\n        >>> 10 in S\r\n```\r\nThe point of `__contains__` is to overload the built-in `in` operator",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1351582320",
    "pr_number": 9967,
    "pr_file": "graphs/dijkstra_2.py",
    "created_at": "2023-10-10T06:16:40+00:00",
    "commented_code": "def print_dist(dist, v):\n+    \"\"\"\n+    Print vertex distances.\n+\n+    Parameters:\n+    dist (list): A list of distances.\n+    v (int): The number of vertices.",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1351582320",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 9967,
        "pr_file": "graphs/dijkstra_2.py",
        "discussion_id": "1351582320",
        "commented_code": "@@ -1,14 +1,41 @@\n def print_dist(dist, v):\n+    \"\"\"\n+    Print vertex distances.\n+\n+    Parameters:\n+    dist (list): A list of distances.\n+    v (int): The number of vertices.",
        "comment_created_at": "2023-10-10T06:16:40+00:00",
        "comment_author": "cclauss",
        "comment_body": "Putting the type hints into the function signature allows [mypy](https://mypy.readthedocs.io) to check them in our tests.  Please do not repeat them in the docstring below because readers will get confused if one is changed but the other is not.\r\n```suggestion\r\ndef print_dist(dist: list[float], v: int):\r\n    \"\"\"\r\n    Print vertex distances.\r\n\r\n    Parameters:\r\n    dist: A list of distances.\r\n    v: The number of vertices.\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1375115538",
    "pr_number": 11042,
    "pr_file": "digital_image_processing/edge_detection/watershed.py",
    "created_at": "2023-10-27T23:21:29+00:00",
    "commented_code": "+# import the necessary packages\n+from skimage.feature import peak_local_max\n+from skimage.segmentation import watershed\n+from scipy import ndimage\n+import numpy as np\n+import imutils\n+import cv2\n+\n+\n+def watershed_image(path_image) -> None:",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1375115538",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 11042,
        "pr_file": "digital_image_processing/edge_detection/watershed.py",
        "discussion_id": "1375115538",
        "commented_code": "@@ -0,0 +1,54 @@\n+# import the necessary packages\n+from skimage.feature import peak_local_max\n+from skimage.segmentation import watershed\n+from scipy import ndimage\n+import numpy as np\n+import imutils\n+import cv2\n+\n+\n+def watershed_image(path_image) -> None:",
        "comment_created_at": "2023-10-27T23:21:29+00:00",
        "comment_author": "cclauss",
        "comment_body": "```suggestion\r\ndef watershed_image(path_image: str) -> None:\r\n    \"\"\"\r\n    https://en.wikipedia.org/wiki/Watershed_(image_processing)\r\n    \"\"\"\r\n```",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1368150428",
    "pr_number": 10237,
    "pr_file": "physics/terminal_velocity.py",
    "created_at": "2023-10-23T05:38:13+00:00",
    "commented_code": "+from scipy.constants import g\n+\n+\"\"\"\n+Title : Computing the terminal velocity of an object falling\n+        through a fluid.\n+\n+Terminal velocity is defined as the highest velocity attained by an\n+object falling through a fluid. It is observed when the sum of drag force\n+and buoyancy is equal to the downward gravity force acting on the\n+object. The acceleration of the object is zero as the net force acting on\n+the object is zero.\n+\n+Vt = ((2 * m * g)/(\u03c1 * A * Cd))^0.5\n+\n+where :\n+Vt = Terminal velocity (in m/s)\n+m = Mass of the falling object (in Kg)\n+g = Acceleration due to gravity (value taken : imported from scipy)\n+\u03c1 = Density of the fluid through which the object is falling (in Kg/m^3)\n+A = Projected area of the object (in m^2)\n+Cd = Drag coefficient (dimensionless)\n+\n+Reference : https://byjus.com/physics/derivation-of-terminal-velocity/\n+\"\"\"",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1368150428",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 10237,
        "pr_file": "physics/terminal_velocity.py",
        "discussion_id": "1368150428",
        "commented_code": "@@ -0,0 +1,60 @@\n+from scipy.constants import g\n+\n+\"\"\"\n+Title : Computing the terminal velocity of an object falling\n+        through a fluid.\n+\n+Terminal velocity is defined as the highest velocity attained by an\n+object falling through a fluid. It is observed when the sum of drag force\n+and buoyancy is equal to the downward gravity force acting on the\n+object. The acceleration of the object is zero as the net force acting on\n+the object is zero.\n+\n+Vt = ((2 * m * g)/(\u03c1 * A * Cd))^0.5\n+\n+where :\n+Vt = Terminal velocity (in m/s)\n+m = Mass of the falling object (in Kg)\n+g = Acceleration due to gravity (value taken : imported from scipy)\n+\u03c1 = Density of the fluid through which the object is falling (in Kg/m^3)\n+A = Projected area of the object (in m^2)\n+Cd = Drag coefficient (dimensionless)\n+\n+Reference : https://byjus.com/physics/derivation-of-terminal-velocity/\n+\"\"\"",
        "comment_created_at": "2023-10-23T05:38:13+00:00",
        "comment_author": "tianyizheng02",
        "comment_body": "```suggestion\r\n\"\"\"\r\nTitle : Computing the terminal velocity of an object falling\r\n        through a fluid.\r\n\r\nTerminal velocity is defined as the highest velocity attained by an\r\nobject falling through a fluid. It is observed when the sum of drag force\r\nand buoyancy is equal to the downward gravity force acting on the\r\nobject. The acceleration of the object is zero as the net force acting on\r\nthe object is zero.\r\n\r\nVt = ((2 * m * g)/(\u03c1 * A * Cd))^0.5\r\n\r\nwhere :\r\nVt = Terminal velocity (in m/s)\r\nm = Mass of the falling object (in Kg)\r\ng = Acceleration due to gravity (value taken : imported from scipy)\r\n\u03c1 = Density of the fluid through which the object is falling (in Kg/m^3)\r\nA = Projected area of the object (in m^2)\r\nCd = Drag coefficient (dimensionless)\r\n\r\nReference : https://byjus.com/physics/derivation-of-terminal-velocity/\r\n\"\"\"\r\n\r\nfrom scipy.constants import g\r\n```\r\n\r\nGenerally the docstring should be at the very top of the file, before the imports",
        "pr_file_module": null
      },
      {
        "comment_id": "1368150957",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 10237,
        "pr_file": "physics/terminal_velocity.py",
        "discussion_id": "1368150428",
        "commented_code": "@@ -0,0 +1,60 @@\n+from scipy.constants import g\n+\n+\"\"\"\n+Title : Computing the terminal velocity of an object falling\n+        through a fluid.\n+\n+Terminal velocity is defined as the highest velocity attained by an\n+object falling through a fluid. It is observed when the sum of drag force\n+and buoyancy is equal to the downward gravity force acting on the\n+object. The acceleration of the object is zero as the net force acting on\n+the object is zero.\n+\n+Vt = ((2 * m * g)/(\u03c1 * A * Cd))^0.5\n+\n+where :\n+Vt = Terminal velocity (in m/s)\n+m = Mass of the falling object (in Kg)\n+g = Acceleration due to gravity (value taken : imported from scipy)\n+\u03c1 = Density of the fluid through which the object is falling (in Kg/m^3)\n+A = Projected area of the object (in m^2)\n+Cd = Drag coefficient (dimensionless)\n+\n+Reference : https://byjus.com/physics/derivation-of-terminal-velocity/\n+\"\"\"",
        "comment_created_at": "2023-10-23T05:39:24+00:00",
        "comment_author": "pluto-tofu",
        "comment_body": "okay sure , I'll keep this in mind ",
        "pr_file_module": null
      },
      {
        "comment_id": "1368151453",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 10237,
        "pr_file": "physics/terminal_velocity.py",
        "discussion_id": "1368150428",
        "commented_code": "@@ -0,0 +1,60 @@\n+from scipy.constants import g\n+\n+\"\"\"\n+Title : Computing the terminal velocity of an object falling\n+        through a fluid.\n+\n+Terminal velocity is defined as the highest velocity attained by an\n+object falling through a fluid. It is observed when the sum of drag force\n+and buoyancy is equal to the downward gravity force acting on the\n+object. The acceleration of the object is zero as the net force acting on\n+the object is zero.\n+\n+Vt = ((2 * m * g)/(\u03c1 * A * Cd))^0.5\n+\n+where :\n+Vt = Terminal velocity (in m/s)\n+m = Mass of the falling object (in Kg)\n+g = Acceleration due to gravity (value taken : imported from scipy)\n+\u03c1 = Density of the fluid through which the object is falling (in Kg/m^3)\n+A = Projected area of the object (in m^2)\n+Cd = Drag coefficient (dimensionless)\n+\n+Reference : https://byjus.com/physics/derivation-of-terminal-velocity/\n+\"\"\"",
        "comment_created_at": "2023-10-23T05:40:23+00:00",
        "comment_author": "pluto-tofu",
        "comment_body": "I had another pull request in which I had used the value of g as 9.8 I'll change the value there as well",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1899615390",
    "pr_number": 12436,
    "pr_file": "graphs/graphs_floyd_warshall.py",
    "created_at": "2024-12-30T15:18:52+00:00",
    "commented_code": "# floyd_warshall.py\n \"\"\"\n-The problem is to find the shortest distance between all pairs of vertices in a\n-weighted directed graph that can have negative edge weights.\n+The problem is to find and return the shortest distance between all pairs of vertices\n+in a weighted directed graph that can have negative edge weights.\n \"\"\"\n \n \n-def _print_dist(dist, v):\n-    print(\"\nThe shortest path matrix using Floyd Warshall algorithm\n\")\n-    for i in range(v):\n-        for j in range(v):\n-            if dist[i][j] != float(\"inf\"):\n-                print(int(dist[i][j]), end=\"\\t\")\n-            else:\n-                print(\"INF\", end=\"\\t\")\n-        print()\n+def floyd_warshall(graph: list[list[float]], vertex: int) -> tuple:\n+    # 1. For all edges from v to n, distance[i][j] = weight(edge(i, j)).\n+\n+    # 2. distance[i][j] = min(distance[i][j], distance[i][k] + distance[k][j]).\n \n+    # 3. Step 2 is true for each pair of vertices.Repeat for k vertex in the graph.\n+\n+    # 4. Whenever distance[i][j] is given a new minimum value,\n+    #   next vertex[i][j] = next vertex[i][k].",
    "repo_full_name": "TheAlgorithms/Python",
    "discussion_comments": [
      {
        "comment_id": "1899615390",
        "repo_full_name": "TheAlgorithms/Python",
        "pr_number": 12436,
        "pr_file": "graphs/graphs_floyd_warshall.py",
        "discussion_id": "1899615390",
        "commented_code": "@@ -1,62 +1,112 @@\n # floyd_warshall.py\n \"\"\"\n-The problem is to find the shortest distance between all pairs of vertices in a\n-weighted directed graph that can have negative edge weights.\n+The problem is to find and return the shortest distance between all pairs of vertices\n+in a weighted directed graph that can have negative edge weights.\n \"\"\"\n \n \n-def _print_dist(dist, v):\n-    print(\"\\nThe shortest path matrix using Floyd Warshall algorithm\\n\")\n-    for i in range(v):\n-        for j in range(v):\n-            if dist[i][j] != float(\"inf\"):\n-                print(int(dist[i][j]), end=\"\\t\")\n-            else:\n-                print(\"INF\", end=\"\\t\")\n-        print()\n+def floyd_warshall(graph: list[list[float]], vertex: int) -> tuple:\n+    # 1. For all edges from v to n, distance[i][j] = weight(edge(i, j)).\n+\n+    # 2. distance[i][j] = min(distance[i][j], distance[i][k] + distance[k][j]).\n \n+    # 3. Step 2 is true for each pair of vertices.Repeat for k vertex in the graph.\n+\n+    # 4. Whenever distance[i][j] is given a new minimum value,\n+    #   next vertex[i][j] = next vertex[i][k].",
        "comment_created_at": "2024-12-30T15:18:52+00:00",
        "comment_author": "tianyizheng02",
        "comment_body": "This algorithm explanation should be moved into the docstring at the top of the file",
        "pr_file_module": null
      }
    ]
  }
]