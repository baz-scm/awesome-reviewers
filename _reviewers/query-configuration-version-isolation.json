[
  {
    "discussion_id": "2383962675",
    "pr_number": 9493,
    "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
    "created_at": "2025-09-27T07:26:49+00:00",
    "commented_code": "options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2383962675",
        "repo_full_name": "TanStack/query",
        "pr_number": 9493,
        "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
        "discussion_id": "2383962675",
        "commented_code": "@@ -24,48 +23,62 @@ export function createMutation<\n   options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
        "comment_created_at": "2025-09-27T07:26:49+00:00",
        "comment_author": "hmnd",
        "comment_body": "@lachlancollins do you think we need to keep the Svelte peer dep pinned to as low as `5.7.0`, or could we bump that to at least `5.25.0` [so that assigning to `$derived` like this is supported](https://svelte.dev/docs/svelte/$derived#Overriding-derived-values)? This was part of `eslint --fix`, but if we need to stay on the lower version, I'll have to revert this back to a manually managed `$state`.",
        "pr_file_module": null
      },
      {
        "comment_id": "2383963594",
        "repo_full_name": "TanStack/query",
        "pr_number": 9493,
        "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
        "discussion_id": "2383962675",
        "commented_code": "@@ -24,48 +23,62 @@ export function createMutation<\n   options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
        "comment_created_at": "2025-09-27T07:29:25+00:00",
        "comment_author": "lachlancollins",
        "comment_body": "I'm happy to bump the peer dependency version!",
        "pr_file_module": null
      },
      {
        "comment_id": "2383973200",
        "repo_full_name": "TanStack/query",
        "pr_number": 9493,
        "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
        "discussion_id": "2383962675",
        "commented_code": "@@ -24,48 +23,62 @@ export function createMutation<\n   options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
        "comment_created_at": "2025-09-27T07:41:21+00:00",
        "comment_author": "lachlancollins",
        "comment_body": "@hmnd I've updated this on the target branch.",
        "pr_file_module": null
      },
      {
        "comment_id": "2383985882",
        "repo_full_name": "TanStack/query",
        "pr_number": 9493,
        "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
        "discussion_id": "2383962675",
        "commented_code": "@@ -24,48 +23,62 @@ export function createMutation<\n   options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
        "comment_created_at": "2025-09-27T08:03:14+00:00",
        "comment_author": "lachlancollins",
        "comment_body": "It seems like this change(?) might have caused tests to start failing. It's difficult to know though, as the force pushes have erased the history.",
        "pr_file_module": null
      },
      {
        "comment_id": "2384121712",
        "repo_full_name": "TanStack/query",
        "pr_number": 9493,
        "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
        "discussion_id": "2383962675",
        "commented_code": "@@ -24,48 +23,62 @@ export function createMutation<\n   options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
        "comment_created_at": "2025-09-27T12:41:19+00:00",
        "comment_author": "lachlancollins",
        "comment_body": "Nevermind, I found the diff in a branch I cloned earlier. Tests are all passing again, so I think I'll merge this! Thanks heaps for your time @hmnd ! ",
        "pr_file_module": null
      },
      {
        "comment_id": "2386323200",
        "repo_full_name": "TanStack/query",
        "pr_number": 9493,
        "pr_file": "packages/svelte-query/src/createMutation.svelte.ts",
        "discussion_id": "2383962675",
        "commented_code": "@@ -24,48 +23,62 @@ export function createMutation<\n   options: Accessor<CreateMutationOptions<TData, TError, TVariables, TContext>>,\n   queryClient?: Accessor<QueryClient>,\n ): CreateMutationResult<TData, TError, TVariables, TContext> {\n-  const client = useQueryClient(queryClient?.())\n+  const client = $derived(useQueryClient(queryClient?.()))\n \n-  const observer = $derived(\n+  // svelte-ignore state_referenced_locally - intentional, initial value\n+  let observer = $state(\n+    // svelte-ignore state_referenced_locally - intentional, initial value\n     new MutationObserver<TData, TError, TVariables, TContext>(\n       client,\n       options(),\n     ),\n   )\n \n-  const mutate = $state<\n-    CreateMutateFunction<TData, TError, TVariables, TContext>\n-  >((variables, mutateOptions) => {\n-    observer.mutate(variables, mutateOptions).catch(noop)\n-  })\n+  watchChanges(\n+    () => client,\n+    'pre',\n+    () => {\n+      observer = new MutationObserver(client, options())\n+    },\n+  )\n \n   $effect.pre(() => {\n     observer.setOptions(options())\n   })\n \n-  const result = $state(observer.getCurrentResult())\n-\n-  const unsubscribe = observer.subscribe((val) => {\n-    notifyManager.batchCalls(() => {\n-      Object.assign(result, val)\n-    })()\n+  const mutate = <CreateMutateFunction<TData, TError, TVariables, TContext>>((\n+    variables,\n+    mutateOptions,\n+  ) => {\n+    observer.mutate(variables, mutateOptions).catch(noop)\n   })\n \n-  onDestroy(() => {\n-    unsubscribe()\n+  let result = $derived(observer.getCurrentResult())",
        "comment_created_at": "2025-09-28T20:30:23+00:00",
        "comment_author": "hmnd",
        "comment_body": "Thank you for getting this merged @lachlancollins! Sorry about the force pushes :sweat_smile: ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2202424635",
    "pr_number": 9410,
    "pr_file": "packages/eslint-plugin-query/tsup.config.ts",
    "created_at": "2025-07-12T07:57:39+00:00",
    "commented_code": "import { defineConfig } from 'tsup'\n-import { legacyConfig, modernConfig } from './root.tsup.config.js'\n+\n+// @ts-ignore out of scope\n+import { legacyConfig, modernConfig } from '../../scripts/getTsupConfig.js'\n \n export default defineConfig([\n-  modernConfig({ entry: ['src/*.ts'] }),\n-  legacyConfig({ entry: ['src/*.ts'] }),\n+  {\n+    ...modernConfig({ entry: ['src/*.ts'] }),\n+    external: ['typescript'],",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2202424635",
        "repo_full_name": "TanStack/query",
        "pr_number": 9410,
        "pr_file": "packages/eslint-plugin-query/tsup.config.ts",
        "discussion_id": "2202424635",
        "commented_code": "@@ -1,7 +1,26 @@\n import { defineConfig } from 'tsup'\n-import { legacyConfig, modernConfig } from './root.tsup.config.js'\n+\n+// @ts-ignore out of scope\n+import { legacyConfig, modernConfig } from '../../scripts/getTsupConfig.js'\n \n export default defineConfig([\n-  modernConfig({ entry: ['src/*.ts'] }),\n-  legacyConfig({ entry: ['src/*.ts'] }),\n+  {\n+    ...modernConfig({ entry: ['src/*.ts'] }),\n+    external: ['typescript'],",
        "comment_created_at": "2025-07-12T07:57:39+00:00",
        "comment_author": "ss0526100",
        "comment_body": "In eslint-plugin-query, applying the parent-level esbuildPlugin settings directly caused build failures.\r\nTo address this, previous versions used a modified copy of scripts/getTsupConfig.js instead of the original.\r\nConsidering potential future changes to getTsupConfig, this issue needed to be handled.\r\nThus, scripts/getTsupConfig.js was imported using a relative path, and settings specific to eslint-plugin-query were added separately.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1750334210",
    "pr_number": 7988,
    "pr_file": "packages/react-query/src/useBaseQuery.ts",
    "created_at": "2024-09-09T14:02:34+00:00",
    "commented_code": "result,\n   )\n \n+  if (!isServer && willFetch(result, isRestoring) && !observer.hasListeners()) {",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1750334210",
        "repo_full_name": "TanStack/query",
        "pr_number": 7988,
        "pr_file": "packages/react-query/src/useBaseQuery.ts",
        "discussion_id": "1750334210",
        "commented_code": "@@ -131,6 +132,21 @@ export function useBaseQuery<\n     result,\n   )\n \n+  if (!isServer && willFetch(result, isRestoring) && !observer.hasListeners()) {",
        "comment_created_at": "2024-09-09T14:02:34+00:00",
        "comment_author": "TkDodo",
        "comment_body": "should we hide this whole thing behind an `experimental_autoprefetch` option? We could make it a requirement to turn it on if yo want to work with the `experimental_promise`. \r\n\r\nThe thing is I'm not sure if this fetching during render will not cause troubles for other cases that we can't forsee yet.",
        "pr_file_module": null
      },
      {
        "comment_id": "1750337768",
        "repo_full_name": "TanStack/query",
        "pr_number": 7988,
        "pr_file": "packages/react-query/src/useBaseQuery.ts",
        "discussion_id": "1750334210",
        "commented_code": "@@ -131,6 +132,21 @@ export function useBaseQuery<\n     result,\n   )\n \n+  if (!isServer && willFetch(result, isRestoring) && !observer.hasListeners()) {",
        "comment_created_at": "2024-09-09T14:04:24+00:00",
        "comment_author": "TkDodo",
        "comment_body": "alternatively, we could ship a new experimental hook that has this turned on ?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1316017599",
    "pr_number": 5953,
    "pr_file": "packages/eslint-plugin-query/src/rules/prefer-query-object-syntax/prefer-query-object-syntax.test.ts",
    "created_at": "2023-09-05T14:51:55+00:00",
    "commented_code": "import { name, rule } from './prefer-query-object-syntax'\n \n const ruleTester = new ESLintUtils.RuleTester({\n-  parser: '@typescript-eslint/parser',\n+  /** HACK\n+   * We are using a newer version of typescript than the one at the repository root,\n+   * to be able to test code that uses newer typescript features.\n+   * To prevent RuleTester from using the typescript version at the root\n+   * we resolve '@typescript-eslint/parser' here instead of letting it do it internally.\n+   * Once the version at the root is updated to >=4.9 this can be replaced by just \"@typescript-eslint/parser\"\n+   */\n+  parser: require.resolve('@typescript-eslint/parser') as any,",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1316017599",
        "repo_full_name": "TanStack/query",
        "pr_number": 5953,
        "pr_file": "packages/eslint-plugin-query/src/rules/prefer-query-object-syntax/prefer-query-object-syntax.test.ts",
        "discussion_id": "1316017599",
        "commented_code": "@@ -3,7 +3,14 @@ import { normalizeIndent } from '../../utils/test-utils'\n import { name, rule } from './prefer-query-object-syntax'\n \n const ruleTester = new ESLintUtils.RuleTester({\n-  parser: '@typescript-eslint/parser',\n+  /** HACK\n+   * We are using a newer version of typescript than the one at the repository root,\n+   * to be able to test code that uses newer typescript features.\n+   * To prevent RuleTester from using the typescript version at the root\n+   * we resolve '@typescript-eslint/parser' here instead of letting it do it internally.\n+   * Once the version at the root is updated to >=4.9 this can be replaced by just \"@typescript-eslint/parser\"\n+   */\n+  parser: require.resolve('@typescript-eslint/parser') as any,",
        "comment_created_at": "2023-09-05T14:51:55+00:00",
        "comment_author": "TkDodo",
        "comment_body": "where are we using an older version of `@typescript-eslint/parser` ?",
        "pr_file_module": null
      },
      {
        "comment_id": "1316042234",
        "repo_full_name": "TanStack/query",
        "pr_number": 5953,
        "pr_file": "packages/eslint-plugin-query/src/rules/prefer-query-object-syntax/prefer-query-object-syntax.test.ts",
        "discussion_id": "1316017599",
        "commented_code": "@@ -3,7 +3,14 @@ import { normalizeIndent } from '../../utils/test-utils'\n import { name, rule } from './prefer-query-object-syntax'\n \n const ruleTester = new ESLintUtils.RuleTester({\n-  parser: '@typescript-eslint/parser',\n+  /** HACK\n+   * We are using a newer version of typescript than the one at the repository root,\n+   * to be able to test code that uses newer typescript features.\n+   * To prevent RuleTester from using the typescript version at the root\n+   * we resolve '@typescript-eslint/parser' here instead of letting it do it internally.\n+   * Once the version at the root is updated to >=4.9 this can be replaced by just \"@typescript-eslint/parser\"\n+   */\n+  parser: require.resolve('@typescript-eslint/parser') as any,",
        "comment_created_at": "2023-09-05T15:09:34+00:00",
        "comment_author": "eg-bernardo",
        "comment_body": "here: https://github.com/TanStack/query/blob/ef5125f68bc41c6c01219f7cb4b098b8d35a2045/package.json#L61\r\n\r\nI came to the conclusion that I also needed to update typescript to >=4.9 to be able to test `satisfies` and I didn't wan't to change the typescript version in the respository root. But maybe I can update @typescript-eslint in the root. I will give it another look.",
        "pr_file_module": null
      },
      {
        "comment_id": "1316096252",
        "repo_full_name": "TanStack/query",
        "pr_number": 5953,
        "pr_file": "packages/eslint-plugin-query/src/rules/prefer-query-object-syntax/prefer-query-object-syntax.test.ts",
        "discussion_id": "1316017599",
        "commented_code": "@@ -3,7 +3,14 @@ import { normalizeIndent } from '../../utils/test-utils'\n import { name, rule } from './prefer-query-object-syntax'\n \n const ruleTester = new ESLintUtils.RuleTester({\n-  parser: '@typescript-eslint/parser',\n+  /** HACK\n+   * We are using a newer version of typescript than the one at the repository root,\n+   * to be able to test code that uses newer typescript features.\n+   * To prevent RuleTester from using the typescript version at the root\n+   * we resolve '@typescript-eslint/parser' here instead of letting it do it internally.\n+   * Once the version at the root is updated to >=4.9 this can be replaced by just \"@typescript-eslint/parser\"\n+   */\n+  parser: require.resolve('@typescript-eslint/parser') as any,",
        "comment_created_at": "2023-09-05T15:50:45+00:00",
        "comment_author": "eg-bernardo",
        "comment_body": "I have tried updating only `@typescript-eslint/*` packages to oldest version that suports `satisfies` (5.45.0), but that is not enough, `typescript` >=4.9 is also needed, otherwise the test that uses `satisfies` fails with `A fatal parsing error occurred: Parsing error: ',' expected.`\r\n\r\nI didn't want to update the typescript version in the root, because I assumed that it is probably there to ensure that packages stay compatible with that version of typescript. ",
        "pr_file_module": null
      },
      {
        "comment_id": "1316164878",
        "repo_full_name": "TanStack/query",
        "pr_number": 5953,
        "pr_file": "packages/eslint-plugin-query/src/rules/prefer-query-object-syntax/prefer-query-object-syntax.test.ts",
        "discussion_id": "1316017599",
        "commented_code": "@@ -3,7 +3,14 @@ import { normalizeIndent } from '../../utils/test-utils'\n import { name, rule } from './prefer-query-object-syntax'\n \n const ruleTester = new ESLintUtils.RuleTester({\n-  parser: '@typescript-eslint/parser',\n+  /** HACK\n+   * We are using a newer version of typescript than the one at the repository root,\n+   * to be able to test code that uses newer typescript features.\n+   * To prevent RuleTester from using the typescript version at the root\n+   * we resolve '@typescript-eslint/parser' here instead of letting it do it internally.\n+   * Once the version at the root is updated to >=4.9 this can be replaced by just \"@typescript-eslint/parser\"\n+   */\n+  parser: require.resolve('@typescript-eslint/parser') as any,",
        "comment_created_at": "2023-09-05T17:05:42+00:00",
        "comment_author": "TkDodo",
        "comment_body": "ah yes, I see. what we'd want is to develop with TS 5.2, but have a type-testing suite that works against all sorts of TS versions.\r\n\r\n@ZackDeRose started with this some time back, but we never followed through:\r\n\r\n- https://github.com/TanStack/query/pull/5640\r\n\r\nI'll take a look at setting this up later this week, then we should be good without this workaround \ud83d\udc4d ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1508068465",
    "pr_number": 6999,
    "pr_file": "packages/query-core/src/queryObserver.ts",
    "created_at": "2024-02-29T19:02:55+00:00",
    "commented_code": "): boolean {\n   return (\n     options.enabled !== false &&\n+    options.queryFn !== skipToken &&",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1508068465",
        "repo_full_name": "TanStack/query",
        "pr_number": 6999,
        "pr_file": "packages/query-core/src/queryObserver.ts",
        "discussion_id": "1508068465",
        "commented_code": "@@ -683,6 +687,7 @@ function shouldLoadOnMount(\n ): boolean {\n   return (\n     options.enabled !== false &&\n+    options.queryFn !== skipToken &&",
        "comment_created_at": "2024-02-29T19:02:55+00:00",
        "comment_author": "Jaaneek",
        "comment_body": "Alternate solution is to assign `options.queryFn !== skipToken` to `options.enabled`.\r\nWhenever we initiate or update the observer we would \r\n```\r\noptions.enabled = options.queryFn !== skipToken\r\n```\r\n\r\nDownside of this is that we will mutate the options.enabled and if someone is checking it (nobody should do it) it might give him a value that he didn't assign. \r\nSo it's potentially not backwards compatible",
        "pr_file_module": null
      },
      {
        "comment_id": "1508547716",
        "repo_full_name": "TanStack/query",
        "pr_number": 6999,
        "pr_file": "packages/query-core/src/queryObserver.ts",
        "discussion_id": "1508068465",
        "commented_code": "@@ -683,6 +687,7 @@ function shouldLoadOnMount(\n ): boolean {\n   return (\n     options.enabled !== false &&\n+    options.queryFn !== skipToken &&",
        "comment_created_at": "2024-03-01T06:49:46+00:00",
        "comment_author": "TkDodo",
        "comment_body": "I think we should centralize the check in the `defaultQueryOptions` function. There, we already set options depending on other options. It would be fine to set `enabled: false` if the `queryFn` is a `skipToken`",
        "pr_file_module": null
      },
      {
        "comment_id": "1509536440",
        "repo_full_name": "TanStack/query",
        "pr_number": 6999,
        "pr_file": "packages/query-core/src/queryObserver.ts",
        "discussion_id": "1508068465",
        "commented_code": "@@ -683,6 +687,7 @@ function shouldLoadOnMount(\n ): boolean {\n   return (\n     options.enabled !== false &&\n+    options.queryFn !== skipToken &&",
        "comment_created_at": "2024-03-01T21:06:57+00:00",
        "comment_author": "Jaaneek",
        "comment_body": "Done! ",
        "pr_file_module": null
      },
      {
        "comment_id": "1509945389",
        "repo_full_name": "TanStack/query",
        "pr_number": 6999,
        "pr_file": "packages/query-core/src/queryObserver.ts",
        "discussion_id": "1508068465",
        "commented_code": "@@ -683,6 +687,7 @@ function shouldLoadOnMount(\n ): boolean {\n   return (\n     options.enabled !== false &&\n+    options.queryFn !== skipToken &&",
        "comment_created_at": "2024-03-02T10:52:56+00:00",
        "comment_author": "TkDodo",
        "comment_body": "can this check not be removed now that we set `enabled: false` if the queryFn is a skipToken ?",
        "pr_file_module": null
      }
    ]
  }
]