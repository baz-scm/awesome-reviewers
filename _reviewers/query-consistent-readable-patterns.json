[
  {
    "discussion_id": "3178162657",
    "pr_number": 10609,
    "pr_file": "packages/angular-query-experimental/src/devtools/with-devtools.ts",
    "created_at": "2026-05-03T13:15:28+00:00",
    "commented_code": "buttonPosition && devtools.setButtonPosition(buttonPosition)\n                 typeof initialIsOpen === 'boolean' &&\n                   devtools.setInitialIsOpen(initialIsOpen)\n+                if (theme) devtools.setTheme(theme)",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "3178162657",
        "repo_full_name": "TanStack/query",
        "pr_number": 10609,
        "pr_file": "packages/angular-query-experimental/src/devtools/with-devtools.ts",
        "discussion_id": "3178162657",
        "commented_code": "@@ -142,6 +143,7 @@ export const withDevtools: WithDevtools = (\n                 buttonPosition && devtools.setButtonPosition(buttonPosition)\n                 typeof initialIsOpen === 'boolean' &&\n                   devtools.setInitialIsOpen(initialIsOpen)\n+                if (theme) devtools.setTheme(theme)",
        "comment_created_at": "2026-05-03T13:15:28+00:00",
        "comment_author": "TkDodo",
        "comment_body": "should probably keep the same pattern here:\n\n\n```suggestion\n                theme && devtools.setTheme(theme)\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "3178226336",
        "repo_full_name": "TanStack/query",
        "pr_number": 10609,
        "pr_file": "packages/angular-query-experimental/src/devtools/with-devtools.ts",
        "discussion_id": "3178162657",
        "commented_code": "@@ -142,6 +143,7 @@ export const withDevtools: WithDevtools = (\n                 buttonPosition && devtools.setButtonPosition(buttonPosition)\n                 typeof initialIsOpen === 'boolean' &&\n                   devtools.setInitialIsOpen(initialIsOpen)\n+                if (theme) devtools.setTheme(theme)",
        "comment_created_at": "2026-05-03T14:07:38+00:00",
        "comment_author": "grzdev",
        "comment_body": "updated, thanks \ud83d\udc4d",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2967042583",
    "pr_number": 10074,
    "pr_file": "packages/query-core/src/hydration.ts",
    "created_at": "2026-03-20T17:28:01+00:00",
    "commented_code": "}\n }\n \n+function isInfiniteQuery(query: Query): boolean {\n+  const options = query.options as any",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2967042583",
        "repo_full_name": "TanStack/query",
        "pr_number": 10074,
        "pr_file": "packages/query-core/src/hydration.ts",
        "discussion_id": "2967042583",
        "commented_code": "@@ -70,6 +72,11 @@ function dehydrateMutation(mutation: Mutation): DehydratedMutation {\n   }\n }\n \n+function isInfiniteQuery(query: Query): boolean {\n+  const options = query.options as any",
        "comment_created_at": "2026-03-20T17:28:01+00:00",
        "comment_author": "TkDodo",
        "comment_body": "the `as any` seems super unnecessary\n\n\n```suggestion\n  const options = query.options\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "2981527779",
        "repo_full_name": "TanStack/query",
        "pr_number": 10074,
        "pr_file": "packages/query-core/src/hydration.ts",
        "discussion_id": "2967042583",
        "commented_code": "@@ -70,6 +72,11 @@ function dehydrateMutation(mutation: Mutation): DehydratedMutation {\n   }\n }\n \n+function isInfiniteQuery(query: Query): boolean {\n+  const options = query.options as any",
        "comment_created_at": "2026-03-24T13:20:21+00:00",
        "comment_author": "chosule",
        "comment_body": "Removed `as any` from `query.options`.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2121649575",
    "pr_number": 9212,
    "pr_file": "packages/query-core/src/utils.ts",
    "created_at": "2025-06-02T16:31:04+00:00",
    "commented_code": "export function noop(): undefined\n export function noop() {}\n \n+/**\n+ * Type guard that checks if a value is the function variant of a union type.\n+ *\n+ * This utility is designed for the common pattern in TanStack Query where options\n+ * can be either a direct value or a function that computes that value.\n+ *\n+ * @template T - The direct value type\n+ * @template TArgs - Array of argument types that the function variant accepts\n+ * @param value - The value to check, which can be either T or a function that returns something\n+ * @returns True if the value is a function, false otherwise. When true, TypeScript narrows the type to the function variant.\n+ *\n+ * @example\n+ * ```ts\n+ * // Basic usage with no arguments\n+ * const initialData: string | (() => string) = getValue()\n+ * if (isFunctionVariant(initialData)) {\n+ *   // TypeScript knows initialData is () => string here\n+ *   const result = initialData()\n+ * }\n+ * ```\n+ *\n+ * @example\n+ * ```ts\n+ * // Usage with function arguments\n+ * const staleTime: number | ((query: Query) => number) = getStaleTime()\n+ * if (isFunctionVariant<number, [Query]>(staleTime)) {\n+ *   // TypeScript knows staleTime is (query: Query) => number here\n+ *   const result = staleTime(query)\n+ * }\n+ * ```\n+ */\n+function isFunctionVariant<T, TArgs extends Array<any> = []>(\n+  value: T | ((...args: TArgs) => any),\n+): value is (...args: TArgs) => any {\n+  return typeof value === 'function'\n+}\n+\n+/**\n+ * Resolves a value that can either be a direct value or a function that computes the value.\n+ *\n+ * This utility eliminates the need for repetitive `typeof value === 'function'` checks\n+ * throughout the codebase and provides a clean way to handle the common pattern where\n+ * options can be static values or dynamic functions.\n+ *\n+ * @template T - The type of the resolved value\n+ * @template TArgs - Array of argument types when resolving function variants\n+ * @param value - Either a direct value of type T or a function that returns T\n+ * @param args - Arguments to pass to the function if value is a function\n+ * @returns The resolved value of type T\n+ *\n+ * @example\n+ * ```ts\n+ * // Zero-argument function resolution (like initialData)\n+ * const initialData: string | (() => string) = 'hello'\n+ * const resolved = resolveValueOrFunction(initialData) // 'hello'\n+ *\n+ * const initialDataFn: string | (() => string) = () => 'world'\n+ * const resolved2 = resolveValueOrFunction(initialDataFn) // 'world'\n+ * ```\n+ *\n+ * @example\n+ * ```ts\n+ * // Function with arguments (like staleTime, retryDelay)\n+ * const staleTime: number | ((query: Query) => number) = (query) => query.state.dataUpdatedAt + 5000\n+ * const resolved = resolveValueOrFunction(staleTime, query) // number\n+ *\n+ * const retryDelay: number | ((failureCount: number, error: Error) => number) = 1000\n+ * const resolved2 = resolveValueOrFunction(retryDelay, 3, new Error()) // 1000\n+ * ```\n+ *\n+ * @example\n+ * ```ts\n+ * // Replaces verbose patterns like:\n+ * // const delay = typeof retryDelay === 'function'\n+ * //   ? retryDelay(failureCount, error)\n+ * //   : retryDelay\n+ *\n+ * // With:\n+ * const delay = resolveValueOrFunction(retryDelay, failureCount, error)\n+ * ```\n+ */\n+export function resolveValueOrFunction<T, TArgs extends Array<any>>(\n+  value: T | ((...args: TArgs) => T),\n+  ...args: TArgs\n+): T {\n+  return isFunctionVariant(value) ? value(...args) : value\n+}\n+\n export function functionalUpdate<TInput, TOutput>(",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2121649575",
        "repo_full_name": "TanStack/query",
        "pr_number": 9212,
        "pr_file": "packages/query-core/src/utils.ts",
        "discussion_id": "2121649575",
        "commented_code": "@@ -80,6 +77,94 @@ export function noop(): void\n export function noop(): undefined\n export function noop() {}\n \n+/**\n+ * Type guard that checks if a value is the function variant of a union type.\n+ *\n+ * This utility is designed for the common pattern in TanStack Query where options\n+ * can be either a direct value or a function that computes that value.\n+ *\n+ * @template T - The direct value type\n+ * @template TArgs - Array of argument types that the function variant accepts\n+ * @param value - The value to check, which can be either T or a function that returns something\n+ * @returns True if the value is a function, false otherwise. When true, TypeScript narrows the type to the function variant.\n+ *\n+ * @example\n+ * ```ts\n+ * // Basic usage with no arguments\n+ * const initialData: string | (() => string) = getValue()\n+ * if (isFunctionVariant(initialData)) {\n+ *   // TypeScript knows initialData is () => string here\n+ *   const result = initialData()\n+ * }\n+ * ```\n+ *\n+ * @example\n+ * ```ts\n+ * // Usage with function arguments\n+ * const staleTime: number | ((query: Query) => number) = getStaleTime()\n+ * if (isFunctionVariant<number, [Query]>(staleTime)) {\n+ *   // TypeScript knows staleTime is (query: Query) => number here\n+ *   const result = staleTime(query)\n+ * }\n+ * ```\n+ */\n+function isFunctionVariant<T, TArgs extends Array<any> = []>(\n+  value: T | ((...args: TArgs) => any),\n+): value is (...args: TArgs) => any {\n+  return typeof value === 'function'\n+}\n+\n+/**\n+ * Resolves a value that can either be a direct value or a function that computes the value.\n+ *\n+ * This utility eliminates the need for repetitive `typeof value === 'function'` checks\n+ * throughout the codebase and provides a clean way to handle the common pattern where\n+ * options can be static values or dynamic functions.\n+ *\n+ * @template T - The type of the resolved value\n+ * @template TArgs - Array of argument types when resolving function variants\n+ * @param value - Either a direct value of type T or a function that returns T\n+ * @param args - Arguments to pass to the function if value is a function\n+ * @returns The resolved value of type T\n+ *\n+ * @example\n+ * ```ts\n+ * // Zero-argument function resolution (like initialData)\n+ * const initialData: string | (() => string) = 'hello'\n+ * const resolved = resolveValueOrFunction(initialData) // 'hello'\n+ *\n+ * const initialDataFn: string | (() => string) = () => 'world'\n+ * const resolved2 = resolveValueOrFunction(initialDataFn) // 'world'\n+ * ```\n+ *\n+ * @example\n+ * ```ts\n+ * // Function with arguments (like staleTime, retryDelay)\n+ * const staleTime: number | ((query: Query) => number) = (query) => query.state.dataUpdatedAt + 5000\n+ * const resolved = resolveValueOrFunction(staleTime, query) // number\n+ *\n+ * const retryDelay: number | ((failureCount: number, error: Error) => number) = 1000\n+ * const resolved2 = resolveValueOrFunction(retryDelay, 3, new Error()) // 1000\n+ * ```\n+ *\n+ * @example\n+ * ```ts\n+ * // Replaces verbose patterns like:\n+ * // const delay = typeof retryDelay === 'function'\n+ * //   ? retryDelay(failureCount, error)\n+ * //   : retryDelay\n+ *\n+ * // With:\n+ * const delay = resolveValueOrFunction(retryDelay, failureCount, error)\n+ * ```\n+ */\n+export function resolveValueOrFunction<T, TArgs extends Array<any>>(\n+  value: T | ((...args: TArgs) => T),\n+  ...args: TArgs\n+): T {\n+  return isFunctionVariant(value) ? value(...args) : value\n+}\n+\n export function functionalUpdate<TInput, TOutput>(",
        "comment_created_at": "2025-06-02T16:31:04+00:00",
        "comment_author": "TkDodo",
        "comment_body": "could we use `resolveValueOrFunction` here as well, as this also uses a `typeof updater === 'function'` check ?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2315298546",
    "pr_number": 9592,
    "pr_file": "packages/query-core/src/queriesObserver.ts",
    "created_at": "2025-09-02T08:19:02+00:00",
    "commented_code": ")\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error\n+          )\n+        })\n+\n+        if (resultChanged) {\n+          this.#result = newResult\n+          this.#notify()\n+        }",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2315298546",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315298546",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error\n+          )\n+        })\n+\n+        if (resultChanged) {\n+          this.#result = newResult\n+          this.#notify()\n+        }",
        "comment_created_at": "2025-09-02T08:19:02+00:00",
        "comment_author": "TkDodo",
        "comment_body": "I don\u2019t really like the early returns here and the duplicated assignment to `#result` as well as calls to `#notify`, makes it kinda hard to read what is going on. I think we\u2019d want something like this:\n\n- first, check for `hasIndexChange`\n  - if we have an index change, we want to compute the difference for observers and destroy / re-create them, then assign `this.#observers = newObservers`\n- then, we check for `hasResultChange`\n  - if that\u2019s the case, we want to assign `this.#result = newResult`, then check for listeners, then call `this.#notify()`.\n\nthis flow ties logic better to the conditions: when indices change, we need to re-do observers and assign them; when result changes, we need to re-assign it. If anything changes, we need to `notify` (I\u2019m assuming here that if indices change, we\u2019ll also get a resultChange)",
        "pr_file_module": null
      },
      {
        "comment_id": "2315529368",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315298546",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error\n+          )\n+        })\n+\n+        if (resultChanged) {\n+          this.#result = newResult\n+          this.#notify()\n+        }",
        "comment_created_at": "2025-09-02T09:40:31+00:00",
        "comment_author": "joseph0926",
        "comment_body": "[999c8d2](https://github.com/TanStack/query/pull/9592/commits/999c8d2da2d28dcaddd1745a06b97585f6d22d7e)\r\n\r\nThank you for the detailed feedback! I've updated the code based on your suggestions.\r\nTo be honest, I was struggling to find the right approach, but the CodeRabbitAI feedback really helped clarify the path forward.\r\n\r\nWould appreciate another review when you have time. Thanks!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1369602582",
    "pr_number": 6230,
    "pr_file": "packages/solid-query/src/useMutationState.ts",
    "created_at": "2023-10-24T03:51:03+00:00",
    "commented_code": "+import { type Accessor, createMemo, createSignal, onCleanup } from \"solid-js\";\n+import {\n+    type DefaultError,\n+    type Mutation,\n+    type MutationCache,\n+    type MutationFilters,\n+    type MutationState,\n+    type QueryClient,\n+    replaceEqualDeep,\n+    useQueryClient,\n+} from \".\"",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1369602582",
        "repo_full_name": "TanStack/query",
        "pr_number": 6230,
        "pr_file": "packages/solid-query/src/useMutationState.ts",
        "discussion_id": "1369602582",
        "commented_code": "@@ -0,0 +1,58 @@\n+import { type Accessor, createMemo, createSignal, onCleanup } from \"solid-js\";\n+import {\n+    type DefaultError,\n+    type Mutation,\n+    type MutationCache,\n+    type MutationFilters,\n+    type MutationState,\n+    type QueryClient,\n+    replaceEqualDeep,\n+    useQueryClient,\n+} from \".\"",
        "comment_created_at": "2023-10-24T03:51:03+00:00",
        "comment_author": "ardeora",
        "comment_body": "Imports should be like so \r\n```ts\r\nimport { createMemo, createSignal, onCleanup } from 'solid-js'\r\nimport { replaceEqualDeep } from '@tanstack/query-core'\r\nimport { useQueryClient } from './QueryClientProvider'\r\nimport type {\r\n  DefaultError,\r\n  Mutation,\r\n  MutationCache,\r\n  MutationFilters,\r\n  MutationState,\r\n} from '@tanstack/query-core'\r\nimport type { Accessor } from 'solid-js'\r\nimport type { QueryClient } from './QueryClient'\r\n```",
        "pr_file_module": null
      }
    ]
  }
]