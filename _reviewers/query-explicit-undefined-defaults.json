[
  {
    "discussion_id": "986473466",
    "pr_number": 4258,
    "pr_file": "packages/react-query-devtools/src/devtools.tsx",
    "created_at": "2022-10-04T06:22:13+00:00",
    "commented_code": "const queries = React.useMemo(() => {\n     const unsortedQueries = queryCache.getAll()\n-    const sorted = queriesCount > 0 ? [...unsortedQueries].sort(sortFn) : []\n \n-    if (sortDesc) {\n-      sorted.reverse()\n+    if (queriesCount === 0) {\n+      return []\n     }\n \n-    if (!filter) {\n-      return sorted\n-    }\n-\n-    let ranked = sorted.map(\n-      (item) => [item, rankItem(item.queryHash, filter)] as const,\n-    )\n-\n-    ranked = ranked.filter((d) => d[1].passed)\n+    const filtered = filter\n+      ? unsortedQueries.filter(\n+          (item) => rankItem(item.queryHash, filter).passed,\n+        )\n+      : [...unsortedQueries]\n \n-    ranked = ranked.sort((a, b) => compareItems(a[1], b[1]))\n+    const sorted = sortFn\n+      ? filtered.sort((a, b) => sortFn(a, b) * (baseSort as number))",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "986473466",
        "repo_full_name": "TanStack/query",
        "pr_number": 4258,
        "pr_file": "packages/react-query-devtools/src/devtools.tsx",
        "discussion_id": "986473466",
        "commented_code": "@@ -438,26 +438,23 @@ export const ReactQueryDevtoolsPanel = React.forwardRef<\n \n   const queries = React.useMemo(() => {\n     const unsortedQueries = queryCache.getAll()\n-    const sorted = queriesCount > 0 ? [...unsortedQueries].sort(sortFn) : []\n \n-    if (sortDesc) {\n-      sorted.reverse()\n+    if (queriesCount === 0) {\n+      return []\n     }\n \n-    if (!filter) {\n-      return sorted\n-    }\n-\n-    let ranked = sorted.map(\n-      (item) => [item, rankItem(item.queryHash, filter)] as const,\n-    )\n-\n-    ranked = ranked.filter((d) => d[1].passed)\n+    const filtered = filter\n+      ? unsortedQueries.filter(\n+          (item) => rankItem(item.queryHash, filter).passed,\n+        )\n+      : [...unsortedQueries]\n \n-    ranked = ranked.sort((a, b) => compareItems(a[1], b[1]))\n+    const sorted = sortFn\n+      ? filtered.sort((a, b) => sortFn(a, b) * (baseSort as number))",
        "comment_created_at": "2022-10-04T06:22:13+00:00",
        "comment_author": "TkDodo",
        "comment_body": "all of our usages of `useLocalStorage` provide the optional default value, so if we make it mandatory, we don't have to do type assertions here. Also, with the current implementation, there is a render cycle where the value is indeed `undefined`. We can fix that by passing the `defaultValue` directly to `useState` inside `useLocalStorage` and can then also get rid of this whole block:\r\n\r\nhttps://github.com/TanStack/query/blob/357ec041a6fcc4a550f3df02c12ecc7bcdefbc05/packages/react-query-devtools/src/useLocalStorage.ts#L24-L27",
        "pr_file_module": null
      },
      {
        "comment_id": "988360385",
        "repo_full_name": "TanStack/query",
        "pr_number": 4258,
        "pr_file": "packages/react-query-devtools/src/devtools.tsx",
        "discussion_id": "986473466",
        "commented_code": "@@ -438,26 +438,23 @@ export const ReactQueryDevtoolsPanel = React.forwardRef<\n \n   const queries = React.useMemo(() => {\n     const unsortedQueries = queryCache.getAll()\n-    const sorted = queriesCount > 0 ? [...unsortedQueries].sort(sortFn) : []\n \n-    if (sortDesc) {\n-      sorted.reverse()\n+    if (queriesCount === 0) {\n+      return []\n     }\n \n-    if (!filter) {\n-      return sorted\n-    }\n-\n-    let ranked = sorted.map(\n-      (item) => [item, rankItem(item.queryHash, filter)] as const,\n-    )\n-\n-    ranked = ranked.filter((d) => d[1].passed)\n+    const filtered = filter\n+      ? unsortedQueries.filter(\n+          (item) => rankItem(item.queryHash, filter).passed,\n+        )\n+      : [...unsortedQueries]\n \n-    ranked = ranked.sort((a, b) => compareItems(a[1], b[1]))\n+    const sorted = sortFn\n+      ? filtered.sort((a, b) => sortFn(a, b) * (baseSort as number))",
        "comment_created_at": "2022-10-05T21:15:00+00:00",
        "comment_author": "DamianOsipiuk",
        "comment_body": "@TkDodo Not sure why but when i touch anything about `useLocalStorage` to get rid of initial `undefined` render for values, tests start to break.\r\nBasically this code block stops devtools from being rendered:\r\n```ts\r\n// Do not render on the server\r\nif (!isMounted()) return null\r\n```\r\n\ud83e\udd37 ",
        "pr_file_module": null
      },
      {
        "comment_id": "990245768",
        "repo_full_name": "TanStack/query",
        "pr_number": 4258,
        "pr_file": "packages/react-query-devtools/src/devtools.tsx",
        "discussion_id": "986473466",
        "commented_code": "@@ -438,26 +438,23 @@ export const ReactQueryDevtoolsPanel = React.forwardRef<\n \n   const queries = React.useMemo(() => {\n     const unsortedQueries = queryCache.getAll()\n-    const sorted = queriesCount > 0 ? [...unsortedQueries].sort(sortFn) : []\n \n-    if (sortDesc) {\n-      sorted.reverse()\n+    if (queriesCount === 0) {\n+      return []\n     }\n \n-    if (!filter) {\n-      return sorted\n-    }\n-\n-    let ranked = sorted.map(\n-      (item) => [item, rankItem(item.queryHash, filter)] as const,\n-    )\n-\n-    ranked = ranked.filter((d) => d[1].passed)\n+    const filtered = filter\n+      ? unsortedQueries.filter(\n+          (item) => rankItem(item.queryHash, filter).passed,\n+        )\n+      : [...unsortedQueries]\n \n-    ranked = ranked.sort((a, b) => compareItems(a[1], b[1]))\n+    const sorted = sortFn\n+      ? filtered.sort((a, b) => sortFn(a, b) * (baseSort as number))",
        "comment_created_at": "2022-10-07T16:01:42+00:00",
        "comment_author": "TkDodo",
        "comment_body": "ok, \ud83e\udd37 ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1977771841",
    "pr_number": 8737,
    "pr_file": "packages/query-core/src/__tests__/types.test-d.tsx",
    "created_at": "2025-03-03T16:00:32+00:00",
    "commented_code": "+import { describe, expectTypeOf, it } from 'vitest'\n+import type { MutateFunction } from 'src/types'\n+\n+describe('MutateFunction', () => {\n+  it('optional undefinable variables', () => {\n+    const mutate = {} as MutateFunction<\n+      unknown,\n+      unknown,\n+      number | undefined,\n+      unknown\n+    >\n+\n+    expectTypeOf<Parameters<typeof mutate>[0]>().toEqualTypeOf<\n+      number | undefined\n+    >()\n+\n+    mutate() // can be called with no arguments",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1977771841",
        "repo_full_name": "TanStack/query",
        "pr_number": 8737,
        "pr_file": "packages/query-core/src/__tests__/types.test-d.tsx",
        "discussion_id": "1977771841",
        "commented_code": "@@ -0,0 +1,59 @@\n+import { describe, expectTypeOf, it } from 'vitest'\n+import type { MutateFunction } from 'src/types'\n+\n+describe('MutateFunction', () => {\n+  it('optional undefinable variables', () => {\n+    const mutate = {} as MutateFunction<\n+      unknown,\n+      unknown,\n+      number | undefined,\n+      unknown\n+    >\n+\n+    expectTypeOf<Parameters<typeof mutate>[0]>().toEqualTypeOf<\n+      number | undefined\n+    >()\n+\n+    mutate() // can be called with no arguments",
        "comment_created_at": "2025-03-03T16:00:32+00:00",
        "comment_author": "TkDodo",
        "comment_body": "that\u2019s a good improvement \ud83d\udc4d . I\u2019m missing a test for the use-case where we have a mutation function that doesn\u2019t take anything in, so the variables are `void`.\r\n\r\nAlso, I\u2019d prefer if we create the mutation functions not via type assertions, but by whatever a `MutationObserver` returns. Something like:\r\n\r\n```\r\nconst { mutate } = new MutationObserver(new QueryClient(), {\r\n  mutationFn: async (_vars: number | undefined) => {\r\n    return null\r\n  },\r\n})\r\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "1979375141",
        "repo_full_name": "TanStack/query",
        "pr_number": 8737,
        "pr_file": "packages/query-core/src/__tests__/types.test-d.tsx",
        "discussion_id": "1977771841",
        "commented_code": "@@ -0,0 +1,59 @@\n+import { describe, expectTypeOf, it } from 'vitest'\n+import type { MutateFunction } from 'src/types'\n+\n+describe('MutateFunction', () => {\n+  it('optional undefinable variables', () => {\n+    const mutate = {} as MutateFunction<\n+      unknown,\n+      unknown,\n+      number | undefined,\n+      unknown\n+    >\n+\n+    expectTypeOf<Parameters<typeof mutate>[0]>().toEqualTypeOf<\n+      number | undefined\n+    >()\n+\n+    mutate() // can be called with no arguments",
        "comment_created_at": "2025-03-04T12:54:54+00:00",
        "comment_author": "dinwwwh",
        "comment_body": "I've added the `void` case as requested. However, I couldn\u2019t create a test using `MutationObserver` because the `mutate` function is implemented as a standalone function rather than using `MutateFunction`. As a result, the changes don\u2019t impact its behavior. Let me know if you have any suggestions on how to approach testing this scenario.",
        "pr_file_module": null
      },
      {
        "comment_id": "1979383791",
        "repo_full_name": "TanStack/query",
        "pr_number": 8737,
        "pr_file": "packages/query-core/src/__tests__/types.test-d.tsx",
        "discussion_id": "1977771841",
        "commented_code": "@@ -0,0 +1,59 @@\n+import { describe, expectTypeOf, it } from 'vitest'\n+import type { MutateFunction } from 'src/types'\n+\n+describe('MutateFunction', () => {\n+  it('optional undefinable variables', () => {\n+    const mutate = {} as MutateFunction<\n+      unknown,\n+      unknown,\n+      number | undefined,\n+      unknown\n+    >\n+\n+    expectTypeOf<Parameters<typeof mutate>[0]>().toEqualTypeOf<\n+      number | undefined\n+    >()\n+\n+    mutate() // can be called with no arguments",
        "comment_created_at": "2025-03-04T13:00:51+00:00",
        "comment_author": "TkDodo",
        "comment_body": "oh interesting. question is if it would also work with the `mutate` function returned from `useMutation`, as that is what most users will use. The type is defined here:\r\n\r\nhttps://github.com/TanStack/query/blob/3e3fba997bfd356f9a0e0aaa8fed453fcdf2f6f4/packages/react-query/src/types.ts#L197-L204\r\n\r\nwould be great to have a test in `useMutation.test-d.tsx` in the react-query adapter for this then \ud83d\ude4f ",
        "pr_file_module": null
      },
      {
        "comment_id": "1979399724",
        "repo_full_name": "TanStack/query",
        "pr_number": 8737,
        "pr_file": "packages/query-core/src/__tests__/types.test-d.tsx",
        "discussion_id": "1977771841",
        "commented_code": "@@ -0,0 +1,59 @@\n+import { describe, expectTypeOf, it } from 'vitest'\n+import type { MutateFunction } from 'src/types'\n+\n+describe('MutateFunction', () => {\n+  it('optional undefinable variables', () => {\n+    const mutate = {} as MutateFunction<\n+      unknown,\n+      unknown,\n+      number | undefined,\n+      unknown\n+    >\n+\n+    expectTypeOf<Parameters<typeof mutate>[0]>().toEqualTypeOf<\n+      number | undefined\n+    >()\n+\n+    mutate() // can be called with no arguments",
        "comment_created_at": "2025-03-04T13:09:42+00:00",
        "comment_author": "dinwwwh",
        "comment_body": "I've already covered this case. Please check my tests.\r\n\r\nhttps://github.com/TanStack/query/pull/8737/files#diff-fd352cb3d35bea507f0ff667365ff9ab3c51a78c493656207dc3a5291a2491b2R67-R133",
        "pr_file_module": null
      },
      {
        "comment_id": "1981542575",
        "repo_full_name": "TanStack/query",
        "pr_number": 8737,
        "pr_file": "packages/query-core/src/__tests__/types.test-d.tsx",
        "discussion_id": "1977771841",
        "commented_code": "@@ -0,0 +1,59 @@\n+import { describe, expectTypeOf, it } from 'vitest'\n+import type { MutateFunction } from 'src/types'\n+\n+describe('MutateFunction', () => {\n+  it('optional undefinable variables', () => {\n+    const mutate = {} as MutateFunction<\n+      unknown,\n+      unknown,\n+      number | undefined,\n+      unknown\n+    >\n+\n+    expectTypeOf<Parameters<typeof mutate>[0]>().toEqualTypeOf<\n+      number | undefined\n+    >()\n+\n+    mutate() // can be called with no arguments",
        "comment_created_at": "2025-03-05T14:43:17+00:00",
        "comment_author": "TkDodo",
        "comment_body": "> I've already covered this case\r\n\r\nyeah that\u2019s okay for the `query-core`, but it doesn\u2019t actually test what `useMutation` in react-query does. It\u2019s great that we cover the functionality that we have in `useMutation` _now_, but if we refactor that, it won\u2019t be covered by the test in the core.\r\n\r\nThat\u2019s why each adapter should have its own tests for the types they are doing. But we can do this in a follow-up.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1044759854",
    "pr_number": 4616,
    "pr_file": "packages/query-core/src/tests/queryCache.test.tsx",
    "created_at": "2022-12-09T19:15:29+00:00",
    "commented_code": "await queryClient.prefetchQuery(key2, () => 'data2')\n       expect(queryCache.findAll().length).toBe(2)\n     })\n+\n+    test('should return all the queries when key contains object with an undefined property', async () => {\n+      const baseKey = queryKey()\n+      await queryClient.prefetchQuery([...baseKey, { a: 1 }], () => 'data1')\n+      expect(queryCache.findAll([...baseKey, { a: undefined }])).toHaveLength(1)\n+    })",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1044759854",
        "repo_full_name": "TanStack/query",
        "pr_number": 4616,
        "pr_file": "packages/query-core/src/tests/queryCache.test.tsx",
        "discussion_id": "1044759854",
        "commented_code": "@@ -203,6 +203,12 @@ describe('queryCache', () => {\n       await queryClient.prefetchQuery(key2, () => 'data2')\n       expect(queryCache.findAll().length).toBe(2)\n     })\n+\n+    test('should return all the queries when key contains object with an undefined property', async () => {\n+      const baseKey = queryKey()\n+      await queryClient.prefetchQuery([...baseKey, { a: 1 }], () => 'data1')\n+      expect(queryCache.findAll([...baseKey, { a: undefined }])).toHaveLength(1)\n+    })",
        "comment_created_at": "2022-12-09T19:15:29+00:00",
        "comment_author": "TkDodo",
        "comment_body": "thanks. If I get this right, the more realistic example would be something like this?\r\n\r\n```\r\n    test('should return all the queries when key contains object with an undefined property', async () => {\r\n      const baseKey = queryKey()\r\n\r\n      const createKey = (id?: number) => [{ ...baseKey, a: id }]\r\n\r\n      await queryClient.prefetchQuery(createKey(1), () => 'data1')\r\n      await queryClient.prefetchQuery(createKey(), () => 'data-nothing')\r\n      expect(queryCache.findAll(createKey())).toHaveLength(2)\r\n    })\r\n```\r\n\r\nso when we use the same `createKey` function to create our keys with optional filters, and then use it e.g. for invalidation, it doesn't match them all if we don't provide the param, but it should?\r\n\r\nThe thing is, there isn't really a way to make this work with either object or array keys. As soon as you add an entry to the array or object, it can't fuzzily match a non-existing entry",
        "pr_file_module": null
      },
      {
        "comment_id": "1044945078",
        "repo_full_name": "TanStack/query",
        "pr_number": 4616,
        "pr_file": "packages/query-core/src/tests/queryCache.test.tsx",
        "discussion_id": "1044759854",
        "commented_code": "@@ -203,6 +203,12 @@ describe('queryCache', () => {\n       await queryClient.prefetchQuery(key2, () => 'data2')\n       expect(queryCache.findAll().length).toBe(2)\n     })\n+\n+    test('should return all the queries when key contains object with an undefined property', async () => {\n+      const baseKey = queryKey()\n+      await queryClient.prefetchQuery([...baseKey, { a: 1 }], () => 'data1')\n+      expect(queryCache.findAll([...baseKey, { a: undefined }])).toHaveLength(1)\n+    })",
        "comment_created_at": "2022-12-10T01:49:51+00:00",
        "comment_author": "maxandron",
        "comment_body": "Precisely, I'll edit the test accordingly.\r\nIt can also happen when simply using a custom hook with an optional parameter (a key factory is not really required).\r\n\r\nIn addition, any query created with `createKey()` will be hashed as if it were created with just `[{ ...baseKey }]`\r\n\r\nA temporary workaround is to wrap the filter value with an object and replace it with an empty object if undefined:\r\n\r\n```typescript\r\nconst baseKey = queryKey()\r\nconst createKey = (filter?: string) => [{...baseKey, filter: filter !== undefined ? { filter } : {}}]\r\n// Instead of: const createKey = (filter?: string) => [{...baseKey, filter }]\r\nawait queryClient.prefetchQuery(createKey(\"filter1\"), () => 'filtered-data')\r\nawait queryClient.prefetchQuery(createKey(), () => 'all-data')\r\nexpect(queryCache.findAll(createKey())).toHaveLength(2)\r\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "1045001520",
        "repo_full_name": "TanStack/query",
        "pr_number": 4616,
        "pr_file": "packages/query-core/src/tests/queryCache.test.tsx",
        "discussion_id": "1044759854",
        "commented_code": "@@ -203,6 +203,12 @@ describe('queryCache', () => {\n       await queryClient.prefetchQuery(key2, () => 'data2')\n       expect(queryCache.findAll().length).toBe(2)\n     })\n+\n+    test('should return all the queries when key contains object with an undefined property', async () => {\n+      const baseKey = queryKey()\n+      await queryClient.prefetchQuery([...baseKey, { a: 1 }], () => 'data1')\n+      expect(queryCache.findAll([...baseKey, { a: undefined }])).toHaveLength(1)\n+    })",
        "comment_created_at": "2022-12-10T07:07:13+00:00",
        "comment_author": "TkDodo",
        "comment_body": "the root problem is that we somehow assume that passing an `undefined` filter to the `createKey` function would result in filter not being present in the queryKey. This might be amplified by the devtools showing hashed keys, and in the standard hashing process (JSON.stringify), `undefined` is removed.\r\n\r\nBut in an object, `undefined` as a value will be present because we add a valid key. It's the same for arrays:\r\n\r\n```\r\nconst createKey = (filter?: string) => [...baseKey, filter]\r\n```\r\n\r\nthis will have the exact same problem. And we can't just remove `undefined` from arrays because order matters.\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "1045030432",
        "repo_full_name": "TanStack/query",
        "pr_number": 4616,
        "pr_file": "packages/query-core/src/tests/queryCache.test.tsx",
        "discussion_id": "1044759854",
        "commented_code": "@@ -203,6 +203,12 @@ describe('queryCache', () => {\n       await queryClient.prefetchQuery(key2, () => 'data2')\n       expect(queryCache.findAll().length).toBe(2)\n     })\n+\n+    test('should return all the queries when key contains object with an undefined property', async () => {\n+      const baseKey = queryKey()\n+      await queryClient.prefetchQuery([...baseKey, { a: 1 }], () => 'data1')\n+      expect(queryCache.findAll([...baseKey, { a: undefined }])).toHaveLength(1)\n+    })",
        "comment_created_at": "2022-12-10T08:56:09+00:00",
        "comment_author": "maxandron",
        "comment_body": "Yep, the dev tools showing the hashed keys amplifies the problem. It stems from the fact that the hash is not representative of the key (which is not what you would expect from a hash function)\r\n\r\nThat means the hash for two different keys can be the same (which means it's an inadequate hash function).\r\n\r\nPerhaps it's more expected for tuples to be of the same length regardless of the values we pass inside, while it's unexpected for objects.\r\nI may be wrong, as referencing an out-of-bounds element in an array also produces the value `undefined`.\r\n\r\nSo there are a few ways to go about this, I guess:\r\n\r\n1. Don't change anything and add documentation to help users in this scenario (perhaps even a note on your blog can go a long way)\r\n2. Ignore undefined in objects only (not in arrays) when doing comparisons (e.g., through a new opt-in option)\r\n3. Fix the hash function to act similarly to the comparison\r\n4. Make the dev tools show the key instead of the hash\r\n5. Ignore the problem\r\n\r\nWhat do you think?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1468498001",
    "pr_number": 6753,
    "pr_file": "packages/react-query-next-experimental/src/HydrationStreamProvider.tsx",
    "created_at": "2024-01-27T14:54:39+00:00",
    "commented_code": "onEntriesRef.current = props.onEntries\n     })\n \n-    React.useEffect(() => {\n+    // Setup and run the onEntries handler on the client only, but do it during\n+    // the initial render so children have access to the data immediately\n+    // This is important to avoid the client suspending during the initial render\n+    // if the data has not yet been hydrated.\n+    // @ts-expect-error window isn't defined on the server\n+    if (typeof window !== 'undefined' && !window[id]?.initialized) {",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1468498001",
        "repo_full_name": "TanStack/query",
        "pr_number": 6753,
        "pr_file": "packages/react-query-next-experimental/src/HydrationStreamProvider.tsx",
        "discussion_id": "1468498001",
        "commented_code": "@@ -143,31 +143,28 @@ export function createHydrationStreamProvider<TShape>() {\n       onEntriesRef.current = props.onEntries\n     })\n \n-    React.useEffect(() => {\n+    // Setup and run the onEntries handler on the client only, but do it during\n+    // the initial render so children have access to the data immediately\n+    // This is important to avoid the client suspending during the initial render\n+    // if the data has not yet been hydrated.\n+    // @ts-expect-error window isn't defined on the server\n+    if (typeof window !== 'undefined' && !window[id]?.initialized) {",
        "comment_created_at": "2024-01-27T14:54:39+00:00",
        "comment_author": "TkDodo",
        "comment_body": "we have an `isServer` util function that works slightly different than just checking for window. could we re-use that one ?",
        "pr_file_module": null
      },
      {
        "comment_id": "1477430934",
        "repo_full_name": "TanStack/query",
        "pr_number": 6753,
        "pr_file": "packages/react-query-next-experimental/src/HydrationStreamProvider.tsx",
        "discussion_id": "1468498001",
        "commented_code": "@@ -143,31 +143,28 @@ export function createHydrationStreamProvider<TShape>() {\n       onEntriesRef.current = props.onEntries\n     })\n \n-    React.useEffect(() => {\n+    // Setup and run the onEntries handler on the client only, but do it during\n+    // the initial render so children have access to the data immediately\n+    // This is important to avoid the client suspending during the initial render\n+    // if the data has not yet been hydrated.\n+    // @ts-expect-error window isn't defined on the server\n+    if (typeof window !== 'undefined' && !window[id]?.initialized) {",
        "comment_created_at": "2024-02-04T20:08:04+00:00",
        "comment_author": "mgreenw",
        "comment_body": "Done!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1375240978",
    "pr_number": 6261,
    "pr_file": "packages/query-core/src/tests/queryClient.test.tsx",
    "created_at": "2023-10-28T11:53:17+00:00",
    "commented_code": ").resolves.toEqual('bar')\n     })\n \n+    test('should return the cached query data if the query is found and cached query data is falsey', async () => {\n+      const key = queryKey()\n+      const queryFn = () => Promise.resolve('data')",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1375240978",
        "repo_full_name": "TanStack/query",
        "pr_number": 6261,
        "pr_file": "packages/query-core/src/tests/queryClient.test.tsx",
        "discussion_id": "1375240978",
        "commented_code": "@@ -433,6 +433,17 @@ describe('queryClient', () => {\n       ).resolves.toEqual('bar')\n     })\n \n+    test('should return the cached query data if the query is found and cached query data is falsey', async () => {\n+      const key = queryKey()\n+      const queryFn = () => Promise.resolve('data')",
        "comment_created_at": "2023-10-28T11:53:17+00:00",
        "comment_author": "TkDodo",
        "comment_body": "good fix, but the returned data here isn't falsy?",
        "pr_file_module": null
      },
      {
        "comment_id": "1375243343",
        "repo_full_name": "TanStack/query",
        "pr_number": 6261,
        "pr_file": "packages/query-core/src/tests/queryClient.test.tsx",
        "discussion_id": "1375240978",
        "commented_code": "@@ -433,6 +433,17 @@ describe('queryClient', () => {\n       ).resolves.toEqual('bar')\n     })\n \n+    test('should return the cached query data if the query is found and cached query data is falsey', async () => {\n+      const key = queryKey()\n+      const queryFn = () => Promise.resolve('data')",
        "comment_created_at": "2023-10-28T12:14:07+00:00",
        "comment_author": "LineGu",
        "comment_body": "Thanks. My intention was to test the case where the cache data is falsy(\"\") and the new data is not falsy(\"data\"). And expect the value of ensureQueryData to match with falsy cached data\r\n\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "1375259494",
        "repo_full_name": "TanStack/query",
        "pr_number": 6261,
        "pr_file": "packages/query-core/src/tests/queryClient.test.tsx",
        "discussion_id": "1375240978",
        "commented_code": "@@ -433,6 +433,17 @@ describe('queryClient', () => {\n       ).resolves.toEqual('bar')\n     })\n \n+    test('should return the cached query data if the query is found and cached query data is falsey', async () => {\n+      const key = queryKey()\n+      const queryFn = () => Promise.resolve('data')",
        "comment_created_at": "2023-10-28T14:05:50+00:00",
        "comment_author": "LineGu",
        "comment_body": "0557237b22bf09cb9b4d0e5b15d0efe1b997dce1\r\nmake the test case more intuitive, and I have fixed all the returned data covered by the test case to be falsy values.\r\nIf there are any parts that need improvement, please let me know.",
        "pr_file_module": null
      }
    ]
  }
]