[
  {
    "discussion_id": "3142373813",
    "pr_number": 10159,
    "pr_file": "packages/query-core/src/hydration.ts",
    "created_at": "2026-04-25T18:17:26+00:00",
    "commented_code": "if (\n         promise &&\n-        // If the data was synchronously available, there is no need to set up\n-        // a retryer and thus no reason to call fetch\n-        !syncData &&\n-        !existingQueryIsPending &&\n-        !existingQueryIsFetching &&\n+        (existingQueryIsIdleUseQuery ||\n+          // If the data was synchronously available, there is no need to set up\n+          // a retryer and thus no reason to call fetch\n+          (!syncData &&",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "3142373813",
        "repo_full_name": "TanStack/query",
        "pr_number": 10159,
        "pr_file": "packages/query-core/src/hydration.ts",
        "discussion_id": "3142373813",
        "commented_code": "@@ -290,11 +295,12 @@ export function hydrate(\n \n       if (\n         promise &&\n-        // If the data was synchronously available, there is no need to set up\n-        // a retryer and thus no reason to call fetch\n-        !syncData &&\n-        !existingQueryIsPending &&\n-        !existingQueryIsFetching &&\n+        (existingQueryIsIdleUseQuery ||\n+          // If the data was synchronously available, there is no need to set up\n+          // a retryer and thus no reason to call fetch\n+          (!syncData &&",
        "comment_created_at": "2026-04-25T18:17:26+00:00",
        "comment_author": "Ephem",
        "comment_body": "I think this logic is wrong. If `existingQueryIsIdleUseQuery` is true, we'll always call fetch, even if data was synchronously available. This causes a regression where the query quickly flashes the loading state when it shouldn't.",
        "pr_file_module": null
      },
      {
        "comment_id": "3142848892",
        "repo_full_name": "TanStack/query",
        "pr_number": 10159,
        "pr_file": "packages/query-core/src/hydration.ts",
        "discussion_id": "3142373813",
        "commented_code": "@@ -290,11 +295,12 @@ export function hydrate(\n \n       if (\n         promise &&\n-        // If the data was synchronously available, there is no need to set up\n-        // a retryer and thus no reason to call fetch\n-        !syncData &&\n-        !existingQueryIsPending &&\n-        !existingQueryIsFetching &&\n+        (existingQueryIsIdleUseQuery ||\n+          // If the data was synchronously available, there is no need to set up\n+          // a retryer and thus no reason to call fetch\n+          (!syncData &&",
        "comment_created_at": "2026-04-26T01:53:25+00:00",
        "comment_author": "ColemanDunn",
        "comment_body": "ThanksI I overcorrected on that follow up commit after the merge. I have updated the code and added a test to check the loading state is in the correct state. \r\n\r\nI know you mentioned it is hard to foresee the side effects, but I hope there are some tests we can add to determine if we can find a safe enough fix for this. ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2315279694",
    "pr_number": 9592,
    "pr_file": "packages/query-core/src/queriesObserver.ts",
    "created_at": "2025-09-02T08:12:07+00:00",
    "commented_code": ")\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2315279694",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315279694",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
        "comment_created_at": "2025-09-02T08:12:07+00:00",
        "comment_author": "TkDodo",
        "comment_body": "checking for these 3 props seems a bit random. I think we\u2019d want to use our `shallowEqualObjects` helper from the query-core to just compare `result` with `prev`. This is also what we do inside `queryObserver`:\n\nhttps://github.com/TanStack/query/blob/abab082593424bd4b6a3c3e494f0cf4a8c40dcc1/packages/query-core/src/queryObserver.ts#L656-L659",
        "pr_file_module": null
      },
      {
        "comment_id": "2315491701",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315279694",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
        "comment_created_at": "2025-09-02T09:26:59+00:00",
        "comment_author": "joseph0926",
        "comment_body": "I found that using shallowEqualObjects causes test failures because it compares ALL properties of QueryObserverResult, including timestamps like dataUpdatedAt. This means it detects changes even when the actual data hasn't changed. The individual property comparison (data, isPending, error) passes all tests. Should we stick with the explicit property checks, or would you prefer a different approach?",
        "pr_file_module": null
      },
      {
        "comment_id": "2315497317",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315279694",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
        "comment_created_at": "2025-09-02T09:29:05+00:00",
        "comment_author": "TkDodo",
        "comment_body": "but if `dataUpdatedAt` changes, we need to inform observers about it, right? A component could  render `dataUpdatedAt` on the screen so it will want that information to be correct \ud83e\udd14 ",
        "pr_file_module": null
      },
      {
        "comment_id": "2315530905",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315279694",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
        "comment_created_at": "2025-09-02T09:41:08+00:00",
        "comment_author": "joseph0926",
        "comment_body": "You're absolutely right. Components could render any property including dataUpdatedAt, so we need to detect all changes. I'll use shallowEqualObjects as you suggested.\r\nThe test failures appear to be because the tests expect combine not to re-run when metadata changes, but that expectation seems incorrect. When any property changes (even metadata), combine should re-execute to ensure the UI stays in sync.\r\nShould I update the test expectations to reflect this correct behavior?\r\n\r\ntest file: packages/react-query/src/__tests__/useQueries.test.tsx > `should optimize combine if it is a stable reference`",
        "pr_file_module": null
      },
      {
        "comment_id": "2315724310",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315279694",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
        "comment_created_at": "2025-09-02T10:55:37+00:00",
        "comment_author": "joseph0926",
        "comment_body": "[4bc297b](https://github.com/TanStack/query/pull/9592/commits/4bc297b538476c69ad2a48809193e4bf5be3c703)\r\n\r\nI've updated the test expectations to account for the shallowEqualObjects comparison. The tests now expect combine to re-run when metadata changes occur.\r\nHowever, I'm not entirely confident whether having combine re-run on metadata changes is the intended behavior from this PR... Perhaps there's a performance consideration I'm missing, or my implementation itself might be incorrect.\r\nThe tests are passing with the updated expectations, but I believe a different approach might be more appropriate. I defer to your judgment on whether this is the desired behavior.",
        "pr_file_module": null
      },
      {
        "comment_id": "2315727339",
        "repo_full_name": "TanStack/query",
        "pr_number": 9592,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2315279694",
        "commented_code": "@@ -123,6 +123,21 @@ export class QueriesObserver<\n       )\n \n       if (prevObservers.length === newObservers.length && !hasIndexChange) {\n+        const resultChanged = newResult.some((result, index) => {\n+          const prev = this.#result[index]\n+          return (\n+            !prev ||\n+            result.data !== prev.data ||\n+            result.isPending !== prev.isPending ||\n+            result.error !== prev.error",
        "comment_created_at": "2025-09-02T10:56:54+00:00",
        "comment_author": "TkDodo",
        "comment_body": "yeah that test seems slightly off, and actually shows the bug: we get new metadata (`dataUpdatedAt`), which is part of `results` passed to `combine`, but we don\u2019t run it :/\r\n\r\nI think I was basing this test off of `select` on `useQuery`, but the difference is that `select` only gets `data` passed while `combine` gets passed _everything_.\r\n\r\n> Should I update the test expectations to reflect this correct behavior?\r\n\r\nyes please, and we need to test combine with a stable reference differently, likely by triggering a re-render differently, e.g. just having an `increment` that triggers an unrelated re-render, because in those cases, a stable combine shouldn\u2019t re-run.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2121589045",
    "pr_number": 9201,
    "pr_file": "packages/query-core/src/queriesObserver.ts",
    "created_at": "2025-06-02T16:00:54+00:00",
    "commented_code": "import type { QueryClient } from './queryClient'\n \n function difference<T>(array1: Array<T>, array2: Array<T>): Array<T> {\n-  return array1.filter((x) => !array2.includes(x))\n+  return array1.filter((x) => !new Set(array2).has(x))",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2121589045",
        "repo_full_name": "TanStack/query",
        "pr_number": 9201,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2121589045",
        "commented_code": "@@ -10,7 +10,7 @@ import type {\n import type { QueryClient } from './queryClient'\n \n function difference<T>(array1: Array<T>, array2: Array<T>): Array<T> {\n-  return array1.filter((x) => !array2.includes(x))\n+  return array1.filter((x) => !new Set(array2).has(x))",
        "comment_created_at": "2025-06-02T16:00:54+00:00",
        "comment_author": "TkDodo",
        "comment_body": "can we move the `new Set` creation out of the `filter` function to only create the Set once ?",
        "pr_file_module": null
      },
      {
        "comment_id": "2123461680",
        "repo_full_name": "TanStack/query",
        "pr_number": 9201,
        "pr_file": "packages/query-core/src/queriesObserver.ts",
        "discussion_id": "2121589045",
        "commented_code": "@@ -10,7 +10,7 @@ import type {\n import type { QueryClient } from './queryClient'\n \n function difference<T>(array1: Array<T>, array2: Array<T>): Array<T> {\n-  return array1.filter((x) => !array2.includes(x))\n+  return array1.filter((x) => !new Set(array2).has(x))",
        "comment_created_at": "2025-06-03T11:00:23+00:00",
        "comment_author": "novice0840",
        "comment_body": "Of course, I did it. Please check ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2024306538",
    "pr_number": 8849,
    "pr_file": "packages/query-broadcast-client-experimental/src/index.test.ts",
    "created_at": "2025-04-02T08:12:16+00:00",
    "commented_code": "+import { QueryClient, QueryCache } from '@tanstack/query-core'\n+import { describe, expect, it, beforeEach } from 'vitest'\n+import { broadcastQueryClient } from '.'\n+\n+describe('broadcastQueryClient', () => {\n+  let queryClient: QueryClient\n+  let queryCache: QueryCache\n+\n+  beforeEach(() => {\n+    queryClient = new QueryClient()\n+    queryCache = queryClient.getQueryCache()\n+  })\n+\n+  it('should subscribe to the query cache', async () => {\n+    broadcastQueryClient({",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "2024306538",
        "repo_full_name": "TanStack/query",
        "pr_number": 8849,
        "pr_file": "packages/query-broadcast-client-experimental/src/index.test.ts",
        "discussion_id": "2024306538",
        "commented_code": "@@ -0,0 +1,21 @@\n+import { QueryClient, QueryCache } from '@tanstack/query-core'\n+import { describe, expect, it, beforeEach } from 'vitest'\n+import { broadcastQueryClient } from '.'\n+\n+describe('broadcastQueryClient', () => {\n+  let queryClient: QueryClient\n+  let queryCache: QueryCache\n+\n+  beforeEach(() => {\n+    queryClient = new QueryClient()\n+    queryCache = queryClient.getQueryCache()\n+  })\n+\n+  it('should subscribe to the query cache', async () => {\n+    broadcastQueryClient({",
        "comment_created_at": "2025-04-02T08:12:16+00:00",
        "comment_author": "TkDodo",
        "comment_body": "I think we are generally missing a cleanup function here that should be returned from `broadcastQueryClient`. It should:\r\n\r\n- call `unsubscribe` returned from `queryClient.getQuryCache().subscribe`, otherwise, we are leaking a subscription\r\n- call [channel.close()](https://developer.mozilla.org/en-US/docs/Web/API/BroadcastChannel/close) on the created `BroadcastChannel`, otherwise, it can\u2019t be garbage collected\r\n\r\nthen of course the test should call that, too.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1749361518",
    "pr_number": 8030,
    "pr_file": "packages/query-core/src/utils.ts",
    "created_at": "2024-09-08T21:18:16+00:00",
    "commented_code": "} else if (options.structuralSharing !== false) {\n     if (process.env.NODE_ENV !== 'production') {\n       try {\n-        JSON.stringify(prevData)\n-        JSON.stringify(data)\n+        replaceEqualDeep(prevData, data)",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1749361518",
        "repo_full_name": "TanStack/query",
        "pr_number": 8030,
        "pr_file": "packages/query-core/src/utils.ts",
        "discussion_id": "1749361518",
        "commented_code": "@@ -356,15 +356,13 @@ export function replaceData<\n   } else if (options.structuralSharing !== false) {\n     if (process.env.NODE_ENV !== 'production') {\n       try {\n-        JSON.stringify(prevData)\n-        JSON.stringify(data)\n+        replaceEqualDeep(prevData, data)",
        "comment_created_at": "2024-09-08T21:18:16+00:00",
        "comment_author": "akshatmittal",
        "comment_body": "I believe you can return here to avoid the double `replaceEqualDeep` calls.",
        "pr_file_module": null
      },
      {
        "comment_id": "1749366042",
        "repo_full_name": "TanStack/query",
        "pr_number": 8030,
        "pr_file": "packages/query-core/src/utils.ts",
        "discussion_id": "1749361518",
        "commented_code": "@@ -356,15 +356,13 @@ export function replaceData<\n   } else if (options.structuralSharing !== false) {\n     if (process.env.NODE_ENV !== 'production') {\n       try {\n-        JSON.stringify(prevData)\n-        JSON.stringify(data)\n+        replaceEqualDeep(prevData, data)",
        "comment_created_at": "2024-09-08T21:36:07+00:00",
        "comment_author": "jxom",
        "comment_body": "good catch!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1308481513",
    "pr_number": 5125,
    "pr_file": "packages/query-core/src/infiniteQueryBehavior.ts",
    "created_at": "2023-08-29T09:25:45+00:00",
    "commented_code": "return result\n       }\n+      if (context.options.persister) {\n+        context.fetchFn = () => {\n+          return context.options.persister?.(fetchFn as any, {\n+            queryKey: context.queryKey,\n+            meta: context.options.meta,\n+            signal: context.signal,\n+          })\n+        }\n+      } else {\n+        context.fetchFn = fetchFn\n+      }",
    "repo_full_name": "TanStack/query",
    "discussion_comments": [
      {
        "comment_id": "1308481513",
        "repo_full_name": "TanStack/query",
        "pr_number": 5125,
        "pr_file": "packages/query-core/src/infiniteQueryBehavior.ts",
        "discussion_id": "1308481513",
        "commented_code": "@@ -114,6 +114,17 @@ export function infiniteQueryBehavior<TQueryFnData, TError, TData, TPageParam>(\n \n         return result\n       }\n+      if (context.options.persister) {\n+        context.fetchFn = () => {\n+          return context.options.persister?.(fetchFn as any, {\n+            queryKey: context.queryKey,\n+            meta: context.options.meta,\n+            signal: context.signal,\n+          })\n+        }\n+      } else {\n+        context.fetchFn = fetchFn\n+      }",
        "comment_created_at": "2023-08-29T09:25:45+00:00",
        "comment_author": "TkDodo",
        "comment_body": "this is a good solution because it will only wrap the whole `fetchFn` once, and not the fetch we do for every page \ud83d\udc4d ",
        "pr_file_module": null
      }
    ]
  }
]