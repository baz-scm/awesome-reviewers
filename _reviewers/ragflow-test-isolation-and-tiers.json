[
  {
    "discussion_id": "2980774025",
    "pr_number": 13710,
    "pr_file": "rag/svr/task_executor.py",
    "created_at": "2026-03-24T16:13:56+00:00",
    "commented_code": "DocumentService.increment_chunk_num(task_doc_id, task_dataset_id, token_count, chunk_count, 0)\n \n+        # Table parser (manual): push metadata/both column values to document-level metadata for UI / chat filters\n+        if task.get(\"parser_id\", \"\").lower() == \"table\":\n+            eff_pc = merge_table_parser_config_from_kb(task)\n+            logging.info(\n+                f\"[TABLE_META_DEBUG] table post-index: table_column_mode={eff_pc.get('table_column_mode')!r}\"\n+            )\n+            if eff_pc.get(\"table_column_mode\") == \"manual\":\n+                try:\n+                    agg = aggregate_table_manual_doc_metadata(chunks, task)",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "2982748477",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13710,
        "pr_file": "rag/svr/task_executor.py",
        "discussion_id": "2980774025",
        "commented_code": "@@ -1177,6 +1361,41 @@ async def _maybe_insert_chunks(_chunks):\n \n         DocumentService.increment_chunk_num(task_doc_id, task_dataset_id, token_count, chunk_count, 0)\n \n+        # Table parser (manual): push metadata/both column values to document-level metadata for UI / chat filters\n+        if task.get(\"parser_id\", \"\").lower() == \"table\":\n+            eff_pc = merge_table_parser_config_from_kb(task)\n+            logging.info(\n+                f\"[TABLE_META_DEBUG] table post-index: table_column_mode={eff_pc.get('table_column_mode')!r}\"\n+            )\n+            if eff_pc.get(\"table_column_mode\") == \"manual\":\n+                try:\n+                    agg = aggregate_table_manual_doc_metadata(chunks, task)",
        "comment_created_at": "2026-03-24T16:13:56+00:00",
        "comment_author": "ahmadintisar",
        "comment_body": "Agreed \u2014 will add unit tests covering:\r\n\r\n- Default role behavior (missing/partial `table_column_roles`)\r\n- Manual mode: vectorize-only, metadata-only, both columns in chunk text vs chunk_data\r\n- Empty `field_map` with ES typed field probe fallback\r\n- Metadata aggregation output (raw values, `_tks` list fallback)\r\n- Auto mode: no column filtering, no metadata write\r\n\r\nWill push in a follow-up commit.",
        "pr_file_module": null
      },
      {
        "comment_id": "2984444546",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13710,
        "pr_file": "rag/svr/task_executor.py",
        "discussion_id": "2980774025",
        "commented_code": "@@ -1177,6 +1361,41 @@ async def _maybe_insert_chunks(_chunks):\n \n         DocumentService.increment_chunk_num(task_doc_id, task_dataset_id, token_count, chunk_count, 0)\n \n+        # Table parser (manual): push metadata/both column values to document-level metadata for UI / chat filters\n+        if task.get(\"parser_id\", \"\").lower() == \"table\":\n+            eff_pc = merge_table_parser_config_from_kb(task)\n+            logging.info(\n+                f\"[TABLE_META_DEBUG] table post-index: table_column_mode={eff_pc.get('table_column_mode')!r}\"\n+            )\n+            if eff_pc.get(\"table_column_mode\") == \"manual\":\n+                try:\n+                    agg = aggregate_table_manual_doc_metadata(chunks, task)",
        "comment_created_at": "2026-03-24T21:42:52+00:00",
        "comment_author": "ahmadintisar",
        "comment_body": "Added 27 unit tests in `test/unit_test/rag/svr/`:\r\n\r\n- `test_table_column_roles_helpers.py` \u2014 ES typed key probe, field_map vs probe priority, `_raw` field derivation, value conversion, config merge\r\n- `test_table_metadata_aggregation.py` \u2014 manual/auto mode, partial roles with default-to-both, dedup, `_raw` preference over `_tks`, list fallback, KB reload mock, Infinity path\r\n\r\nHelper functions extracted to `rag/utils/table_es_metadata.py` to enable clean imports without pulling in heavy `task_executor` dependencies (xgboost, etc.).\r\n\r\nAll 27 tests passing locally.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3146690354",
    "pr_number": 14390,
    "pr_file": "test/testcases/test_http_api/test_dataset_management/test_ingestion_logs.py",
    "created_at": "2026-04-27T10:51:23+00:00",
    "commented_code": "def test_get_ingestion_log_invalid_dataset(self, HttpApiAuth):\n         res = get_ingestion_log(HttpApiAuth, \"invalid_id\", \"some_log_id\")\n         assert res[\"code\"] != 0, res\n+\n+\n+@pytest.mark.usefixtures(\"clear_datasets\")\n+class TestListIngestionLogsEdgeCases:\n+    @pytest.mark.p3",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "3146690354",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 14390,
        "pr_file": "test/testcases/test_http_api/test_dataset_management/test_ingestion_logs.py",
        "discussion_id": "3146690354",
        "commented_code": "@@ -51,3 +51,21 @@ def test_get_ingestion_log_not_found(self, HttpApiAuth, add_dataset_func):\n     def test_get_ingestion_log_invalid_dataset(self, HttpApiAuth):\n         res = get_ingestion_log(HttpApiAuth, \"invalid_id\", \"some_log_id\")\n         assert res[\"code\"] != 0, res\n+\n+\n+@pytest.mark.usefixtures(\"clear_datasets\")\n+class TestListIngestionLogsEdgeCases:\n+    @pytest.mark.p3",
        "comment_created_at": "2026-04-27T10:51:23+00:00",
        "comment_author": "xugangqiang",
        "comment_body": "By default p3 will not be executed.  Please change to p2 if want to execute it.",
        "pr_file_module": null
      },
      {
        "comment_id": "3146716632",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 14390,
        "pr_file": "test/testcases/test_http_api/test_dataset_management/test_ingestion_logs.py",
        "discussion_id": "3146690354",
        "commented_code": "@@ -51,3 +51,21 @@ def test_get_ingestion_log_not_found(self, HttpApiAuth, add_dataset_func):\n     def test_get_ingestion_log_invalid_dataset(self, HttpApiAuth):\n         res = get_ingestion_log(HttpApiAuth, \"invalid_id\", \"some_log_id\")\n         assert res[\"code\"] != 0, res\n+\n+\n+@pytest.mark.usefixtures(\"clear_datasets\")\n+class TestListIngestionLogsEdgeCases:\n+    @pytest.mark.p3",
        "comment_created_at": "2026-04-27T10:56:41+00:00",
        "comment_author": "euvre",
        "comment_body": "Because it was originally a p3-level test.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3083793342",
    "pr_number": 14020,
    "pr_file": "common/metadata_utils.py",
    "created_at": "2026-04-15T03:33:46+00:00",
    "commented_code": "def metadata_schema(metadata: dict|list|None) -> Dict[str, Any]:\n+    \"\"\"Convert metadata field definitions into a JSON Schema object.",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "3083793342",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 14020,
        "pr_file": "common/metadata_utils.py",
        "discussion_id": "3083793342",
        "commented_code": "@@ -272,6 +281,11 @@ def update_metadata_to(metadata, meta):\n \n \n def metadata_schema(metadata: dict|list|None) -> Dict[str, Any]:\n+    \"\"\"Convert metadata field definitions into a JSON Schema object.",
        "comment_created_at": "2026-04-15T03:33:46+00:00",
        "comment_author": "xugangqiang",
        "comment_body": "May I ask for your help to add unit tests for metadata_schema? So that we know the logic of metadata_schema is not broken",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3047610164",
    "pr_number": 13965,
    "pr_file": "test/unit_test/common/test_google_drive_sync_buffer.py",
    "created_at": "2026-04-07T20:26:01+00:00",
    "commented_code": "+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"Tests for Google Drive connector time buffer and time range filter.\n+\n+Verifies:\n+1. The sync time buffer config is correctly defined and env-configurable.\n+2. The _adjust_start_for_query logic correctly shifts start backward.\n+3. The generate_time_range_filter function uses both modifiedTime and\n+   createdTime to catch newly uploaded files whose modifiedTime predates\n+   the last sync cutoff (issue #13939).\n+\"\"\"\n+\n+import importlib\n+import os\n+import sys\n+from pathlib import Path\n+from types import ModuleType\n+from unittest.mock import patch\n+\n+import common\n+\n+# Bootstrap common.data_source without triggering __init__.py imports\n+# (which pull in boto3, googleapiclient, etc.)\n+repo_root = Path(__file__).resolve().parents[3]\n+_ds_pkg = ModuleType(\"common.data_source\")\n+_ds_pkg.__path__ = [str(repo_root / \"common\" / \"data_source\")]\n+sys.modules[\"common.data_source\"] = _ds_pkg\n+setattr(common, \"data_source\", _ds_pkg)\n+\n+# Stub out google_drive subpackage dependencies before importing file_retrieval\n+_gd_pkg = ModuleType(\"common.data_source.google_drive\")\n+_gd_pkg.__path__ = [str(repo_root / \"common\" / \"data_source\" / \"google_drive\")]\n+sys.modules[\"common.data_source.google_drive\"] = _gd_pkg\n+\n+_gu_pkg = ModuleType(\"common.data_source.google_util\")\n+_gu_pkg.__path__ = [str(repo_root / \"common\" / \"data_source\" / \"google_util\")]\n+sys.modules[\"common.data_source.google_util\"] = _gu_pkg\n+\n+# Stub heavy third-party modules that file_retrieval and its deps import\n+_stub_modules = [\n+    \"googleapiclient\", \"googleapiclient.discovery\", \"googleapiclient.errors\",\n+    \"google\", \"google.auth\", \"google.auth.exceptions\",\n+    \"google.oauth2\", \"google.oauth2.credentials\", \"google.oauth2.service_account\",\n+]\n+for mod_name in _stub_modules:\n+    if mod_name not in sys.modules:\n+        stub = ModuleType(mod_name)\n+        # Add common attributes that imports expect\n+        stub.__dict__.setdefault(\"HttpError\", type(\"HttpError\", (Exception,), {}))\n+        stub.__dict__.setdefault(\"Resource\", type(\"Resource\", (), {}))\n+        stub.__dict__.setdefault(\"Credentials\", type(\"Credentials\", (), {}))\n+        stub.__dict__.setdefault(\"RefreshError\", type(\"RefreshError\", (Exception,), {}))\n+        stub.__dict__.setdefault(\"build\", lambda *a, **kw: None)\n+        stub.__dict__.setdefault(\"override\", lambda f: f)\n+        sys.modules[mod_name] = stub\n+\n+cfg = importlib.import_module(\"common.data_source.config\")\n+ONE_DAY = cfg.ONE_DAY\n+\n+# Import models and util before file_retrieval (which depends on them)\n+importlib.import_module(\"common.data_source.models\")\n+importlib.import_module(\"common.data_source.google_util.util\")\n+importlib.import_module(\"common.data_source.google_util.resource\")\n+importlib.import_module(\"common.data_source.google_drive.constant\")\n+importlib.import_module(\"common.data_source.google_drive.model\")\n+\n+file_retrieval = importlib.import_module(\"common.data_source.google_drive.file_retrieval\")\n+generate_time_range_filter = file_retrieval.generate_time_range_filter\n+\n+\n+class TestGoogleDriveSyncTimeBufferConfig:\n+    \"\"\"Tests for the GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS config constant.\"\"\"\n+\n+    def test_default_value_is_one_day(self):\n+        with patch.dict(os.environ, {}, clear=False):\n+            os.environ.pop(\"GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS\", None)\n+            importlib.reload(cfg)\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == ONE_DAY\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == 86400\n+\n+    def test_env_override(self):\n+        with patch.dict(os.environ, {\"GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS\": \"3600\"}):\n+            importlib.reload(cfg)\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == 3600\n+\n+    def test_env_override_zero(self):\n+        with patch.dict(os.environ, {\"GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS\": \"0\"}):\n+            importlib.reload(cfg)\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == 0\n+\n+\n+class TestAdjustStartForQuery:\n+    \"\"\"Tests for the _adjust_start_for_query logic.\"\"\"\n+\n+    @staticmethod\n+    def _adjust(start, time_buffer_seconds):\n+        \"\"\"Replicate the connector's _adjust_start_for_query logic.\"\"\"\n+        buffer = max(0, time_buffer_seconds)\n+        if not start or start <= 0:\n+            return start\n+        if buffer <= 0:\n+            return start\n+        return max(0.0, start - buffer)\n+\n+    def test_subtracts_buffer(self):\n+        assert self._adjust(1_000_000.0, 86400) == 1_000_000.0 - 86400\n+\n+    def test_clamps_to_zero(self):\n+        assert self._adjust(100.0, 86400) == 0.0\n+\n+    def test_zero_start_unchanged(self):\n+        assert self._adjust(0.0, 86400) == 0.0\n+\n+    def test_none_start_unchanged(self):\n+        assert self._adjust(None, 86400) is None\n+\n+    def test_zero_buffer_no_change(self):\n+        assert self._adjust(1_000_000.0, 0) == 1_000_000.0\n+\n+    def test_negative_buffer_clamped_to_zero(self):\n+        assert self._adjust(1_000_000.0, -100) == 1_000_000.0\n+\n+    def test_custom_buffer_value(self):\n+        assert self._adjust(1_000_000.0, 7200) == 1_000_000.0 - 7200\n+\n+    def test_exact_boundary(self):\n+        assert self._adjust(86400.0, 86400) == 0.0\n+\n+    def test_buffer_larger_than_start(self):\n+        assert self._adjust(3600.0, 86400) == 0.0\n+\n+\n+class TestGenerateTimeRangeFilter:\n+    \"\"\"Tests for generate_time_range_filter with createdTime support.\"\"\"\n+\n+    def test_no_filters_when_no_args(self):\n+        assert generate_time_range_filter() == \"\"\n+\n+    def test_no_filters_when_none(self):\n+        assert generate_time_range_filter(start=None, end=None) == \"\"\n+\n+    def test_start_includes_both_modified_and_created_time(self):\n+        result = generate_time_range_filter(start=1_000_000.0)\n+        assert \"modifiedTime\" in result\n+        assert \"createdTime\" in result\n+        assert \" or \" in result\n+\n+    def test_start_filter_uses_strict_greater_than(self):\n+        result = generate_time_range_filter(start=1_000_000.0)\n+        assert \"modifiedTime >\" in result\n+        assert \"createdTime >\" in result\n+\n+    def test_start_filter_is_parenthesized(self):\n+        \"\"\"The OR condition must be grouped to combine correctly with AND.\"\"\"\n+        result = generate_time_range_filter(start=1_000_000.0)\n+        assert result.strip().startswith(\"and (\")\n+        assert result.strip().endswith(\")\")\n+\n+    def test_end_filter_only_uses_modified_time(self):\n+        result = generate_time_range_filter(end=2_000_000.0)\n+        assert \"modifiedTime <=\" in result\n+        assert \"createdTime\" not in result\n+\n+    def test_both_start_and_end(self):\n+        result = generate_time_range_filter(start=1_000_000.0, end=2_000_000.0)\n+        assert \"modifiedTime >\" in result\n+        assert \"createdTime >\" in result\n+        assert \"modifiedTime <=\" in result\n+\n+    def test_zero_start_still_generates_filter(self):\n+        \"\"\"start=0.0 is not None, so a filter should be generated.\"\"\"\n+        result = generate_time_range_filter(start=0.0)\n+        assert \"modifiedTime\" in result\n+        assert \"createdTime\" in result",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "3047679519",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13965,
        "pr_file": "test/unit_test/common/test_google_drive_sync_buffer.py",
        "discussion_id": "3047610164",
        "commented_code": "@@ -0,0 +1,188 @@\n+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"Tests for Google Drive connector time buffer and time range filter.\n+\n+Verifies:\n+1. The sync time buffer config is correctly defined and env-configurable.\n+2. The _adjust_start_for_query logic correctly shifts start backward.\n+3. The generate_time_range_filter function uses both modifiedTime and\n+   createdTime to catch newly uploaded files whose modifiedTime predates\n+   the last sync cutoff (issue #13939).\n+\"\"\"\n+\n+import importlib\n+import os\n+import sys\n+from pathlib import Path\n+from types import ModuleType\n+from unittest.mock import patch\n+\n+import common\n+\n+# Bootstrap common.data_source without triggering __init__.py imports\n+# (which pull in boto3, googleapiclient, etc.)\n+repo_root = Path(__file__).resolve().parents[3]\n+_ds_pkg = ModuleType(\"common.data_source\")\n+_ds_pkg.__path__ = [str(repo_root / \"common\" / \"data_source\")]\n+sys.modules[\"common.data_source\"] = _ds_pkg\n+setattr(common, \"data_source\", _ds_pkg)\n+\n+# Stub out google_drive subpackage dependencies before importing file_retrieval\n+_gd_pkg = ModuleType(\"common.data_source.google_drive\")\n+_gd_pkg.__path__ = [str(repo_root / \"common\" / \"data_source\" / \"google_drive\")]\n+sys.modules[\"common.data_source.google_drive\"] = _gd_pkg\n+\n+_gu_pkg = ModuleType(\"common.data_source.google_util\")\n+_gu_pkg.__path__ = [str(repo_root / \"common\" / \"data_source\" / \"google_util\")]\n+sys.modules[\"common.data_source.google_util\"] = _gu_pkg\n+\n+# Stub heavy third-party modules that file_retrieval and its deps import\n+_stub_modules = [\n+    \"googleapiclient\", \"googleapiclient.discovery\", \"googleapiclient.errors\",\n+    \"google\", \"google.auth\", \"google.auth.exceptions\",\n+    \"google.oauth2\", \"google.oauth2.credentials\", \"google.oauth2.service_account\",\n+]\n+for mod_name in _stub_modules:\n+    if mod_name not in sys.modules:\n+        stub = ModuleType(mod_name)\n+        # Add common attributes that imports expect\n+        stub.__dict__.setdefault(\"HttpError\", type(\"HttpError\", (Exception,), {}))\n+        stub.__dict__.setdefault(\"Resource\", type(\"Resource\", (), {}))\n+        stub.__dict__.setdefault(\"Credentials\", type(\"Credentials\", (), {}))\n+        stub.__dict__.setdefault(\"RefreshError\", type(\"RefreshError\", (Exception,), {}))\n+        stub.__dict__.setdefault(\"build\", lambda *a, **kw: None)\n+        stub.__dict__.setdefault(\"override\", lambda f: f)\n+        sys.modules[mod_name] = stub\n+\n+cfg = importlib.import_module(\"common.data_source.config\")\n+ONE_DAY = cfg.ONE_DAY\n+\n+# Import models and util before file_retrieval (which depends on them)\n+importlib.import_module(\"common.data_source.models\")\n+importlib.import_module(\"common.data_source.google_util.util\")\n+importlib.import_module(\"common.data_source.google_util.resource\")\n+importlib.import_module(\"common.data_source.google_drive.constant\")\n+importlib.import_module(\"common.data_source.google_drive.model\")\n+\n+file_retrieval = importlib.import_module(\"common.data_source.google_drive.file_retrieval\")\n+generate_time_range_filter = file_retrieval.generate_time_range_filter\n+\n+\n+class TestGoogleDriveSyncTimeBufferConfig:\n+    \"\"\"Tests for the GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS config constant.\"\"\"\n+\n+    def test_default_value_is_one_day(self):\n+        with patch.dict(os.environ, {}, clear=False):\n+            os.environ.pop(\"GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS\", None)\n+            importlib.reload(cfg)\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == ONE_DAY\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == 86400\n+\n+    def test_env_override(self):\n+        with patch.dict(os.environ, {\"GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS\": \"3600\"}):\n+            importlib.reload(cfg)\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == 3600\n+\n+    def test_env_override_zero(self):\n+        with patch.dict(os.environ, {\"GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS\": \"0\"}):\n+            importlib.reload(cfg)\n+            assert cfg.GOOGLE_DRIVE_SYNC_TIME_BUFFER_SECONDS == 0\n+\n+\n+class TestAdjustStartForQuery:\n+    \"\"\"Tests for the _adjust_start_for_query logic.\"\"\"\n+\n+    @staticmethod\n+    def _adjust(start, time_buffer_seconds):\n+        \"\"\"Replicate the connector's _adjust_start_for_query logic.\"\"\"\n+        buffer = max(0, time_buffer_seconds)\n+        if not start or start <= 0:\n+            return start\n+        if buffer <= 0:\n+            return start\n+        return max(0.0, start - buffer)\n+\n+    def test_subtracts_buffer(self):\n+        assert self._adjust(1_000_000.0, 86400) == 1_000_000.0 - 86400\n+\n+    def test_clamps_to_zero(self):\n+        assert self._adjust(100.0, 86400) == 0.0\n+\n+    def test_zero_start_unchanged(self):\n+        assert self._adjust(0.0, 86400) == 0.0\n+\n+    def test_none_start_unchanged(self):\n+        assert self._adjust(None, 86400) is None\n+\n+    def test_zero_buffer_no_change(self):\n+        assert self._adjust(1_000_000.0, 0) == 1_000_000.0\n+\n+    def test_negative_buffer_clamped_to_zero(self):\n+        assert self._adjust(1_000_000.0, -100) == 1_000_000.0\n+\n+    def test_custom_buffer_value(self):\n+        assert self._adjust(1_000_000.0, 7200) == 1_000_000.0 - 7200\n+\n+    def test_exact_boundary(self):\n+        assert self._adjust(86400.0, 86400) == 0.0\n+\n+    def test_buffer_larger_than_start(self):\n+        assert self._adjust(3600.0, 86400) == 0.0\n+\n+\n+class TestGenerateTimeRangeFilter:\n+    \"\"\"Tests for generate_time_range_filter with createdTime support.\"\"\"\n+\n+    def test_no_filters_when_no_args(self):\n+        assert generate_time_range_filter() == \"\"\n+\n+    def test_no_filters_when_none(self):\n+        assert generate_time_range_filter(start=None, end=None) == \"\"\n+\n+    def test_start_includes_both_modified_and_created_time(self):\n+        result = generate_time_range_filter(start=1_000_000.0)\n+        assert \"modifiedTime\" in result\n+        assert \"createdTime\" in result\n+        assert \" or \" in result\n+\n+    def test_start_filter_uses_strict_greater_than(self):\n+        result = generate_time_range_filter(start=1_000_000.0)\n+        assert \"modifiedTime >\" in result\n+        assert \"createdTime >\" in result\n+\n+    def test_start_filter_is_parenthesized(self):\n+        \"\"\"The OR condition must be grouped to combine correctly with AND.\"\"\"\n+        result = generate_time_range_filter(start=1_000_000.0)\n+        assert result.strip().startswith(\"and (\")\n+        assert result.strip().endswith(\")\")\n+\n+    def test_end_filter_only_uses_modified_time(self):\n+        result = generate_time_range_filter(end=2_000_000.0)\n+        assert \"modifiedTime <=\" in result\n+        assert \"createdTime\" not in result\n+\n+    def test_both_start_and_end(self):\n+        result = generate_time_range_filter(start=1_000_000.0, end=2_000_000.0)\n+        assert \"modifiedTime >\" in result\n+        assert \"createdTime >\" in result\n+        assert \"modifiedTime <=\" in result\n+\n+    def test_zero_start_still_generates_filter(self):\n+        \"\"\"start=0.0 is not None, so a filter should be generated.\"\"\"\n+        result = generate_time_range_filter(start=0.0)\n+        assert \"modifiedTime\" in result\n+        assert \"createdTime\" in result",
        "comment_created_at": "2026-04-07T20:26:01+00:00",
        "comment_author": "itdeveloper1988",
        "comment_body": "I fixed code for your review.\r\n\r\nAdd @pytest.mark.p2 markers to all test classes. Isolate sys.modules\r\nstub injection with autouse fixtures that restore original state after\r\ntests complete, preventing cross-test-suite leakage.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3108740061",
    "pr_number": 14219,
    "pr_file": "test/unit_test/deepdoc/parser/test_mineru_crop.py",
    "created_at": "2026-04-20T07:06:34+00:00",
    "commented_code": "+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"Unit tests for MinerUParser.crop() overlay logic.\n+\n+Regression tests for issue #14197: content images were incorrectly darkened\n+when the top context strip had zero height and was skipped.\n+\"\"\"\n+\n+import logging\n+\n+from PIL import Image\n+\n+from deepdoc.parser.mineru_parser import MinerUParser",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "3108829202",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 14219,
        "pr_file": "test/unit_test/deepdoc/parser/test_mineru_crop.py",
        "discussion_id": "3108740061",
        "commented_code": "@@ -0,0 +1,122 @@\n+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"Unit tests for MinerUParser.crop() overlay logic.\n+\n+Regression tests for issue #14197: content images were incorrectly darkened\n+when the top context strip had zero height and was skipped.\n+\"\"\"\n+\n+import logging\n+\n+from PIL import Image\n+\n+from deepdoc.parser.mineru_parser import MinerUParser",
        "comment_created_at": "2026-04-20T07:06:34+00:00",
        "comment_author": "web-dev0521",
        "comment_body": "Added `@pytest.mark.p1` to the core regression test (`test_single_image_not_darkened_when_both_context_strips_skipped`) and `@pytest.mark.p2` to the remaining tests. The corresponding markers have been properly registered in `pyproject.toml` under `[tool.pytest.ini_options]`.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3056976705",
    "pr_number": 13990,
    "pr_file": "mcp/server/server.py",
    "created_at": "2026-04-09T10:00:18+00:00",
    "commented_code": "return \"\n\".join(result_list)\n         return \"\"\n \n+    async def resolve_dataset_ids(self, *, api_key: str):\n+        dataset_ids = []\n+        page = 1\n+\n+        while True:\n+            params = {\n+                \"page\": page,\n+                \"page_size\": self._DATASET_PAGE_SIZE,\n+                \"orderby\": \"create_time\",\n+                \"desc\": True,\n+            }\n+            res = await self._get(\"/datasets\", params, api_key=api_key)\n+            if not res or res.status_code != 200:\n+                error_message = None\n+                if res is not None:\n+                    try:\n+                        error_message = res.json().get(\"message\")\n+                    except Exception:\n+                        error_message = None\n+                logging.warning(\n+                    \"resolve_dataset_ids failed to fetch /datasets page=%s status=%s message=%s\",\n+                    page,\n+                    getattr(res, \"status_code\", None),\n+                    error_message,\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=\"Cannot process this operation.\")])\n+\n+            res_json = res.json()\n+            if res_json.get(\"code\") != 0:\n+                logging.warning(\n+                    \"resolve_dataset_ids received error response page=%s code=%s message=%s\",\n+                    page,\n+                    res_json.get(\"code\"),\n+                    res_json.get(\"message\"),\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=res_json.get(\"message\", \"Cannot process this operation.\"))])\n+\n+            datasets = res_json.get(\"data\", [])\n+            logging.debug(\"resolve_dataset_ids received %s datasets from page=%s\", len(datasets), page)\n+            dataset_ids.extend(data[\"id\"] for data in datasets if data.get(\"id\"))\n+            total = res_json.get(\"total\", len(dataset_ids))\n+            if not datasets or len(dataset_ids) >= total:\n+                break\n+            page += 1\n+\n+        resolved = list(dict.fromkeys(dataset_ids))\n+        logging.info(\"resolve_dataset_ids resolved %s accessible dataset IDs\", len(resolved))\n+        return resolved",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "3056976705",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13990,
        "pr_file": "mcp/server/server.py",
        "discussion_id": "3056976705",
        "commented_code": "@@ -157,6 +158,55 @@ async def list_datasets(\n             return \"\\n\".join(result_list)\n         return \"\"\n \n+    async def resolve_dataset_ids(self, *, api_key: str):\n+        dataset_ids = []\n+        page = 1\n+\n+        while True:\n+            params = {\n+                \"page\": page,\n+                \"page_size\": self._DATASET_PAGE_SIZE,\n+                \"orderby\": \"create_time\",\n+                \"desc\": True,\n+            }\n+            res = await self._get(\"/datasets\", params, api_key=api_key)\n+            if not res or res.status_code != 200:\n+                error_message = None\n+                if res is not None:\n+                    try:\n+                        error_message = res.json().get(\"message\")\n+                    except Exception:\n+                        error_message = None\n+                logging.warning(\n+                    \"resolve_dataset_ids failed to fetch /datasets page=%s status=%s message=%s\",\n+                    page,\n+                    getattr(res, \"status_code\", None),\n+                    error_message,\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=\"Cannot process this operation.\")])\n+\n+            res_json = res.json()\n+            if res_json.get(\"code\") != 0:\n+                logging.warning(\n+                    \"resolve_dataset_ids received error response page=%s code=%s message=%s\",\n+                    page,\n+                    res_json.get(\"code\"),\n+                    res_json.get(\"message\"),\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=res_json.get(\"message\", \"Cannot process this operation.\"))])\n+\n+            datasets = res_json.get(\"data\", [])\n+            logging.debug(\"resolve_dataset_ids received %s datasets from page=%s\", len(datasets), page)\n+            dataset_ids.extend(data[\"id\"] for data in datasets if data.get(\"id\"))\n+            total = res_json.get(\"total\", len(dataset_ids))\n+            if not datasets or len(dataset_ids) >= total:\n+                break\n+            page += 1\n+\n+        resolved = list(dict.fromkeys(dataset_ids))\n+        logging.info(\"resolve_dataset_ids resolved %s accessible dataset IDs\", len(resolved))\n+        return resolved",
        "comment_created_at": "2026-04-09T10:00:18+00:00",
        "comment_author": "xugangqiang",
        "comment_body": "Can we utilize self.list_datasets - to avoid dup logic?",
        "pr_file_module": null
      },
      {
        "comment_id": "3060730017",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13990,
        "pr_file": "mcp/server/server.py",
        "discussion_id": "3056976705",
        "commented_code": "@@ -157,6 +158,55 @@ async def list_datasets(\n             return \"\\n\".join(result_list)\n         return \"\"\n \n+    async def resolve_dataset_ids(self, *, api_key: str):\n+        dataset_ids = []\n+        page = 1\n+\n+        while True:\n+            params = {\n+                \"page\": page,\n+                \"page_size\": self._DATASET_PAGE_SIZE,\n+                \"orderby\": \"create_time\",\n+                \"desc\": True,\n+            }\n+            res = await self._get(\"/datasets\", params, api_key=api_key)\n+            if not res or res.status_code != 200:\n+                error_message = None\n+                if res is not None:\n+                    try:\n+                        error_message = res.json().get(\"message\")\n+                    except Exception:\n+                        error_message = None\n+                logging.warning(\n+                    \"resolve_dataset_ids failed to fetch /datasets page=%s status=%s message=%s\",\n+                    page,\n+                    getattr(res, \"status_code\", None),\n+                    error_message,\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=\"Cannot process this operation.\")])\n+\n+            res_json = res.json()\n+            if res_json.get(\"code\") != 0:\n+                logging.warning(\n+                    \"resolve_dataset_ids received error response page=%s code=%s message=%s\",\n+                    page,\n+                    res_json.get(\"code\"),\n+                    res_json.get(\"message\"),\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=res_json.get(\"message\", \"Cannot process this operation.\"))])\n+\n+            datasets = res_json.get(\"data\", [])\n+            logging.debug(\"resolve_dataset_ids received %s datasets from page=%s\", len(datasets), page)\n+            dataset_ids.extend(data[\"id\"] for data in datasets if data.get(\"id\"))\n+            total = res_json.get(\"total\", len(dataset_ids))\n+            if not datasets or len(dataset_ids) >= total:\n+                break\n+            page += 1\n+\n+        resolved = list(dict.fromkeys(dataset_ids))\n+        logging.info(\"resolve_dataset_ids resolved %s accessible dataset IDs\", len(resolved))\n+        return resolved",
        "comment_created_at": "2026-04-09T21:21:21+00:00",
        "comment_author": "spider-yamet",
        "comment_body": "Yes. Now I refactored the `/datasets` fetch path into a shared lower-level helper, and now both `list_datasets()` and `resolve_dataset_ids()` reuse it instead of duplicating the request logic.\r\n",
        "pr_file_module": null
      },
      {
        "comment_id": "3061652221",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13990,
        "pr_file": "mcp/server/server.py",
        "discussion_id": "3056976705",
        "commented_code": "@@ -157,6 +158,55 @@ async def list_datasets(\n             return \"\\n\".join(result_list)\n         return \"\"\n \n+    async def resolve_dataset_ids(self, *, api_key: str):\n+        dataset_ids = []\n+        page = 1\n+\n+        while True:\n+            params = {\n+                \"page\": page,\n+                \"page_size\": self._DATASET_PAGE_SIZE,\n+                \"orderby\": \"create_time\",\n+                \"desc\": True,\n+            }\n+            res = await self._get(\"/datasets\", params, api_key=api_key)\n+            if not res or res.status_code != 200:\n+                error_message = None\n+                if res is not None:\n+                    try:\n+                        error_message = res.json().get(\"message\")\n+                    except Exception:\n+                        error_message = None\n+                logging.warning(\n+                    \"resolve_dataset_ids failed to fetch /datasets page=%s status=%s message=%s\",\n+                    page,\n+                    getattr(res, \"status_code\", None),\n+                    error_message,\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=\"Cannot process this operation.\")])\n+\n+            res_json = res.json()\n+            if res_json.get(\"code\") != 0:\n+                logging.warning(\n+                    \"resolve_dataset_ids received error response page=%s code=%s message=%s\",\n+                    page,\n+                    res_json.get(\"code\"),\n+                    res_json.get(\"message\"),\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=res_json.get(\"message\", \"Cannot process this operation.\"))])\n+\n+            datasets = res_json.get(\"data\", [])\n+            logging.debug(\"resolve_dataset_ids received %s datasets from page=%s\", len(datasets), page)\n+            dataset_ids.extend(data[\"id\"] for data in datasets if data.get(\"id\"))\n+            total = res_json.get(\"total\", len(dataset_ids))\n+            if not datasets or len(dataset_ids) >= total:\n+                break\n+            page += 1\n+\n+        resolved = list(dict.fromkeys(dataset_ids))\n+        logging.info(\"resolve_dataset_ids resolved %s accessible dataset IDs\", len(resolved))\n+        return resolved",
        "comment_created_at": "2026-04-10T02:05:49+00:00",
        "comment_author": "xugangqiang",
        "comment_body": "Could you please do me a favor - upload the testing evidence after your latest change (refactor)? ",
        "pr_file_module": null
      },
      {
        "comment_id": "3061721149",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 13990,
        "pr_file": "mcp/server/server.py",
        "discussion_id": "3056976705",
        "commented_code": "@@ -157,6 +158,55 @@ async def list_datasets(\n             return \"\\n\".join(result_list)\n         return \"\"\n \n+    async def resolve_dataset_ids(self, *, api_key: str):\n+        dataset_ids = []\n+        page = 1\n+\n+        while True:\n+            params = {\n+                \"page\": page,\n+                \"page_size\": self._DATASET_PAGE_SIZE,\n+                \"orderby\": \"create_time\",\n+                \"desc\": True,\n+            }\n+            res = await self._get(\"/datasets\", params, api_key=api_key)\n+            if not res or res.status_code != 200:\n+                error_message = None\n+                if res is not None:\n+                    try:\n+                        error_message = res.json().get(\"message\")\n+                    except Exception:\n+                        error_message = None\n+                logging.warning(\n+                    \"resolve_dataset_ids failed to fetch /datasets page=%s status=%s message=%s\",\n+                    page,\n+                    getattr(res, \"status_code\", None),\n+                    error_message,\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=\"Cannot process this operation.\")])\n+\n+            res_json = res.json()\n+            if res_json.get(\"code\") != 0:\n+                logging.warning(\n+                    \"resolve_dataset_ids received error response page=%s code=%s message=%s\",\n+                    page,\n+                    res_json.get(\"code\"),\n+                    res_json.get(\"message\"),\n+                )\n+                raise Exception([types.TextContent(type=\"text\", text=res_json.get(\"message\", \"Cannot process this operation.\"))])\n+\n+            datasets = res_json.get(\"data\", [])\n+            logging.debug(\"resolve_dataset_ids received %s datasets from page=%s\", len(datasets), page)\n+            dataset_ids.extend(data[\"id\"] for data in datasets if data.get(\"id\"))\n+            total = res_json.get(\"total\", len(dataset_ids))\n+            if not datasets or len(dataset_ids) >= total:\n+                break\n+            page += 1\n+\n+        resolved = list(dict.fromkeys(dataset_ids))\n+        logging.info(\"resolve_dataset_ids resolved %s accessible dataset IDs\", len(resolved))\n+        return resolved",
        "comment_created_at": "2026-04-10T02:32:44+00:00",
        "comment_author": "spider-yamet",
        "comment_body": "Sure. I re-ran the local controlled verification after the latest update, and the behavior is unchanged from the intended fix.\r\n\r\nVerification result after refactor:\r\nBefore fix:\r\n\r\n```\r\nScenario 1: no accessible datasets\r\n[before] resolved dataset_ids=[]\r\n[before] POST /retrieval response={'code': 102, 'message': '`dataset_ids` is required.'}\r\n\r\nScenario 2: 1500 accessible datasets\r\n[before] resolved dataset count=1000\r\n[before] POST /retrieval response={'code': 0, 'message': 'ok (1000 dataset ids)'}\r\n```\r\n\r\nAfter fix:\r\n```\r\nScenario 1: no accessible datasets\r\n[after-refactor] retrieval: dataset_ids omitted/empty; resolving before POST /retrieval\r\n[after-refactor] resolve_dataset_ids: start\r\n[after-refactor] resolve_dataset_ids: fetch page=1 page_size=1000\r\n[after-refactor] resolve_dataset_ids: received 0 dataset rows\r\n[after-refactor] resolve_dataset_ids: resolved 0 dataset ids\r\n[after-refactor] resolved dataset_ids=[]\r\n[after-refactor] MCP-side result={'code': None, 'message': 'No accessible datasets found.'}\r\n\r\nScenario 2: 1500 accessible datasets\r\n[after-refactor] retrieval: dataset_ids omitted/empty; resolving before POST /retrieval\r\n[after-refactor] resolve_dataset_ids: start\r\n[after-refactor] resolve_dataset_ids: fetch page=1 page_size=1000\r\n[after-refactor] resolve_dataset_ids: received 1000 dataset rows\r\n[after-refactor] resolve_dataset_ids: fetch page=2 page_size=1000\r\n[after-refactor] resolve_dataset_ids: received 500 dataset rows\r\n[after-refactor] resolve_dataset_ids: resolved 1500 dataset ids\r\n[after-refactor] resolved dataset count=1500\r\n[after-refactor] POST /retrieval response={'code': 0, 'message': 'ok (1500 dataset ids)'}\r\n```\r\n",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2584292776",
    "pr_number": 11699,
    "pr_file": "test/unit_test/services/test_checkpoint_service.py",
    "created_at": "2025-12-03T09:23:20+00:00",
    "commented_code": "+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"\n+Unit tests for Checkpoint Service\n+\n+Tests cover:\n+- Checkpoint creation and retrieval\n+- Document state management\n+- Pause/resume/cancel operations\n+- Retry logic\n+- Progress tracking\n+\"\"\"\n+\n+import pytest\n+from unittest.mock import Mock\n+\n+\n+class TestCheckpointCreation:\n+    \"\"\"Tests for checkpoint creation\"\"\"\n+    \n+    @pytest.fixture\n+    def mock_checkpoint_service(self):\n+        \"\"\"Mock CheckpointService - using Mock directly for unit tests\"\"\"\n+        mock = Mock()",
    "repo_full_name": "infiniflow/ragflow",
    "discussion_comments": [
      {
        "comment_id": "2584292776",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 11699,
        "pr_file": "test/unit_test/services/test_checkpoint_service.py",
        "discussion_id": "2584292776",
        "commented_code": "@@ -0,0 +1,464 @@\n+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"\n+Unit tests for Checkpoint Service\n+\n+Tests cover:\n+- Checkpoint creation and retrieval\n+- Document state management\n+- Pause/resume/cancel operations\n+- Retry logic\n+- Progress tracking\n+\"\"\"\n+\n+import pytest\n+from unittest.mock import Mock\n+\n+\n+class TestCheckpointCreation:\n+    \"\"\"Tests for checkpoint creation\"\"\"\n+    \n+    @pytest.fixture\n+    def mock_checkpoint_service(self):\n+        \"\"\"Mock CheckpointService - using Mock directly for unit tests\"\"\"\n+        mock = Mock()",
        "comment_created_at": "2025-12-03T09:23:20+00:00",
        "comment_author": "KevinHuSh",
        "comment_body": "Appreciations!\r\nHow come the `mock` is designated to `CheckpointService`? I did not get it.",
        "pr_file_module": null
      },
      {
        "comment_id": "2584356353",
        "repo_full_name": "infiniflow/ragflow",
        "pr_number": 11699,
        "pr_file": "test/unit_test/services/test_checkpoint_service.py",
        "discussion_id": "2584292776",
        "commented_code": "@@ -0,0 +1,464 @@\n+#\n+#  Copyright 2025 The InfiniFlow Authors. All Rights Reserved.\n+#\n+#  Licensed under the Apache License, Version 2.0 (the \"License\");\n+#  you may not use this file except in compliance with the License.\n+#  You may obtain a copy of the License at\n+#\n+#      http://www.apache.org/licenses/LICENSE-2.0\n+#\n+#  Unless required by applicable law or agreed to in writing, software\n+#  distributed under the License is distributed on an \"AS IS\" BASIS,\n+#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+#  See the License for the specific language governing permissions and\n+#  limitations under the License.\n+#\n+\n+\"\"\"\n+Unit tests for Checkpoint Service\n+\n+Tests cover:\n+- Checkpoint creation and retrieval\n+- Document state management\n+- Pause/resume/cancel operations\n+- Retry logic\n+- Progress tracking\n+\"\"\"\n+\n+import pytest\n+from unittest.mock import Mock\n+\n+\n+class TestCheckpointCreation:\n+    \"\"\"Tests for checkpoint creation\"\"\"\n+    \n+    @pytest.fixture\n+    def mock_checkpoint_service(self):\n+        \"\"\"Mock CheckpointService - using Mock directly for unit tests\"\"\"\n+        mock = Mock()",
        "comment_created_at": "2025-12-03T09:41:15+00:00",
        "comment_author": "NikolaPantel",
        "comment_body": "Great question! The mocks are intentional - they're for unit tests that run fast without a database.\r\n\r\nI've now added integration tests that use the real \r\nCheckpointService\r\n with actual database operations.\r\n\r\nTwo types of tests:\r\nUnit Tests (mocked) - \r\ntest/unit_test/services/test_checkpoint_service.py\r\n\r\nFast: 0.04 seconds\r\nNo database needed\r\nTests the interface\r\nIntegration Tests (real) - \r\ntest/integration_test/services/test_checkpoint_service_integration.py\r\n\r\nTests actual implementation\r\nReal database operations\r\nComplete workflows\r\nBoth are valuable - unit tests for quick feedback, integration tests for thorough validation.\r\n\r\nAll 22 unit tests passing \u2705",
        "pr_file_module": null
      }
    ]
  }
]