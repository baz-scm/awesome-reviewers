[
  {
    "discussion_id": "2988016043",
    "pr_number": 14907,
    "pr_file": "src/server.h",
    "created_at": "2026-03-25T12:52:31+00:00",
    "commented_code": "/* Max number of IO threads */\n #define IO_THREADS_MAX_NUM 128\n \n+/* Per-IO-thread stats, cache-line padded to prevent false sharing.\n+ * Stat counters are updated by their respective IO thread;\n+ * clients_num is updated by the main thread but infrequently. */\n+typedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\n+    redisAtomic long long io_reads_processed;  /* Number of read events processed */",
    "repo_full_name": "redis/redis",
    "discussion_comments": [
      {
        "comment_id": "2988016043",
        "repo_full_name": "redis/redis",
        "pr_number": 14907,
        "pr_file": "src/server.h",
        "discussion_id": "2988016043",
        "commented_code": "@@ -223,6 +223,17 @@ extern int configOOMScoreAdjValuesDefaults[CONFIG_OOM_COUNT];\n /* Max number of IO threads */\n #define IO_THREADS_MAX_NUM 128\n \n+/* Per-IO-thread stats, cache-line padded to prevent false sharing.\n+ * Stat counters are updated by their respective IO thread;\n+ * clients_num is updated by the main thread but infrequently. */\n+typedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\n+    redisAtomic long long io_reads_processed;  /* Number of read events processed */",
        "comment_created_at": "2026-03-25T12:52:31+00:00",
        "comment_author": "ShooterIT",
        "comment_body": "why not put them in `struct IOThread`",
        "pr_file_module": null
      },
      {
        "comment_id": "2992179078",
        "repo_full_name": "redis/redis",
        "pr_number": 14907,
        "pr_file": "src/server.h",
        "discussion_id": "2988016043",
        "commented_code": "@@ -223,6 +223,17 @@ extern int configOOMScoreAdjValuesDefaults[CONFIG_OOM_COUNT];\n /* Max number of IO threads */\n #define IO_THREADS_MAX_NUM 128\n \n+/* Per-IO-thread stats, cache-line padded to prevent false sharing.\n+ * Stat counters are updated by their respective IO thread;\n+ * clients_num is updated by the main thread but infrequently. */\n+typedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\n+    redisAtomic long long io_reads_processed;  /* Number of read events processed */",
        "comment_created_at": "2026-03-26T02:59:40+00:00",
        "comment_author": "YangboLong",
        "comment_body": "Currently IOThread is just used in iothread.c and IOThreads[] is declared as static instead of extern. Adding the stats counters into IOThread would also make them share cache lines with the other fields. Ideally the stats counters don't want to share cache lines even with clients_num i think, but the performance impact of doing so is probably neglectable.",
        "pr_file_module": null
      },
      {
        "comment_id": "2994898295",
        "repo_full_name": "redis/redis",
        "pr_number": 14907,
        "pr_file": "src/server.h",
        "discussion_id": "2988016043",
        "commented_code": "@@ -223,6 +223,17 @@ extern int configOOMScoreAdjValuesDefaults[CONFIG_OOM_COUNT];\n /* Max number of IO threads */\n #define IO_THREADS_MAX_NUM 128\n \n+/* Per-IO-thread stats, cache-line padded to prevent false sharing.\n+ * Stat counters are updated by their respective IO thread;\n+ * clients_num is updated by the main thread but infrequently. */\n+typedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\n+    redisAtomic long long io_reads_processed;  /* Number of read events processed */",
        "comment_created_at": "2026-03-26T13:28:06+00:00",
        "comment_author": "ShooterIT",
        "comment_body": "i think we can remove `static` key word, I think we should put every variable on its own cache line, it is too waste. besides, besides, we may also add some fields into `IOThreadStats`, and generally only the io thread itself accesses its' fields\r\n\r\nfor clients_num, only the main thread accesses and changes them, maybe it is better to put them in server struct.",
        "pr_file_module": null
      },
      {
        "comment_id": "2998697137",
        "repo_full_name": "redis/redis",
        "pr_number": 14907,
        "pr_file": "src/server.h",
        "discussion_id": "2988016043",
        "commented_code": "@@ -223,6 +223,17 @@ extern int configOOMScoreAdjValuesDefaults[CONFIG_OOM_COUNT];\n /* Max number of IO threads */\n #define IO_THREADS_MAX_NUM 128\n \n+/* Per-IO-thread stats, cache-line padded to prevent false sharing.\n+ * Stat counters are updated by their respective IO thread;\n+ * clients_num is updated by the main thread but infrequently. */\n+typedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\n+    redisAtomic long long io_reads_processed;  /* Number of read events processed */",
        "comment_created_at": "2026-03-27T02:45:13+00:00",
        "comment_author": "YangboLong",
        "comment_body": "Are you suggesting a struct layout like below?\r\n```c\r\ntypedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\r\n    redisAtomic long long io_reads_processed;\r\n    redisAtomic long long io_writes_processed;\r\n    redisAtomic long long net_input_bytes;\r\n    redisAtomic long long net_output_bytes;\r\n    // Other per-thread entries that are under contention\r\n} IOThreadStats;\r\n```\r\nThis sounds fair to me.\r\n@fcostaoliveira are the benchmark results available now?",
        "pr_file_module": null
      },
      {
        "comment_id": "2999858922",
        "repo_full_name": "redis/redis",
        "pr_number": 14907,
        "pr_file": "src/server.h",
        "discussion_id": "2988016043",
        "commented_code": "@@ -223,6 +223,17 @@ extern int configOOMScoreAdjValuesDefaults[CONFIG_OOM_COUNT];\n /* Max number of IO threads */\n #define IO_THREADS_MAX_NUM 128\n \n+/* Per-IO-thread stats, cache-line padded to prevent false sharing.\n+ * Stat counters are updated by their respective IO thread;\n+ * clients_num is updated by the main thread but infrequently. */\n+typedef struct __attribute__((aligned(CACHE_LINE_SIZE))) {\n+    redisAtomic long long io_reads_processed;  /* Number of read events processed */",
        "comment_created_at": "2026-03-27T09:20:04+00:00",
        "comment_author": "ShooterIT",
        "comment_body": "yes, but i prefer to put them in IOThreads. or put IOThreadStats  into IOThread, don't like to have separate variables, cc @sundb \r\n\r\nwe can handle the `io_reads_processed` and `io_writes_processed` first, and mitigate the contention of net_input/output_bytes variables in the future PR.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1878030548",
    "pr_number": 13682,
    "pr_file": "src/server.h",
    "created_at": "2024-12-10T12:43:40+00:00",
    "commented_code": "typedef struct client {\n     uint64_t id;            /* Client incremental unique ID. */\n     uint64_t flags;         /* Client flags: CLIENT_* macros. */\n+    listNode clients_pending_write_node; /* list node in clients_pending_write list.\n+                                            accessed together with flags field */\n     connection *conn;\n+    sds querybuf;           /* Buffer we use to accumulate client queries. */\n+    size_t qb_pos;          /* The position we have read in querybuf. */\n+    int reqtype;            /* Request protocol type: PROTO_REQ_* */",
    "repo_full_name": "redis/redis",
    "discussion_comments": [
      {
        "comment_id": "1878030548",
        "repo_full_name": "redis/redis",
        "pr_number": 13682,
        "pr_file": "src/server.h",
        "discussion_id": "1878030548",
        "commented_code": "@@ -1158,39 +1158,48 @@ typedef struct {\n typedef struct client {\n     uint64_t id;            /* Client incremental unique ID. */\n     uint64_t flags;         /* Client flags: CLIENT_* macros. */\n+    listNode clients_pending_write_node; /* list node in clients_pending_write list.\n+                                            accessed together with flags field */\n     connection *conn;\n+    sds querybuf;           /* Buffer we use to accumulate client queries. */\n+    size_t qb_pos;          /* The position we have read in querybuf. */\n+    int reqtype;            /* Request protocol type: PROTO_REQ_* */",
        "comment_created_at": "2024-12-10T12:43:40+00:00",
        "comment_author": "ShooterIT",
        "comment_body": "`reqtype`, `resp` and `authenticated` can be `uint_8`\r\n`slot` can be `uint_16`.\r\nif we rearrange these variables, we also can consider to change their types as well to reduce memory usage",
        "pr_file_module": null
      },
      {
        "comment_id": "1901123649",
        "repo_full_name": "redis/redis",
        "pr_number": 13682,
        "pr_file": "src/server.h",
        "discussion_id": "1878030548",
        "commented_code": "@@ -1158,39 +1158,48 @@ typedef struct {\n typedef struct client {\n     uint64_t id;            /* Client incremental unique ID. */\n     uint64_t flags;         /* Client flags: CLIENT_* macros. */\n+    listNode clients_pending_write_node; /* list node in clients_pending_write list.\n+                                            accessed together with flags field */\n     connection *conn;\n+    sds querybuf;           /* Buffer we use to accumulate client queries. */\n+    size_t qb_pos;          /* The position we have read in querybuf. */\n+    int reqtype;            /* Request protocol type: PROTO_REQ_* */",
        "comment_created_at": "2025-01-02T17:56:35+00:00",
        "comment_author": "fcostaoliveira",
        "comment_body": "WRT:\r\n> slot can be uint_16\r\n\r\nusing int_16 given we have logic that sets the slot to -1. \r\n\r\nWRT:\r\n> reqtype, resp and authenticated can be uint_8\r\n\r\naddressed in last commit and also included replstate ",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1197733530",
    "pr_number": 12159,
    "pr_file": "src/server.h",
    "created_at": "2023-05-18T12:01:29+00:00",
    "commented_code": "typedef struct client {\n     uint64_t id;            /* Client incremental unique ID. */\n     uint64_t flags;         /* Client flags: CLIENT_* macros. */\n+    uint64_t reset_flags;   /* flags to reset in persistent clients after each execution, generally set by RM_Call */",
    "repo_full_name": "redis/redis",
    "discussion_comments": [
      {
        "comment_id": "1197733530",
        "repo_full_name": "redis/redis",
        "pr_number": 12159,
        "pr_file": "src/server.h",
        "discussion_id": "1197733530",
        "commented_code": "@@ -1150,6 +1150,7 @@ typedef struct {\n typedef struct client {\n     uint64_t id;            /* Client incremental unique ID. */\n     uint64_t flags;         /* Client flags: CLIENT_* macros. */\n+    uint64_t reset_flags;   /* flags to reset in persistent clients after each execution, generally set by RM_Call */",
        "comment_created_at": "2023-05-18T12:01:29+00:00",
        "comment_author": "oranagra",
        "comment_body": "we previously had attempts to make the important fields of the client struct fit in the same cache line.\r\nconsidering this one isn't highly used, let's move it further down (after resp or db)",
        "pr_file_module": null
      },
      {
        "comment_id": "1197897862",
        "repo_full_name": "redis/redis",
        "pr_number": 12159,
        "pr_file": "src/server.h",
        "discussion_id": "1197733530",
        "commented_code": "@@ -1150,6 +1150,7 @@ typedef struct {\n typedef struct client {\n     uint64_t id;            /* Client incremental unique ID. */\n     uint64_t flags;         /* Client flags: CLIENT_* macros. */\n+    uint64_t reset_flags;   /* flags to reset in persistent clients after each execution, generally set by RM_Call */",
        "comment_created_at": "2023-05-18T14:27:14+00:00",
        "comment_author": "sjpotter",
        "comment_body": "done",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2825688316",
    "pr_number": 14797,
    "pr_file": "src/commands.def",
    "created_at": "2026-02-19T04:59:08+00:00",
    "commented_code": "{MAKE_ARG(\"key\",ARG_TYPE_KEY,0,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n };\n \n+/********** XNACK ********************/\n+\n+#ifndef SKIP_CMD_HISTORY_TABLE\n+/* XNACK history */\n+#define XNACK_History NULL\n+#endif\n+\n+#ifndef SKIP_CMD_TIPS_TABLE\n+/* XNACK tips */\n+#define XNACK_Tips NULL\n+#endif\n+\n+#ifndef SKIP_CMD_KEY_SPECS_TABLE\n+/* XNACK key specs */\n+keySpec XNACK_Keyspecs[1] = {\n+{NULL,CMD_KEY_RW|CMD_KEY_UPDATE,KSPEC_BS_INDEX,.bs.index={1},KSPEC_FK_RANGE,.fk.range={0,1,0}}\n+};\n+#endif\n+\n+/* XNACK mode argument table */\n+struct COMMAND_ARG XNACK_mode_Subargs[] = {\n+{MAKE_ARG(\"silent\",ARG_TYPE_PURE_TOKEN,-1,\"SILENT\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"fail\",ARG_TYPE_PURE_TOKEN,-1,\"FAIL\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"fatal\",ARG_TYPE_PURE_TOKEN,-1,\"FATAL\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+};\n+\n+/* XNACK ids argument table */\n+struct COMMAND_ARG XNACK_ids_Subargs[] = {\n+{MAKE_ARG(\"numids\",ARG_TYPE_INTEGER,-1,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"id\",ARG_TYPE_STRING,-1,NULL,NULL,NULL,CMD_ARG_MULTIPLE,0,NULL)},\n+};\n+\n+/* XNACK argument table */\n+struct COMMAND_ARG XNACK_Args[] = {\n+{MAKE_ARG(\"key\",ARG_TYPE_KEY,0,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"group\",ARG_TYPE_STRING,-1,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"mode\",ARG_TYPE_ONEOF,-1,NULL,NULL,NULL,CMD_ARG_NONE,3,NULL),.subargs=XNACK_mode_Subargs},\n+{MAKE_ARG(\"ids\",ARG_TYPE_BLOCK,-1,\"IDS\",NULL,NULL,CMD_ARG_NONE,2,NULL),.subargs=XNACK_ids_Subargs},",
    "repo_full_name": "redis/redis",
    "discussion_comments": [
      {
        "comment_id": "2825688316",
        "repo_full_name": "redis/redis",
        "pr_number": 14797,
        "pr_file": "src/commands.def",
        "discussion_id": "2825688316",
        "commented_code": "@@ -10625,6 +10625,48 @@ struct COMMAND_ARG XLEN_Args[] = {\n {MAKE_ARG(\"key\",ARG_TYPE_KEY,0,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n };\n \n+/********** XNACK ********************/\n+\n+#ifndef SKIP_CMD_HISTORY_TABLE\n+/* XNACK history */\n+#define XNACK_History NULL\n+#endif\n+\n+#ifndef SKIP_CMD_TIPS_TABLE\n+/* XNACK tips */\n+#define XNACK_Tips NULL\n+#endif\n+\n+#ifndef SKIP_CMD_KEY_SPECS_TABLE\n+/* XNACK key specs */\n+keySpec XNACK_Keyspecs[1] = {\n+{NULL,CMD_KEY_RW|CMD_KEY_UPDATE,KSPEC_BS_INDEX,.bs.index={1},KSPEC_FK_RANGE,.fk.range={0,1,0}}\n+};\n+#endif\n+\n+/* XNACK mode argument table */\n+struct COMMAND_ARG XNACK_mode_Subargs[] = {\n+{MAKE_ARG(\"silent\",ARG_TYPE_PURE_TOKEN,-1,\"SILENT\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"fail\",ARG_TYPE_PURE_TOKEN,-1,\"FAIL\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"fatal\",ARG_TYPE_PURE_TOKEN,-1,\"FATAL\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+};\n+\n+/* XNACK ids argument table */\n+struct COMMAND_ARG XNACK_ids_Subargs[] = {\n+{MAKE_ARG(\"numids\",ARG_TYPE_INTEGER,-1,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"id\",ARG_TYPE_STRING,-1,NULL,NULL,NULL,CMD_ARG_MULTIPLE,0,NULL)},\n+};\n+\n+/* XNACK argument table */\n+struct COMMAND_ARG XNACK_Args[] = {\n+{MAKE_ARG(\"key\",ARG_TYPE_KEY,0,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"group\",ARG_TYPE_STRING,-1,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"mode\",ARG_TYPE_ONEOF,-1,NULL,NULL,NULL,CMD_ARG_NONE,3,NULL),.subargs=XNACK_mode_Subargs},\n+{MAKE_ARG(\"ids\",ARG_TYPE_BLOCK,-1,\"IDS\",NULL,NULL,CMD_ARG_NONE,2,NULL),.subargs=XNACK_ids_Subargs},",
        "comment_created_at": "2026-02-19T04:59:08+00:00",
        "comment_author": "shahsb",
        "comment_body": "XNACK with many IDs might block the event loop if not handled carefully. Have you considered adding a COUNT option to limit the number of entries processed per call, similar to other stream commands?",
        "pr_file_module": null
      },
      {
        "comment_id": "2851223658",
        "repo_full_name": "redis/redis",
        "pr_number": 14797,
        "pr_file": "src/commands.def",
        "discussion_id": "2825688316",
        "commented_code": "@@ -10625,6 +10625,48 @@ struct COMMAND_ARG XLEN_Args[] = {\n {MAKE_ARG(\"key\",ARG_TYPE_KEY,0,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n };\n \n+/********** XNACK ********************/\n+\n+#ifndef SKIP_CMD_HISTORY_TABLE\n+/* XNACK history */\n+#define XNACK_History NULL\n+#endif\n+\n+#ifndef SKIP_CMD_TIPS_TABLE\n+/* XNACK tips */\n+#define XNACK_Tips NULL\n+#endif\n+\n+#ifndef SKIP_CMD_KEY_SPECS_TABLE\n+/* XNACK key specs */\n+keySpec XNACK_Keyspecs[1] = {\n+{NULL,CMD_KEY_RW|CMD_KEY_UPDATE,KSPEC_BS_INDEX,.bs.index={1},KSPEC_FK_RANGE,.fk.range={0,1,0}}\n+};\n+#endif\n+\n+/* XNACK mode argument table */\n+struct COMMAND_ARG XNACK_mode_Subargs[] = {\n+{MAKE_ARG(\"silent\",ARG_TYPE_PURE_TOKEN,-1,\"SILENT\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"fail\",ARG_TYPE_PURE_TOKEN,-1,\"FAIL\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"fatal\",ARG_TYPE_PURE_TOKEN,-1,\"FATAL\",NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+};\n+\n+/* XNACK ids argument table */\n+struct COMMAND_ARG XNACK_ids_Subargs[] = {\n+{MAKE_ARG(\"numids\",ARG_TYPE_INTEGER,-1,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"id\",ARG_TYPE_STRING,-1,NULL,NULL,NULL,CMD_ARG_MULTIPLE,0,NULL)},\n+};\n+\n+/* XNACK argument table */\n+struct COMMAND_ARG XNACK_Args[] = {\n+{MAKE_ARG(\"key\",ARG_TYPE_KEY,0,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"group\",ARG_TYPE_STRING,-1,NULL,NULL,NULL,CMD_ARG_NONE,0,NULL)},\n+{MAKE_ARG(\"mode\",ARG_TYPE_ONEOF,-1,NULL,NULL,NULL,CMD_ARG_NONE,3,NULL),.subargs=XNACK_mode_Subargs},\n+{MAKE_ARG(\"ids\",ARG_TYPE_BLOCK,-1,\"IDS\",NULL,NULL,CMD_ARG_NONE,2,NULL),.subargs=XNACK_ids_Subargs},",
        "comment_created_at": "2026-02-25T06:55:42+00:00",
        "comment_author": "sggeorgiev",
        "comment_body": "The event loop concern applies equally to XACK, XCLAIM, and XDEL, and the Redis convention for all of them is: the client controls the batch size by choosing how many IDs to pass. The per-ID work in XNACK is O(1) with a small constant, making it comparable to XACK. A COUNT option would add API complexity without real benefit since the client already has full control over batching.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "2911861277",
    "pr_number": 14869,
    "pr_file": "tests/unit/type/stream.tcl",
    "created_at": "2026-03-10T13:45:46+00:00",
    "commented_code": "assert_equal $id3 [r XADD mystream IDMP p3 \"req-1\" * field \"dup\"]\n     } {} {external:skip}\n \n+    test {XADD IDMP cron expiration works after RDB load} {\n+        r DEL mystream\n+\n+        # Create stream and set IDMP-DURATION before adding entries,\n+        # since XCFGSET clears existing entries when the duration changes.\n+        r XADD mystream IDMP p1 \"init\" * field \"init\"\n+        r XCFGSET mystream IDMP-DURATION 3\n+        r XADD mystream IDMP p1 \"req-1\" * field \"v1\"\n+        r XADD mystream IDMP p2 \"req-1\" * field \"v2\"\n+\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Save and restart \u2014 this triggers RDB load which should\n+        # register the stream in stream_idmp_keys for cron cleanup.\n+        # IDMP-DURATION is 5s so entries survive the load-side filtering.\n+        r SAVE\n+        restart_server 0 true false\n+\n+        # Verify entries survived the load (not filtered as expired)\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Wait for IDMP entries to expire and for the cron to clean them up.\n+        # The cron runs every 1s; IDMP-DURATION is 5s, so 7s is enough margin.\n+        after 5000",
    "repo_full_name": "redis/redis",
    "discussion_comments": [
      {
        "comment_id": "2911861277",
        "repo_full_name": "redis/redis",
        "pr_number": 14869,
        "pr_file": "tests/unit/type/stream.tcl",
        "discussion_id": "2911861277",
        "commented_code": "@@ -927,6 +927,47 @@ start_server {\n         assert_equal $id3 [r XADD mystream IDMP p3 \"req-1\" * field \"dup\"]\n     } {} {external:skip}\n \n+    test {XADD IDMP cron expiration works after RDB load} {\n+        r DEL mystream\n+\n+        # Create stream and set IDMP-DURATION before adding entries,\n+        # since XCFGSET clears existing entries when the duration changes.\n+        r XADD mystream IDMP p1 \"init\" * field \"init\"\n+        r XCFGSET mystream IDMP-DURATION 3\n+        r XADD mystream IDMP p1 \"req-1\" * field \"v1\"\n+        r XADD mystream IDMP p2 \"req-1\" * field \"v2\"\n+\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Save and restart \u2014 this triggers RDB load which should\n+        # register the stream in stream_idmp_keys for cron cleanup.\n+        # IDMP-DURATION is 5s so entries survive the load-side filtering.\n+        r SAVE\n+        restart_server 0 true false\n+\n+        # Verify entries survived the load (not filtered as expired)\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Wait for IDMP entries to expire and for the cron to clean them up.\n+        # The cron runs every 1s; IDMP-DURATION is 5s, so 7s is enough margin.\n+        after 5000",
        "comment_created_at": "2026-03-10T13:45:46+00:00",
        "comment_author": "sundb",
        "comment_body": "5 secs is a bit too long for a test. can we reduce IDMP-DURATION to make the test faster?\n",
        "pr_file_module": null
      },
      {
        "comment_id": "2911865653",
        "repo_full_name": "redis/redis",
        "pr_number": 14869,
        "pr_file": "tests/unit/type/stream.tcl",
        "discussion_id": "2911861277",
        "commented_code": "@@ -927,6 +927,47 @@ start_server {\n         assert_equal $id3 [r XADD mystream IDMP p3 \"req-1\" * field \"dup\"]\n     } {} {external:skip}\n \n+    test {XADD IDMP cron expiration works after RDB load} {\n+        r DEL mystream\n+\n+        # Create stream and set IDMP-DURATION before adding entries,\n+        # since XCFGSET clears existing entries when the duration changes.\n+        r XADD mystream IDMP p1 \"init\" * field \"init\"\n+        r XCFGSET mystream IDMP-DURATION 3\n+        r XADD mystream IDMP p1 \"req-1\" * field \"v1\"\n+        r XADD mystream IDMP p2 \"req-1\" * field \"v2\"\n+\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Save and restart \u2014 this triggers RDB load which should\n+        # register the stream in stream_idmp_keys for cron cleanup.\n+        # IDMP-DURATION is 5s so entries survive the load-side filtering.\n+        r SAVE\n+        restart_server 0 true false\n+\n+        # Verify entries survived the load (not filtered as expired)\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Wait for IDMP entries to expire and for the cron to clean them up.\n+        # The cron runs every 1s; IDMP-DURATION is 5s, so 7s is enough margin.\n+        after 5000",
        "comment_created_at": "2026-03-10T13:46:30+00:00",
        "comment_author": "sundb",
        "comment_body": "isn't IDMP-DURATION 3s?",
        "pr_file_module": null
      },
      {
        "comment_id": "2917605520",
        "repo_full_name": "redis/redis",
        "pr_number": 14869,
        "pr_file": "tests/unit/type/stream.tcl",
        "discussion_id": "2911861277",
        "commented_code": "@@ -927,6 +927,47 @@ start_server {\n         assert_equal $id3 [r XADD mystream IDMP p3 \"req-1\" * field \"dup\"]\n     } {} {external:skip}\n \n+    test {XADD IDMP cron expiration works after RDB load} {\n+        r DEL mystream\n+\n+        # Create stream and set IDMP-DURATION before adding entries,\n+        # since XCFGSET clears existing entries when the duration changes.\n+        r XADD mystream IDMP p1 \"init\" * field \"init\"\n+        r XCFGSET mystream IDMP-DURATION 3\n+        r XADD mystream IDMP p1 \"req-1\" * field \"v1\"\n+        r XADD mystream IDMP p2 \"req-1\" * field \"v2\"\n+\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Save and restart \u2014 this triggers RDB load which should\n+        # register the stream in stream_idmp_keys for cron cleanup.\n+        # IDMP-DURATION is 5s so entries survive the load-side filtering.\n+        r SAVE\n+        restart_server 0 true false\n+\n+        # Verify entries survived the load (not filtered as expired)\n+        set reply [r XINFO STREAM mystream]\n+        assert_equal 2 [dict get $reply pids-tracked]\n+        assert_equal 2 [dict get $reply iids-tracked]\n+\n+        # Wait for IDMP entries to expire and for the cron to clean them up.\n+        # The cron runs every 1s; IDMP-DURATION is 5s, so 7s is enough margin.\n+        after 5000",
        "comment_created_at": "2026-03-11T11:06:33+00:00",
        "comment_author": "sggeorgiev",
        "comment_body": "Changing the fixed 5-second sleep to a polling loop (wait_for_condition) and reducing IDMP-DURATION from 3s to 1s to make the test faster.",
        "pr_file_module": null
      }
    ]
  }
]