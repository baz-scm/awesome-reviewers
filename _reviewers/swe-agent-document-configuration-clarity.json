[
  {
    "discussion_id": "2167057687",
    "pr_number": 1222,
    "pr_file": "docs/reference/agent_config.md",
    "created_at": "2025-06-25T15:49:37+00:00",
    "commented_code": "```\n </details>\n \n-Currently, there are two main agent classes:\n+Currently, there are three main agent classes:\n \n * `DefaultAgentConfig`: This is the default agent.\n * `RetryAgentConfig`: A \"meta agent\" that instantiates multiple agents for multiple attempts and then picks the best solution.\n+* `ShellAgentConfig`: An agent that provides shell-based interaction capabilities.",
    "repo_full_name": "SWE-agent/SWE-agent",
    "discussion_comments": [
      {
        "comment_id": "2167057687",
        "repo_full_name": "SWE-agent/SWE-agent",
        "pr_number": 1222,
        "pr_file": "docs/reference/agent_config.md",
        "discussion_id": "2167057687",
        "commented_code": "@@ -13,11 +13,14 @@ It might be easiest to simply look at some of our example configurations in the\n ```\n </details>\n \n-Currently, there are two main agent classes:\n+Currently, there are three main agent classes:\n \n * `DefaultAgentConfig`: This is the default agent.\n * `RetryAgentConfig`: A \"meta agent\" that instantiates multiple agents for multiple attempts and then picks the best solution.\n+* `ShellAgentConfig`: An agent that provides shell-based interaction capabilities.",
        "comment_created_at": "2025-06-25T15:49:37+00:00",
        "comment_author": "klieret",
        "comment_body": "\"Config for `ShellAgent` (envoked with `sweagent sh`), which is an experimental mode for quick &  interactive (human-in-the-loop) workflows\"",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "1550107480",
    "pr_number": 30,
    "pr_file": "README.md",
    "created_at": "2024-04-03T16:50:52+00:00",
    "commented_code": "## \ud83d\ude80 Setup <a name=\"setup\"></a>\n 1. [Install Docker](https://docs.docker.com/engine/install/), then start Docker locally.\n-2. [Install Miniconda](https://docs.anaconda.com/free/miniconda/miniconda-install/), then create the `swe-agent` environment with `conda env create -f environment.yml`\n-3. Activate using `conda activate swe-agent`.\n-4. Run `./setup.sh` to create the `swe-agent` docker image.\n-5. Create a `keys.cfg` file at the root of this repository and fill in the following:\n+2. Clone the repository with `git clone https://github.com/princeton-nlp/SWE-agent.git && cd SWE-agent`\n+3. [Install Miniconda](https://docs.anaconda.com/free/miniconda/miniconda-install/), then create the `swe-agent` environment with `conda env create -f environment.yml`",
    "repo_full_name": "SWE-agent/SWE-agent",
    "discussion_comments": [
      {
        "comment_id": "1550107480",
        "repo_full_name": "SWE-agent/SWE-agent",
        "pr_number": 30,
        "pr_file": "README.md",
        "discussion_id": "1550107480",
        "commented_code": "@@ -44,16 +44,17 @@ Read our paper for more details.\n \n ## \ud83d\ude80 Setup <a name=\"setup\"></a>\n 1. [Install Docker](https://docs.docker.com/engine/install/), then start Docker locally.\n-2. [Install Miniconda](https://docs.anaconda.com/free/miniconda/miniconda-install/), then create the `swe-agent` environment with `conda env create -f environment.yml`\n-3. Activate using `conda activate swe-agent`.\n-4. Run `./setup.sh` to create the `swe-agent` docker image.\n-5. Create a `keys.cfg` file at the root of this repository and fill in the following:\n+2. Clone the repository with `git clone https://github.com/princeton-nlp/SWE-agent.git && cd SWE-agent`\n+3. [Install Miniconda](https://docs.anaconda.com/free/miniconda/miniconda-install/), then create the `swe-agent` environment with `conda env create -f environment.yml`",
        "comment_created_at": "2024-04-03T16:50:52+00:00",
        "comment_author": "klyap",
        "comment_body": "Maybe link to the command line install? Seems to be the easier way: https://docs.anaconda.com/free/miniconda/#quick-command-line-install ",
        "pr_file_module": null
      },
      {
        "comment_id": "1550112209",
        "repo_full_name": "SWE-agent/SWE-agent",
        "pr_number": 30,
        "pr_file": "README.md",
        "discussion_id": "1550107480",
        "commented_code": "@@ -44,16 +44,17 @@ Read our paper for more details.\n \n ## \ud83d\ude80 Setup <a name=\"setup\"></a>\n 1. [Install Docker](https://docs.docker.com/engine/install/), then start Docker locally.\n-2. [Install Miniconda](https://docs.anaconda.com/free/miniconda/miniconda-install/), then create the `swe-agent` environment with `conda env create -f environment.yml`\n-3. Activate using `conda activate swe-agent`.\n-4. Run `./setup.sh` to create the `swe-agent` docker image.\n-5. Create a `keys.cfg` file at the root of this repository and fill in the following:\n+2. Clone the repository with `git clone https://github.com/princeton-nlp/SWE-agent.git && cd SWE-agent`\n+3. [Install Miniconda](https://docs.anaconda.com/free/miniconda/miniconda-install/), then create the `swe-agent` environment with `conda env create -f environment.yml`",
        "comment_created_at": "2024-04-03T16:54:01+00:00",
        "comment_author": "zachblume",
        "comment_body": "```suggestion\r\n3. [Install Miniconda](https://docs.anaconda.com/free/miniconda/#quick-command-line-install), then create the `swe-agent` environment with `conda env create -f environment.yml`\r\n```",
        "pr_file_module": null
      }
    ]
  }
]