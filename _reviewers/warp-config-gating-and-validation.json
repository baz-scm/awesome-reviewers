[
  {
    "discussion_id": "3208762286",
    "pr_number": 10409,
    "pr_file": "app/src/ai/agent_sdk/telemetry.rs",
    "created_at": "2026-05-08T12:53:31+00:00",
    "commented_code": "Self::HarnessSupportPing\n             | Self::HarnessSupportReportArtifact\n             | Self::HarnessSupportNotifyUser\n-            | Self::HarnessSupportFinishTask => EnablementState::Flag(FeatureFlag::AgentHarness),\n+            | Self::HarnessSupportFinishTask\n+            | Self::HarnessSupportReportShutdown => {",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3208762286",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10409,
        "pr_file": "app/src/ai/agent_sdk/telemetry.rs",
        "discussion_id": "3208762286",
        "commented_code": "@@ -407,7 +416,10 @@ impl TelemetryEventDesc for CliTelemetryEventDiscriminants {\n             Self::HarnessSupportPing\n             | Self::HarnessSupportReportArtifact\n             | Self::HarnessSupportNotifyUser\n-            | Self::HarnessSupportFinishTask => EnablementState::Flag(FeatureFlag::AgentHarness),\n+            | Self::HarnessSupportFinishTask\n+            | Self::HarnessSupportReportShutdown => {",
        "comment_created_at": "2026-05-08T12:53:31+00:00",
        "comment_author": "bnavetta",
        "comment_body": "Should this one be gated on `FeatureFlag::AgentHarness`? It doesn't seem tied to multi-harness",
        "pr_file_module": null
      },
      {
        "comment_id": "3210323706",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10409,
        "pr_file": "app/src/ai/agent_sdk/telemetry.rs",
        "discussion_id": "3208762286",
        "commented_code": "@@ -407,7 +416,10 @@ impl TelemetryEventDesc for CliTelemetryEventDiscriminants {\n             Self::HarnessSupportPing\n             | Self::HarnessSupportReportArtifact\n             | Self::HarnessSupportNotifyUser\n-            | Self::HarnessSupportFinishTask => EnablementState::Flag(FeatureFlag::AgentHarness),\n+            | Self::HarnessSupportFinishTask\n+            | Self::HarnessSupportReportShutdown => {",
        "comment_created_at": "2026-05-08T17:34:24+00:00",
        "comment_author": "captainsafia",
        "comment_body": "Good call!",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3196658413",
    "pr_number": 10255,
    "pr_file": "app/src/local_api.rs",
    "created_at": "2026-05-07T07:17:17+00:00",
    "commented_code": "+//! `LocalApiServer` \u2014 exposes a UDS-based control plane to the `wp` CLI.\n+//!\n+//! Background `ipc::Service` handlers run on the executor; each request is\n+//! forwarded over an `async_channel` to the main thread, where a stream\n+//! handler dispatches it as if a key binding had fired and replies via a\n+//! oneshot. This mirrors `iTermAPIServer`'s \"background socket reads, main\n+//! thread mutations\" pattern and avoids holding `TerminalModel` locks across\n+//! await points.\n+//!\n+//! # Same-UID hardening\n+//!\n+//! - The socket binds under `$TMPDIR` (per-user dir on macOS, mode 0700) and\n+//!   is chmod'd to 0600 immediately after bind.\n+//! - The address file is written 0600 and contains a per-session random\n+//!   cookie alongside the socket path. The server rejects requests whose\n+//!   cookie doesn't match.\n+//! - The dispatch queue is bounded; oversized `SendText` payloads are\n+//!   rejected at the boundary so the main thread can't be wedged by a\n+//!   misbehaving same-UID client.\n+\n+use std::fs;\n+use std::io::Write as _;\n+use std::os::unix::fs::{OpenOptionsExt as _, PermissionsExt as _};",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3199584715",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10255,
        "pr_file": "app/src/local_api.rs",
        "discussion_id": "3196658413",
        "commented_code": "@@ -0,0 +1,319 @@\n+//! `LocalApiServer` \u2014 exposes a UDS-based control plane to the `wp` CLI.\n+//!\n+//! Background `ipc::Service` handlers run on the executor; each request is\n+//! forwarded over an `async_channel` to the main thread, where a stream\n+//! handler dispatches it as if a key binding had fired and replies via a\n+//! oneshot. This mirrors `iTermAPIServer`'s \"background socket reads, main\n+//! thread mutations\" pattern and avoids holding `TerminalModel` locks across\n+//! await points.\n+//!\n+//! # Same-UID hardening\n+//!\n+//! - The socket binds under `$TMPDIR` (per-user dir on macOS, mode 0700) and\n+//!   is chmod'd to 0600 immediately after bind.\n+//! - The address file is written 0600 and contains a per-session random\n+//!   cookie alongside the socket path. The server rejects requests whose\n+//!   cookie doesn't match.\n+//! - The dispatch queue is bounded; oversized `SendText` payloads are\n+//!   rejected at the boundary so the main thread can't be wedged by a\n+//!   misbehaving same-UID client.\n+\n+use std::fs;\n+use std::io::Write as _;\n+use std::os::unix::fs::{OpenOptionsExt as _, PermissionsExt as _};",
        "comment_created_at": "2026-05-07T07:17:17+00:00",
        "comment_author": "Nikitzu",
        "comment_body": "Addressed in c39473a \u2014 `mod local_api;` is now `#[cfg(unix)] mod local_api;` in `app/src/lib.rs`, so on Windows the module (including the `std::os::unix::fs` imports on this line) is never compiled. The singleton registration in `lib.rs:1039` is gated by the same `#[cfg(unix)]`. Until a Windows transport / ACL-based permission story is in place this is a deliberate scope-bound rather than a stub.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3203441299",
    "pr_number": 10233,
    "pr_file": "app/src/uri/mod.rs",
    "created_at": "2026-05-07T17:26:34+00:00",
    "commented_code": "Some(PathBuf::from(shellexpand::tilde(&raw).into_owned()))\n }\n \n+fn parse_positive_usize_query_param(url: &Url, name: &str) -> Result<Option<usize>> {\n+    let Some(raw) = url.query_pairs().find(|(k, _)| k == name).map(|(_, v)| v) else {\n+        return Ok(None);\n+    };\n+\n+    let value = raw.parse::<usize>()?;\n+    ensure!(value > 0, \"`{name}` must be greater than 0\");\n+    Ok(Some(value))\n+}\n+\n+fn parse_open_file_editor_url(url: &Url) -> Result<(PathBuf, Option<LineAndColumnArg>)> {\n+    let raw_path = url\n+        .query_pairs()\n+        .find(|(k, _)| k == \"path\")\n+        .map(|(_, v)| v)\n+        .ok_or_else(|| anyhow!(\"missing path for open_file_editor action\"))?;\n+    let path = PathBuf::from(raw_path.into_owned());\n+    ensure!(\n+        path.is_absolute(),",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3203441299",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10233,
        "pr_file": "app/src/uri/mod.rs",
        "discussion_id": "3203441299",
        "commented_code": "@@ -719,16 +667,60 @@ fn parse_tab_path(url: &Url) -> Option<PathBuf> {\n     Some(PathBuf::from(shellexpand::tilde(&raw).into_owned()))\n }\n \n+fn parse_positive_usize_query_param(url: &Url, name: &str) -> Result<Option<usize>> {\n+    let Some(raw) = url.query_pairs().find(|(k, _)| k == name).map(|(_, v)| v) else {\n+        return Ok(None);\n+    };\n+\n+    let value = raw.parse::<usize>()?;\n+    ensure!(value > 0, \"`{name}` must be greater than 0\");\n+    Ok(Some(value))\n+}\n+\n+fn parse_open_file_editor_url(url: &Url) -> Result<(PathBuf, Option<LineAndColumnArg>)> {\n+    let raw_path = url\n+        .query_pairs()\n+        .find(|(k, _)| k == \"path\")\n+        .map(|(_, v)| v)\n+        .ok_or_else(|| anyhow!(\"missing path for open_file_editor action\"))?;\n+    let path = PathBuf::from(raw_path.into_owned());\n+    ensure!(\n+        path.is_absolute(),",
        "comment_created_at": "2026-05-07T17:26:34+00:00",
        "comment_author": "seemeroland",
        "comment_body": "It'd be nice to expand home like `~/dir`. We did similar in https://github.com/warpdotdev/warp/pull/9277",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3198982456",
    "pr_number": 9832,
    "pr_file": "app/src/ai/agent_sdk/ambient.rs",
    "created_at": "2026-05-07T04:44:15+00:00",
    "commented_code": "let harness_override = (args.harness != Harness::Oz).then_some(HarnessConfig {\n                 harness_type: args.harness,\n+                model_id: None,",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3198982456",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9832,
        "pr_file": "app/src/ai/agent_sdk/ambient.rs",
        "discussion_id": "3198982456",
        "commented_code": "@@ -417,6 +417,7 @@ impl AmbientAgentRunner {\n \n             let harness_override = (args.harness != Harness::Oz).then_some(HarnessConfig {\n                 harness_type: args.harness,\n+                model_id: None,",
        "comment_created_at": "2026-05-07T04:44:15+00:00",
        "comment_author": "liliwilson",
        "comment_body": "Not blocking this PR, but we should support passing in a model from the CLI for `run-cloud`, and make sure it validates against the harness model list",
        "pr_file_module": null
      },
      {
        "comment_id": "3205458559",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9832,
        "pr_file": "app/src/ai/agent_sdk/ambient.rs",
        "discussion_id": "3198982456",
        "commented_code": "@@ -417,6 +417,7 @@ impl AmbientAgentRunner {\n \n             let harness_override = (args.harness != Harness::Oz).then_some(HarnessConfig {\n                 harness_type: args.harness,\n+                model_id: None,",
        "comment_created_at": "2026-05-08T00:30:16+00:00",
        "comment_author": "abhishekp106",
        "comment_body": "yes we should, filed",
        "pr_file_module": null
      },
      {
        "comment_id": "3205460855",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9832,
        "pr_file": "app/src/ai/agent_sdk/ambient.rs",
        "discussion_id": "3198982456",
        "commented_code": "@@ -417,6 +417,7 @@ impl AmbientAgentRunner {\n \n             let harness_override = (args.harness != Harness::Oz).then_some(HarnessConfig {\n                 harness_type: args.harness,\n+                model_id: None,",
        "comment_created_at": "2026-05-08T00:30:50+00:00",
        "comment_author": "abhishekp106",
        "comment_body": "https://linear.app/warpdotdev/issue/REMOTE-1613/in-app-auth-secret-creation-flow",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3189778159",
    "pr_number": 9325,
    "pr_file": "crates/ai/src/project_context/model.rs",
    "created_at": "2026-05-05T15:47:24+00:00",
    "commented_code": "use repo_metadata::entry::{Entry, FileMetadata};\n         use repo_metadata::repository::RepositorySubscriber;\n         use repo_metadata::{Repository, DirectoryWatcher, RepositoryUpdate};\n+        use repo_metadata::repository::SubscriberId;\n         use ignore::gitignore::Gitignore;\n         use async_channel::Sender;\n+        use warp_core::safe_warn;\n+        use warp_util::standardized_path::StandardizedPath;\n+        use warpui::ModelHandle;\n+        use watcher::{HomeDirectoryWatcher, HomeDirectoryWatcherEvent};\n \n         const RULES_FILE_PATTERN: [&str; 2] = [\"WARP.md\", \"AGENTS.md\"];\n         const MAX_SCAN_DEPTH: usize = 3;\n         const MAX_FILES_TO_SCAN: usize = 5000;\n     }\n }\n \n+/// A well-known location under `$HOME` that may contain a global rule file.\n+#[cfg_attr(not(feature = \"local_fs\"), allow(dead_code))]",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3189778159",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9325,
        "pr_file": "crates/ai/src/project_context/model.rs",
        "discussion_id": "3189778159",
        "commented_code": "@@ -10,15 +10,57 @@ cfg_if::cfg_if! {\n         use repo_metadata::entry::{Entry, FileMetadata};\n         use repo_metadata::repository::RepositorySubscriber;\n         use repo_metadata::{Repository, DirectoryWatcher, RepositoryUpdate};\n+        use repo_metadata::repository::SubscriberId;\n         use ignore::gitignore::Gitignore;\n         use async_channel::Sender;\n+        use warp_core::safe_warn;\n+        use warp_util::standardized_path::StandardizedPath;\n+        use warpui::ModelHandle;\n+        use watcher::{HomeDirectoryWatcher, HomeDirectoryWatcherEvent};\n \n         const RULES_FILE_PATTERN: [&str; 2] = [\"WARP.md\", \"AGENTS.md\"];\n         const MAX_SCAN_DEPTH: usize = 3;\n         const MAX_FILES_TO_SCAN: usize = 5000;\n     }\n }\n \n+/// A well-known location under `$HOME` that may contain a global rule file.\n+#[cfg_attr(not(feature = \"local_fs\"), allow(dead_code))]",
        "comment_created_at": "2026-05-05T15:47:24+00:00",
        "comment_author": "peicodes",
        "comment_body": "wouldn't it be easier to just make this build only if fs is on instead of allow dead code when it's not?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3176851014",
    "pr_number": 9917,
    "pr_file": "app/src/code_review/diff_state.rs",
    "created_at": "2026-05-02T16:43:26+00:00",
    "commented_code": "file_diff.is_autogenerated =\n             super::is_file_autogenerated(file_path, content_at_head.as_deref());\n \n+        // Compute entity-level (semantic) diff when feature is enabled.\n+        let entity_diff = if FeatureFlag::SemanticDiff.is_enabled()\n+            && !file_diff.is_binary\n+            && !matches!(status, GitFileStatus::Untracked | GitFileStatus::New)\n+            && content_at_head.is_some()\n+        {\n+            let current_path = repo_path.join(file_path);\n+            languages::semantic_diff::compute_entity_diff(\n+                content_at_head.as_ref().unwrap(),\n+                &fs::read_to_string(&current_path).unwrap_or_default(),",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3176921009",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9917,
        "pr_file": "app/src/code_review/diff_state.rs",
        "discussion_id": "3176851014",
        "commented_code": "@@ -1899,9 +1918,26 @@ impl DiffStateModel {\n         file_diff.is_autogenerated =\n             super::is_file_autogenerated(file_path, content_at_head.as_deref());\n \n+        // Compute entity-level (semantic) diff when feature is enabled.\n+        let entity_diff = if FeatureFlag::SemanticDiff.is_enabled()\n+            && !file_diff.is_binary\n+            && !matches!(status, GitFileStatus::Untracked | GitFileStatus::New)\n+            && content_at_head.is_some()\n+        {\n+            let current_path = repo_path.join(file_path);\n+            languages::semantic_diff::compute_entity_diff(\n+                content_at_head.as_ref().unwrap(),\n+                &fs::read_to_string(&current_path).unwrap_or_default(),",
        "comment_created_at": "2026-05-02T16:43:26+00:00",
        "comment_author": "monotykamary",
        "comment_body": "Fixed in b1f7e7e. Wrapped both entity diff computation blocks in `#[cfg(feature = \\\"local_fs\\\")]` with `#[cfg(not(feature = \\\"local_fs\\\"))] let entity_diff: Option<...> = None;` fallback. This ensures `fs::read_to_string` is only compiled when the `fs` import is available, and WASM/non-local-fs builds compile cleanly with `entity_diff = None`.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3176862799",
    "pr_number": 9399,
    "pr_file": "app/src/ai/blocklist/orchestration_event_streamer.rs",
    "created_at": "2026-05-02T15:55:57+00:00",
    "commented_code": "let config = AgentEventDriverConfig::retry_forever(run_ids.clone(), cursor);\n         let source = ServerApiAgentEventSource::new(server_api);\n-        let hydrator = MessageHydrator::new(ai_client);\n+        let hydrator = self.message_hydrator_for_run_id(&self_run_id);\n \n         ctx.spawn(\n             async move {\n                 let mut consumer = SseForwardingConsumer {\n                     tx,\n                     self_run_id,\n                     hydrator,\n+                    hydrate_new_messages: true,",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3176862799",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9399,
        "pr_file": "app/src/ai/blocklist/orchestration_event_streamer.rs",
        "discussion_id": "3176862799",
        "commented_code": "@@ -804,14 +1094,15 @@ impl OrchestrationEventStreamer {\n \n         let config = AgentEventDriverConfig::retry_forever(run_ids.clone(), cursor);\n         let source = ServerApiAgentEventSource::new(server_api);\n-        let hydrator = MessageHydrator::new(ai_client);\n+        let hydrator = self.message_hydrator_for_run_id(&self_run_id);\n \n         ctx.spawn(\n             async move {\n                 let mut consumer = SseForwardingConsumer {\n                     tx,\n                     self_run_id,\n                     hydrator,\n+                    hydrate_new_messages: true,",
        "comment_created_at": "2026-05-02T15:55:57+00:00",
        "comment_author": "cephalonaut",
        "comment_body": "**`hydrate_new_messages` \u2014 dead flag in production?**\n\nThe struct field is only set `false` in the test `sse_forwarding_consumer_skips_message_hydration_when_disabled`. `start_sse_connection` (this line) hardcodes `true`. Either remove (YAGNI) or document the upcoming caller that will toggle it.",
        "pr_file_module": null
      }
    ]
  }
]