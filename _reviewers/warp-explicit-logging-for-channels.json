[
  {
    "discussion_id": "3190287909",
    "pr_number": 10140,
    "pr_file": "script/macos/run",
    "created_at": "2026-05-05T17:17:47+00:00",
    "commented_code": "if  [ \"$OPEN_WITH_LAUNCHD\" = true ]; then\n         echo \"Launching with MacOS application launcher\"\n         PATH=\"\" /usr/bin/open \"./$WARP_APP_PATH\"\n-        tail -f ~/Library/Logs/warp_local.log\n+        if [ \"$WARP_CHANNEL\" = \"local\" ]; then\n+            LOG_FILE=~/Library/Logs/warp_local.log\n+        else",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3190287909",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10140,
        "pr_file": "script/macos/run",
        "discussion_id": "3190287909",
        "commented_code": "@@ -134,7 +134,12 @@ if [ \"$DONT_OPEN\" = false ] ; then\n     if  [ \"$OPEN_WITH_LAUNCHD\" = true ]; then\n         echo \"Launching with MacOS application launcher\"\n         PATH=\"\" /usr/bin/open \"./$WARP_APP_PATH\"\n-        tail -f ~/Library/Logs/warp_local.log\n+        if [ \"$WARP_CHANNEL\" = \"local\" ]; then\n+            LOG_FILE=~/Library/Logs/warp_local.log\n+        else",
        "comment_created_at": "2026-05-05T17:17:47+00:00",
        "comment_author": "peicodes",
        "comment_body": "I would prefer we `elif` here and be specific about the OSS channel, and in the else case log a warning that the channel is unrecognized",
        "pr_file_module": null
      },
      {
        "comment_id": "3191513987",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10140,
        "pr_file": "script/macos/run",
        "discussion_id": "3190287909",
        "commented_code": "@@ -134,7 +134,12 @@ if [ \"$DONT_OPEN\" = false ] ; then\n     if  [ \"$OPEN_WITH_LAUNCHD\" = true ]; then\n         echo \"Launching with MacOS application launcher\"\n         PATH=\"\" /usr/bin/open \"./$WARP_APP_PATH\"\n-        tail -f ~/Library/Logs/warp_local.log\n+        if [ \"$WARP_CHANNEL\" = \"local\" ]; then\n+            LOG_FILE=~/Library/Logs/warp_local.log\n+        else",
        "comment_created_at": "2026-05-05T20:54:54+00:00",
        "comment_author": "sebryu",
        "comment_body": "Done in 679b8cb \u2014 added an explicit `elif` for `oss` and a warning on unrecognized channels.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3190068756",
    "pr_number": 10130,
    "pr_file": "script/macos/bootstrap",
    "created_at": "2026-05-05T16:34:21+00:00",
    "commented_code": ". \"$PWD/script/warp_sudo\"\n \n-if ! [ -d \"/Applications/Xcode.app\" ]; then\n-    echo \"Please install Xcode from the App Store before continuing.\"\n-    exit 1\n+DEVELOPER_DIR=\"$(xcode-select -p 2>/dev/null || true)\"\n+if [ -z \"$DEVELOPER_DIR\" ] || [ ! -x \"$DEVELOPER_DIR/usr/bin/xcodebuild\" ] || [ \"$DEVELOPER_DIR\" = \"/Library/Developer/CommandLineTools\" ]; then\n+    XCODE_APP=\"\"\n+    # Prefer the stable /Applications/Xcode.app when present so users with\n+    # both stable and beta/versioned installs aren't switched away from it.\n+    if [ -x \"/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild\" ]; then\n+        XCODE_APP=\"/Applications/Xcode.app\"\n+    else\n+        for candidate in /Applications/Xcode*.app; do\n+            if [ -x \"$candidate/Contents/Developer/usr/bin/xcodebuild\" ]; then\n+                XCODE_APP=\"$candidate\"\n+                break\n+            fi\n+        done\n+    fi\n+    if [ -z \"$XCODE_APP\" ]; then\n+        echo \"Please install Xcode before continuing.\"\n+        exit 1\n+    fi",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3190068756",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10130,
        "pr_file": "script/macos/bootstrap",
        "discussion_id": "3190068756",
        "commented_code": "@@ -7,12 +7,27 @@ set -e\n \n . \"$PWD/script/warp_sudo\"\n \n-if ! [ -d \"/Applications/Xcode.app\" ]; then\n-    echo \"Please install Xcode from the App Store before continuing.\"\n-    exit 1\n+DEVELOPER_DIR=\"$(xcode-select -p 2>/dev/null || true)\"\n+if [ -z \"$DEVELOPER_DIR\" ] || [ ! -x \"$DEVELOPER_DIR/usr/bin/xcodebuild\" ] || [ \"$DEVELOPER_DIR\" = \"/Library/Developer/CommandLineTools\" ]; then\n+    XCODE_APP=\"\"\n+    # Prefer the stable /Applications/Xcode.app when present so users with\n+    # both stable and beta/versioned installs aren't switched away from it.\n+    if [ -x \"/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild\" ]; then\n+        XCODE_APP=\"/Applications/Xcode.app\"\n+    else\n+        for candidate in /Applications/Xcode*.app; do\n+            if [ -x \"$candidate/Contents/Developer/usr/bin/xcodebuild\" ]; then\n+                XCODE_APP=\"$candidate\"\n+                break\n+            fi\n+        done\n+    fi\n+    if [ -z \"$XCODE_APP\" ]; then\n+        echo \"Please install Xcode before continuing.\"\n+        exit 1\n+    fi",
        "comment_created_at": "2026-05-05T16:34:21+00:00",
        "comment_author": "captainsafia",
        "comment_body": "Small nit if you're able to get to it: can we echo the candidate XCode instance that was picked for visibility here?",
        "pr_file_module": null
      },
      {
        "comment_id": "3190193216",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10130,
        "pr_file": "script/macos/bootstrap",
        "discussion_id": "3190068756",
        "commented_code": "@@ -7,12 +7,27 @@ set -e\n \n . \"$PWD/script/warp_sudo\"\n \n-if ! [ -d \"/Applications/Xcode.app\" ]; then\n-    echo \"Please install Xcode from the App Store before continuing.\"\n-    exit 1\n+DEVELOPER_DIR=\"$(xcode-select -p 2>/dev/null || true)\"\n+if [ -z \"$DEVELOPER_DIR\" ] || [ ! -x \"$DEVELOPER_DIR/usr/bin/xcodebuild\" ] || [ \"$DEVELOPER_DIR\" = \"/Library/Developer/CommandLineTools\" ]; then\n+    XCODE_APP=\"\"\n+    # Prefer the stable /Applications/Xcode.app when present so users with\n+    # both stable and beta/versioned installs aren't switched away from it.\n+    if [ -x \"/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild\" ]; then\n+        XCODE_APP=\"/Applications/Xcode.app\"\n+    else\n+        for candidate in /Applications/Xcode*.app; do\n+            if [ -x \"$candidate/Contents/Developer/usr/bin/xcodebuild\" ]; then\n+                XCODE_APP=\"$candidate\"\n+                break\n+            fi\n+        done\n+    fi\n+    if [ -z \"$XCODE_APP\" ]; then\n+        echo \"Please install Xcode before continuing.\"\n+        exit 1\n+    fi",
        "comment_created_at": "2026-05-05T16:58:22+00:00",
        "comment_author": "SagarSDagdu",
        "comment_body": "Done!",
        "pr_file_module": null
      }
    ]
  }
]