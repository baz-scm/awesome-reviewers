[
  {
    "discussion_id": "3209487821",
    "pr_number": 9971,
    "pr_file": "app/src/default_terminal/mod.rs",
    "created_at": "2026-05-08T14:56:45+00:00",
    "commented_code": "self.is_warp_default\n     }\n \n-    /// This is a one-way operation. Once we set the default terminal to Warp, we can't really\n-    /// \"unset\" it unless we pick a new default terminal. Picking a new default is complicated.\n+    /// Sets Warp as the default terminal.\n     pub fn make_warp_default(&mut self, ctx: &mut ModelContext<Self>) {\n         if let Err(e) = set_warp_as_default_terminal() {\n             log::error!(\"Error setting Warp as default terminal: {e:#}\");\n         } else {\n             self.set_is_warp_default(true, ctx);\n         }\n     }\n+\n+    pub fn unset_warp_default(&mut self, ctx: &mut ModelContext<Self>) {",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3209487821",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9971,
        "pr_file": "app/src/default_terminal/mod.rs",
        "discussion_id": "3209487821",
        "commented_code": "@@ -90,15 +95,22 @@ impl DefaultTerminal {\n         self.is_warp_default\n     }\n \n-    /// This is a one-way operation. Once we set the default terminal to Warp, we can't really\n-    /// \"unset\" it unless we pick a new default terminal. Picking a new default is complicated.\n+    /// Sets Warp as the default terminal.\n     pub fn make_warp_default(&mut self, ctx: &mut ModelContext<Self>) {\n         if let Err(e) = set_warp_as_default_terminal() {\n             log::error!(\"Error setting Warp as default terminal: {e:#}\");\n         } else {\n             self.set_is_warp_default(true, ctx);\n         }\n     }\n+\n+    pub fn unset_warp_default(&mut self, ctx: &mut ModelContext<Self>) {",
        "comment_created_at": "2026-05-08T14:56:45+00:00",
        "comment_author": "lucieleblanc",
        "comment_body": "This isn't really \"unset\" -- it resets to the native terminal app. Could we rename this to \"restore_macos_terminal_as_default\" or something of the like?",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3209496143",
    "pr_number": 9971,
    "pr_file": "app/src/default_terminal/mac.rs",
    "created_at": "2026-05-08T14:58:21+00:00",
    "commented_code": "set_default_terminal(&bundle_id)\n }\n \n+pub fn unset_warp_as_default_terminal() -> Result<(), String> {",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3209496143",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9971,
        "pr_file": "app/src/default_terminal/mac.rs",
        "discussion_id": "3209496143",
        "commented_code": "@@ -71,6 +71,12 @@ pub fn set_warp_as_default_terminal() -> Result<(), String> {\n     set_default_terminal(&bundle_id)\n }\n \n+pub fn unset_warp_as_default_terminal() -> Result<(), String> {",
        "comment_created_at": "2026-05-08T14:58:21+00:00",
        "comment_author": "lucieleblanc",
        "comment_body": "Same here: please rename identifiers to use the \"restore\" wording instead of the \"unset\" wording.",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3190791580",
    "pr_number": 9905,
    "pr_file": "app/src/context_chips/display_chip.rs",
    "created_at": "2026-05-05T18:47:16+00:00",
    "commented_code": "}\n }\n \n+fn shell_single_quote(value: &str) -> String {\n+    format!(\"'{}'\", value.replace(\"'\", \"'\\\\''\"))\n+}\n+\n fn format_change_directory_command(dir_name: &str) -> String {\n-    format!(\"cd '{}'\", dir_name.replace(\"'\", \"'\\\\'''\"))\n+    format!(\"cd {}\", shell_single_quote(dir_name))\n }\n \n-pub fn format_git_branch_command(branch_name: &str) -> String {\n-    format!(\"git checkout {branch_name}\")\n+pub fn format_git_branch_command(value: &str) -> String {",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3190791580",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9905,
        "pr_file": "app/src/context_chips/display_chip.rs",
        "discussion_id": "3190791580",
        "commented_code": "@@ -1774,12 +1781,31 @@ impl ActionButtonTheme for EnterAgentViewButton {\n     }\n }\n \n+fn shell_single_quote(value: &str) -> String {\n+    format!(\"'{}'\", value.replace(\"'\", \"'\\\\''\"))\n+}\n+\n fn format_change_directory_command(dir_name: &str) -> String {\n-    format!(\"cd '{}'\", dir_name.replace(\"'\", \"'\\\\'''\"))\n+    format!(\"cd {}\", shell_single_quote(dir_name))\n }\n \n-pub fn format_git_branch_command(branch_name: &str) -> String {\n-    format!(\"git checkout {branch_name}\")\n+pub fn format_git_branch_command(value: &str) -> String {",
        "comment_created_at": "2026-05-05T18:47:16+00:00",
        "comment_author": "moirahuang",
        "comment_body": "let's use a more descriptive name than `value` here to make it clear that we're expecting a `GitBranchOnClickValue` encoded string",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3196968034",
    "pr_number": 10143,
    "pr_file": "app/src/notebooks/editor/model.rs",
    "created_at": "2026-05-06T19:34:55+00:00",
    "commented_code": "self.content.as_ref(app).link_url_at_offset(offset)\n     }\n \n+    pub fn scroll_to_markdown_anchor(",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3196968034",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 10143,
        "pr_file": "app/src/notebooks/editor/model.rs",
        "discussion_id": "3196968034",
        "commented_code": "@@ -1257,6 +1263,102 @@ impl NotebooksEditorModel {\n         self.content.as_ref(app).link_url_at_offset(offset)\n     }\n \n+    pub fn scroll_to_markdown_anchor(",
        "comment_created_at": "2026-05-06T19:34:55+00:00",
        "comment_author": "kevinyang372",
        "comment_body": "This is semantically not an anchor -- we have a dedicated Anchor type in the core editor infra and this could confuse readers. It's really just `scroll_to_matching_header`",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3182727517",
    "pr_number": 9877,
    "pr_file": "app/src/terminal/model/grid/grid_handler.rs",
    "created_at": "2026-05-04T15:44:13+00:00",
    "commented_code": "/// `bottommost_visible_content_row` via backward scan. Set by the owning\n     /// `BlockGrid` when `trim_trailing_blank_rows` is active.\n     track_content_length: bool,\n+\n+    /// Treat primary-screen full clears and resizes as live frame updates\n+    /// instead of preserving the current visible rows into scrollback.",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3182727517",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9877,
        "pr_file": "app/src/terminal/model/grid/grid_handler.rs",
        "discussion_id": "3182727517",
        "commented_code": "@@ -343,6 +343,10 @@ pub struct GridHandler {\n     /// `bottommost_visible_content_row` via backward scan. Set by the owning\n     /// `BlockGrid` when `trim_trailing_blank_rows` is active.\n     track_content_length: bool,\n+\n+    /// Treat primary-screen full clears and resizes as live frame updates\n+    /// instead of preserving the current visible rows into scrollback.",
        "comment_created_at": "2026-05-04T15:44:13+00:00",
        "comment_author": "vorporeal",
        "comment_body": "ooc, where did the \"frame\" terminology come from?\n\nalso, unless this is a well-known term-of-art, i'd consider having the name reference the specific control sequence it modifies the behavior of.  could even make it clearer via a simple two-state enum, e.g.:\n\n```rust\nenum FullGridClearBehavior {\n  /// Clears the grid by resetting cells to the template cell.\n  Clear,\n  /// Clears the grid by scrolling cells into scrollback.\n  Scroll,\n}\n\n...\n\nfull_grid_clear_behavior: FullGridClearBehavior,\n```",
        "pr_file_module": null
      },
      {
        "comment_id": "3183371425",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9877,
        "pr_file": "app/src/terminal/model/grid/grid_handler.rs",
        "discussion_id": "3182727517",
        "commented_code": "@@ -343,6 +343,10 @@ pub struct GridHandler {\n     /// `bottommost_visible_content_row` via backward scan. Set by the owning\n     /// `BlockGrid` when `trim_trailing_blank_rows` is active.\n     track_content_length: bool,\n+\n+    /// Treat primary-screen full clears and resizes as live frame updates\n+    /// instead of preserving the current visible rows into scrollback.",
        "comment_created_at": "2026-05-04T17:43:58+00:00",
        "comment_author": "zachbai",
        "comment_body": "im into it",
        "pr_file_module": null
      }
    ]
  },
  {
    "discussion_id": "3157714641",
    "pr_number": 9305,
    "pr_file": "app/src/terminal/input/slash_commands/mod.rs",
    "created_at": "2026-04-28T23:01:21+00:00",
    "commented_code": "ctx.dispatch_typed_action(&WorkspaceAction::SetActiveTabName(name.to_owned()));\n             }\n+            set_tab_color if command.name == commands::SET_TAB_COLOR.name => {\n+                let Some(arg) = argument\n+                    .map(|name| name.trim())\n+                    .filter(|name| !name.is_empty())\n+                else {\n+                    show_error_toast(\n+                        \"Please provide a color after /set-tab-color (red, green, yellow, blue, magenta, cyan, none, or default)\"\n+                            .to_owned(),\n+                        ctx,\n+                    );\n+                    return true;\n+                };\n+\n+                // `none`/`clear` explicitly clear the color (suppressing any\n+                // directory default); `default`/`reset` remove the manual override\n+                // so the directory default can apply when `DirectoryTabColors`\n+                // is enabled.\n+                let color = match arg.to_ascii_lowercase().as_str() {\n+                    \"red\" => SelectedTabColor::Color(AnsiColorIdentifier::Red),\n+                    \"green\" => SelectedTabColor::Color(AnsiColorIdentifier::Green),\n+                    \"yellow\" => SelectedTabColor::Color(AnsiColorIdentifier::Yellow),\n+                    \"blue\" => SelectedTabColor::Color(AnsiColorIdentifier::Blue),\n+                    \"magenta\" => SelectedTabColor::Color(AnsiColorIdentifier::Magenta),\n+                    \"cyan\" => SelectedTabColor::Color(AnsiColorIdentifier::Cyan),\n+                    \"none\" | \"clear\" => SelectedTabColor::Cleared,",
    "repo_full_name": "warpdotdev/warp",
    "discussion_comments": [
      {
        "comment_id": "3157714641",
        "repo_full_name": "warpdotdev/warp",
        "pr_number": 9305,
        "pr_file": "app/src/terminal/input/slash_commands/mod.rs",
        "discussion_id": "3157714641",
        "commented_code": "@@ -420,6 +422,45 @@ impl Input {\n \n                 ctx.dispatch_typed_action(&WorkspaceAction::SetActiveTabName(name.to_owned()));\n             }\n+            set_tab_color if command.name == commands::SET_TAB_COLOR.name => {\n+                let Some(arg) = argument\n+                    .map(|name| name.trim())\n+                    .filter(|name| !name.is_empty())\n+                else {\n+                    show_error_toast(\n+                        \"Please provide a color after /set-tab-color (red, green, yellow, blue, magenta, cyan, none, or default)\"\n+                            .to_owned(),\n+                        ctx,\n+                    );\n+                    return true;\n+                };\n+\n+                // `none`/`clear` explicitly clear the color (suppressing any\n+                // directory default); `default`/`reset` remove the manual override\n+                // so the directory default can apply when `DirectoryTabColors`\n+                // is enabled.\n+                let color = match arg.to_ascii_lowercase().as_str() {\n+                    \"red\" => SelectedTabColor::Color(AnsiColorIdentifier::Red),\n+                    \"green\" => SelectedTabColor::Color(AnsiColorIdentifier::Green),\n+                    \"yellow\" => SelectedTabColor::Color(AnsiColorIdentifier::Yellow),\n+                    \"blue\" => SelectedTabColor::Color(AnsiColorIdentifier::Blue),\n+                    \"magenta\" => SelectedTabColor::Color(AnsiColorIdentifier::Magenta),\n+                    \"cyan\" => SelectedTabColor::Color(AnsiColorIdentifier::Cyan),\n+                    \"none\" | \"clear\" => SelectedTabColor::Cleared,",
        "comment_created_at": "2026-04-28T23:01:21+00:00",
        "comment_author": "harryalbert",
        "comment_body": "I don't know if users are going to grok the difference between \"cleared\" and \"unset\" (we maintain both of these variants internally so that we can distinguish between a tab that had its color manually set to none versus a tab that just started as none and should therefore be auto-assigned a color based on directory). IMO we should just have \"none\" (no \"reset\", \"default', or \"clear\") and that should map to Cleared.",
        "pr_file_module": null
      }
    ]
  }
]