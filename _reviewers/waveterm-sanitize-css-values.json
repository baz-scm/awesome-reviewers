[
  {
    "discussion_id": "1552437366",
    "pr_number": 547,
    "pr_file": "src/app/common/elements/styleblock.tsx",
    "created_at": "2024-04-04T20:59:45+00:00",
    "commented_code": "+// Copyright 2023, Command Line Inc.\n+// SPDX-License-Identifier: Apache-2.0\n+\n+import * as React from \"react\";\n+import * as mobxReact from \"mobx-react\";\n+import * as mobx from \"mobx\";\n+import { GlobalModel } from \"@/models\";\n+import { isBlank } from \"@/util/util\";\n+\n+@mobxReact.observer\n+class StyleBlock extends React.Component<\n+    { scope: \"main\" | \"session\" | \"screen\"; themeSrcEl: HTMLElement; themeKey: string; termTheme: TermThemeType },\n+    { styleRules: string }\n+> {\n+    styleRules: OV<string> = mobx.observable.box(\"\", { name: \"StyleBlock-styleRules\" });\n+    theme: string;\n+\n+    componentDidMount(): void {\n+        this.loadThemeStyles();\n+    }\n+\n+    componentDidUpdate(prevProps): void {\n+        const { themeKey, termTheme } = this.props;\n+        const currTheme = termTheme[themeKey];\n+        if (themeKey !== prevProps.themeKey || currTheme !== this.theme) {\n+            this.loadThemeStyles();\n+        }\n+    }\n+\n+    loadThemeStyles() {\n+        const { themeKey, termTheme, scope } = this.props;\n+        const currTheme = termTheme[themeKey];\n+\n+        if (currTheme && currTheme !== this.theme && currTheme) {\n+            const rtn = GlobalModel.getTermThemeJson(currTheme);\n+            rtn.then((termThemeJson) => {\n+                if (termThemeJson && typeof termThemeJson === \"object\") {\n+                    const styleProperties = Object.entries(termThemeJson)\n+                        .map(([key, value]) => `--term-${key}: ${value};`)",
    "repo_full_name": "wavetermdev/waveterm",
    "discussion_comments": [
      {
        "comment_id": "1552437366",
        "repo_full_name": "wavetermdev/waveterm",
        "pr_number": 547,
        "pr_file": "src/app/common/elements/styleblock.tsx",
        "discussion_id": "1552437366",
        "commented_code": "@@ -0,0 +1,72 @@\n+// Copyright 2023, Command Line Inc.\n+// SPDX-License-Identifier: Apache-2.0\n+\n+import * as React from \"react\";\n+import * as mobxReact from \"mobx-react\";\n+import * as mobx from \"mobx\";\n+import { GlobalModel } from \"@/models\";\n+import { isBlank } from \"@/util/util\";\n+\n+@mobxReact.observer\n+class StyleBlock extends React.Component<\n+    { scope: \"main\" | \"session\" | \"screen\"; themeSrcEl: HTMLElement; themeKey: string; termTheme: TermThemeType },\n+    { styleRules: string }\n+> {\n+    styleRules: OV<string> = mobx.observable.box(\"\", { name: \"StyleBlock-styleRules\" });\n+    theme: string;\n+\n+    componentDidMount(): void {\n+        this.loadThemeStyles();\n+    }\n+\n+    componentDidUpdate(prevProps): void {\n+        const { themeKey, termTheme } = this.props;\n+        const currTheme = termTheme[themeKey];\n+        if (themeKey !== prevProps.themeKey || currTheme !== this.theme) {\n+            this.loadThemeStyles();\n+        }\n+    }\n+\n+    loadThemeStyles() {\n+        const { themeKey, termTheme, scope } = this.props;\n+        const currTheme = termTheme[themeKey];\n+\n+        if (currTheme && currTheme !== this.theme && currTheme) {\n+            const rtn = GlobalModel.getTermThemeJson(currTheme);\n+            rtn.then((termThemeJson) => {\n+                if (termThemeJson && typeof termThemeJson === \"object\") {\n+                    const styleProperties = Object.entries(termThemeJson)\n+                        .map(([key, value]) => `--term-${key}: ${value};`)",
        "comment_created_at": "2024-04-04T20:59:45+00:00",
        "comment_author": "sawka",
        "comment_body": "need some validation on key/value so they don't break the CSS.",
        "pr_file_module": null
      },
      {
        "comment_id": "1558732539",
        "repo_full_name": "wavetermdev/waveterm",
        "pr_number": 547,
        "pr_file": "src/app/common/elements/styleblock.tsx",
        "discussion_id": "1552437366",
        "commented_code": "@@ -0,0 +1,72 @@\n+// Copyright 2023, Command Line Inc.\n+// SPDX-License-Identifier: Apache-2.0\n+\n+import * as React from \"react\";\n+import * as mobxReact from \"mobx-react\";\n+import * as mobx from \"mobx\";\n+import { GlobalModel } from \"@/models\";\n+import { isBlank } from \"@/util/util\";\n+\n+@mobxReact.observer\n+class StyleBlock extends React.Component<\n+    { scope: \"main\" | \"session\" | \"screen\"; themeSrcEl: HTMLElement; themeKey: string; termTheme: TermThemeType },\n+    { styleRules: string }\n+> {\n+    styleRules: OV<string> = mobx.observable.box(\"\", { name: \"StyleBlock-styleRules\" });\n+    theme: string;\n+\n+    componentDidMount(): void {\n+        this.loadThemeStyles();\n+    }\n+\n+    componentDidUpdate(prevProps): void {\n+        const { themeKey, termTheme } = this.props;\n+        const currTheme = termTheme[themeKey];\n+        if (themeKey !== prevProps.themeKey || currTheme !== this.theme) {\n+            this.loadThemeStyles();\n+        }\n+    }\n+\n+    loadThemeStyles() {\n+        const { themeKey, termTheme, scope } = this.props;\n+        const currTheme = termTheme[themeKey];\n+\n+        if (currTheme && currTheme !== this.theme && currTheme) {\n+            const rtn = GlobalModel.getTermThemeJson(currTheme);\n+            rtn.then((termThemeJson) => {\n+                if (termThemeJson && typeof termThemeJson === \"object\") {\n+                    const styleProperties = Object.entries(termThemeJson)\n+                        .map(([key, value]) => `--term-${key}: ${value};`)",
        "comment_created_at": "2024-04-10T02:28:34+00:00",
        "comment_author": "adred",
        "comment_body": "addressed",
        "pr_file_module": null
      }
    ]
  }
]